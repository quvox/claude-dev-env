.env
workspace/
!workspace/.gitkeep
.claude

# orchestrator のローカルビルド生成物（イメージ用は Dockerfile でビルド）
/orchestrator/orchestrator

# orchestrator の実行時運用状態（成果物ではない。docs/impl/60_orchestrator.md）
/.orchestrator/

# プロジェクト直下の claude-dev 設定（ユーザー固有の SSH 鍵選択。マシン依存・成果物ではない）
/.claude-dev.yaml
