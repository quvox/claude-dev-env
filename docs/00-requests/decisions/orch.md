---
id: orch
version: 1.2.1
updated: 2026-08-05
source:
  - docs/00-requests/request.md
summary: AIオーケストレーターの制御・介入・状態に関する決定事項(D0-orch-*)
keywords: [オーケストレーター, 決定事項]
verified:
  at: 2026-08-04
  version: 1.2.0
  against:
    - doc: docs/00-requests/request.md
      version: 1.2.0
---

<!-- 2026-08-04 /doc-check ssot task-impl-depth(新しい実行): 合格証を再発行した(1.2.0)。
     2026-08-04 に合格証を削除した理由(docs/issues/039 = D0-orch-15「構造化出力(スキーマ強制)」
     ⇄ FR-orch-06 受入基準3「ツールによるスキーマ強制は行わない」)は、人間が案A(00 を下流に
     合わせる)で裁定し、D0-orch-15 に「★2026-08-04 改め」を追記して解消した(1.1.0 → 1.2.0)。
     独立監査 Codex docs(00+01。gpt-5.6-terra / reasoning max)が
     「現行 D0-orch-15 と FR-orch-06 #3/#6/#7 は両立している」と独立に確認した。
     残る「中」: D0-orch-16 / D0-orch-17 は区分「要確認」で、CLAUDE.md の A0 規則により
     検証を妨げない(期限は事象条件で書かれている)。 -->

# オーケストレーションの決定事項

## D0-orch-01 コントローラの生存判定とセッション名の扱い

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: コントローラが生きているかをどう判定するか(プロセス存在で見るかセッション存在で見るか)、
  セッション名を呼び出し側にどう伝えるか。
- 制約(ガードレール): **空き殻のセッションを「生存」と誤判定しないこと**。呼び出し側にセッション名を
  推測させないこと。誤判定は「再実行したのに合流できない/二重起動する」という利用者から見える故障に
  なるため、判定方法を変えるときは再現手順で確認すること。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-orch-01` と共に記録する
- 関連: FR-orch-02 / AC-05

## D0-orch-02 worker・対話 Claude の起動パラメータと指示の渡し方

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: 子プロセスとして起動する Claude の起動引数(権限モード・モデル・effort・再開指定)、
  指示テンプレートをどこに置きどう渡すか、再開/新規をどう判定するか、中断をどう表現するか。
- 制約(ガードレール): モデル/effort は 1 箇所のポリシー表で決め、利用者設定では変えられないようにする
  (`FR-orch-07`)。指示テンプレートはイメージ同梱とし、プロジェクト側に置かない。中断は終了コード 0 の
  クリーン終了にする(`FR-orch-05`)。ヘッドレス実行で人間の応答を要する状態を作らないこと。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-orch-02` と共に記録する
- 関連: FR-orch-03 / FR-orch-05 / FR-orch-07

## D0-orch-03 運用状態の永続化方式

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: 状態ファイルの書き方(原子的置換か追記か)、一時ファイルの置き場所、`.orchestrator/` の
  所有権の考え方。
- 制約(ガードレール): **起動時の自動処理で plan / 状態 / 履歴を削除しないこと**(片付けは必ず
  `history/` への退避。これは不変条件)。人間に運用状態の直接編集を促さないこと(`FR-orch-08`)。
  途中で落ちても既存の記録が壊れない書き方にすること。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-orch-03` と共に記録する
- 関連: FR-orch-05

## D0-orch-04 介入と統合の制御構造

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: 介入をどのデータ構造で保持するか、突合をどのメモリ上の実体に対して行うか、統合(マージ)を
  誰が実行するか、古い指示をどう捨てるか。
- 制約(ガードレール): **要判断のタスク1件だけを待機させ、他の worker を止めないこと**
  (`FR-orch-04`)。最上位状態としての「介入中」を置かないこと。統合を並列に行わないこと。
  介入の突合をディスクの別コピーに対して行わないこと(共有中の実体と乖離すると実行が恒久停止する)。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-orch-04` と共に記録する
- 関連: FR-orch-03 / FR-orch-04

## D0-orch-05 レビューの採点基準と再試行の規則

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: レビュー結果の解釈(整形と判定の分離、散文から構造化への再整形を何回許すか)、
  差し戻し時に試行回数を数えるかどうか。
- 制約(ガードレール): **採点基準は当該タスクの完了条件のみ**とし、プラン全体のゴールへ
  フォールバックしないこと(`FR-orch-06`)。フォーマット違反と内容不合格を混同しないこと。
  同一フォーマットエラーが 2 回続いたら自動リトライを打ち切り介入へ回すこと。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-orch-05` と共に記録する
- 関連: FR-orch-06

## D0-orch-06 ダッシュボードの描画と操作の規則

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: 表示の整形方法、未知の情報をどう扱うか、補助情報の取得に失敗したときの描画、
  端末状態の復元をどこに置くか。
- 制約(ガードレール): **カーソル選択と Enter 確定でのみウィンドウを移動する**(カーソル移動だけで
  画面が飛ばない。`FR-orch-08`)。人間に判断を求める表示は日本語にし、選択肢に番号を付けること。
  補助情報の取得失敗で描画を止めないこと。どの終了経路でも端末状態を復元すること。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-orch-06` と共に記録する
- 関連: FR-orch-08

## D0-orch-07 通知の失敗許容

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: 通知(Slack・フック)の送信に失敗したときに、呼び出し元をどう扱うか。重複通知をどう抑えるか。
- 制約(ガードレール): **通知の失敗で実行を止めないこと**(通知は補助であり、実行の可否を左右させない)。
  失敗を握りつぶす場合でも、利用者が状況を追える手段(ログ)を残すこと。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-orch-07` と共に記録する
- 関連: FR-orch-07 / NFR-ops-01

## D0-orch-08 自己検証題材の作り込み範囲

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: 自己検証に使う題材(サンプルプロジェクト)の粒度・堅牢性をどこまで作り込むか、
  検証の起点(seed plan)を必須にするか任意にするか。
- 制約(ガードレール): 合否が自動テストで機械判定できること。題材の作り込みを目的化しないこと
  (題材はオーケストレーターの振る舞いを確認する手段である)。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-orch-08` と共に記録する
- 関連: FR-orch-09

## D0-orch-09 オーケストレーションは自作の外部制御ループで行う

- 区分: 決定
- 決めた日: 2026-07-30
- 内容: コントローラがループを所有する外部制御ループを自作する。推論ループは `claude -p` / 対話 Claude
  から借りる。
- 理由: 暴走しない・コンテキストを汚さない・再開可能。変化の速い依存を中核に据えると配布の安定性に
  リスクがある。
- 却下した案: Docker Agent 方式(推論と委譲の配管を任せる) — 変化が速く中核に据えられない。
  Stop-hook による力技の連続走行 — 停止条件を LLM の裁量に委ねることになり暴走を止められない。
- 関連: RQ-orch-01 / FR-orch-02

## D0-orch-10 オーケストレーターは「1実体・2モード」とする

- 区分: 決定
- 決めた日: 2026-07-30
- 内容: ブレインストーミング(人間×対話 Claude、自動化しない)と実行(自律・並列)を1つの実体の
  2モードとする。両モードの引き渡し契約は仕様ドキュメントとし、運用状態は `.orchestrator/` に置く。
- 理由: 人間の価値が宿る検討は自動化せず、実装から整合性確認までを自動化する。
- 却下した案: 検討と実行を別ツールに分ける — 引き渡しが手作業になり、状態が二重管理になる。
- 関連: RQ-orch-01 / FR-orch-01 / request.md「やらないこと」4

## D0-orch-11 コントローラは tmux に常駐させる

- 区分: 決定
- 決めた日: 2026-07-30
- 内容: コントローラを `orch-<project>-main` セッションの `dashboard` ウィンドウで常駐させる。
  各 worker とブレインストーミングは同セッションの独立ウィンドウにする。
- 理由: クライアント端末が壊れても tmux サーバがセッションを保持するため、再接続で復旧できる。
  完全デーモン化より単純で、ダッシュボード描画を別プロセスに分けなくて済む。
- 却下した案: `setsid` による完全デーモン化 — 描画を別プロセス化する必要があり複雑になる。
  常駐しない(実行のたびに起動) — 端末を閉じると実行が失われる。
- 関連: RQ-orch-01 / FR-orch-02 / AC-05

## D0-orch-12 介入はタスク単位にする

- 区分: 決定
- 決めた日: 2026-07-30
- 内容: 要判断が出たタスク1件だけを待機状態にし、他の worker は止めない。独立した最上位状態
  「介入中」は置かない。
- 理由: 全体停止方式では1件の判断が全 worker を巻き込み、大量のやり直しを生む。
- 却下した案: ストップ・ザ・ワールド方式(全 worker を止めて人間に諮る) — 前述のとおり。
- 関連: RQ-orch-01 / FR-orch-04 / AC-04

## D0-orch-13 起動時の自動処理で状態を削除しない

- 区分: 決定
- 決めた日: 2026-07-30
- 内容: 起動時の自動処理で plan/状態/履歴を削除しない。片付けは全タスクが完了しているか
  `--fresh` を指定したときのみ行い、その場合も `history/<run_id>/` へ退避する。実削除は利用者の
  明示的な操作だけとする。
- 理由: 中断・再開でのやり直しを構造的に排除する(人間の巡回負荷を下げることが本ツールの価値)。
- 却下した案: 起動時に前回の状態を掃除する — 中断からの再開ができなくなる。
- 関連: RQ-orch-01 / FR-orch-05 / AC-05

## D0-orch-14 ダッシュボードはイベント駆動 TUI にする

- 区分: 決定
- 決めた日: 2026-07-30
- 内容: bubbletea / lipgloss によるイベント駆動 TUI とし、カーソル選択 → Enter で移動する。
  全消去・全再描画方式と数字キーでの即時移動は廃止する。この UI に限り外部依存を許容する
  (依存はリポジトリに同梱する)。
- 理由: ちらつきと意図しない移動を排し、選択と確定を分離する。
- 却下した案: 全画面再描画 + 数字キー即時移動 — ちらつきと誤操作が起きる。
  標準ライブラリだけで自作する — 実装コストに見合わない(この UI だけ方針を変える)。
- 関連: RQ-orch-01 / FR-orch-08

## D0-orch-15 品質ゲートは別 worker による独立レビューとする

- 区分: 決定
- 決めた日: 2026-07-30
- 内容: 実装した worker とは別の worker(できれば別ベンダー)による独立レビューを通す。採点は
  当該タスクの完了条件のみで行う。レビュー結果は構造化出力で受け取り、同一
  フォーマットエラーが 2 回続いたら打ち切って介入へ回す。
  **★2026-08-04 改め**: 「構造化出力(**スキーマ強制**)」という表現を撤回する。**強制はツールでは
  行わない** — プロンプトで JSON の形を明示して要求し、崩れたら1回だけ再整形を試み、
  2回続けば介入へ回す(`FR-orch-06` 受入基準3・6・7 / `CTR-orchestrator-prompt`)。
  この撤回の理由: 同じ決定の後半(フォーマットエラーが続く前提)と前半(強制)が両立しない。
  **形式の検証が一箇所も無い**という事実は、
  `docs/issues/058-bug-unknown-severity-passes-the-review-gate.md`(未知の `severity` が
  「重大でない」扱いで品質ゲートを通過する)と
  `docs/issues/059-modify-review-gate-scoring-criterion-has-no-covering-test.md`
  (採点基準を覆うテストが実在しない)が追跡する。
- 理由: 自己レビューでは見落としが残る。プランのゴールで採点すると差し戻しが恣意的になる。
  自由記述で受け取るとパースに失敗して試行を浪費する。
- 却下した案: 実装した worker 自身にレビューさせる — 独立性が無い。人間が全件レビューする —
  並列度を上げるという目的に反する。
- 関連: RQ-orch-01 / FR-orch-06

## D0-orch-16 モデル/effort ポリシーの将来調整

- 区分: 要確認
- 論点: 工程別のモデル選択(設計系=opus/high、実装・テスト系=sonnet/high)は 2026-07 時点の方針。
  基準を見直すかどうか。
- 選択肢: 現行方針を維持 / 実測に基づいて工程別の割当を変更 / 利用者設定で変更可能にする
- 誰が・いつまでに: 人間(運用実測を見て随時) / 期限なし
- これが決まらないと何が止まるか: 何も止まらない(現行方針で動作する)。変更する場合は
  `FR-orch-07` の受け入れ基準が変わる。
- 関連: FR-orch-07

## D0-orch-17 異種ベンダー worker の常用可否

- 区分: 要確認
- 論点: 現状 worker は主に Claude である。別ベンダー(Codex 等)を worker/レビューアーとして常用するか。
  **Codex CLI の同梱と認証共有は `D0-dist-04` で決定済み**であり、ここで未決なのは常用の可否だけである。
- 選択肢: 常用する / レビューアーだけ別ベンダーにする / 当面 Claude のみ
- 誰が・いつまでに: 人間(品質ゲートが定着した後に判断) / 期限なし
- これが決まらないと何が止まるか: 何も止まらない。`FR-env-12` の対象範囲(開発者の対話利用まで)が
  この未決に依存している。
- 関連: FR-env-12 / FR-orch-06

## D0-orch-18 「人間を呼ぶ判断」と「自律継続してよい判断」の境界

- 区分: 決定
- 決めた日: 2026-08-03
- 内容: 「軽微な判断だから自律継続してよい」という表現を廃止し、**介入を発火させる条件の否定**として
  定義する。すなわち **`orchestrator/trigger.go::Evaluate` が発火しない場合が「自律継続してよい判断」**
  である。発火する条件は次のとおりで、これ以外はすべて自律継続の対象とする。

  | 局面 | 発火する条件 |
  |---|---|
  | ディスパッチ前 | タスクが不可逆と印付けされており、かつ人間の承認がまだ無い |
  | ディスパッチ後 | 同一タスクの試行回数が `stuck_limit`(既定 3)以上に達した |
  | ディスパッチ後 | その試行で行き詰まりが検出された |
  | ディスパッチ後 | worker が「人間が必要」を、曖昧さ / 方針分岐 / 前提崩れ / 重大判断 のいずれかの理由で申告した |

- **射程**: 本項が定めるのは **worker の判断**についての境界(worker が人間を呼ぶか、仮定を置いて
  進むか)である。**これ以外の理由で介入が起きる経路は別に存在する**: レビューゲートの不具合
  (レビュア出力が `review_format_error_limit` 回続けて解釈できない = `review_gate_defect`。
  `FR-orch-06` 受入基準6)は worker の判断ではないため上表に含めず、それ自体で介入へ回る。
- 理由: 「軽微」は測定できず、境界値テストの入力を決められない(issue 008)。実装は移設前から
  上表の条件で動いており(**実装が正**)、要件側の表現だけが人間の言葉のまま残っていた。
- 影響: `FR-orch-04` 受入基準4 / `UC-04` 代替フロー A1 / `NFR-perf-03` の表現を上表の条件へ置き換える。
  **振る舞いは変わらない。**
- 関連: `FR-orch-04` / `FR-orch-06` / `D0-orch-04`(起点は issue 008。**同 issue は本決定の反映と同時に解消・削除**するため、経緯は `docs/histories/` を参照)
