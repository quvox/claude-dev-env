---
id: scope
version: 1.2.0
updated: 2026-08-05
source:
  - docs/00-requests/request.md
summary: 記述粒度と実装範囲に関する決定事項(D0-scope-*)
keywords: [スコープ, 粒度, 決定事項]
verified:
  at: 2026-08-07
  version: 1.2.0
  against:
    - doc: docs/00-requests/request.md
      version: 1.3.0
---

# スコープ・実装粒度の決定事項

## D0-scope-01 03-impl の記述粒度と実コードとの対応づけ

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: 各機能の `03-impl/relations/MODULE-*.md` にどこまで詳細を書くか、コードのどの範囲を
  1つの機能として束ねるか。
- 制約(ガードレール): `02-design/system.md` のモジュール分割定義を逸脱しない(モジュールの一覧は
  同表が正本)。同じコードを複数の機能仕様書に重複させない。データ構造・インターフェース・
  ロジックの意図を書き、行単位のコードは書かない。**機能表(`03-impl/features.md`)そのものは
  委任範囲外**で、人間の合意を要する。
- 報告義務: 決めた内容を書いたドキュメントに `D0-scope-01` と共に記録する
- 関連: RQ-env-01 / `.claude/directions/features.md`

## D0-scope-02 実装内部の構造上の判断(観測可能な振る舞いを変えない範囲)

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: ヘルパー関数の分割・統合、処理をどの経路に置くか、資源(プロファイル・ボリューム・
  一時ファイル)をどの単位で分けるか、部分的な失敗を成功として扱うか、後方互換のための残骸掃除を
  するか——といった**実装内部の構造の選択**。
- 制約(ガードレール): 利用者から観測できる振る舞い(コマンドの成否・出力・生成物・ポート・
  ファイル配置)を変えないこと。契約(`CTR-*`)を変えないこと。これらを変える判断は委任範囲外で、
  00 起点の変更として扱う。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-scope-02` と共に記録する
- 関連: RQ-env-01 / FR-env-01

## D0-scope-03 macOS 実装の差分の扱い

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: macOS 版(`claude-dev-mac`)で Linux 版と実装が分かれる箇所について、どちらに寄せるか・
  片方にしか置かない関数を作るか・後方互換の残骸を掃除するか。
- 制約(ガードレール): **同じサブコマンドは同じコマンド面(引数・出力・成否)を保つ**こと。
  OS 都合で利用者から見える手順が変わる場合(VM/KVM 非対応など)は、要件
  (`FR-env-10`)に書かれた範囲に限る。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-scope-03` と共に記録する
- 関連: RQ-env-06 / FR-env-10

## D0-scope-04 VM モードの運用パラメータと停止・再構築の手段

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: ゲスト VM の監視周期・判定しきい値・サンプリング窓、停止手段の優先順(正常終了と
  強制終了の使い分け)、再構築時にどこまで作り直すか。
- 制約(ガードレール): VM モードはオプトインであり、既定の軽量構成(DooD)の振る舞いを変えないこと。
  利用者のデータ(`/workspace`)を失う操作を自動で行わないこと。完全な作り直しは利用者の明示指定
  (`--vm-fresh`)に限る。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-scope-04` と共に記録する
- 関連: RQ-env-05 / FR-env-08

## D0-scope-05 利用者が編集しうる設定ファイルの更新方式

- 区分: 委任
- 決めた日: 2026-07-31
- 委任範囲: 利用者が手で書き換えうる設定ファイル(`config.toml` など)に既定値を補完するときの
  実装方式(行単位の書き換えか、構造としてパースして検証するか)。
- 制約(ガードレール): **利用者が書いた鍵と値を書き換えないこと**、**何度実行しても結果が
  変わらない(冪等)こと**。この 2 つを満たせない実装方式は選べない。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-scope-05` と共に記録する
- 関連: FR-env-12

## D0-scope-06 既存実装から仕様を起こすときの表現の食い違いの穴埋め

- 区分: 委任
- 決めた日: 2026-07-30(**2026-08-05 に委任範囲の書き方を対象ファイルによる限定へ改めた**。
  範囲そのものは広げていない)
- 委任範囲: **`docs/03-impl/relations/` と `docs/03-impl/contracts/` の散文**について、
  旧ドキュメントとコードで表現が食い違う点を、**コードを正**として記述すること。
- 制約(ガードレール): 次のいずれかに当たる食い違いは**委任範囲外**である
  (黙ってどちらかに寄せず、事実を報告して人間に問う)。
  1. 01 の受け入れ基準の意味が変わるもの
  2. 契約(`CTR-*`)の項目・型・エラー・列挙値が変わるもの
  3. 観測可能な振る舞い(利用者に見える出力・終了コード・作成/削除される資源)が変わるもの
  4. `docs/00-requests/` `docs/01-requirements/` `docs/02-design/` の本文
     (対象は 03 の散文だけであり、上流の層は範囲に含まない)
- 報告義務: 穴埋めした箇所をタスクの調査メモに残す
- 関連: `docs/issues/` / `D0-scope-07`(同じ委任を対象ファイルで限った先例)

<!-- 2026-08-05 の改定で失われた表現と、それを失ってよい理由:
     旧「軽微な点について」は、範囲の広さを**程度**で表していた。程度語は実行ごとに解釈が揺れ、
     現に過去の検証は「D0-scope-06 の委任は軽微な曖昧さに限られる」を理由に 00 層への書き込みを
     手続き逸脱と判定しており(経緯は `docs/histories/` の 2026-08-04 のエントリ)、
     **同じ語が判定の根拠として使われている**。
     新しい書き方は対象を「03 の散文」に限り、旧ガードレール(要件・契約・観測可能な振る舞い)を
     1〜3 として明示的に残したうえで、4 として上流の層を加えた。したがって委任範囲は
     **旧よりも狭いか同じ**であり、広がっていない。 -->

## D0-scope-07 03-impl を「再実装可能な深度」まで掘り下げるときの書き方

- 区分: 委任
- 決めた日: 2026-08-03
- 委任範囲: `03-impl/relations/` と `contracts/` を「ドキュメントだけから再実装・再試験できる」
  深度へ掘り下げるときの、(1) どこまで書けば十分かの停止条件、(2) 実装が振る舞いを定めていない
  観点(排他・再試行・タイムアウトなど)の書き方、(3) 掘り下げ中に見つけた実装の欠陥をどこへ記録するか。
- 制約(ガードレール):
  - **コードから一意に読み取れる事実だけを書く。** 読み取れない=「決めなければならない」ものは
    書かず、`docs/issues/` に起票する。**推測を仕様として書いてはならない。**
  - 実装が排他・再試行・タイムアウトを持たない場合は、**「持たない」という事実と、そのときの
    観測結果**を書く。あるべき姿を書いてはならない(SSOT は現在の姿だけを持つ — CLAUDE.md 原則1)。
  - 観測可能な振る舞い・契約・モジュール分割を変えないこと。これらを変える判断は委任範囲外。
  - **排他の欠如・入力検証の欠落・秘密値のログ出力**を見つけたら、下の**起票の閾値**に従って
    `docs/issues/` へ起票する(severity は影響範囲で決めてよい)。その場で**コードを直してはならない**。
  - **起票の閾値**(2026-08-03 追記。この閾値の外にあるものは 03 の「既知の制限」に記述だけ残す):
    次の**両方**を満たすものを起票する。
    (a) 観測可能な被害がある — データの破壊 / 秘密の漏洩 / **利用者が失敗に気づけない** /
    01・02 の記述との不一致 のいずれかに当たる。
    (b) その被害を説明する issue が**まだ無い**。
    逆に、**被害が既知の issue で説明できるもの**、**利用者がその場で失敗に気づけるもの**、
    **00/02 が「そう決めた」と明示しているもの**は、03 の「既知の制限」に事実を書くだけでよい。
    **判定の根拠(どちらに当たるか)を必ず書く。**
  - **観測点の定義**(上の判定を一意にするため):
    「**その場で気づける**」= 失敗が標準出力・標準エラーに現れる / 終了コードが非0になる /
    利用者が通常見る画面(ダッシュボード・`claude-dev ports` / `list` の出力)に現れる、
    のいずれかを満たす。
    「**利用者が失敗に気づけない**」= 上のいずれにも現れず、**成功時と同じ表示・同じ終了コード**に
    なる。「既知の issue で説明できる」= その issue の「事象」がこの被害を含む(ID を書く)。
  - 02 の `logging.md` が定める必須フィールド・マスキング方針に実装が従っていない箇所を見つけたら、
    03 には**実装の現状**を書き、差は `docs/issues/` へ起票する。
- 報告義務: 掘り下げた各ドキュメントの「実装上の判断」表に `D0-scope-07` と共に記録する。
  **起票の閾値で「留める」と判定したものは、03 の「既知の制限」に理由も書く**(黙って落とさない)。
  起票した issue はタスクの histories に一覧する。
- 関連: `docs/issues/004-modify-03-impl-lacks-reimplementation-depth.md`(**閉じずに残る**。残件は同 issue の「経緯」)/ `D0-scope-01` / `D0-scope-06`(起点のもう1つは issue 008。**同 issue は本決定の反映と同時に解消・削除**するため、経緯は `docs/histories/` を参照)
