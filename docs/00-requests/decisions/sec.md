---
id: sec
version: 1.3.0
updated: 2026-08-10
source:
  - docs/00-requests/request.md
summary: 隔離境界と権限制限に関する決定事項(D0-sec-*)
keywords: [セキュリティ, 隔離, 決定事項]
verified:
  at: 2026-08-10
  version: 1.3.0
  against:
    - {doc: docs/00-requests/request.md, version: 1.4.0}
---

<!-- 2026-08-04 /doc-check ssot task-impl-depth(新しい実行): 合格証を 1.0.1 → 1.1.0 で再発行した。
     D0-sec-10 に「VM モード(--vm / --vm-fresh)は /dev/kvm 必須、無ければ終了コード 1」を
     追記した分(docs/issues/039 の案A・人間裁定)を認証した。実装で裏取り済み
     (claude-dev:704 で --vm-fresh が USE_VM=1、:709 で /dev/kvm 不在なら exit 1、
     :858〜861 で --kvm 単独は警告して続行)。
     独立監査 Codex docs(00+01)が「D0-sec-10 は --vm-fresh を含むのに FR-env-08 #5 は --vm
     だけ」というカバレッジ欠落を単独で検出したため、01 側を修正した(00 は正しい)。
     残る「中」: D0-sec-05 のガードレールは「判定に必要な情報が手元に無い場合はその事実を
     『既知の制限』に書く」と定めるが、パース不能ボディの中継は
     MODULE-docker-proxy-serve の「実装上の判断」に書かれていて「既知の制限」に無い
     (docs/issues/005)。これは「高」ではないため認証を妨げない。

     2026-08-06 /doc-check full: **上の「残る『中』」は解消済みである。**
     2026-08-05 の task-relations-code-sync が対処案1 を適用し、
     `docs/03-impl/relations/MODULE-docker-proxy-serve.md` の `## 既知の制限` に
     「解釈できないリクエストボディは検査せず中継する」の行が入った
     (経緯は `docs/histories/2026-08-05-relations-code-sync.md`)。
     issue 005 自体はコードが未修正のため残っている。 -->

# セキュリティ・隔離の決定事項

## D0-sec-01 コンテナの confinement を緩めない範囲での対処の選び方

- 区分: 委任
- 決めた日: 2026-07-31
- 委任範囲: コンテナ内のツールが Docker の既定 confinement(seccomp / AppArmor)の下で動かないとき、
  どう対処するか(ツール側の設定で回避する / 機能を落とす / 別バックエンドを使う)。
  リスナーの待ち受けアドレスや、強い権限を持つ MCP を既定で有効にするかどうかの判断も含む。
- 制約(ガードレール): **`docker run` の `--security-opt` を付けてコンテナ側の confinement を
  緩める選択肢は採れない**(隔離境界はコンテナ/ホスト間のみという前提〈`D0-sec-06`〉を支えている
  ものであるため)。ホストへ新規のポートを公開しないこと。強権限の機能は既定で無効にすること。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-sec-01` と共に記録する
- 関連: FR-env-12 / NFR-sec-01

## D0-sec-02 ホスト資産をコンテナへ渡すときの絞り込み方

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: ホストの設定ファイルをコンテナへ渡す際、そのまま渡すか・一部を除去した写しを渡すか・
  読み取り専用にするか。
- 制約(ガードレール): **ホストにある秘密鍵・認証情報の実体をコンテナへ渡さない**こと(この決定が
  対象にするのは上記の委任範囲=ホスト資産の受け渡しに限る。コンテナ内で `login` して作られた認証を
  共有ボリューム経由でコンテナ間に持ち回る `D0-auth-03` は、ホスト資産の受け渡しではないため
  この制約の対象外である)。渡すものは読み取り専用にすること。除去したことで利用者の作業が黙って壊れないよう、影響のある項目は該当機能の異常系に書くこと。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-sec-02` と共に記録する
- 関連: FR-env-04 / NFR-sec-01

## D0-sec-04 ファイアウォールの実装方式と適用範囲

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: ブロック対象の表現方法(ホスト名指定か IP 集合か)、名前解決のタイミング(起動時の
  一回解決か追従か)、**規則をどこに置くか**。
- 制約(ガードレール): 適用に失敗してもコンテナの起動を止めないこと(`FR-env-05` の受け入れ基準)。
  適用の成否が起動ログから判別できること。既定の通信(Anthropic API 等、開発に必要な宛先)を
  塞がないこと。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-sec-04` と共に記録する
- 関連: FR-env-05 / NFR-sec-01

## D0-sec-05 Docker API 検査の厳密さと安全側の倒し方

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: docker-proxy がリクエストを検査するときの厳密さ(字句的な判定に留めるか、実体解決まで
  行うか)、解釈できない入力を通すか止めるか。
- 制約(ガードレール): 拒否すべき操作(作業領域外のホストバインド・特権・ホストのネットワーク/
  プロセス空間)を**通してはならない**。判定に必要な情報がプロキシの手元に無い場合は、その事実を
  該当機能の「既知の制限」に書くこと。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-sec-05` と共に記録する
- 関連: FR-env-07 / NFR-sec-01

## D0-sec-06 エージェントの隔離モデルは単一コンテナ同居型とする

- 区分: 決定
- 決めた日: 2026-07-30
- 内容: コンテナ内で動くすべてのプロセスは同一コンテナ・同一ファイルシステムを共有し、
  プロセスごとに個別隔離しない。**隔離境界はコンテナ/ホスト間にのみ置く**。
- 理由: 信頼できる社内開発用途に限定するという前提のもとでの割り切り。プロセスごとの隔離は
  実装・運用コストが高い。この前提は下流の全判断(Codex サンドボックスの扱い・confinement を
  緩めない方針)の根拠になっている。
- 却下した案: microVM 等によるプロセス単位の個別隔離 — 実装・運用コストに見合わない。
  個別隔離が要る用途は別構成(Docker Sandboxes 等)で検討する。
- 関連: RQ-env-01 / NFR-sec-01 / request.md「やらないこと」1
## D0-sec-07 Docker アクセスは生ソケットではなく検査プロキシ経由にする

- 区分: 決定
- 決めた日: 2026-07-30
- 内容: 生ソケットをマウントせず、Go 製の `docker-proxy` 経由で制限付き Docker API を使う。
  作業領域(`/workspace`)配下を除くホストバインドマウント・privileged・ホストの
  ネットワーク/プロセス空間の共有を拒否する。
- 理由: 生ソケット共有はホスト掌握と同義のリスクがある。既定で危険操作を遮断する。
- 却下した案: 生ソケットの直マウント — 前述のリスク。DinD — 特権が必要で同じ問題を持つ。
- 関連: RQ-env-04 / FR-env-07

## D0-sec-08 SSH 秘密鍵はマウントせず agent 転送だけにする

- 区分: 決定
- 決めた日: 2026-07-30
- 内容: 秘密鍵ファイルはコンテナにマウントしない。**プロジェクトごとの設定で指定された鍵だけ**を
  プロジェクト専用 ssh-agent に登録し、そのソケットだけを転送する
  (**設定の置き場と書式は 02/03 が定める**。macOS での転送手段は `D0-env-04` が定める)。
- 理由: 鍵ファイルの露出を避けつつ、ディレクトリごとに異なる鍵を使えるようにする。
- 却下した案: `~/.ssh` の読み取り専用マウント — 別プロジェクトの鍵まで見える。
  ホストの共有 ssh-agent の転送 — 登録済みの全鍵が使えてしまい限定にならない。
- 関連: RQ-env-01 / FR-env-04

## D0-sec-09 レビュー前コードの外向き通信はコンテナ内のファイアウォールで制御する

- 区分: 決定
- 決めた日: 2026-07-30
- 内容: レビュー前コードの外向き通信を、**ホスト側ではなくコンテナ内で制御する**。
  したがって制御はプロジェクト(コンテナ)ごとに独立し、ホストのネットワーク設定には触れない。
  **実装方式(ブロック対象の表現方法・名前解決のタイミング・規則の置き場)は `D0-sec-04` で
  AI に委任する。**
- 理由: レビュー前コードの外部通信を制御する。ホスト側で制御するとプロジェクト単位に分けられない。
- 却下した案: ホスト側ファイアウォールでの制御 — プロジェクト単位に分けられない。
  ネットワークを完全遮断する — 開発に必要な通信(パッケージ取得・API)まで塞がる。
- 関連: RQ-env-01 / FR-env-05

## D0-sec-10 KVM デバイスは既定で渡さない

- 区分: 決定
- 決めた日: 2026-07-30
- 内容: 既定では `/dev/kvm` 等を渡さない。`--kvm` を指定したときだけデバイスを渡す
  (無ければ警告してソフトウェアエミュレーションにする)。
  **★2026-08-04 追記**: **VM モード(`--vm` / `--vm-fresh`)は `/dev/kvm` を必須とし、無ければ
  中止して終了コード 1 で終わる**(ソフトウェアエミュレーションでは実用的な速度が出ないため、
  VM 無しで続行させるのではなく利用者に VM モードでない起動を選ばせる)。`--kvm` 単独指定の「警告して続行」は
  上のとおり変わらない。**`CTR-cli-container` のエラーケースが正**である
  (`docs/issues/018`)。
- 理由: ブラウザでの確認はコンテナ内 Chrome で足り、CPU 仮想化デバイスを要さない。過剰な特権付与を避ける。
- 却下した案: 常時渡す — 必要のない利用者にまで特権を与える。
- 関連: RQ-env-05 / FR-env-08
