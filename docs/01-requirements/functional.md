---
id: functional
version: 1.8.1
updated: 2026-08-06
source:
  - docs/00-requests/request.md
  - docs/00-requests/acceptances.md
  - docs/00-requests/decisions/auth.md
  - docs/00-requests/decisions/env.md
  - docs/00-requests/decisions/orch.md
  - docs/00-requests/decisions/sec.md
summary: 機能要件 FR-env-01〜12 と FR-orch-01〜09。受け入れ基準は EARS で書く
keywords: [機能要件, EARS, FR]
verified:
  at: 2026-08-06
  version: 1.8.1
  against:
    - doc: docs/00-requests/request.md
      version: 1.3.0
    - doc: docs/00-requests/acceptances.md
      version: 1.2.0
    - doc: docs/00-requests/decisions/auth.md
      version: 1.2.0
    - doc: docs/00-requests/decisions/env.md
      version: 1.2.0
    - doc: docs/00-requests/decisions/orch.md
      version: 1.3.0
    - doc: docs/00-requests/decisions/sec.md
      version: 1.1.2
---

<!-- 2026-08-04 /doc-check ssot task-impl-depth(新しい実行): **合格証を再発行した(1.3.1)。**
     **解消した**: docs/issues/039(D0-orch-15 ⇄ FR-orch-06 受入基準3)は人間が案A(00 を下流に
     合わせる)で裁定し、decisions/orch.md 1.2.0 で D0-orch-15 の「スキーマ強制」表現を撤回した。
     独立監査 Codex docs(00+01)も「現行 D0-orch-15 と FR-orch-06 #3/#6/#7 は両立している」と
     確認した。

     **docs/issues/040(認証ファイルの置き場所と symlink。旧「高」)も解消した**: 人間が案A
     (実装が正)で裁定し、FR-env-03 受入基準2・7 を実装の形(プロジェクトディレクトリ配下へ
     コピーし、コンテナのホームからは symlink で参照)へ改めた。起点の 00(D0-auth-03)も
     1.2.0 で同じ形へ改まっている。本実行でも claude-dev:690,:749〜766,:908 と
     scripts/entrypoint-claude.sh:199,:212〜215,:226 で実装との一致を再確認した。

     **2026-08-04 に自動修正した箇所**: FR-env-08 受入基準5 に `--vm-fresh` を追加した
     (D0-sec-10 の追記が --vm / --vm-fresh の双方を対象にしているのに 01 が --vm だけを
     書いていた A1 のカバレッジ欠落。claude-dev:704,709 で --vm-fresh が USE_VM=1 を立て
     同じ中止経路に入ることを確認済み)。独立監査 Codex docs(00+01)が単独で検出。

     あわせて残る「中」: FR-env-09 受入基準10 と FR-env-07 受入基準8 が SHALL でなく
     「してよい」(check C8。3レンズが検出)/ FR-env-07 の「解釈できないボディは中継してよい」が
     D0-sec-05 のガードレール「拒否すべき操作を通してはならない」と衝突する(docs/issues/005。
     独立レンズ2本が独立に「高」と評価)。

     2026-08-06 /doc-check full: **上の「残る『中』」2件はいずれも解消した。**
     (a) FR-env-07 受入基準8 と FR-env-09 受入基準10 の帰結節を SHALL へ改めた
         (人間の裁定。実装はどちらも分岐を持たないので振る舞いは変わらない)。
     (b) D0-sec-05 との衝突に見えた件は、AC-03 の側を直して解消した — この割り切りが
         D0-sec-05 の**委任範囲内**で下した判断であることを acceptances.md 1.2.0 が明示した。
         受入基準8 にも同じ根拠を書いたので、独立レビューがこれを矛盾として再検出しない。
         残存リスクの追跡先は docs/issues/005 のままである。 -->

# 機能要件

## FR-env-01 コンテナのライフサイクル管理

- 対応要求: RQ-env-01 / AC-01
- 内容: プロジェクトごとに隔離コンテナを起動・再接続・停止・一覧できること。SSH 切断後も作業が
  維持されること。**破壊的操作(`stop` / `logout` / `reset`)が他プロジェクトの資源を巻き込まないこと**
  (`D0-env-08`。共有 docker-proxy と共有ネットワークの停止条件は受入基準9)。
  **共有ボリュームまたは docker-proxy を触るサブコマンドが互いに排他されること**(同項6)。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN `claude-dev start` を実行したとき、システムはカレントディレクトリを `/workspace` にマウントしたコンテナ(既定はブラウザ確認あり)を起動し、tmux にアタッチしなければならない |
| 2 | 正常系 | WHERE `--no-vnc` が指定された場合、システムはブラウザ確認資産を持たない軽量イメージを使わなければならない |
| 3 | 正常系 | WHILE コンテナが起動している間、SSH 切断や tmux デタッチが起きてもコンテナは動作を継続しなければならない |
| 4 | 正常系 | WHEN 同一ディレクトリで `claude-dev start` を再実行したとき、システムは既存コンテナへ再接続しなければならない(tmux セッションが無ければ再作成する) |
| 5 | 正常系 | WHEN `claude-dev list` を実行したとき、システムは実行中セッションの一覧(noVNC URL とフォワード状況を含む)を表示しなければならない |
| 6 | 正常系 | WHEN `claude-dev stop <name>` を実行したとき、システムは当該コンテナを削除し、当該コンテナ内から起動された compose コンテナ群と当該プロジェクトの compose 既定ネットワークを削除し(名前付きボリュームは保持する)、全 Claude コンテナが停止したときは docker-proxy も停止しなければならない |
| 7 | 境界値 | IF tmux の起動待ちが上限(既定 30 秒 / VM モード 420 秒)を超えたならば、システムは終了コード 0 で状況を案内して終わらなければならず、コンテナを削除してはならない(コンテナは稼働を続け、再実行の再接続経路で合流できる) |
| 8 | 境界値 | IF 停止対象のコンテナが存在しないならば、システムは削除をスキップし、その旨を表示して終了コード 0 で終わらなければならない |
| 9 | 境界値 | IF ホスト上に稼働中の Claude コンテナが1つ以上残っているならば、**`stop` / `logout` / `reset` は** docker-proxy を停止してはならず、**`reset` は共有ネットワーク `claude-dev-net` も削除してはならない**(`reset` は残した理由と**完全な初期化になっていない**ことを表示して残りの削除を続行する)。**`stop` と `logout` は遊休かどうかにかかわらず `claude-dev-net` を削除しない**ので、この基準がネットワークについて課す条件は `reset` にだけ効く(`D0-env-08` 項2 の条件は3コマンド共通だが、`claude-dev-net` を削除する経路を持つのは `reset` だけである)。**この判定は、対象コンテナが本システムの管理ラベル(`D0-env-08`)を持つかどうか、およびどのイメージ ID から作られたかに依存してはならない**(イメージを再ビルド・再取得した後に起動していたコンテナを数え落としてはならない。管理ラベルを持たない既存コンテナも稼働中として数える) |
| 10 | 異常系 | IF 前提コマンド(`docker` / `jq`、macOS では `socat` も)が不足しているならば、システムは不足を列挙して導入方法を案内し、終了コード 1 で終わらなければならない |
| 11 | 異常系 | IF compose 既定ネットワークが他から使用中で削除できないならば、システムは処理を続行しなければならない(停止全体を失敗させない。これは `D0-env-08` 項5 が明示する例外である) |
| 12 | 異常系 | IF `claude-dev start` のコンテナ作成が**同名のコンテナが既に存在すること**を理由に失敗したならば、システムはその既存コンテナを削除してはならず、**同名のコンテナが稼働中であること**と**別ディレクトリの同名プロジェクトである可能性**を表示して、終了コード 1 で終わらなければならない(再試行してはならない) |
| 13 | 境界値 | IF コンテナ作成の失敗後に後片付け(作りかけのコンテナの削除)を行うならば、システムは対象が稼働中でないことを確かめてから削除しなければならない。**稼働中のコンテナは、作成の失敗理由を問わず削除してはならない** |
| 14 | 正常系 | WHEN `claude-dev start` が Claude コンテナを作成するとき、システムは本システムが作ったことと起動元のホスト側絶対パスを示す**管理ラベル**を付与しなければならない(名前と値は `CTR-cli-container` が定める。**Claude コンテナ以外の資源にはラベルを付けない**。名前が固定であり名前で識別できるため) |
| 15 | 境界値 | IF `claude-dev stop <name>` の対象コンテナが管理ラベルを持たないならば(管理ラベルが付く前に起動されたコンテナなど)、システムはそのコンテナを削除しつつ、**管理ラベルを持たないことを表示**しなければならない(名前で1件を指した削除は `D0-env-08` 項1 の例外である) |
| 16 | 異常系 | IF `start` / `stop` / `logout` / `reset` / `login` / `login-codex` のいずれかが、同じプロジェクト名または同じ共有資源(共有ボリューム・docker-proxy)を対象として既に実行中ならば、システムは**待たずに**、保持している操作の名前と再実行の方法を表示して終了コード 1 で終わらなければならない。**このとき、取得できなかったロックが守る資源に対する削除も作成も行ってはならない**(プロジェクト単位のロックが守るのは当該コンテナとそのプロジェクト配下の生成物、共有資源単位のロックが守るのは共有ボリューム**の中身**・docker-proxy・**共有ボリュームを読んで作られたコンテナ**である。**冪等に作られる共有資源の「用意」は保護対象ではない**: イメージのビルドと、共有ネットワーク `claude-dev-net`・共有ボリューム3本そのものの作成。既に在れば何も起きず、無いときに作られるのは中身の無い器だけであるため) |
| 17 | 境界値 | IF 排他のためのロックが、既に存在しないプロセスに保持されたまま残っているならば、システムはその事実を表示してからロックを引き継いで処理を続行しなければならない(永久に取得できない状態を作ってはならない) |
| 18 | 境界値 | IF `claude-dev stop <name>` の `<name>` が `[A-Za-z0-9._-]` 以外の文字を含むならば、システムは**何も削除せず**、受理できない文字を含むことを表示して終了コード 1 で終わらなければならない(排他のロックはこの名前をパス要素として使うため、`/` や `..` を含む名前を受理してはならない) |
| 19 | 境界値 | IF 別のディレクトリで起動されたセッションの compose 資源が存在するならば、`stop` はそれを削除してはならない。**起動ディレクトリ名が正規化後に一致する場合も削除してはならない**(compose プロジェクト名は起動ディレクトリの絶対パスを含めて一意化する。方法は `CTR-cli-container` が定める) |
| 20 | 境界値 | IF 本要件の一意化より前に起動された compose 資源が残っているならば、`stop` はそれを削除してはならず、**残っている可能性と手動で片付ける方法を表示**しなければならない(旧い名前は別ディレクトリと衝突しうるため対象に含めない) |
| 21 | 境界値 | IF `claude-dev stop <name>` の対象コンテナが管理ラベル `claude-dev.project-dir` を持たない、または既に存在しないならば、システムは**compose 資源の削除を行ってはならず**、compose 資源が残っている可能性と手動で片付ける方法を表示しなければならない(compose 一意化名は起動ディレクトリの絶対パスから作られ、その値はこのラベルにしか無い。**推測して削除すると別ディレクトリを巻き込む**) |

## FR-env-02 UID/GID 追従とホスト資産の共有

- 対応要求: RQ-env-01 / AC-01
- 内容: コンテナ内のファイル所有権がホストと一致し、git 設定とシェル資産を引き継げること。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN コンテナを起動したとき、システムはコンテナ内ユーザーの UID/GID を `/workspace` の所有者に一致させなければならない |
| 2 | 正常系 | WHERE ホストに `~/.gitconfig` が存在する場合、システムはそれを読み取り専用でマウントしなければならない |
| 3 | 正常系 | WHILE コンテナが稼働している間、システムはシェル設定を `claude-dev-config` ボリュームでコンテナ間共有し、コマンド履歴を `claude-dev-history` ボリュームで永続化しなければならない |
| 4 | 境界値 | IF 割り当てようとする UID/GID がコンテナ内の既存エントリと衝突するならば、システムは既存エントリを一時的な空き ID へ退避してから割り当てなければならない(起動は成功する) |
| 5 | 境界値 | IF `/workspace` の所有者が root(UID 0)ならば、システムは UID/GID を変更してはならない |
| 6 | 異常系 | IF ホストの設定ファイルが存在しないならば、システムはその項目をマウントせずに起動を続行しなければならない(起動を失敗させない) |

## FR-env-03 認証の共有とセッション分離

- 対応要求: RQ-env-03 / AC-01 / AC-06
- 内容: 一度のログインで全コンテナが認証を共有し、セッションと設定はコンテナごとに独立すること。
  同梱する両方のエージェント CLI(Claude Code / Codex CLI)について成り立つこと。
  **認証を削除する操作(`logout` / `reset`)が、確認なしに他プロジェクトの作業を落としたり、
  削除できていないのに成功を表示したり、消したつもりの認証をプロジェクト配下に残したりしないこと**
  (`D0-env-08` 項3〜5)。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN `claude-dev login` を実行したとき、システムは一時コンテナで対話認証を行い、完了後に認証ファイル(`.credentials.json` / `.claude.json`)を `claude-dev-auth` ボリュームへ保存しなければならない |
| 2 | 正常系 | WHEN コンテナを起動したとき、システムは認証ファイルを共有ボリュームから**プロジェクトディレクトリ配下(`/workspace/.claude/`)へ**コピーし、コンテナ内のホームからは symlink で参照させなければならない(`D0-auth-03`) |
| 3 | 正常系 | WHILE コンテナが起動している間、システムは 30 秒ごとに認証ファイルの変更を検知し、共有ボリュームへ書き戻さなければならない |
| 4 | 正常系 | WHILE コンテナが稼働している間、システムはセッション・設定(`settings.json` / `projects/` / `sessions/`)をコンテナ固有に保たなければならない |
| 5 | 正常系 | WHEN `claude-dev logout` を実行したとき、システムは共有ボリュームの認証情報を削除し、**管理ラベルを持つ**実行中コンテナを停止しなければならない(共有ボリューム全体を空にするため Claude Code と Codex CLI 双方の認証が消える)。**管理ラベルを持たない実行中コンテナは停止も削除もせず、受入基準17 に従って名前を表示して残す** |
| 6 | 正常系 | WHEN `claude-dev login-codex` を実行したとき、システムは一時コンテナでデバイス認証を行い、完了後に `auth.json` を `claude-dev-auth` ボリュームの `codex/` サブディレクトリへ保存しなければならない |
| 7 | 正常系 | WHEN コンテナを起動したとき、システムは codex の認証ファイルを**プロジェクトディレクトリ配下(`/workspace/.codex/auth.json`)へ**コピーし、`~/.codex` を同ディレクトリへの symlink にしなければならない |
| 8 | 正常系 | WHILE コンテナが起動している間、システムは 30 秒ごとに codex 認証ファイルの変更を検知し共有ボリュームへ書き戻さなければならない(トークンリフレッシュの伝播) |
| 9 | 正常系 | WHILE コンテナが稼働している間、システムは codex の設定(`config.toml`)とセッション履歴をコンテナ固有に保たなければならない |
| 10 | 境界値 | IF 共有ボリューム側に codex 認証が存在しないならば、システムは何もコピーせずに起動を完了しなければならない(未ログインのままコンテナを使える) |
| 11 | 境界値 | IF 共有ボリュームのファイル所有者がコンテナ内ユーザーと異なるならば、システムは所有者を修復してから処理を進めなければならない |
| 12 | 異常系 | IF 利用者が認証を完了せずにログイン操作を終えたならば、システムは共有ボリュームを変更してはならない(次回起動は未ログインのままとなる) |
| 13 | 異常系 | IF ログイン操作が端末を持たない環境で実行されたならば、システムは非0の終了コードで失敗しなければならない(黙って未認証のまま成功を返さない) |
| 14 | 正常系 | WHEN `claude-dev logout` または `claude-dev reset` を実行したとき、システムは**削除対象のコンテナ名を列挙してから**確認を求め、`y` または `Y` 以外の入力では何も削除せずに終了コード 0 で終わらなければならない |
| 15 | 境界値 | IF `claude-dev logout` または `claude-dev reset` が標準入力を TTY として持たない環境で実行され、かつ `--yes` が指定されていないならば、システムは**何も削除せずに中止**し、`--yes` の指定方法を表示して終了コード 1 で終わらなければならない(黙って 0 を返してはならない) |
| 16 | 正常系 | WHERE `--yes` が指定された場合、`logout` / `reset` は確認を求めずに削除を実行しなければならない |
| 17 | 正常系 | WHEN `logout` / `reset` が削除対象を**集合として列挙する**とき、Claude コンテナは本システムの**管理ラベルを持つものに限らなければならない**(`fwd-*` コンテナ・docker-proxy・共有ボリューム・ネットワーク・イメージは本システムが決めた固定名または固定接頭辞で識別する)。管理ラベルを持たない Claude コンテナは削除せず、その名前と「本変更より前に起動した可能性がある」ことを表示して残さなければならない |
| 18 | 異常系 | IF `logout` または `reset` の削除対象のうち1つ以上が削除できなかったならば、システムは**消えなかった資源を1件ずつ列挙**し、終了コード 1 で終わらなければならない。**「削除しました」「完了」と表示してはならない** |
| 19 | 境界値 | IF `logout` の削除対象が0件(稼働中コンテナが無く、共有ボリュームも空、プロジェクト配下の認証コピーも無い)ならば、システムは確認を求めずに、削除対象が無いことを表示して終了コード 0 で終わらなければならない |
| 20 | 正常系 | WHEN `claude-dev logout` を実行したとき、システムは共有ボリュームに加えて**カレントディレクトリのプロジェクト配下にコピーされた認証**(`.claude/.credentials.json` / `.claude/.claude.json` / `.codex/auth.json`)も削除し、**削除したパスを表示**しなければならない。**他のディレクトリのコピーには触れてはならない** |
| 21 | 境界値 | IF カレントディレクトリにプロジェクト配下の認証コピーが存在しないならば、`logout` はその項目をスキップして処理を続行しなければならない(存在しないことを失敗として数えない) |
| 22 | 正常系 | WHILE `claude-dev reset` を実行している間、システムはプロジェクトディレクトリ配下のファイル(`.claude/` / `.codex/` / `.claude-dev.yaml`)を削除してはならない(`reset` は対象を絞る引数を持たず、どのディレクトリを触るべきか決められないため。`logout` との非対称は `D0-env-08` 項4 が明示する) |
| 23 | 異常系 | IF `logout` または `reset` が削除の途中で `INT` または `TERM` を受けたならば、システムは**進行中の1件を終えてから中断**し、**そこまでに削除した資源と未削除のまま残った資源を1件ずつ列挙**して**終了コード 130** で終わらなければならない。**「削除しました」「完了」と表示してはならない**(1件の削除を途中で切ると、より中途半端な状態が残る。`D0-env-08` 項5 の「失敗を握りつぶさない」は中断時にも及ぶ) |

## FR-env-04 SSH 鍵の限定転送

- 対応要求: RQ-env-01 / AC-01
- 内容: プロジェクトごとに必要な SSH 鍵だけをコンテナへ渡し、秘密鍵ファイルは露出させないこと。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN コンテナを作成するとき、システムは SSH 秘密鍵ファイルをコンテナにマウントしてはならない(転送するのは agent ソケットだけであり、コンテナのライフサイクルのどの時点でも秘密鍵ファイルは渡らない) |
| 2 | 正常系 | WHERE プロジェクト直下 `.claude-dev.yaml` の `ssh_keys` が指定された場合、システムはプロジェクト専用の ssh-agent を起動し、その鍵だけを登録して転送しなければならない |
| 3 | 正常系 | WHEN `claude-dev ssh-keys` を実行したとき、システムは `~/.ssh` の鍵を対話選択させて `.claude-dev.yaml` を生成しなければならない |
| 4 | 境界値 | IF `.claude-dev.yaml` が無い、または `ssh_keys` が空ならば、システムは SSH 転送なしで起動し、指定方法を案内しなければならない(起動を止めない) |
| 5 | 境界値 | IF 指定された鍵ファイルが存在しないならば、システムは当該の鍵を警告してスキップし、残りの鍵で起動を続行しなければならない |
| 6 | 異常系 | IF agent ソケットの位置に壊れた残骸が存在するならば、システムはそれを自動で除去し、除去できない場合も起動を止めず、手動での対処方法を案内しなければならない |
| 7 | 異常系 | IF 端末を持たない環境で `.claude-dev.yaml` が無いならば、システムは対話選択を行わず、空の `ssh_keys` を書き出して起動を続行しなければならない |

## FR-env-05 ネットワーク隔離とファイアウォール

- 対応要求: RQ-env-01 / AC-01
- 内容: レビュー前コードの外向き通信を制御すること。**塞ぐ対象は「ブロック対象ドメイン」**
  (用語集に定義。**設定で与えられるドメイン集合**であり、集合の中身は要件で固定しない。
  既定の内訳は `MODULE-firewall-init` が列挙する)。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN コンテナを起動したとき、システムはコンテナ内でファイアウォールを設定しなければならない |
| 2 | 正常系 | WHILE コンテナが稼働している間、システムはコンテナ間通信を専用ネットワーク `claude-dev-net` 上で行わなければならない |
| 3 | 正常系 | WHEN ファイアウォールを再適用したとき、システムは既存のルールの有無にかかわらず同じ結果にならなければならない(冪等) |
| 4 | 境界値 | IF ブロック対象ドメイン(**設定に列挙されたドメイン**)の名前解決に失敗したならば、システムはその1件だけをスキップして残りの適用を続けなければならない(当該ドメインは塞がれないまま残る) |
| 5 | 異常系 | IF ファイアウォールの適用に失敗したならば、システムはコンテナの起動を中止してはならず、警告を起動ログへ出力したうえで起動を継続しなければならない |
| 6 | 異常系 | IF 適用直後の到達性スモークテストで想定外の結果(**設定に列挙したドメインのいずれかへ到達できた**、または Anthropic API へ到達できない)が出たならば、システムは警告行を出力しなければならない(起動は継続する) |
| 7 | 正常系 | WHEN ファイアウォールの適用を終えたとき、システムは**設定に列挙したドメインの件数と、実際に登録できた宛先 IP の件数**を起動ログへ出力しなければならない(受入基準4 でスキップが起きたことを利用者が数で判別できるようにするため) |

## FR-env-06 ポートフォワード

- 対応要求: RQ-env-01 / AC-02
- 内容: 必要なときだけコンテナ内 Web アプリのポートを公開できること。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN `claude-dev start` したとき、システムは Web アプリ用のポートマッピングを行ってはならない(ブラウザ確認用の noVNC ポートを除く) |
| 2 | 正常系 | WHEN `claude-dev forward <port> [name]` を実行したとき、システムはホスト側ポートを 8100 番台から動的に割り当て、中継プロキシを立てなければならない |
| 3 | 正常系 | WHEN `claude-dev unforward <port> [name]` を実行したとき、システムは当該フォワードを解除しなければならない |
| 4 | 正常系 | WHEN `claude-dev ports [name]` を実行したとき、システムはアクティブなフォワードと noVNC URL を表示しなければならない |
| 5 | 境界値 | IF 同じポートのフォワードが既に存在するならば、システムは二重に作らず、現在のホストポートを表示して終了コード 0 で終わらなければならない |
| 6 | 境界値 | IF 割り当て範囲(8100〜8999)に空きが無いならば、システムはフォワードの作成に失敗し、非0の終了コードで終わらなければならない |
| 7 | 異常系 | IF 対象コンテナが起動していないならば、システムは日本語のエラーを表示して終了コード 1 で終わらなければならない |
| 8 | 異常系 | IF コンテナ側でそのポートが待ち受けていないならば、システムは**中継の作成に成功したこと**(割り当てたホストポートと接続方法)だけを報告し、終了コード 0 で終わらなければならない。**到達性は保証しない**(接続時に接続拒否となる)。到達性の確認は利用者の操作に委ねる |
| 9 | 境界値 | IF **`forward` に** `<port>` として 1〜65535 の範囲外の値(`0` / 負数 / 65536 以上)、または数値として解釈できない文字列が与えられたならば、システムは**自前の検証を行わず** Docker へそのまま渡し、Docker のエラーにより非0の終了コードで終わらなければならない(専用のエラーメッセージを持たない)。**`unforward` に同じ値が与えられた場合は受入基準12 に従う**(該当するフォワードが無いものとして終了コード 0) |
| 10 | 境界値 | IF `<port>` に先頭ゼロを含む表記(`0080`)を与えたならば、システムは(`forward` / `unforward` のいずれでも)それを `80` とは別のフォワードとして扱わなければならない(中継コンテナ名に文字列としてそのまま含めるため) |
| 11 | 異常系 | IF `<port>` を省略して `forward` または `unforward` を実行したならば、システムは使い方を表示して終了コード 1 で終わらなければならない |
| 12 | 異常系 | IF `unforward` の対象となるフォワードが存在しないならば、システムはその旨を表示して**終了コード 0** で終わらなければならない(存在しないことをエラーにしない) |
| 13 | 境界値 | IF `forward` と同時に別のプロセスが同じホストポートを取得したならば、システムはリトライせず Docker の bind 失敗によって非0の終了コードで終わらなければならない(`docs/issues/010` で追跡) |

## FR-env-07 Docker アクセスの制限

- 対応要求: RQ-env-04 / AC-03
- 内容: コンテナ内から Docker を使えるが、ホストを危険に晒す操作は拒否されること。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHILE コンテナが稼働している間、システムはコンテナに Docker 生ソケットをマウントしてはならず、`DOCKER_HOST=tcp://claude-dev-docker-proxy:2375` 経由で使わせなければならない |
| 2 | 正常系 | IF リクエストがホストバインドマウント(`/workspace` 配下を除く)・privileged・ホストのネットワーク/プロセス空間の共有を含むならば、システムはそれを拒否しなければならない |
| 3 | 正常系 | WHERE 呼び出し元の `/workspace` 配下の bind である場合、システムは実ホストパスへ書き換えて許可しなければならない(既定有効。環境変数で切替可能) |
| 4 | 正常系 | WHILE docker-proxy が稼働している間、システムは docker-proxy をホストに公開せず、`claude-dev-net` 内でのみアクセス可能にしなければならない |
| 5 | 正常系 | WHEN 複数プロジェクトのコンテナで同時に `docker compose` を実行したとき、システムは compose プロジェクト名を**起動ディレクトリの絶対パスを含めて**一意化し、生成されるネットワーク名・コンテナ名を衝突させてはならない(**起動ディレクトリ名だけでは正規化後に衝突するため一意にならない**。方法は `CTR-cli-container` が定める。受入基準は `FR-env-01` 受入基準19 と同じ一意化を指す) |
| 6 | 境界値 | IF bind のパスが `..` を含み、正規化するとプロジェクトディレクトリの外を指すならば、システムはそれを拒否しなければならない |
| 7 | 境界値 | IF 呼び出し元コンテナを特定できないならば、システムは `/` 始まりの bind をすべて拒否しなければならない(名前付きボリュームのみ許可) |
| 8 | 異常系 | IF リクエストのボディが解釈できないならば、システムはそれを**中継しなければならない**(独自解釈で誤って弾かない。最終判断は Docker Engine が行う。**この帰結は `D0-sec-05` の委任範囲内で選んだもので、残存リスクは `docs/issues/005` が追跡する**) |
| 9 | 異常系 | IF 上流の Docker Engine への中継に失敗したならば、システムは 502 を返しログへ記録しなければならない |
| 10 | 異常系 | IF 起動時に Docker ソケットが存在しないならば、docker-proxy は起動を中止しなければならない(不正な状態で待ち受けない) |

## FR-env-08 VM モード(オプトイン)

- 対応要求: RQ-env-05
- 内容: bind/compose/privileged が要る重い Docker 案件を、Claude コンテナを特権化せずに扱えること。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHERE `claude-dev start --vm` が指定された場合、システムはゲスト VM を起動し、その中でネイティブ Docker を利用可能にしなければならない |
| 2 | 正常系 | WHERE VM モードで起動した場合、システムは VM モードでも `/workspace` を同一パスで共有(ライブ反映)し、Claude コンテナを privileged 化してはならない |
| 3 | 正常系 | WHILE VM モードでない間、システムは既定の軽量構成(DooD + docker-proxy)で動作しなければならない |
| 4 | 正常系 | WHILE VM モードで動作している間、システムは**資源逼迫**(用語集に定義。ゲスト VM の QEMU プロセスの CPU 使用率が割り当て上限に対して 60% 以上である状態が 15 秒周期で 12 回連続して観測された状態)を検知し、**ヘルスファイルへ `STATE=WARN` を書き、tmux のステータス行へ警告を表示**しなければならない(**この基準が課すのは VM モード側の2つの出力だけである**。用語集「資源逼迫」が挙げるダッシュボードのバナー表示は、同じヘルスファイルを読むオーケストレーター側の描画であり `FR-orch-08` が担う) |
| 5 | 境界値 | IF `--vm` **または `--vm-fresh`** が指定されたが `/dev/kvm` が利用できないならば、システムは起動を中止し終了コード 1 で終わらなければならない(`--kvm` 単独指定は対象外で、警告を出して続行する) |
| 6 | 境界値 | WHEN VM を停止するとき、システムはゲストの正常終了を優先し、それが失敗または利用不能な場合にだけ強制終了へフォールバックしなければならない |
| 7 | 異常系 | IF ゲスト VM の起動に失敗したならば、システムは失敗を明示したうえで既定の DooD 経路を維持しなければならない(VM は使えないが Docker は使える) |
| 8 | 異常系 | IF 再構築が指示されたならば、システムは取得済みのベースイメージを保持しなければならない(完全な作り直しは利用者の明示指定に限る) |

## FR-env-09 イメージ配布(GHCR)

- 対応要求: RQ-dist-01
- 内容: ビルドせずに同一構成のイメージを取得できること。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN 配布ワークフローが起動したとき(日次スケジュールまたは手動実行)、システムは GitHub Actions によりイメージを GHCR へマルチアーキ(amd64/arm64)で push しなければならない |
| 2 | 正常系 | WHEN 日次ビルド(03:30 JST)が実行されたとき、システムはイメージに `YYYYMMDDHHmm`(JST)形式のタイムスタンプタグと `latest` タグを付与して push しなければならない |
| 3 | 正常系 | WHEN 日次ビルドが実行されたとき、システムは同梱する Claude Code のバージョンをその時点の `latest` チャネル公開バージョンに一致させなければならない |
| 4 | 正常系 | WHEN 日次ビルドが実行されたとき、システムは同梱する Codex CLI のバージョンをその時点の npm registry 最新公開バージョンに一致させなければならない |
| 5 | 正常系 | WHEN 利用者が配布イメージを取得したとき、システムはイメージのラベルとして同梱バージョンを参照可能にしなければならない |
| 6 | 正常系 | WHEN `claude-dev pull` を実行したとき、システムは取得したイメージを以降の存在判定で使う名前へ付け替えなければならない |
| 7 | 境界値 | WHERE エージェント CLI の特定バージョンが手動で指定された場合、システムは同梱バージョンをその指定値にしなければならない(不良版を引いた際の切り戻し手段) |
| 8 | 境界値 | IF 同梱するエージェント CLI のバージョンが前回ビルドと同一ならば、システムは当該レイヤーを再ビルドしてはならない(利用者の取得を増分に保つ) |
| 9 | 異常系 | IF **すべての**イメージの取得に失敗したならば、システムは非0の終了コードで失敗を報告しなければならない |
| 10 | 境界値 | IF **一部のイメージだけ**取得でき、**かつ取得できたものの付け替えがすべて成功した**ならば、システムは終了コード 0 で**完了を報告しなければならない**(不足分は次の起動時のローカルビルドで補う) |
| 11 | 異常系 | IF 取得したイメージのローカル名への付け替えに失敗したならば、システムはそれを成功として報告してはならない(**古いイメージを黙って使い続けない**) |

## FR-env-10 macOS 対応

- 対応要求: RQ-env-06
- 内容: macOS でも Linux と同じコマンド体系で開発できること。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN `make install` を実行したとき、システムは OS を判定し、`/usr/local/bin/claude-dev` を Linux では `claude-dev`、macOS では `claude-dev-mac` への symlink にしなければならない |
| 2 | 正常系 | WHERE macOS の場合、システムは SSH agent を TCP ブリッジで転送し、ポートは直結とし、VM/KVM は非対応としなければならない |
| 3 | 正常系 | WHERE Apple Silicon の場合、システムは arm64 ネイティブで動作しなければならない |
| 4 | 正常系 | WHERE Linux と macOS のいずれで実行する場合でも、システムは同名のサブコマンドについて、OS によらず同じ引数と同じ観測可能な結果を提供しなければならない(非対応として明示された機能を除く) |
| 5 | 境界値 | IF macOS で `--kvm` / `--vm` / `--vm-fresh` が指定されたならば、システムはイメージのビルドより前に非対応である旨を表示して終了コード 1 で終わらなければならない |
| 6 | 異常系 | IF macOS で Docker ソケットが検出できないならば、システムは docker-proxy を起動せず `DOCKER_HOST` を設定せずに起動を続行しなければならない(コンテナ内から Docker は使えない) |

## FR-env-11 ブラウザ確認

- 対応要求: RQ-env-02 / AC-01
- 内容: コンテナ内 Chrome の画面をクライアントのブラウザで確認しながらエージェントに操作させられること。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHERE ブラウザ確認ありのイメージの場合、システムは VNC サーバ(ホスト非公開)→ ウィンドウマネージャ → Chrome(リモートデバッグ有効)→ noVNC を起動し、noVNC ポートを 6080 番台から動的に割り当てなければならない |
| 2 | 正常系 | WHEN ブラウザ確認ありの構成でコンテナを起動したとき、システムは chrome-devtools MCP を起動時に自動設定し、既存の MCP 設定の他エントリを保持しなければならない |
| 3 | 正常系 | WHERE ブラウザ確認ありの構成において、システムは日本語入力を提供しなければならない |
| 4 | 正常系 | WHEN コンテナが起動したとき、システムは noVNC の接続 URL を表示しなければならない |
| 5 | 境界値 | IF noVNC に割り当てようとしたポートが競合したならば、システムは別のポートを取り直して最大 20 回まで再試行しなければならない |
| 6 | 境界値 | IF Chrome のプロファイルが他のコンテナと共有されうるならば、システムはコンテナごとに分離しなければならない(同時起動でプロファイルを壊さない) |
| 7 | 異常系 | IF 再試行の上限を超えたならば、システムは作成途中のコンテナを削除し、エラーを出して終了コード 1 で終わらなければならない |
| 8 | 異常系 | IF noVNC ポートが公開されていないならば、システムは URL 行を表示せずに起動を続行しなければならない |

## FR-env-12 同梱エージェント CLI(Claude Code / Codex CLI)

- 対応要求: RQ-env-03 / AC-06
- 内容: コンテナ内で Claude Code と Codex CLI の双方が、追加のセットアップなしに使えること。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN 配布イメージをビルドするとき、システムはブラウザ確認あり/なしの**両方**の配布イメージに Claude Code と Codex CLI の双方を同梱しなければならない |
| 2 | 正常系 | WHEN コンテナ内で `codex` を実行したとき、システムは同梱した Codex CLI を起動しなければならない。コマンドは対話シェル・非対話シェル・`docker exec` のいずれからも解決できなければならない |
| 3 | 正常系 | WHEN 配布イメージをビルドするとき、システムはエージェント CLI を具体バージョンでピン留めして導入しなければならず、起動時や実行時のダウンロードに依存してはならない |
| 4 | 正常系 | WHEN codex が既定設定でシェルコマンド(疎通確認は `/bin/true`)を実行したとき、システムはそれを終了コード 0 で完了させなければならない。**コマンド自身の失敗と、Codex サンドボックスが起動できないことによる失敗を区別**し、後者を起こしてはならない(bubblewrap が使えない環境でも landlock バックエンドで成立させる) |
| 5 | 正常系 | WHEN コンテナを起動するとき、システムは Codex サンドボックスの既定設定 3 鍵(`sandbox_mode = "danger-full-access"` / `approval_policy = "never"` / `[features] use_legacy_landlock = true`)をコンテナ起動時に置かなければならない |
| 6 | 正常系 | WHEN codex が `--sandbox read-only` のようにサンドボックスを明示指定して起動されたとき、システムはそのコマンド実行を成功させなければならない(読み取りは成功し、書き込みは拒否される) |
| 7 | 正常系 | WHILE コンテナが稼働している間、システムは Codex サンドボックスを動作させるためにコンテナの seccomp/AppArmor プロファイルを緩めてはならない |
| 8 | 境界値 | IF 起動時に codex の設定ファイルが既に存在するならば、システムは既に書かれている鍵と値を書き換えてはならず、受け入れ基準5の 3 鍵のうち**書かれていない鍵だけを追記**しなければならない(同じ設定で何度起動しても結果が変わらないこと) |
| 9 | 境界値 | WHERE `workspace-write` が指定された場合は landlock バックエンドでは書き込みが成立しないため本要件の対象外とし、書き込みを伴う自動化は既定設定で実行する |
| 10 | 異常系 | IF 既存の設定ファイルが解釈できない、または追記内容が検証に通らないならば、システムは既存ファイルを書き換えてはならず、警告だけを出して起動を続行しなければならない |
| 11 | 異常系 | IF 既定設定の生成・追記に失敗したならば、システムはコンテナの起動を止めてはならない(codex を使わない利用者の起動を妨げない) |
| 12 | 対象外 | 本要件の対象は開発者が対話的に codex を使うことであり、オーケストレーターが worker/レビューアーとして codex を常用することは対象外とする(`D0-orch-17` 未決) |

## FR-orch-01 2モードのオーケストレーション

- 対応要求: RQ-orch-01 / AC-04
- 内容: 検討(人間主導)と実装(自律・並列)を1つの実体の2モードとして提供すること。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN `claude-dev orchestrate` を実行したとき、システムはブレインストーミングモード(人間×対話 Claude)から開始しなければならない |
| 2 | 正常系 | WHILE ブレインストーミングモードで動作している間、システムはブレインストーミング(人間の検討)を自動化してはならない |
| 3 | 正常系 | WHEN ブレインストーミングで plan が確定し実行が要求されたとき、システムは実行モードへ遷移し、各タスクを worker へディスパッチしなければならない |
| 4 | 正常系 | WHILE オーケストレーターが動作している間、システムはモード間の引き渡し契約を仕様ドキュメントとし、運用状態は `.orchestrator/` に保持しなければならない |
| 5 | 境界値 | IF plan が実行不可(未 ready、または完了条件が欠落)ならば、システムは理由を端末に明示してブレインストーミングへ戻さなければならない |
| 6 | 境界値 | IF 引き渡しの意図が判別できないならば、システムは選択肢(続ける/実行する/終了する)を提示しなければならず、勝手に実行を開始してはならない |
| 7 | 異常系 | IF 古い引き渡し指示が残っているならば、システムは対話を起こす直前にそれを破棄しなければならない(古い指示を誤って消費しない) |

## FR-orch-02 外部制御ループとコントローラ常駐

- 対応要求: RQ-orch-01 / AC-05
- 内容: 継続/停止をコードで判定し、端末が壊れても実行が失われないこと。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHILE 実行モードで動作している間、システムは継続/停止をコード(外部制御ループ)で判定しなければならない(LLM の裁量に委ねない) |
| 2 | 正常系 | WHEN `orchestrate` でコントローラを起動したとき、システムはコントローラを tmux セッション `orch-<project>-main` の `dashboard` ウィンドウで常駐させ、worker とブレインストーミングを同セッションの独立ウィンドウとして管理しなければならない |
| 3 | 正常系 | WHILE 反復が進む間、システムは worker へ渡す**タスク固有の文脈**を次の4種だけで構成しなければならない: (a) plan のゴールと完了条件、(b) 当該タスクの説明と完了条件、(c) **完了した依存タスクの結果要約**(成果物そのものは渡さない)、(d) 前回試行のフィードバック。**リポジトリ全体を渡してはならない**(**全プロンプトに共通して前置・後置される要素はこの4種に数えない**: `ORCHESTRATOR.md` の前置は `FR-orch-08` 受入基準5、VM モードの周知は `FR-env-08`、結果の出力形式の指示は契約 `CTR-orchestrator-prompt` がそれぞれ別に要求する) |
| 4 | 境界値 | IF セッションは存在するがコントローラのプロセスが存在しないならば、システムはそれを空き殻とみなして作り直さなければならない(生存と誤判定してはならない) |
| 5 | 異常系 | IF 対象コンテナが起動していないならば、システムは自動で起動してから続行するか(Linux)、起動を促して終了コード 1 で終わらなければならない(macOS) |

<!-- 受入基準3 の「4種で構成する」は `DSN-prompt-03`(`CTR-orchestrator-prompt`)の列挙と
     1項目も違えないこと。**文脈の長さの上限は要件として課さない**(2026-08-04 に `NFR-perf-03`
     第2文を要件から外して `DSN-prompt-03` へ降ろした裁定を維持する。上限に達した場合の扱いは
     設計判断であり、ここへ引き上げ直さない)。 -->

## FR-orch-03 worker の並列実行と分離

- 対応要求: RQ-orch-01 / AC-04
- 内容: 複数の worker が競合せず並行作業すること。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN worker をディスパッチするとき、システムは各 worker を `/workspace` 内の git worktree で作業させ、ファイル競合を防がなければならない |
| 2 | 正常系 | WHILE worker が実行中である間、システムは worker のライブ出力を当該 worker ウィンドウで確認可能にしなければならない |
| 3 | 正常系 | WHERE 設定ファイルが存在する構成において、システムは並行度(`max_workers`。既定 5)・行き詰まり上限(`stuck_limit`。既定 3)・レビュー反復上限(`max_review_rounds`。既定 10)・レビュー形式エラー上限(`review_format_error_limit`。既定 2)・worker 猶予秒(`worker_grace_seconds`。既定 10)・統合方式(`merge_strategy`。既定 `merge`)を設定ファイルで変更可能にしなければならない |
| 4 | 正常系 | WHEN worker の成果を統合するとき、システムはその統合を直列に(コントローラが)実行しなければならない |
| 5 | 境界値 | IF 着手可能なタスクが並行度の上限(`max_workers`。既定 5)を超えているならば、システムは上限までしか同時にディスパッチしてはならず、超過分は次の空きまで待たせなければならない |
| 6 | 境界値 | IF 依存先のタスクが失敗したならば、システムは依存するタスクをブロック状態にしなければならない(依存を無視して着手しない) |
| 7 | 異常系 | IF worker の出力が期待する形式で得られないならば、システムはそれを失敗として扱い、成功と解釈してはならない |
| 8 | 境界値 | IF 整数の設定値(`max_workers` / `stuck_limit` / `max_review_rounds` / `review_format_error_limit`)に 0 以下または数値として解釈できない値が与えられたならば、システムはその値を**黙って無視**し、より弱い段のマージ結果を保たなければならない(起動を止めてはならない) |
| 9 | 境界値 | IF `worker_grace_seconds` に 0 が与えられたならば、システムはそれを有効な値として受理しなければならない(**猶予を置かずに**強制終了する = 中断シグナルの後に待ち時間を設けない) |
| 10 | 境界値 | IF `merge_strategy` に `merge` / `rebase` 以外の値が与えられたならば、システムは `merge` として扱わなければならない(未知の値をエラーにしない) |
| 11 | 境界値 | WHERE 設定の解決において、システムは 組込既定 → ユーザー設定(`~/.config/claude-dev.yaml` の `orchestrator:` 節)→ プロジェクト設定(`<workspace>/.orchestrator/config.yaml`)→ 環境変数 の4段でマージしなければならない。**環境変数の段は Slack の資格情報(`SLACK_BOT_TOKEN` / `SLACK_CHANNEL`)にのみ適用する** |

## FR-orch-04 介入はタスク単位

- 対応要求: RQ-orch-01 / AC-04
- 内容: 1件の要判断が他の worker を止めないこと。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN あるタスクが介入トリガー(重大判断/曖昧さ/行き詰まり/方針分岐/前提崩れ)に該当したとき、システムはそのタスクだけを待機状態にし、他の worker を継続させなければならない |
| 2 | 正常系 | WHEN あるタスクが判断待ちになったとき、システムは判断待ちを介入キューに積み、ダッシュボードのセレクタで一覧・選択可能にしなければならない |
| 3 | 正常系 | WHEN 人間が回答して対話を終えたとき、システムは同じ worker ウィンドウでそのタスクの実行を復帰させなければならない |
| 4 | 正常系 | IF 判断が `D0-orch-18` の発火条件のいずれにも該当しないならば、システムは仮定を1つ置いて自律継続し、その仮定を状態ストアへ記録しなければならない(記録が無いまま進めてはならない) |
| 5 | 境界値 | IF 同一タスクの試行回数が `stuck_limit`(既定 3)以上に達したならば、システムは介入へ回さなければならない |
| 6 | 異常系 | IF 介入の突合に使う状態が実行中の実体と異なるならば実行が恒久停止するため、システムは実行中の実体に対して突合しなければならない |
| 7 | 境界値 | IF ディスパッチ前のタスクが不可逆と印付けされており、かつ人間の承認がまだ無いならば、システムは worker を起動する前に介入へ回さなければならない |
| 8 | 境界値 | IF 不可逆なタスクが介入で人間に承認されたならば、システムは同じ条件で再度介入へ回してはならない(再開時の無限ループを起こさない) |
| 9 | 境界値 | IF `stuck_limit` の実効値が 0 以下であるならば、システムは**試行回数による発火を行ってはならない**(その試行での行き詰まり検出と worker からの申告による発火だけが残る)。**設定ファイル経由ではこの状態に到達しない**(`FR-orch-03` 受入基準8 により 0 以下は採用されず既定 3 が残る) |

## FR-orch-05 状態の保全と中断・再開

- 対応要求: RQ-orch-01 / AC-05
- 内容: 中断・再開で完了済み作業をやり直さないこと。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN 起動時の自動処理を行うとき、システムは起動時の自動処理で plan/状態/履歴を削除してはならない。片付けは全タスクが完了しているか `--fresh` 指定時のみとし、その場合も `history/<run_id>/` へ退避しなければならない |
| 2 | 正常系 | WHEN 未完了の plan が残る状態で `orchestrate` したとき、システムはその run を継続しなければならない |
| 3 | 正常系 | WHEN 中断した run を再開するとき、システムは完了・失敗・ブロックのタスクを再開時に変更してはならない(途中状態のみ正規化する) |
| 4 | 正常系 | WHEN 中断シグナルまたは終了操作を受けたとき、システムは実行中の worker に中間コミットの猶予を与えて状態を保存し、終了コード 0 でクリーンに終了しなければならない |
| 5 | 正常系 | WHEN 中断した run を再開するとき、システムは各 worker のセッション ID を保存し、同一 Attempt の再開時は続きから再開しなければならない |
| 6 | 境界値 | IF 状態ファイルの書き込み中に異常終了したならば、既存の記録が壊れてはならない(原子的置換または追記のみとする) |
| 7 | 異常系 | IF **永続状態ファイル**(`plan.json` / `state.json` / `intervention/open.json` と履歴)が読み取れないならば、システムは既存の内容を破壊してはならない。**1回だけ消費される制御ファイル(`control.json`)はこれに含まない**(壊れていれば削除して「無い」と同じ扱いにする。契約 `CTR-orchestrator-prompt` が定める) |
| 8 | 境界値 | WHERE 一貫性の単位において、システムは **1ファイル1書き込みを一貫性の境界**としなければならない。複数ファイル(plan・state・追記型ログ・git の統合結果)をまたぐ原子性は提供しない。したがって異常終了の直後に plan と state が食い違う状態が観測されうる |
| 9 | 境界値 | IF 追記型ログ(JSON Lines)への追記中にプロセスが異常終了したならば、システムは末尾に不完全な行が残ることを許容しなければならない(追記時に同期書き込みを行わない)。**製品コードはこれらのログを読み戻さない**ため、再開の判断には影響しない |
| 10 | 異常系 | IF 状態ファイル・plan ファイルが存在しないならば、システムはそれをエラーとせず「未作成」として扱わなければならない |

## FR-orch-06 品質ゲート(相互レビュー)

- 対応要求: RQ-orch-01
- 内容: 実装を独立した観点でレビューして品質を担保すること。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN worker が実装を終えたとき、システムは実装した worker とは別の worker による独立レビューを通さなければならない |
| 2 | 正常系 | WHEN レビューを採点するとき、システムはレビューを当該タスクの完了条件のみを基準に採点しなければならない(プランのゴールで採点しない) |
| 3 | 正常系 | WHEN レビュー結果を受け取るとき、システムは**プロンプトで JSON の形を明示して**構造化出力を要求しなければならない。**ツールによるスキーマ強制は行わない**(形式が崩れた場合の扱いは受入基準7 と6 が定める)。**受け取るフィールドの必須性・型・列挙値は契約 `CTR-orchestrator-prompt` が定める** |
| 4 | 正常系 | WHILE 改訂ループを回している間、システムは改訂ループに終了条件(重大指摘の解消、または最大回数)を設けなければならない |
| 5 | 境界値 | IF タスクに完了条件が未設定ならば、システムはディスパッチ前の検証で弾かなければならない |
| 6 | 境界値 | IF 同一のフォーマットエラーが 2 回続いたならば、システムは自動リトライを打ち切り介入へ回さなければならない |
| 7 | 異常系 | IF レビュー結果が構造化されていない散文で返ったならば、システムは1回だけ再整形を試み、判定内容を変えてはならない |

## FR-orch-07 モデル選択・完了検証・通知

- 対応要求: RQ-orch-01 / AC-04
- 内容: 工程に応じたモデルで動き、状況を非同期に把握できること。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN エージェントを呼び出すとき、システムは工程別にモデル/effort を選ばなければならない(設計系=opus/high、実装・テスト系=sonnet/high、検討・介入・レビュー=opus/high) |
| 2 | 正常系 | WHEN 全タスクが完了したとき、システムは完了条件に対する助言的検証を行い、未充足の可能性を通知しなければならない(実行はブロックしない) |
| 3 | 正常系 | WHILE 実行モードで動作している間、システムは状況サマリ(進捗/ブロッカー/要判断/仮定/次にやること)を非同期に通知しなければならない |
| 4 | 境界値 | IF 通知先が設定されていないならば、システムは通知を行わず、実行を継続しなければならない |
| 5 | 異常系 | IF 通知の送信に失敗したならば、システムは実行を止めてはならない |
| 6 | 異常系 | IF 同一の作業から重複して通知が発火しうるならば、システムは重複を抑制しなければならない |

## FR-orch-08 ダッシュボード UI と日本語提示

- 対応要求: RQ-orch-01 / AC-04
- 内容: 今どのモードで何を待っているかが分かり、誤操作なく移動できること。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHILE ダッシュボードを表示している間、システムはダッシュボードをイベント駆動 TUI で描画し、カーソル選択(↑↓/jk)と Enter 確定でのみウィンドウ移動しなければならない(数字キーでの即時移動・全画面再描画は行わない) |
| 2 | 正常系 | WHEN 人間に選択を求めるとき、システムは人間に選択を求めるとき、選択肢へ必ず番号を付け、番号での回答を受理しなければならない |
| 3 | 正常系 | WHEN 人間の判断・入力を求める表示を行うとき、システムは人間の判断・入力を求める表示(質問・選択肢・メニュー・バナー・理由・人間向け要約)をすべて日本語にしなければならない |
| 4 | 正常系 | WHILE オーケストレーターが動作している間、システムは人間に運用状態ファイルの直接編集を促してはならない(修正は対話へ誘導する) |
| 5 | 正常系 | WHERE リポジトリルートに `ORCHESTRATOR.md` が存在する場合、システムはその内容を各プロンプトの先頭へ前置しなければならない |
| 6 | 境界値 | IF 端末が対話的でないならば、システムは選択を求めずに既定値で処理を進めなければならない |
| 7 | 異常系 | IF 補助情報(VM のヘルス等)の取得に失敗したならば、システムは描画を止めてはならない |
| 8 | 異常系 | IF 異常終了したとしても、システムは端末の状態を復元しなければならない |

## FR-orch-09 自己検証(バンドル題材での実走)

- 対応要求: RQ-orch-01
- 内容: 保守者がオーケストレーターの振る舞いを実題材で確認できること。

受け入れ基準(EARS):

| # | 種別 | 基準 |
|---|---|---|
| 1 | 正常系 | WHEN `make orch-sample` を実行したとき、システムはバンドル題材を使い捨ての作業コピー(独立した git リポジトリ)へ配置しなければならない |
| 2 | 正常系 | WHEN 自己検証を行うとき、システムは配置した題材に対してオーケストレーターを実走させ、自己検証できなければならない(実走は E2E シナリオが担い、`make orch-sample` 自体は配置までとする) |
| 3 | 正常系 | WHEN `make orch-sample-clean` を実行したとき、システムは題材の作業コピーを初期化しなければならない |
| 4 | 正常系 | WHEN 題材の合否を判定するとき、システムは題材の合否を自動テストで機械判定できなければならない |
| 5 | 境界値 | IF 作業コピーが既に存在するならば、システムは再実行で結果を変えてはならない(冪等。明示指定時のみ再初期化する) |
| 6 | 異常系 | IF 題材の配置に失敗したならば、システムは非0の終了コードで失敗を報告しなければならない |

## 要求カバレッジ確認

| 要求 ID | 対応する要件 |
|---|---|
| RQ-env-01 | FR-env-01, FR-env-02, FR-env-04, FR-env-05, FR-env-06 |
| RQ-env-02 | FR-env-11 |
| RQ-env-03 | FR-env-03, FR-env-12 |
| RQ-env-04 | FR-env-07 |
| RQ-env-05 | FR-env-08 |
| RQ-env-06 | FR-env-10 |
| RQ-orch-01 | FR-orch-01 〜 FR-orch-09 |
| RQ-dist-01 | FR-env-09 |

## 未解決事項

- なし(未決の論点は `docs/00-requests/decisions/` の 要確認 `D0-orch-16` / `D0-orch-17` / `D0-env-07` に集約)
