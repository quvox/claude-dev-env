---
id: MODULE-cli-logout
module: MOD-cli-logout
kind: tool
sync: sync
impl: claude-dev::main#logout, claude-dev-mac::main#logout
callers: なし
callees: MODULE-cli-common-container-exists, MODULE-cli-common-lock, MODULE-cli-common-require-setup
contracts: CTR-cli-container
design: DSN-mod-01, DSN-mod-02, DSN-auth-01, DSN-env-01, DSN-env-02
requirements: FR-env-03
tests: なし(未実装。シェル実装のため自動テストランナーが無く実機確認で代替する)
updated: 2026-08-04
summary: Claude と Codex の認証情報を共有ボリュームごと削除する
---
# MODULE-cli-logout 認証の削除

## 目的

共有ボリュームに残っている認証を消し、以後のコンテナが未ログイン状態で起動するようにする
(FR-env-03)。claude と codex の認証が同じボリュームに同居しているため、両方が同時に消える。

## 処理の流れ

1. 引数を解析する(`--yes` のみ。それ以外は無視する)。
2. `MODULE-cli-common-require-setup` で削除用の一時コンテナに使うイメージをそろえる。
3. `MODULE-cli-common-lock` で**共有資源単位**のロック(キー `shared`、操作名 `logout`)を取る。
   取得できなければ**削除を一切行わずに**非0で終わる(`FR-env-01` 受入基準16)。
   **手順2 の `require_setup` によるイメージのビルドはロックの保護対象外**である
   (契約の「排他(ロックキー)」)。
4. 削除対象を列挙する(契約 `CTR-cli-container` の規則A)。
   - **Claude コンテナ**: **管理ラベル `claude-dev.managed=1` を持つものだけ**(稼働中・停止中の
     両方、プロジェクトを問わない)。**ラベルを持たないものは削除対象に入れず**、その名前と
     「本変更より前に起動した可能性がある」ことを別枠で表示する(`FR-env-03` 受入基準17)。
     表示する集合の求め方は契約の「残したものをどう列挙するか」に従う
     (`claude-dev-net` に接続している稼働中コンテナから固定名・固定接頭辞・管理ラベル付きを除いた
     残り)。**停止中のラベル無しコンテナは列挙できないので、その旨も表示する。**
   - **docker-proxy**: 固定名 `claude-dev-docker-proxy` で識別する(ラベルを見ない)。
     ただし削除してよいかは手順8 の遊休判定で決める。
   - **`fwd-*` 中継コンテナは対象にしない**(現行実装の対象範囲を広げない。`logout` は認証を
     消すコマンドであり、中継コンテナの片付けは `stop` / `unforward` / `reset` の担当である)。
   - **`docker ps --filter ancestor=<イメージ>` による逆引きは使わない**(イメージ更新前に
     起動したコンテナを取りこぼし、かつラベルを持たない他人の資源を巻き込むため)。
5. **カレントディレクトリのプロジェクト配下の認証コピー**を削除対象に加える
   (`FR-env-03` 受入基準20)。対象は `$(pwd)/.claude/.credentials.json` /
   `$(pwd)/.claude/.claude.json` / `$(pwd)/.codex/auth.json` の**3ファイルだけ**である。
   ディレクトリ(`.claude/` `.codex/`)は消さない(`settings.json` や `host-hooks.json` など
   認証以外のものが入っている)。**他のディレクトリのコピーには触れない**
   (どこにあるかを本システムは知らず、知る手段も持たない)。
   存在しないファイルは対象から外す(`FR-env-03` 受入基準21)。
6. 手順4・5 の削除対象が0件で、かつ共有ボリューム `claude-dev-auth` が空ならば、削除対象が無いことを
   表示して終了コード 0 で終わる(`FR-env-03` 受入基準19。確認は求めない)。
   **この経路では、手順4 で集めたラベル無しコンテナの列挙も、認証の書き戻しの警告も表示しない**
   (受入基準17 は「削除対象を**集合として列挙するとき**」に掛かる条件であり、
   列挙する集合が空のこの経路には掛からない)。**この非対称は意図されたものではなく、
   ラベル無しコンテナだけが稼働している状態で `logout` を再実行しても書き戻しの警告に
   到達しない** — `docs/issues/052` で追跡する。
7. 確認を取る(`FR-env-03` 受入基準 14〜16)。
   - 手順4 で列挙した**削除対象のコンテナ名**と、手順5 で挙げた**プロジェクト配下の認証ファイルの
     パス**を1行ずつ表示し、共有ボリュームの認証が消えることを明示して `(y/N)` を求める。`y` / `Y` 以外の入力なら**何も削除せず**終了コード 0 で終わる。
   - **標準入力が TTY でなく、`--yes` も指定されていないなら中止する**: `--yes` の指定方法を
     表示して終了コード 1 で終わる(黙って 0 を返さない)。
   - `--yes` が指定されていれば確認を飛ばす。
8. 手順4 の Claude コンテナを1件ずつ `docker rm -f` し、**1件ごとに終了コードを記録する**
   (`|| true` で握るだけにしない)。
9. **docker-proxy は遊休判定を通してから消す**(契約の規則C。`FR-env-01` 受入基準9)。
   `claude-dev-net` に接続している稼働中コンテナから、名前が `claude-dev-docker-proxy` のものと
   名前が `fwd-` で始まるものを除いた集合が**空のときだけ** docker-proxy を削除する。
   空でなければ docker-proxy を残し、**残した理由(稼働中のコンテナ名)を表示する**。
   手順8 で管理ラベルを持たない Claude コンテナを消さずに残しているため、**この判定は空にならない
   ことがある**。判定の問い合わせが失敗したときは遊休でないと判定して残す(安全側)。
   **削除を試みた場合はその終了コードを記録する**(`FR-env-03` 受入基準18)。
   **遊休でないと判定して残した場合は失敗に数えない**(削除しないと決めたものである)。
10. `claude-dev-auth` をマウントした一時コンテナで `rm -rf /auth/* /auth/.*` を実行する。
   続けて同じ一時コンテナで、**列挙の直前に印(`__CLAUDE_DEV_AUTH_LISTED__`)を出してから**
   `/auth` の中身(隠しファイルを含む)を列挙する。**印が出ていなければ一時コンテナ自体が
   起動できていない**(daemon 断・イメージ欠落・マウント不能)ので、
   「消去を確認できなかった」として**失敗に数える**。印の有無を見ないと、
   起動できなかった場合と「消去に成功して空になった」場合がどちらも空出力になり、
   **認証が1バイトも消えていないのに成功と報告される**。
   **失敗の判定は列挙結果だけで行い、`rm -rf` の終了コードでは行わない**:
   `rm -rf /auth/* /auth/.*` は `.` と `..` に当たって非0を返しうる一方、通常のファイルは消えている
   ため、終了コードは「消えたか」を表さない(契約の「エラーケース」)。
   **列挙に残ったパスを記録する。**
11. 手順5・8・**9**・10 で記録した失敗が1件以上あれば、**消えなかった資源(コンテナ名・
    docker-proxy・ボリューム内のパス・プロジェクト配下のパス)を1件ずつ列挙**し、終了コード 1 で
    終わる。成功時の文言は出さない
    (`FR-env-03` 受入基準18)。失敗が0件なら削除結果を表示して 0 で終わる。
    手順5 のファイル削除も**1件ごとに終了コードを記録し、削除したパスを表示する**
    (`FR-env-03` 受入基準20)。
    **手順4 で削除対象から外したラベル無しコンテナは「失敗」に数えない**(削除しないと決めたもので
    あり、消えていないのは意図した結果である)。
12. **削除中に `INT` / `TERM` を受けた場合**は、進行中の1件を終えてから中断し、
    **この検査は手順10 の共有ボリューム消去の直後にも置く**(最後の削除のあとに検査が無いと、
    そこで中断しても成功の文言を出して 0 を返してしまう)。
    **そこまでに削除した資源と未削除のまま残った資源を1件ずつ列挙**して**終了コード 130** で終わる
    (契約の「エラーケース」)。成功時の文言は出さない。
13. ロックを解放する(`trap` により異常終了時も解放される)。

## 呼び出され方

- 契機: 利用者が `claude-dev logout [--yes]` を実行したとき。
- 前提条件: なし。
- 引数:

| 引数 | 型 | 必須 | 制約 | 実装が行う検証 |
|---|---|---|---|---|
| `--yes` | フラグ | 任意 | 確認プロンプトを飛ばす。**端末を持たない環境で実行する唯一の手段**である | 存在するかどうかだけを見る(値は取らない) |
| 上記以外の引数(位置引数 / 未知のフラグ) | — | — | **すべて無視する** | 検証しない |

**確認プロンプトが受理する入力**(`--yes` が無く、標準入力が TTY で、かつ削除対象が1件以上
あるときだけ出す。`read -p "実行しますか？ (y/N) " -n 1 -r`。**1文字だけ読む**。
**`reset` と同じ読み方に揃える** — 同じ受入基準(`FR-env-03` 受入基準14〜16)が両方に掛かるため):

| 入力 | 結果 |
|---|---|
| `y` / `Y` | 削除を実行する |
| それ以外の1文字(`n` / Enter / 空 / 任意の文字) | 「キャンセルしました」と表示して `exit 0`。**何も削除しない**(`FR-env-03` 受入基準14) |
| **非 TTY(標準入力がパイプ・リダイレクト)で `--yes` が無い** | **プロンプトを出さずに中止**し、`--yes` の指定方法を表示して **`exit 1`**(`FR-env-03` 受入基準15) |
| **削除対象が0件**(手順6) | プロンプトを出さない。対象が無い旨を表示して `exit 0`(`FR-env-03` 受入基準19) |

- 認可: CLI を実行できるホストユーザ。

## 連携先と連携内容

### MODULE-cli-common-lock

- 何のために呼ぶか: `start` の認証コピーと重なって**認証が空のままコンテナが起動する**
  (`docs/issues/020`)のを防ぐため。共有ボリュームを空にする操作なので共有資源単位のキーを取る。
- 何を渡すか: キー `shared` と操作名 `logout`。
- 何を受け取るか: 終了ステータス。
- **失敗したときどうなるか**: **何も削除せず**、保持している操作名と再実行の方法を表示して非0で終わる。

### MODULE-cli-common-require-setup

- 何のために呼ぶか: 削除用の一時コンテナに使うイメージを保証するため。
- 何を渡すか: なし。
- 何を受け取るか: なし。
- **失敗したときどうなるか**: `set -e` で非0終了し、認証は消えない。ロックはまだ取っていない。

### MODULE-cli-common-container-exists

- 何のために呼ぶか: 列挙した各コンテナ(遊休判定を通した docker-proxy を含む)が削除の直前に実在するか
  を確かめ、既に消えているものを「削除できなかった」と誤って数えないため。
- 何を渡すか: コンテナ名。
- 何を受け取るか: 終了ステータス。
- **失敗したときどうなるか**: 「存在しない」と判定され、その1件は削除をスキップする
  (**失敗としては数えない**)。

## 戻り値・副作用

| 種別 | 内容 |
|---|---|
| 戻り値 | **0**(すべての削除対象が消えたことを確認したときだけ)。**1**: ロックを取得できない / 端末が無く `--yes` も無い / **削除できなかった資源が1件以上ある**。**130**: 削除中に `INT` / `TERM` を受けた。**0**: 削除対象が0件、または確認プロンプトで `y`/`Y` 以外を入力した(何も削除しない) |
| 永続化 | 共有ボリューム `claude-dev-auth` の中身(claude の `.credentials.json` / `.claude.json` と codex の `codex/auth.json`)を削除する。**管理ラベル `claude-dev.managed=1` を持つ Claude コンテナ**を削除する(ラベルを持たないものは残す)。**遊休のときだけ** `claude-dev-docker-proxy` を削除する。**カレントディレクトリの認証3ファイル**(`.claude/.credentials.json` / `.claude/.claude.json` / `.codex/auth.json`)を削除する(**ディレクトリと `settings.json` / `host-hooks.json` は残す**)。ロックのシンボリックリンク `${HOME}/.claude-dev/locks/shared.lock` を作成・削除する。**`fwd-*` 中継コンテナと共有ネットワークには触れない** |
| 発火するイベント | なし |
| ログ | 標準出力へ削除対象の一覧・確認プロンプト・削除した資源の列挙・残したラベル無しコンテナの名前・docker-proxy を残した理由・他ディレクトリのコピーに触れていない旨。削除失敗の列挙と中断の報告、ロックの取得失敗は stderr |

### 破壊の範囲と順序

**確認プロンプトがある**(`--yes` で飛ばせる。端末が無く `--yes` も無ければ何も削除せず
終了コード 1)。同意すると次の順で進む。

1. `require_setup`(イメージが無ければビルド。**ロックより前**で、失敗すると `set -e` で中止する)
2. **共有資源単位のロックを取る**(取れなければ何も削除せずに終了コード 1)
3. 削除対象を列挙する — **管理ラベル `claude-dev.managed=1` を持つ Claude コンテナだけ**
   (稼働中・停止中の両方、プロジェクトを問わない)、カレントディレクトリの認証3ファイル、
   共有ボリュームの中身。**ラベルを持たない Claude コンテナは対象に入れず、名前を表示して残す**
4. 確認を取る(対象が0件なら確認を求めず終了コード 0)
5. 管理ラベルを持つ Claude コンテナを1件ずつ `docker rm -f` し、**成否を1件ずつ記録する**
6. **遊休判定を通ったときだけ** docker-proxy を削除する
7. カレントディレクトリの認証3ファイルを削除する
8. 一時コンテナで `rm -rf /auth/* /auth/.*` を実行し、**印を出してから列挙**して成否を判定する

**手順5 の対象は「本システムが作った」Claude コンテナに限る**(イメージからの逆引きはしない)。
**`fwd-*` 中継コンテナと共有ネットワークには触れない。**

### 並行性

**共有資源単位のロックを全区間で保持する**(`CTR-cli-container` の「排他(ロックキー)」)。
**待たない**ので、取得できなければ何も削除せずに終了コード 1 で終わる。

| 同時に起きること | 実際の結果 |
|---|---|
| 他プロジェクトのセッションが稼働中 | **管理ラベルを持つものは確認のうえ削除する**(削除対象として名前を列挙してから同意を求める)。**ラベルを持たないものは削除せず名前を表示して残す** |
| `logout` と `start` | **同じキーのロックで直列化される**。`start` は認証コピーからコンテナ作成の確定まで保持するので、**認証が空のまま起動する経路は閉じた**(`docs/issues/020`) |
| `logout` と `login` / `login-codex` | **同じキーのロックで直列化される**。ログインは対話の全区間で保持するため、その間 `logout` は取得できずに終了コード 1 で終わる |
| **ラベルを持たないコンテナを残したまま `logout` した** | **保護されない。** コンテナ内の同期ループが 30 秒ごとに認証を共有ボリュームへ書き戻すため、**最大 30 秒で認証が復活する**。ロックはホスト CLI のプロセス間だけで有効でコンテナ内のループには届かない。**この可能性を警告として表示する**(確実に消すには先に `claude-dev stop <name>` で止める) |
| 利用者が直接 `docker rm -f` する | **保護されない** |

## 異常系

| 条件 | 実際の振る舞い | 呼び出し元への影響 |
|---|---|---|
| **共有資源単位のロックを取得できない** | 保持している操作名と PID、再実行の方法を stderr へ出し、**何も削除せずに** `exit 1` | `start` の認証コピーと重なって**認証が空のまま起動する**ことを防ぐ(`docs/issues/020`) |
| **標準入力が TTY でなく `--yes` も無い** | **プロンプトを出さずに中止**し、`--yes` の指定方法を stderr へ出して `exit 1` | 自動実行で「何もしなかった」ことに気づける(`FR-env-03` 受入基準15) |
| 確認プロンプトで `y` / `Y` 以外を入力した | 「キャンセルしました」と表示して `exit 0`。**何も削除しない** | なし(受入基準14) |
| **削除対象が0件**(管理ラベル付きコンテナが無く、共有ボリュームも空で、カレントの認証コピーも無い) | **確認を求めず**、対象が無い旨を表示して `exit 0` | 消すものが無いのに確認を求めない(受入基準19) |
| **管理ラベルを持たない Claude コンテナがある** | **削除対象に入れない。** 名前と「本変更より前に起動した可能性がある」ことと「停止中のものは列挙していない」限界を表示して残す | 他プロジェクトの作業中セッションを落とさない(受入基準17) |
| 削除対象のコンテナが削除の直前に既に消えていた | `MODULE-cli-common-container-exists` が偽になり、その1件はスキップする。**失敗には数えない** | なし |
| **1件以上の削除に失敗した** | 消えなかった資源(コンテナ名・docker-proxy・ボリューム内のパス・プロジェクト配下のパス)を**1件ずつ列挙**して `exit 1`。**「削除しました」を出さない** | 認証が残っていることに気づける(受入基準18) |
| 一時コンテナの `rm -rf` が非0を返した | **終了コードを見ない。** 判定は削除後の `/auth` の列挙結果だけで行う(`rm -rf /auth/* /auth/.*` は `.` と `..` に当たって非0を返しうるが、通常のファイルは消えている) | 常に失敗扱いになることを避ける |
| `/auth` 直下に消せないものが残った | 残ったパスを1件ずつ列挙して `exit 1` | 認証が残っている |
| **遊休判定の問い合わせが失敗した** | **遊休でないと判定して docker-proxy を残す**。判定できなかったことを表示する。**残したものは失敗に数えない** | 安全側に倒れる |
| **`claude-dev-net` に接続している稼働中コンテナが残っている** | docker-proxy を残し、残した理由(コンテナ名)を表示する。**失敗に数えない** | 残したコンテナの中から Docker が使え続ける(`FR-env-01` 受入基準9) |
| **削除中に `INT` / `TERM` を受けた** | **進行中の1件を終えてから**中断し、そこまでに削除した資源と未削除のまま残った資源を1件ずつ列挙して **`exit 130`**。成功の文言を出さない | 中途半端な状態が可視化される(受入基準23) |
| カレントディレクトリに認証コピーが無い | その項目を対象から外して続行する。**存在しないことを失敗に数えない** | なし(受入基準21) |
| ボリュームが存在しない | `docker run` がボリュームを自動作成し、空のまま終わる | なし |
| イメージが無い | `require_setup` がビルドする。ビルドに失敗すると `set -e` で非0終了し、**認証は消えない**(ロックはまだ取っていない) | 利用者はイメージを用意する |

## 実装上の判断

| # | 判断内容 | 根拠(委任ID) |
|---|---|---|
| 1 | claude と codex を個別に消す分岐を作らない(同一ボリュームに同居させた D0-auth-01 の帰結) | D0-auth-01 |
| 2 | Claude コンテナの列挙を**管理ラベル**で行う(イメージからの逆引きをやめる)。ラベルを持たない既存コンテナは消さずに名前を表示する。**docker-proxy と `fwd-*` は固定名・固定接頭辞で識別し、ラベルを見ない**(名前で所有権が読み取れるため。移行問題も起きない) | D0-env-10 |
| 3 | 削除の成否を**1件ごとに記録**し、最後にまとめて判定する(1件目の失敗で止めない)。途中で止めると、より中途半端な状態が残るため。**ただし失敗を握って成功と表示することはしない** | D0-env-08 項5 が定める形の実装。分岐の置き方は D0-scope-02 |
| 4 | 共有ボリュームの削除の成否を**削除後の `/auth` の列挙結果だけ**で判定し、**`rm -rf` の終了コードは見ない**。`rm -rf /auth/* /auth/.*` は `.` と `..` に当たって非0を返しうる一方、通常のファイルは消えているため、終了コードを混ぜると**常に失敗扱い**になる | D0-env-08 項5 が定める形の実装。判定手段は D0-scope-02 |
| 5 | 対象が0件のときは確認を求めない(受入基準19)。消すものが無いのに確認を求めるのは、利用者に「何かが消える」と誤解させるため | D0-scope-02 |
| 6 | 非 TTY で `--yes` が無い場合の終了コードを **1** にする。`reset` の現行実装は同じ状況で 0 を返すが、自動実行では「何もしなかった」ことに気づけない(`D0-scope-07` の観測点の定義で「利用者が失敗に気づけない」に当たる)ため、`reset` 側も 1 に揃える | D0-env-08 項3(「中止する」)/ `FR-env-03` 受入基準15 |
| 7 | 削除対象を Claude コンテナと docker-proxy に留め、**`fwd-*` 中継コンテナを対象に加えない**(現行実装の対象範囲を広げない)。本タスクの範囲は「対象を広げること」ではなく「巻き込みを止めること」であり、範囲の拡大は別の判断である | D0-scope-02 |
| 8 | **docker-proxy の削除に遊休判定を通す**(手順9)。`logout` は Claude コンテナを消してから認証を消すので「その後は必ず遊休」に見えるが、**管理ラベルを持たない既存コンテナは消さずに残す**ため遊休にならないことがある。そのまま消すと、残したコンテナの中から Docker が使えなくなる(`FR-env-01` 受入基準9 違反) | D0-env-10(判定をラベルに依存させない)/ `FR-env-01` 受入基準9 |
| 9 | **`INT` / `TERM` は進行中の1件を終えてから受ける**(手順12)。1件の削除を途中で切ると、より中途半端な状態が残る。終了コード 130 は SIGINT の慣行値 | `D0-env-08` 項5 / 契約の「エラーケース」 |
| 10 | **プロジェクト配下の削除対象を認証3ファイルに限り、ディレクトリは消さない**。`.claude/` には `settings.json` / `host-hooks.json` / `host-local-bin/` が同居し、これらは認証ではないため。`D0-scope-02` の「利用者のリポジトリを触らない」を `logout` について撤回するのは `D0-env-08` 項4 の決定だが、**撤回の範囲は認証ファイルだけ**に留める | D0-env-08 項4 / D0-scope-02 |
| 11 | **対象をカレントディレクトリだけに限る**。他のディレクトリのコピーの場所を本システムは知らず、探索すると利用者のファイルシステム全体を走査することになる。`start` を実行したディレクトリの記録を持たないため(`claude-dev.project-dir` ラベルは**稼働中のコンテナにしか無い**)、網羅は原理的にできない。**この限界を表示する** | D0-env-08 項4 |
| 12 | Linux 版・macOS 版の**両方に同じ形で**入れる(同じサブコマンドの成否・出力を OS で変えないため) | D0-scope-03 |
| 13 | **削除コマンドを `( trap '' INT TERM; ... )` のサブシェルで起動する**。非対話シェルではジョブ制御が無効で子プロセスがシェルと同じプロセスグループに入るため、端末で `Ctrl-C` を押すと `docker rm -f` 自身にも SIGINT が直接届き、**「進行中の1件を終えてから中断する」が成立しない**。`trap ''` が設定する `SIG_IGN` は `exec` した子へ継承されるので、`docker` がシグナルを無視して削除を完了する。**両 OS で同じ形に書ける**(`setsid` は macOS に無い) | `D0-env-08` 項5 / D0-scope-03 / `/implement` の独立レンズが検出 |
| 14 | **ラベルを持たないコンテナを残したときは、共有ボリュームの認証が書き戻される可能性を警告する**。コンテナ内の entrypoint は 30 秒ごとに認証を共有ボリュームへ書き戻す(`FR-env-03` 受入基準3・8)ため、**残したコンテナが稼働している限り `logout` は最大 30 秒で巻き戻る**。ロックはホスト CLI のプロセス間でしか効かず、コンテナ内のループには届かない。**削除対象を広げること(=ラベル無しコンテナを消すこと)は受入基準17 が禁じている**ので、実装で選べるのは「利用者に知らせる」ことだけである | `FR-env-03` 受入基準17 と受入基準3 の帰結 / `/implement` の独立レンズが検出 |

## 既知の制限

| 制限 | 影響 | 関連 issue |
|---|---|---|
| codex だけ・claude だけのログアウトができない | 片方だけ消したい場合は手作業になる | なし(閾値の外: **`D0-auth-01` がそう決めた帰結**) |
| **プロジェクト配下の認証コピーはカレントディレクトリの分しか消せない** | 他のディレクトリで `start` したプロジェクトには古い認証が残る。**この限界を表示する** | なし(閾値の外: 本システムはコピー先を記録しておらず、探索すると利用者のファイルシステム全体の走査になる。`D0-env-08` 項4 が明示) |
| **管理ラベルを持たない Claude コンテナは削除できない** | 本変更より前に起動したコンテナは `claude-dev stop <name>` で1件ずつ止める必要がある | なし(閾値の外: 名前を**表示する**ので気づけ、表示が空になれば移行完了と判別できる) |
| **停止中のラベル無しコンテナは列挙できない** | 残したコンテナの表示に停止中のものが出ない | なし(閾値の外: `docker network inspect` は稼働中しか返さない。停止中は利用者の作業を壊さず、`stop <name>` で消せる。**この限界を表示する**) |
| **`fwd-*` 中継コンテナは削除しない** | `logout` 後も中継コンテナが残る | なし(閾値の外: 現行の対象範囲を広げないという判断。片付けは `stop` / `unforward` / `reset` が担う) |
| **削除対象が0件のときは、非 TTY で `--yes` が無くても終了コード 0 を返す** | 「非 TTY の `logout` は必ず 1」を前提にした自動実行では判定がぶれる(`reset` は常に 1) | なし(閾値の外: 受入基準19「対象0件は確認を求めず 0 で終わる」が受入基準15 より先に効く。削除は行われないので害は無い) |
| **削除対象が0件の経路では、ラベル無しコンテナの列挙も認証の書き戻しの警告も出さない** | ラベル無しコンテナだけが稼働している状態で `logout` を再実行しても、`logout` が効かない理由に到達できない | `docs/issues/052`(受入基準17 の条件「集合として列挙するとき」と受入基準19 の隙間。振る舞いを変えるかは `FR-env-03` を動かす判断なので本タスクでは決めない) |
| **集合削除の各要素は他プロセスのプロジェクト単位ロックと排他になっていない** | 並行する `stop <name>` が同じコンテナを先に消すと、実際には目的どおり消えているのに「削除できなかった」と報告しうる | なし(閾値の外: `logout` / `reset` は共有資源単位のキーしか取らない。両方のキーを取ると別プロジェクトの `start` を長時間止めることになり `NFR-scale-01` を損なう) |
| **遊休判定に利用者の compose コンテナが入る** | docker-proxy が回収されにくい。**過剰に数える=消さない側**なので受入基準9 は破らない | なし(2026-08-04 の決定シート #1 で人間が案A=現状のままと裁定) |
