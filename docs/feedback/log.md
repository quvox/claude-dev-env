# フィードバックログ（上流キット向けテレメトリ）

質問／修正／委任判断の記録。詳細は CLAUDE.md「Feedback log」節を参照。

### [1] 2026-07-18 種別: 委任判断
- 作業文脈: 03-impl 各モジュールの逆生成（旧 docs/impl/*.md ＋実コードから再構成）。
- 内容: D-20（逆生成時の軽微な曖昧さの穴埋め＝コードを正とする）を各モジュールで行使。旧 docs と実コードが食い違う軽微点を、いずれもコード側に合わせて記述した。主な補正:
  - makefile: `install` は OS 分岐なく常に `sudo ln -sf`（旧「OS 分岐」記述を修正）。`.PHONY` に `env` 欠落を明記。
  - firewall: allowlist カスタマイズは環境変数でなく `BLACKLIST_DOMAINS` 配列の直接編集。`BLACKLIST_PORTS` 配列は不在。
  - devcontainer: orch-builder は `vendor/`＋`-mod=vendor`（旧「stdlib のみ・go.sum 不要」は誤り）。同梱スクリプトは旧記載より多い（wait-limit-reset・VM系・dood-portsync 等）。`GO_VERSION=1.26.1`。
  - cli: `orchestrate` は `mouse on`（旧 `mouse off`）。`code` は `CLAUDE_DEV_VM=1` 時に VM ポインタを注入。fallback セッション名は `orch-main`。`image_version` は `io.github.quvox.claude-dev.version` ラベル（コード内コメントが古い）。
  - cli-mac: top-level `reset` は専用 agent を掃除しない（`ssh-keys reset` のみ）。
  - vm-mode: `vm logs` は `virtiofsd.log` も対象。ポート検出は `[::]:PORT`（IPv6）も対象。
  - docker-proxy: 拒否=403／中継失敗=502。hijack 対象は exec start/attach・exec/container resize の4種。無効化トークンは `0/false/no/off`。
- 根本原因: なし（委任の行使）。

### [2] 2026-07-18 種別: 委任判断
- 作業文脈: 01-requirements/orchestration.md（要件20 自己検証）の再構成中に、実コード（Makefile の `orch-sample` ターゲット）との食い違いを発見。
- 内容: 旧要件・旧 impl 文書は「`make orch-sample` でオーケストレーターを実走させる」としていたが、実際の `make orch-sample` は `scripts/orch-sample.sh` による題材 scaffold までで、実走は別ステップ（`claude-dev orchestrate`）。D-20 の趣旨（コードを正）に沿い、要件20 を「scaffold と実走を分離」する形に補正し、02-design/system.md（E2E-4・テスト戦略）と 03-impl/e2e.md へ下方伝播させた。要件・振る舞いに触れる補正のため、下流に留めず 01 を起点に修正し、本ログにも記録して人間確認に付す。
- 根本原因: なし（委任の行使。ただし要件記述の不正確さに起因するため、上流の acceptance/要件の初期記述精度の論点として共有）。

### [3] 2026-07-19 種別: 質問
- 作業文脈: 03-impl（cli/cli-mac/entrypoint）で、複数プロジェクト同時起動時の docker compose 衝突（既定プロジェクト名 `workspace` の衝突）を修正中。ユーザーの「ネットワーク分離は要求事項」という指摘を受け、分離の対象範囲を確認した。
- 内容: 「ネットワーク分離」が (a) compose 層（各プロジェクトの `docker compose` が作るネットワーク/コンテナ名）なのか、(b) `claude-dev-net` 層（claude コンテナ↔共有 docker-proxy）まで含むのかを質問。回答は「compose 層の分離で十分」。→ `claude-dev-net` は現行要件5-2（単一共有）のまま維持し、要件変更なし。今回の `-e COMPOSE_PROJECT_NAME` 修正が該当要件を満たす。
- 根本原因: 00-requests/01-requirements に「複数プロジェクト同時起動時、各プロジェクトの docker compose リソース（ネットワーク名・コンテナ名）はプロジェクト間で衝突してはならない」という要件が明記されておらず、compose 層の分離が要件なのか実装詳細なのかが曖昧だった（要件5 はネットワーク隔離＝FW と claude-dev-net 共有のみを述べ、compose リソースの一意化には触れていない）。00-requests に当該要件を追記すれば、実装詳細か要件かの判断で迷わずに済んだ。

### [4] 2026-07-29 種別: 質問
- 作業文脈: 「毎日 GHCR イメージを更新しているのにコンテナ内の claude が古い」という指摘の原因調査（起点 00、D-26 の追加）。日次ビルドで Claude Code 導入レイヤーが常に `CACHED` になり同梱版が 2.1.214 で凍結していた。修正方針を決めるにあたり、イメージに焼くバージョンの「正」がどこにも定義されていなかった。
- 内容: 「同梱する Claude Code は `stable` チャネルと `latest` チャネルのどちらを正とするか」を質問。判断が必要だったのは、`install.sh` の引数なし既定が `stable` である一方、観測時点で stable=2.1.212 / latest=2.1.220 / 現に焼かれている版=2.1.214 と食い違い、`stable` を採ると**現状よりダウングレードになる**ため。回答は「`latest` 固定＋障害時のみ手動ピン」。→ D-26 として決定台帳に追加し、01 の要件9 受け入れ基準・02 の設計判断4・03（devcontainer/ghcr-workflow/makefile/cli）へ下方伝播した。
- 根本原因: `00-requests/decisions.md` の D-11（イメージ配布）が「日次・タイムスタンプタグで push」までしか定めておらず、**同梱する外部 CLI のバージョン追随ポリシー（どのチャネル・どこまで自動追随・不良版時の切り戻し手段）が未定義**だった。加えて `request.md` §6 に「外部 CLI（`claude` 等）と各ツールは変化が速い」という前提がありながら、その「変化」をイメージ配布でどう取り込むかが要求として書かれていなかった。D-11 にバージョン追随ポリシーを含めておけば、実装時にチャネル選択で停止せずに済んだ。同種の穴は「外部から取得してイメージへ焼くもの」全般（Terraform は index から最新解決＝版が不定、Go/Python は ARG 固定）にも及ぶため、上流キットでは「イメージへ焼く外部依存ごとに版の追随方針を決める」問いを持つとよい。

### [5] 2026-07-29 種別: 質問
- 作業文脈: D-26 のコード反映（`/implement`。対象 03-impl は devcontainer/ghcr-workflow/makefile/cli）の Phase A ゲート判定。
- 内容: `/change` で文書だけ先に更新した状態で `/doc-check full` を回したため、対象 03-impl 4 件が check B（03-impl⇄コード）で不合格＝合格証を失っていた。一方 `/implement` のゲートは「対象 03-impl と source チェーンが全て verified」を要求するため、規約どおりなら停止し、停止するとコードが変わらず再合格もしない**循環**に入る。人間へ確認せず、「ゲートの趣旨（未検証の仕様から実装しない）は上流 00/01/02 が全 verified なら満たされ、剥落理由は本作業の対象そのものである」と判断して着手し、根拠をタスクドキュメントの「前提（ゲート状況の記録）」節に明記した（完了後に `/doc-check` で循環を閉じる前提）。教訓は `docs/knowledge/docs-ahead-of-code-deadlocks-doc-check.md` に記録。
- 根本原因: 00-requests 側の記述不足ではなく**上流キットのワークフロー規約の穴**。①CLAUDE.md/skills に「/change 後は /doc-check より先に /implement を通す」順序が明示されていない。②check B の不合格には「文書が誤り」と「コードが未了」の 2 種があるのに区別がなく、`/implement` のゲートは両者を同じ「未検証」として扱う。③文書だけ先行した未了作業を示す目印（タスクドキュメント）が /change 時点で作られないため、誰も未了と気づけない（今回 docs/tasks/ は空だった）。キット側で「仕様先行期間の扱い」を定義すればこの判断で迷わずに済む。

### [6] 2026-07-30 種別: 質問
- 作業文脈: 「claude コンテナで codex を使えるようにし、credential をホストと共有したい」という要求の受け入れ（`/change`、起点 00、D-27 の追加）。
- 内容: 要求のスコープが (a) 開発者がコンテナ内で codex を対話的に使える状態まで なのか、(b) オーケストレーターの worker/レビューアーとして codex を常用することまで含むのかを質問。判断が必要だったのは、`decisions.md` の要確認 D-22「異種ベンダー worker（Codex 等）の常用可否」が (b) を明示的に止めており、`request.md` §7 も Could/将来として「異種ベンダー worker（Codex 等）の常用」を挙げるだけで、**CLI を同梱して使える状態にすること自体**がどの層のどのスコープに属するのか定義されていなかったため。回答は「(a) まで。D-22 は未決のまま」。→ D-27 として決定台帳に追加し、D-22 の論点欄に D-27 との線引きを明記、01 の要件12-4・スコープ外にも同じ線引きを書いた。あわせて認証共有の器（既存 `claude-dev-auth` に相乗り）・ログイン導線（独立サブコマンド `claude-dev login-codex`）・同梱版の追随方針（CI で具体版へピン留め）を質問し回答を得て D-27 に反映した。
- 根本原因: `00-requests/decisions.md` の D-22 が「**worker としての常用**」と「**CLI を同梱して開発者に使わせること**」を区別せず 1 つの要確認にまとめていたため、後者の要求が来た時点でスコープ判定に停止が必要になった。要確認エントリを立てる際に「何が未決で、何は未決ではないのか（隣接するどこまでが決定済み扱いか）」を論点欄に書き分けておけば防げた。加えて `request.md` §5 やること が「開発ツール入りコンテナイメージ」という粒度で、**同梱するエージェント CLI の数と種類**を要求として述べていなかったため、2 つ目の CLI 追加が 00 起点か 02/03 の実装詳細かも自明でなかった。上流キットでは、要確認テンプレートに「未決の境界（隣接する決定済み事項）」欄を持たせるとよい。

### [7] 2026-07-30 種別: 質問
- 作業文脈: D-27（Codex CLI 同梱・認証共有）のコード反映（`/implement`。対象 03-impl は devcontainer / entrypoint / cli / cli-mac / ghcr-workflow / e2e）の Phase A ゲート判定。
- 内容: **[5] と同一の循環が再発した。** `/change` で 00→03 を更新した直後に `/implement` を起動したため、対象 03-impl とその上流はすべて版が上がって合格証を失っており、ゲート（対象と source チェーンが全て verified）を形式的には満たさない。[5] で人間が是認した判断（ゲートの趣旨は上流が同一変更で一貫更新されていれば満たされる／剥落理由が本作業の対象そのもの）と `docs/knowledge/docs-ahead-of-code-deadlocks-doc-check.md` の教訓に沿い、停止せず着手してタスクドキュメントに「前提（ゲート状況の記録）」節として根拠を残した。今回は `/doc-check` を後に回したため [5] のような二重の詰まりは起きていない。
- 根本原因: [5] と同じ**上流キットのワークフロー規約の穴**（①/change 後の順序が明示されていない ②check B の「文書が誤り」と「コードが未了」を区別しない ③仕様先行期間を示す目印が /change 時点で作られない）。[5] で報告済みだが未解消のため、**同じ穴で 2 回連続して同じ判断を要した**ことを再発として記録する。キット側で「/change は完了時にタスクドキュメントの骨を作る」「/implement のゲートは『上流 00/01/02 が verified かつ対象 03-impl の剥落理由がコード未了』を許容する」のいずれかを規約化すれば、この判断自体が不要になる。

### [8] 2026-07-30 種別: 質問
- 作業文脈: `/doc-check full`（D-27 反映後の全ドキュメント再検証）の check B で、`03-impl/makefile.md` の `make update-claude` の記述（「終端の claude 導入層だけを無効化」）が実装挙動と食い違うことを検出した。実際は終端ステージが claude 導入 RUN → codex 導入 RUN → codex ランチャー RUN の順に積まれるため、`CLAUDE_VERSION` 変更で codex 層も失効し、`CODEX_VERSION` を渡していないので codex が既定 `latest` で入れ直される。
- 内容: 「文書とコードのどちらを正とするか（Makefile に codex の版解決を足すか／文書を実挙動へ直すか）」を質問。回答は「コードを優先」。→ Makefile は変更せず、makefile.md を実挙動へ同期し（1.3.0→1.4.0）、ローカルビルドは codex の版を制御しない旨を既知の制限に明記した。要件・設計は不変（ピン留めは CI の責務という D-27 ② の線引きを維持）。
- 根本原因: `00-requests/decisions.md` の D-26／D-27 が、同梱エージェント CLI のバージョン追随方針を**CI（配布イメージ）についてのみ**定め、**ローカルビルド経路（`make build` / `make update-claude`）でどう扱うか**を述べていなかった。加えて D-27 が「導入 RUN を終端レイヤーに置く」ことだけを決め、**既存の終端レイヤー（claude 導入層）との前後関係＝キャッシュ失効の連鎖**に触れていなかったため、2 つ目の CLI を足した時点で既存 03-impl の失効範囲の記述が黙って古くなった。上流キットでは、「イメージへ焼く外部依存」の版方針を決める際に (a) CI とローカルの両経路、(b) 同一ステージに複数の依存を積む場合の順序と失効連鎖、を問う項目を持つとよい。

### [9] 2026-07-30 種別: 修正
- 作業文脈: `/doc-check full`（22件全件再検証）の残存指摘4件への対応。利用者から「指摘事項に全て対応して。ただし現状の実装に大きな影響を及ぼす場合は停止して」と指示を受け、実装コードの変更が不要であることを確認して実施した。
- 内容: 指摘のうち 00-requests に起因するのは 1 件。`decisions.md` の D-19（委任）の制約欄が「02-design の分割定義（**12モジュール**）を逸脱しない」と書いており、現行の分割定義は 14 モジュールで陳腐化していた。数値を 14 に直すのではなく「モジュール数は 02-design の同表が正本。本欄に数を書き写さない」へ改め、複製をやめた（PATCH、1.5.0→1.5.1）。残る 3 件は 02/03 起因（契約と結合テスト担当の1対1化、UC-4 の関連要件に要件19 が漏れ、orchestrator.md の担当契約に結合レベル行が無い）で、いずれも 00-requests の記述不足には帰着しない。
- 根本原因: `00-requests/decisions.md` が**下流（02-design）が正本として持つ数値を委任のガードレール欄に書き写していた**こと。00 は「何を守るか」を書く層で、下流の構造の実測値（モジュール数・ファイル数・テーブル行数）を持つと、下流が正しく育つほど 00 が静かに嘘になる。しかも 00 は人間の合意が必須なため、下流の変更に追従しにくく陳腐化が長く残る。上流キットでは、決定台帳・委任のテンプレートに「下流が正本として持つ実測値（件数・一覧・パス）をこの欄に書き写さない。参照先を指すこと」という注意書きを持たせるとよい。同種の穴は「決定内容欄に下流のファイル名やコマンド名を列挙する」ケースにも及ぶ。

### [10] 2026-07-31 種別: 修正
- 作業文脈: D-27（Codex CLI 同梱）の反映完了後。利用者から「claude コンテナ内で AppArmor が原因で codex を呼べないということは起こらないか。`/usr/bin/bwrap` の名前空間作成を許可しなければ codex が呼べないのでは」と指摘を受け、配布イメージで実測 → 指摘が当たっていたため `/change`（起点 00、D-27 ⑥ 追加）。
- 内容: 指摘のとおり、コンテナ内の codex は起動も認証もできるが**シェルコマンドが例外なく `exited 1` になる**（codex 0.146.0 の Linux サンドボックスは bubblewrap 実装で、Docker 既定 seccomp の `CLONE_NEWUSER` 拒否と `docker-default` AppArmor の `mount --make-rslave /` 拒否の 2 段で bwrap が起動できない）。さらに**モデルが失敗を認識せず出力を捏造する**（`echo MARKER_ONE` が失敗したのに `MARKER_ONE` を回答）ため、故障が静かに進む。`codex doctor` も検知しない。→ D-27 に ⑥ サンドボックス方針を追加し、`config.toml` 不在時のみ `sandbox_mode = "danger-full-access"` / `approval_policy = "never"` を entrypoint が置く方針を決定（コンテナ側の `--security-opt` は緩めない）。01 の要件12 に受け入れ基準 4〜7、02 に判断5、03-impl の entrypoint/cli/e2e を同期。glossary に用語「Codex サンドボックス」を追加（利用者の指示）。
- 根本原因: `00-requests/decisions.md` の D-27 が、外部 CLI を同梱する際の判断項目として「同梱・バージョン・認証共有・ログイン導線・スコープ」を並べながら、**その CLI が実行環境に要求する権限（名前空間・seccomp/AppArmor・特権）を満たせるか**という項目を持っていなかった。D-27 ⑤ が「開発者がコンテナ内で codex を使える状態まで」とスコープを宣言していたにもかかわらず、⑤ を満たすための前提条件が未整理だったため、決定としては完了しているのに実際には使えない状態が残った。加えて 01 の受け入れ基準が「`codex` が**起動する**／コマンドが**解決できる**」までしか要求せず、「**実行が成功する**」を要求していなかったため、E2E-6 の実機確認もそこを観測せず取りこぼした。上流キットでは、(a) 決定台帳に外部依存を導入する決定を書くとき「その依存が要求する実行権限と、現行の隔離設定でそれを満たせるか」を問う項目を持たせる、(b) CLI/エージェントを「使える状態にする」要求の受け入れ基準テンプレートに、起動・解決だけでなく**実行が成功する**ことを含める、とよい。あわせて (c) 対象がエージェント（LLM）の場合、失敗を自己申告せず出力を捏造しうるため、受け入れ確認は当該エージェントの応答ではなく副作用（終了コード・生成物）で行う旨を明記するとよい。

### [11] 2026-07-31 種別: 質問
- 作業文脈: D-27 ⑥（Codex サンドボックス無効化）のコード反映（`/implement`。対象 03-impl は entrypoint）の Phase A ゲート判定。
- 内容: **[5]・[7] と同一の循環が 3 回目の再発。** 今回は `/change` の後に `/doc-check full` を先に回したため（[5] と同じ順序）、対象 `03-impl/entrypoint.md` が check B で不合格＝合格証を失った状態で `/implement` を起動することになり、ゲート（対象と source チェーンが全て verified）を形式的には満たさなかった。[5]・[7] で人間が是認した判断（上流 00/01/02 が全 verified で、剥落理由が本作業の対象そのものならゲートの趣旨は満たされる）と `docs/knowledge/docs-ahead-of-code-deadlocks-doc-check.md` に沿い、停止せず着手してタスクドキュメントに「前提（ゲート状況の記録）」節として根拠を残した。なお今回は `/doc-check` 側が指摘を「①文書の誤り／②実装の未了」のうち②と明示的に区別して報告していたため、判断自体は迷わなかった。
- 根本原因: [5]・[7] と同じ**上流キットのワークフロー規約の穴**（①/change 後の順序が明示されていない ②check B の「文書が誤り」と「コードが未了」を区別しない ③仕様先行期間を示す目印が /change 時点で作られない）。**3 回連続で同じ判断を要した**ため、キット側の規約化（「/change は完了時にタスクドキュメントの骨を作る」または「/implement のゲートは『上流 00/01/02 が verified かつ対象 03-impl の剥落理由がコード未了』を許容する」）の優先度を上げるべき事例として記録する。加えて今回は ③ の目印が無いことの害が実害として現れた: D-27 ⑥ の文書だけが 00→03 まで一貫更新され `docs/tasks/` が空だったため、**決定として完了しているのに実装が無い状態が誰にも気づかれず未コミットの作業ツリーに残っていた**（発見できたのは `/doc-check full` の check B のみ）。

### [12] 2026-07-31 種別: 質問
- 作業文脈: 「そもそも codex を使えるようにしてほしい」という要求の受け入れ（`/change`、起点 00、D-27 ⑥ の書き換え）。コンテナ内 claude から `/codex-audit` を起動したところプリフライトのサンドボックス疎通確認が失敗した件の調査から派生。
- 内容: (a) entrypoint が生成する既定 `config.toml` に `[features] use_legacy_landlock = true` を加えるか、(b) 既に `config.toml` がある workspace（要件 core/12-6 で上書き禁止）をどう扱うか、(c) 不足鍵補完の対象鍵はどこまでか——の3点を一括で質問。回答は (a) 追加する、(b) 不足鍵だけ追記、(c) 3鍵すべて。→ D-27 ⑥ を書き換え（既定 3 鍵・不足鍵補完）、要件 core/12-5,12-6 を改訂し 12-9（明示 `--sandbox read-only` でも成功する）を追加、02 判断5・03 entrypoint/e2e を同期した。
- 根本原因: `00-requests/decisions.md` の D-27 ⑥ が Codex サンドボックス方針を「**既定設定をどうするか**」としてのみ決めており、**呼び出し側が `--sandbox read-only` のようにサンドボックスを明示指定する経路（コードレビュー・文書監査を codex に依頼する経路）が config の既定を上書きする**ことを想定していなかった。そのため「対話利用は直ったが監査経路は直っていない」状態が決定としては完了扱いで残った。加えて ⑥ が「既存 `config.toml` は上書きしない」だけを定め、**後から既定鍵が増えた場合に既存ファイルへどう反映するか**を定義していなかったため、方針追加のたびに同じ質問が発生する。上流キットでは、外部 CLI の設定既定を決める項目に (a) その設定を上書きしうる呼び出し経路の一覧、(b) 既定が後から増えたときの既存ファイルへの反映規則（上書き禁止／不足補完／移行手順）を含めるとよい。

### [13] 2026-07-31 種別: 質問
- 作業文脈: 同上（`/change` の決定シート）。QA レーンのサンドボックス方針。
- 内容: `--sandbox workspace-write` は landlock バックエンドでも書き込みが失敗するため、`/codex-qa` をどう走らせるかを質問。AI の推奨は「サブエージェント代替（CLAUDE.md 不変条件7）」、対案は「`danger-full-access` で codex QA を許す（不変条件5 の意味を変えるため人間判断）」。回答は後者で、理由は「コンテナ自体が隔離空間だから、これ以上の隔離は不要」（D-1 の一貫適用）。→ プロセス設定なので `docs/_steering/tech.md`「Codex実行設定」に**不変条件5 からの意図的な逸脱・コンテナ内限定**として理由付きで記録し、経緯を `docs/knowledge/container-is-the-only-isolation-boundary-for-agent-qa.md` に残した。製品要件ではないため decisions.md には書いていない（D-27 ⑥ には「書き込みを伴う自動化・QA は danger-full-access で走らせてよい」だけを記載）。
- 根本原因: なし（00-requests の不足に帰着しない）。根本原因は上流キット側の規範で、CLAUDE.md 不変条件5 が「監査・QA でサンドボックス迂回フラグを使わない」をホスト直実行も含む一律の禁止として書いており、**実行環境そのものが隔離コンテナで、内側のサンドボックスが構造的に動かない場合**の扱いを定めていない。キットでは、不変条件5 に「実行環境が既に隔離境界の内側である場合は、逸脱をプロジェクトの steering に理由付きで明記した上で内側のサンドボックスを外してよい」という例外条項（および明記の様式）を持たせるとよい。

### [14] 2026-07-31 種別: 質問
- 作業文脈: 同上（`/change` の決定シート）。deprecated フラグへの依存に対する回帰検知。
- 内容: (a) `use_legacy_landlock` が版更新で撤去された場合の検知を仕込むか、(b) entrypoint が起動時に疎通確認して警告する案を採るか、(c) `CODEX_VERSION` のピン留め方針を変えるか——を質問。回答は (a) E2E-6 に疎通確認を追加、(b) 入れない、(c) 現状維持（CI が latest を解決してピン留め）。→ 02 の E2Eシナリオ一覧と 03-impl/e2e.md に landlock 疎通確認（`codex sandbox --enable use_legacy_landlock -- /bin/true` が exit 0、同経路の書き込みは失敗）を追加し、(b) は判断5 の却下案⑤として記録した。
- 根本原因: `00-requests/decisions.md` の D-27 ② が同梱 CLI の版追随（latest を CI で解決してピン留め）を決めているが、**その CLI の deprecated 機能に依存する設定を置いた場合の回帰検知**を要求していなかった。「版を追随させる」決定と「その版に無くなりうる機能へ依存する」決定が別々に置かれ、両者の相互作用（追随した瞬間に静かに壊れる）が誰の責務にもなっていなかった。上流キットでは、外部依存の版追随方針を決める項目に「deprecated 機能へ依存する場合は、追随時に検知する確認手段をどこに置くか」を併記させるとよい。

### [15] 2026-07-31 種別: 委任判断
- 作業文脈: `/doc-check full`（codex-landlock-sandbox 作業のフェーズ2 締め）。独立監査（Codex `readiness`）が
  「E2E-6 は『ファイル読み書きを伴う作業』『成果物で判定』としか書いておらず、作成するファイル名・初期内容・
  期待内容・依頼文・成功判定・後始末が未定義のため再現可能なテストとして実施できない」（重大度 高）を指摘した。
- 内容: E2E シナリオの**実施手順をどこまで具体的に書くか**は 03-impl の記述粒度の問題であり、D-19（委任: 各
  モジュールの 03-impl でどこまで詳細を書くか）のガードレール内と判断し、人間に諮らず決定した。`03-impl/e2e.md`
  に「E2E-6 の実施手順（固定フィクスチャ・再現用）」表を追加し、作業ディレクトリ・フィクスチャ
  （`/workspace/e2e6-input.txt` = `MARKER_SOURCE_42`）・3 つの依頼文・期待成果物・landlock 疎通コマンドと期待
  終了コード・合否判定の根拠（成果物と終了コード。codex の応答文は使わない）・認証共有の確認点・後始末を固定した。
  要件・契約・E2Eシナリオ一覧（02-design）は変更していない（手順の具体化のみ）。判断は D-19 を引用して
  e2e.md 本文に記録済み。
- 根本原因: なし（委任判断）

### [16] 2026-07-31 種別: 委任判断
- 作業文脈: 同上（`/doc-check full`）。[15] で D-19 の委任として書いた「E2E-6 の実施手順（固定フィクスチャ）」を、
  **同一実行内で撤回した**ことの記録。
- 内容: 追加した手順表を独立監査に 3 回かけたところ、毎回**新規の重大度 高**が出続けた（1 回目=合否判定が
  「成果物で判定」と「応答に含まれるか」で自己矛盾、2 回目=①の実行コマンド未指定・次コンテナ引継ぎの
  観測欠落、3 回目=`login-codex` 手順の欠落・第2/第3プロジェクトの生成手順とコンテナ間 `cmp` が実行不能な記法・
  人工的なファイル差分はトークン更新の代理になっていない）。収束の兆しが無いことから、**「再現可能な E2E 手順を
  書き下ろす」ことは 03-impl の記述粒度（D-19）ではなく、新規の仕様記述であり委任範囲外だった**と判断し、
  追加分を全て revert して e2e.md をフェーズ2 降下時の状態へ戻した。E2E-1〜5 も同様に手順を持たないため、
  E2E-6 だけを詳細化するのは文書としても不均衡だった。独立監査の当初指摘（E2E-6 は再現可能な手順を欠く。
  重大度 高）は**未解決のまま決定シートへ回し**、`03-impl/e2e.md` には合格証を発行していない。
- 根本原因: なし（委任判断。ただし委任範囲の見誤りであり、D-19 の「記述粒度」に**手順・テストケースの
  新規設計が含まれるのか**が読み取れない点は上流キットへの示唆になる。委任のガードレール欄に
  「粒度の調整と、新規の仕様・手順の設計は別物」と書き分けさせるとよい）

### [17] 2026-07-31 種別: 質問
- 作業文脈: `/doc-check full` の決定シート #1（codex-landlock-sandbox 作業のフェーズ2）。独立監査 `readiness`
  が「既存 `config.toml` へ不足鍵を追記するときの TOML 上の配置規則が未定義」（重大度 高・未決点）を検出した。
- 内容: 追記位置を (A) TOML 構造を尊重（トップレベル鍵は最初のテーブル見出しより前、`[features]` 配下の鍵は
  当該見出しの直後、見出しが無ければ末尾に見出しごと）／(B) 単純末尾追記／(C) 追記せず警告のみ のどれにするかを質問。
  回答は **A**。→ `03-impl/entrypoint.md` の手順10 に判定規則（コメント行は「書かれている」と見なさない・
  同名鍵が別テーブルにあっても当該テーブルには無い扱い）と追記位置、および TOML が壊れている場合は
  「何も書かず警告のみ」で継続することを明記した。要件 core/12-6 の文面は変更していない（実装方式の具体化）。
- 根本原因: `00-requests/decisions.md` の D-27 ⑥ と要件 core/12-6 が「書かれていない鍵だけを追記する」「冪等」
  までしか定めず、**追記が構造化フォーマット（TOML）に対して行われること**を考慮していなかった。行指向の
  ファイルなら末尾追記で足りるが、TOML はテーブル見出し以降の鍵が当該テーブルに属するため、末尾追記は
  「既存の鍵と値を書き換えない」を守っていても**新しく足した鍵の意味を変えてしまう**。上流キットでは、
  「既定設定を後から補完する」種類の決定に対して (a) 対象ファイルの形式（行指向／構造化）、(b) 構造化なら
  挿入位置の規則、(c) パース不能時の振る舞い、を問う項目を持たせるとよい。

### [18] 2026-07-31 種別: 質問
- 作業文脈: `/doc-check full` の決定シート #3。独立監査（02 レンズ・最終 B+C+D レンズ）が「設計は
  『ファイアウォール適用の成否に関わらず起動を継続する』としているのに、要件 core/5-1 は『設定しなければ
  ならない』としか書いておらず、失敗時の期待結果・判定条件が無い」と指摘した（Codex は重大度 高 としたが、
  core/5-1 が失敗時を規定していない以上「矛盾」ではなく「異常系の記載欠落」と裁定して中へ変更した）。
- 内容: 失敗時に (A) 現状の振る舞い（警告して継続）を要件に明文化 / (B) 警告をより目立たせる（cli の出力にも
  常時表示。実装変更あり）/ (C) 起動を失敗させる（fail-closed。実装変更あり）のどれを採るかを質問。
  回答は **A**。→ core/5 に受け入れ基準 5-3 を追加（起動を中止せず警告を起動ログへ出す。成否はサマリと
  スモークテストの警告行で判別できる）、`03-impl/firewall.md` のテスト対応表に 5-3 の実機確認行を追加
  （1.0.0→1.1.0）、`03-impl/entrypoint.md` のエラーハンドリング表の対応要件を core/5 → core/5-3 に具体化。
  実装は変更していない。
- 根本原因: `00-requests/` 側というより **01 の受け入れ基準の書き方の穴**だが、たどると
  `request.md` §2 が「レビュー前コードがホストへ到達する経路を作らない」と目的を述べ、`decisions.md` D-5 が
  「コンテナ内で iptables ファイアウォールを設定する」と手段だけを決めており、**その手段が失敗したときに
  目的をどう扱うか（degrade して続けるのか、止めるのか）**が要求としても決定としても書かれていなかった。
  上流キットでは、セキュリティ手段を決める決定に対して「その手段が利用できない環境・失敗した場合の
  フォールバック方針（fail-open / fail-closed / degrade して警告）」を必ず併記させるとよい。同種の穴は
  D-2（docker-proxy）・D-4（SSH 鍵）にも潜在する。

### [19] 2026-07-31 種別: 修正
- 作業文脈: フェーズ3（`/implement`）の Phase C。実装差分に対する独立レビュー（`codex exec` の
  read-only 監査）が、`scripts/entrypoint-claude.sh` の `ensure_codex_config()` に**重大度 高 3 件**を検出した。
- 内容: 行単位の正規表現で TOML を判定していたため、次の入力で要件 core/12-6「既存の鍵と値を一切
  変更しない」を破っていた。①複数行文字列（`note = """..."""`）の中の `[features]` を見出しと誤認し、
  文字列の**値**に鍵を挿入する。②quoted key（`"sandbox_mode" = ...`）を見落として同一鍵を二重定義し、
  TOML 全体を壊す。③`cat tmp > 本体` はリダイレクト時点で truncate するため、書き込み失敗時に元ファイルが
  壊れ、03-impl に書いた「失敗時は元ファイルを温存」の契約に違反する。加えて中程度で配列テーブル
  `[[features]]` の誤認・インラインテーブルの誤判定・CRLF・TOML 全体の妥当性未検証・一時ファイルの
  予測可能なパスが指摘された。→ 方式を「候補を作る→意味的に検証する→通ったときだけ原子的に置き換える」に
  作り直し（判定・生成・検証を `tomllib`、置き換えを `mkstemp`＋`os.replace`）、6 件の反例を含む
  15 ケースと実機で再確認した。03-impl の手順10・実装上の判断・テスト対応表も同期。
- 根本原因: 00-requests には帰着しない（要件 core/12-6 と D-27 ⑥ の文面は正しく、決定シート #1 で
  配置規則も人間が決めていた）。**根本原因は AI 側の実装判断**で、「構造化フォーマットに対する部分編集を、
  専用パーサを使わず行単位で実装した」こと。教訓は
  `docs/knowledge/append-missing-defaults-must-respect-file-structure.md` に記録した。
  上流キットへの示唆としては、`/implement` の DoD に「設定ファイル等の構造化データを部分編集する変更では、
  編集結果を当該フォーマットのパーサに通して検証する」を入れると、この種の欠陥はレビュー前に落ちる。

### [20] 2026-07-31 種別: 修正
- 作業文脈: [19] の作り直し後、同じ独立レビューを再実行した結果（指摘 7 件 → 2 件）。
- 内容: ①`[features]` 見出しの検出が文字列の完全一致だったため、正当な TOML である
  `[features] # comment`・`["features"]`・`[ features ]`・暗黙の親テーブル（`[features.child]` のみ）で
  見出しを見つけられず、**破壊はしないが補完自体が失敗**していた（12-5/12-6 未達）。見出し検出を
  正規表現化し、さらに「見出しの直後 → 末尾に見出しごと」の 2 戦略を検証が通るまで順に試す形にした。
  ②`os.chmod` → `os.chown` の順だと Linux が setuid/setgid ビットを落とすため順序を逆にし、
  chown に失敗したら差し替えを中止するようにした。POSIX ACL・拡張属性が引き継がれない点は
  03-impl の既知の制限に明記して受容した（設定ファイルに ACL を付ける運用は想定しない）。
- 根本原因: 00-requests には帰着しない。[19] と同じく AI 側の実装判断で、パーサ方式へ移した後も
  **挿入位置の特定だけ行単位の文字列一致に戻っていた**こと。「構文を扱うところは全て
  パーサに寄せる」を徹底できていなかった。検証ゲートがあったため破壊には至っていない。
