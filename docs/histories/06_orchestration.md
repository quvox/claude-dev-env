# 変更履歴: 06_orchestration.md

> 対応文書: `docs/06_orchestration.md`

## 2026-06-28
- 新規作成。`docs/00_idea-2.md`（AI オーケストレーター導入のアイデア）から本システムに適用可能な範囲を取り出し、設計文書として起こした。
- 壁打ちとの議論で確定した主要な設計判断を反映：
  - オーケストレーターは「1 実体・2 モード（壁打ち／実行）」とし、壁打ちを第一級の構成要素として明示。
  - 連続走行は「外部制御ループ（コントローラがループを所有）」方式を採用。Stop-hook による力技は暴走・不可視化のため却下。
  - オーケストレーションを 3 層（L1 推論ループ＝Claude から借用／L2 論理＝指示書＋コントローラ／L3 配管＝自作）に分解。
  - Docker Agent は当面不採用とし、フェーズ 3 で再評価する位置づけに整理。
  - 壁打ちと実行の境界を実装仕様ドキュメントとし、CLAUDE.md の既存規約に接続。
  - 画面/プロセス像を確定：コントローラが端末前景を所有しモードで差し替える。起動時にゴールは渡さず壁打ちから入る。介入時の対話 Claude はフレッシュ起動＋状態から再構成し、先に起動して待たせる。ウィンドウ構成は単一ウィンドウを既定とする。
  - 対話機能（select→submit）は壁打ち/介入で完全保持、worker は人間に直接問わず「完了かエスカレーション」の二択とする方針を明記。
- コントローラの実装言語を **Go** に確定（既存 Docker Socket Proxy が Go 製でビルド/配布の footprint が揃うため）。§4.1 に反映し、§12 未決事項から実装言語の項目を削除。
- 不整合確認に伴う修正:
  - §9 の監査ログ記述を訂正。`save_prompt.sh` はプロンプト先頭を Slack 通知用に退避する別目的スクリプトであり、監査ログ機構ではない。監査ログはコントローラが状態ストアへ記録する旨に修正。Slack 送信は通知の仕組み（`SLACK_BOT_TOKEN`・チャネル・`chat.postMessage`）を再利用する、と精緻化。
  - §5.1 に `claude-dev orchestrate` が既存 `claude-dev code` と同系統のサブコマンドである旨を補足し、既存 CLI（04_cli-reference.md）との整合を明示。
- §9 に「Slack 通知の発信源はコントローラに一本化する」を明示。worker・壁打ち中の対話 Claude は Slack を送らず、通知主体をオーケストレーターに集約することを明確化（従来の `claude-dev start` の hook 方式は別系統である旨も併記）。
- 設計↔実装の整合性確認（独立エージェントによる複数ラウンド検証）に伴う修正:
  - §2.2 の状態機械の図を修正。介入を独立状態として描き、解決後は**実行（executing）へ復帰**する（壁打ちへは戻らない）ことを図・本文で明確化。実装上の状態名 `intervening` を併記。
  - §4.3 の状態ストアの記述を「実装仕様ドキュメント＋…」から「橋となる**運用状態**を永続化（仕様そのものは `docs/` へ書き状態ストアは参照）」へ訂正し、60 の docs/・.orchestrator/ 分離と一致させた。
  - §2.3 に、実行フェーズは実装仕様ドキュメントから構造化タスク計画を導出し、運用状態は状態ストアに保持する旨を追記。
  - §5.3 に Config B の有効化フラグ `claude-dev orchestrate --workers-window` を明記。
- 既存ドキュメントへの波及確認の結果、`bypassPermissions`（entrypoint が settings.json に設定）・hook スクリプト名（`save_prompt.sh` / `sendslackmsg.sh`）・tmux プレフィックス（`C-_`）の参照が実体と一致することを確認。既存文書の更新は不要と判断（`docs/impl/00_overview.md` の「ホスト側オーケストレーション」は Docker リソース管理を指す別義で、本設計の AI オーケストレーターとは無関係）。
