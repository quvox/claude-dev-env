---
id: 2026-08-07-doc-check-ssot-clause-ids-recertification
date: 2026-08-07
task: task-clause-ids-and-split-policy(`/task-close` フェーズ4 §6 からの `/doc-check ssot task-<slug>`)
origin_layer: 01
issue: docs/issues/073(本実行で解消して削除)/ docs/issues/077・078(本実行で起票)
summary: 条項ID・分割可否の反映後に SSOT 48 ファイルを再認証し、用語集違反3件と「未検証の全件」節の計上漏れ5件と手書き集計1件と理由文の不一致1件を自動修正した
---

# 2026-08-07 条項ID 反映後の SSOT 再認証(`/doc-check ssot`)

## 変更理由

`task-clause-ids-and-split-policy` の変更指示35件が SSOT へ反映され(コミット `d19e806`)、
01 の `functional.md` / `non-functional.md` / `usecases.md`、新設の `decisions/split.md`、
02 の `system.md`、03 の `tests/` 30 ファイルの版が上がった。その下流にあたる文書の
**検証済み記録が失効した**ため、`/task-close` のフェーズ4 §6 が `/doc-check ssot` を起動した。

再認証の前に検査 A〜E を走らせたところ、**反映そのものは全38節が変更指示と逐語一致**していた一方、
下の3種類の欠陥が見つかったので、検証済みにする前に直した。

## 変更内容の要約

- **用語集が定義する「安全」の単独使用を3件直した**(同型の欠陥。走査で全件確定)。
  `docs/00-requests/terminology.md` は「安全」を
  **「ホストのあらゆる情報を破壊しないこと、および鍵情報が直接漏洩しないこと」**と定義し、
  **「定義を指さずにこの語を単独で使わない」**と明記している。
  うち2件は本タスクが今回追加した文(`D1-split-01` の `NFR-scale-01` の理由)に入っていた。
- **「未検証(テスト未実装)の全件」節への計上漏れを5件直した**(同型の欠陥。走査で全件確定)。
  いずれも「契約の結合テスト」表で状態が `未検証(テスト未実装)` の行が、同じファイルの
  全件節に載っていなかった。`.claude/directions/03-impl.md` は
  「全件ここに集約し、**何が満たされれば未検証でなくなるか**を書く」「**Never leave one silently**」
  と定める。
- **`tests/strategy.md` の手書き集計を実数へ直した**(`docs/issues/073` を解消して削除)。
  「機能要件の全 182 基準 / 対応表は 197 行」→ 実数は**条項 201 件 / 対応表 216 行**。
- **`docs/issues/077` を起票した**(直さなかったもの)。条項ID への移行が
  「受入基準 ⇄ テスト対応表」だけに適用され、同じファイルの「未検証の全件」節の「対象」列は
  旧表記のまま残っている(23ファイル・181箇所)。**根本原因は
  `.claude/templates/03-tests-module.md:58` の例示が旧表記であること**で、`docs/` 側だけを
  直しても再発するため `/kit-improve` 案件として残した。
- **`D1-split-01` と `NFR-perf-01` の理由文の不一致を1件直した**(再監査が単独で検出)。
  要件表は「再取得される層が**1つ**減るだけでも」、`split.md` は「減るだけでも」で
  「1つ」が脱落していた。`split.md` 側を要件表へ揃えた
  (`D1-split-02` の委任範囲「表現の平明化」の内側であり、名指す条項の集合は変えていない)。
- **`docs/issues/078` を起票した**(直さなかったもの)。frontmatter の値がバッククォートで
  始まっていて YAML として解析できないファイルが2件ある(`docs/feedbacks/018` と
  `docs/issues/076`)。**仕様ドキュメント4層には1件も無い**ため SSOT 外として残した。
- **版を持つ 48 ファイルの検証済み記録を再発行した**(SSOT の版を持つ 65 ファイルは
  これで全件が検証済みとなり、失効・未検証は0件になった)。

## 更新したドキュメント

| ドキュメント | version 遷移 | 何を変えたか |
|---|---|---|
| `docs/01-requirements/system.md` | 1.0.1 → 1.1.0 | `SR-04` の備考「本システムの安全性は…」を用語集の定義を指す形へ |
| `docs/01-requirements/non-functional.md` | 1.4.0 → 1.5.0 | `NFR-scale-01` の `段階可` の理由から「安全」の単独使用を除いた |
| `docs/01-requirements/decisions/split.md` | 1.0.0 → 1.1.0 | `D1-split-01` の 3.(`NFR-scale-01`)の理由を同上に揃え、1.(`NFR-perf-01`)の理由に脱落していた「1つ」を補って要件表と逐語一致させた |
| `docs/03-impl/tests/strategy.md` | 1.1.2 → 1.2.0 | 「カバレッジの扱い」の手書き集計を実数(201条項 / 216行)へ |
| `docs/03-impl/tests/cli-stop.md` | 1.3.0 → 1.4.0 | 全件節に `CTR-cli-container` の1行を追加 |
| `docs/03-impl/tests/cli-logout.md` | 1.3.0 → 1.4.0 | 同上 |
| `docs/03-impl/tests/cli-reset.md` | 1.2.0 → 1.3.0 | 同上 |
| `docs/03-impl/tests/entrypoint.md` | 1.1.0 → 1.2.0 | 全件節に `CTR-cli-container` と `CTR-entrypoint-firewall` の2行を追加 |
| 上記以外の 40 ファイル | 版は据え置き | 内容は変えず、検証済み記録だけを現在版へ再発行した |

## 検査結果

- **条項単位カバレッジは機械照合で完全一致**: 01 の条項 201 件・NFR 13 件・SR 21 件が
  02 の要件カバレッジ表 235 行と双方向で差分ゼロ。主担当が2行以上ある条項は0件、
  `充足` 列の語彙違反は0件、`部分(P-005)` の2条項は `docs/pendings.md` P-005 が
  条項ID で名指していることを確認した。
- **`不可分` × `部分` は0件**(PASS をブロックする条件に該当しない)。
  `FR-env-01-19` / `FR-env-07-5` は `段階可` × `部分` であり、規範どおり重大度「中」に留める。
- 03 のテスト対応表は 01 の全条項・全 NFR・02 の全 `E2E-nn` を覆っており、
  実在しない条項ID の参照は0件。
- 機械検査: `check-relations.py` 合格 / `check-contracts.py` 合格 /
  `callgraph-check.py` 重大度「高」0件 / コールグラフと `feature-graph.md` は最新 /
  `build-index.py --check` 差分なし / SSOT 一括検査は基準線と同一の31件
  (CS8 8件は `pendings.md` P-002・P-003 が、CS11 23件は `docs/issues/054` と `075` が追跡)。

## 独立レビュー

Codex CLI が利用上限(2026-08-11 まで)のため起動できず、**人間の常設承認に基づき
読み取り専用サブエージェント(model: sonnet)で代替した**(`lens: subagent`)。3本実行:
縦整合(00→01→02)/ 03-impl/tests の整合 / 修正箇所に絞った再監査。
2本目は上の「計上漏れ5件」「手書き集計」「旧表記181箇所」を**独立に検出**しており、
Claude 側の走査と件数まで一致した。
