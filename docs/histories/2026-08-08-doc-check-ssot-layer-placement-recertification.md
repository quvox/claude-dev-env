---
id: 2026-08-08-doc-check-ssot-layer-placement-recertification
date: 2026-08-08
task: task-layer-placement(`/task-close` フェーズ4 §6 からの `/doc-check ssot task-<slug>`)
origin_layer: 02
issue: docs/issues/094(#5 を追加し走査条件の欠陥を記録)/ docs/issues/095(母集団を 34 → 24 に訂正)
summary: 記述を本来あるべき層へ移す反映のあと、02 の検査コマンドの合格条件と logging.md の必須節を直して 58 文書を再認証し、独立レビュー4本が再検出した値の重複(issue 094 #1)を決定シートへ送った
---

# 2026-08-08 層の置き場の反映後の SSOT 再認証(`/doc-check ssot`)

## 変更理由

`task-layer-placement` の変更指示 19 件が SSOT へ反映され(コミット `d213b9d`)、00 の7ファイル・
01 の3ファイル・02 の5ファイル・03 の5ファイルの版が上がった。その下流を含めて
**検証済み記録 58 件が失効した**ため、`/task-close` のフェーズ4 §6 がこの実行を起動した。

再認証の前に検査 A〜E を走らせたところ、**反映そのものは変更指示と一致**しており(削除ゲートの
検査 (a) が 19/19 反映済み、`CS18` が 0 件)、移送はすべて両側が書かれていた。一方で
次の6件を検証済みにする前に直した(4〜6 は**改訂後の再監査**が検出したものである)。

## 直したもの

| # | 種別 | 内容 |
|---|---|---|
| 1 | **02 の合格条件が実測と噛み合っていない(C12)** | `docs/02-design/environments.md`「ドキュメント整合検査コマンド」#6 が `relations-coverage.py` の合格条件を「片側にしか無いものが 0 件」と書いていたが、このスクリプトは**未記載 30 件を検出して終了コード 1 を返し続ける**。30 件は全件が `scan-entrypoints.py` の Go `switch` 誤検出で、その事実は `docs/03-impl/index.md`「`relations-coverage.py` 最終結果」が既に記録している。**同じ節が「この順で実行し、すべて合格して初めてドキュメントが整合しているとみなす」と課しているため、literal に読むと整合は永久に成立しない。** 2026-08-03・2026-08-04 の histories が同じ回避を都度記録しており、判断のやり直しが3回起きていた。合格条件を「既知の誤検出を除いて 0 件」とし、内訳の正を 03 側へ指す形にした |
| 2 | **必須テンプレート節の欠落(C9)** | `docs/02-design/logging.md` に `## 未解決事項` の節が無かった(`.claude/templates/02-logging.md:65` が要求し、02 の他の3ファイル(`architecture.md` / `system.md` / `environments.md`)はすべて持つ)。節が無いと「未解決事項が本当に無いのか、節ごと書き落ちたのか」を文書から判別できない。`- なし。` を追記した(全文を読み、未解決の記述は無いことを確認済み) |
| 3 | **同型の欠陥の母集団が反映でずれた(`docs/issues/095`)** | 程度語「通常」の全件が起票時 34 箇所(00: 4 / 01: 2 / 02: 5 / 03: 23)だったが、本タスクの反映が 10 箇所を消して **24 箇所**(00: 1 / 01: 0 / 02: 4 / 03: 19)になっていた。issue 自身が「進捗は `CS8` の件数で測れる」と定めているため、母集団を実測値へ訂正し、箇所の一覧も現在の行番号へ置き換えた |
| 4 | **テンプレートの行集合の欠落(C9)** | `docs/02-design/environments.md`「Codex実行設定」表が、`.claude/directions/02-design.md:197`「**`.claude/templates/02-environments.md`「Codex実行設定」の行集合が正であり、その全行を保持する(未記入は `未定` で埋める)**」に反していた。`モデル・reasoning` の3行(増分監査 / full監査 / QA)が1行に統合され、`QA シードコマンド` / `QA リセットコマンド` の2行も統合され、**`代替レビュー(サブエージェント)のモデル・reasoning` の行が丸ごと無かった**。**この欠けた行は不変則7 が依存する行である**(Codex 不在時に走る代替レンズの強度をプロジェクトが常設指名する場所であり、本実行の4本もここが未記入だったためキットの既定で走った)。テンプレートどおり6行へ分け、いずれも `未定` とし、**未記入ならキットの既定が効くこと**を各行に書いた。値そのものは決めていない(`docs/issues/031` が「モデルの選択は人間の判断」と裁定済み) |
| 5 | **合格条件が「中」の扱いを書いていない(C12)** | 同ファイルの検査 #3(`callgraph-check.py`)の合格条件が「重大度「高」が 0 件。低・参考は残ってよい」で**「中」に触れていなかった**。実測では「中」6件が残っており(`03-impl/index.md`)、この行だけでは「中」が合格を妨げるのか判定できない。「中・低・参考は残ってよい」とし、件数と内訳の正を 03 側へ指した(同ファイルが既に「full 最終監査で「中」が PASS をブロックするか = いいえ」と定めているのと整合する) |
| 6 | **同型の走査が構造的に取りこぼしていた(`docs/issues/094`)** | 独立レビューが `D0-env-06` の値 `container=docker` が 00(`decisions/env.md:139`)と 02(`contracts/cli-container.md:65`「渡す環境変数」の `container` 行)の両方に在ることを検出した。094 の走査は最終行で **`01` を必ず含む組だけ**を出しており、**00⇄02 の重複を構造的に見落としていた**。走査条件を直して再実行し(候補 35 件。うち 34 件は名前と実測の引用で対象外)、母集団を 4 件 → **5 件・のべ 15 箇所**へ訂正した。あわせて**この型はトークン一致だけでは閉じない**ことを記録した(00 は `container=docker` と1トークンで書き、02 は変数名の欄と既定値の欄に分けて書くため、同じトークンにならない) |

## 更新したドキュメント

| ドキュメント | version 遷移 | 何を変えたか |
|---|---|---|
| `docs/02-design/environments.md` | 1.2.0 → **1.3.0** | 検査 #6 の合格条件を「既知の誤検出を除いて 0 件」とし内訳の正を `03-impl/index.md` へ指した / 検査 #3 の合格条件に「中」の扱いを明記した / 「Codex実行設定」表をテンプレートの行集合へ戻した(`モデル・reasoning` を3行へ、`QA シードコマンド` / `リセットコマンド` を2行へ、**`代替レビュー(サブエージェント)のモデル・reasoning` を新設**)|
| `docs/02-design/logging.md` | 1.4.0 → **1.4.1** | `## 未解決事項`(`- なし。`)を追記。**意味は変えていないので PATCH** |
| 上記2件を含む **58 文書** | 版は据え置き(上記2件を除く) | **検証済み記録(`verified`)を再発行した。** 内訳は 00 が7・01 が5・02 が10・03 が36。**失効 0 / 未検証 0** |

**機械検査の数値**: `CS18` 0 / `CS11` 19 / `CS19` 26 / `CS20` 53(いずれも据え置き)。**`CS8` は 32 → 36 に増えた** — 「Codex実行設定」表をテンプレートの行集合へ戻したことで `未定(` の行が4つ増えたためである。**増加は規範の要求そのもの**(`.claude/directions/02-design.md:198`「未記入は `未定` で埋める」)であり、DoD が凍結した上限 42 を超えていない。

`docs/issues/094` と `docs/issues/095` を書き替え、`docs/issues/index.md` を `build-index.py` で
再生成した。

## 実装したもの

| 対象 | 内容 | コミット |
|---|---|---|
| なし | **コードは1行も変えていない**(この実行はドキュメントの検証と再認証だけを行う) | — |

## 機能間連携仕様書の変化

| 種別 | ID | 内容 |
|---|---|---|
| なし | — | `relations/MODULE-*.md` の本文は1文字も変えていない。`check-relations.py` は 83 ファイル / 83 ID で合格 |

## 副産物

| 種別 | 行き先 | 内容 |
|---|---|---|
| 既存 issue の更新 | `docs/issues/094` | **#5 を追加**(値 `container=docker`。`D0-env-06` ⇄ `CTR-cli-container`)。走査条件の欠陥と再走査の結果を記録。**#1 の severity は起票時の裁定どおり「中」に据え置いた**(AI の裁定。理由と覆し方は issue の「経緯」に明記) |
| 既存 issue の更新 | `docs/issues/095` | 母集団を 34 → 24 箇所へ訂正。反映が消した 10 箇所を書き替え方の実例として記録 |
| 人間へお伺いする論点 | 本実行のレポート(`docs/issues/094` が持続する記録) | **`docs/issues/094` #1 の所有者をどの層にするか。** 独立レビュー2本が重大度「高」で独立に再検出したが、直しは `D0-dist-04` 項6(00)の意味に触れるため `.claude/directions/layer-fit.md` §2.1 により決定シートが要る。**この実行では閉じられない** |
| 検証できなかったこと | — | **独立レビューは Codex ではなくサブエージェント**(Codex はアカウントの利用上限。復旧は 2026-08-11)。`lens: subagent` 4本。**検証済み記録はこの弱いレンズの下で発行されている** |
| 完了を妨げない失効 | `docs/tasks/task-layer-placement/memo.md` | 「読む範囲(読了記録)」の 16 件が反映で失効した(`check-sheet.py` の `SH8`)。これはフェーズ2の入場ゲートであり `close-task.py` の削除ゲート (a)〜(f) には含まれないので完了を妨げない |
