---
id: 2026-08-19-doc-check-ssot-fix-tmux-server-drops-reserved-env
date: 2026-08-19
task: task-fix-tmux-server-drops-reserved-env(フェーズ4 の `/doc-check ssot`。タスクは未完了のまま)
lane: standard
origin_layer: 03
issue: なし(新規の起票なし)
summary: 反映直後の SSOT 9文書を検証して合格証を7件発行し、フェーズ3 の実装で腐ったコード引用14トークンを実コードへ当て直し、02 側に取り残されていた FR-env-14 の3箇所と `03-impl/index.md` の「02 との差分なし」の偽を直した
---

# 2026-08-19 `/doc-check ssot task-fix-tmux-server-drops-reserved-env`

## 変更理由

### R-01 反映で合格証が外れた7文書に、合格証が要る

- 起点層・根拠: 原則6(合格証は文書ごと。`/doc-check` だけが書く)。
- 変更が必要になった条件: `/task-close` が変更指示8本を SSOT へ反映し、closure の9文書の
  `version` を上げて `verified` を落とした。この実行はその状態を検証して合格証を発行する。

### R-02 フェーズ3 の実装で、行番号つきのコード引用が腐った

- 起点層・根拠: 原則2(コード ⇄ 03-impl は完全に一致する。事実の食い違いはコードを正とする)。
- 変更が必要になった条件: コミット `44278fe` が `scripts/entrypoint-claude.sh` を
  **4ハンクで +3 / +2 / +4 / +8 行**、`claude-dev` / `claude-dev-mac` を **+3 行**変えた。
  区間ごとの累積は entrypoint が **旧124行目以降 +5 / 旧488行目以降 +9 / 旧780行目以降 +17**、
  CLI 2本が **旧1604 / 1680 行目以降 +3** である。**フェーズ3 の `/doc-check` は
  一律 +4 / +8 / +16 と +0 を当てており、区間ごとに1行ずつ、CLI 2本は3行ずれたまま反映された。**
- 増分の判定: 旧版と現行版の行対応を `git diff -U0` のハンク表から機械的に作り、
  反映前後の全引用を突き合わせて**ずれている 14 トークンだけ**を対象にした。

### R-03 新設した条項 `FR-env-14-11` に 02 の3箇所が追随していなかった

- 起点層・根拠: 検査 A3・E(02 ⇄ 03 の突き合わせ)。
- 変更が必要になった条件: 03 側(`MODULE-entrypoint-claude` の `requirements`)は `FR-env-14` を
  持つのに、`02-design/system.md`「モジュール分割定義」・`02-design/contracts/cli-container.md`
  「対応要件」・`02-design/relations.md` の `PLAN-entrypoint-claude` の3箇所が持たない。
  フェーズ2 は「変更指示に無く、足すと 10,782 バイトの節を丸ごと置き換えることになる」として
  見送ったが、**`ssot` モードは SSOT を直接編集するのでその制約は無い**。

### R-04 「02 との差分なし」が `docs/issues/108` と矛盾していた

- 起点層・根拠: 検査 C12(pendings / issues が完全性の主張と矛盾しないこと)+ 検査 E。
- 変更が必要になった条件: `docs/03-impl/index.md`「02 との差分(未解消のもの)」が空表 +
  「差分なし」と書く一方、`docs/issues/108`(severity 中 / origin_layer 02)は
  「`CTR-cli-container` の到達の義務が受け側を `entrypoint` と名指しているため、
  ホスト CLI が作る経路2 を覆っていない」と記録している。これは 02 ⇄ 03 の差分そのものである。

### R-05 `MODULE-entrypoint-claude` 手順20 の「各ウィンドウ」が実コードより広かった

- 起点層・根拠: 原則2(検査 B4)。独立レビュー L-18。
- 変更が必要になった条件: 手順20 は「各ウィンドウの対話シェルは `exec zsh -l` で起こす」と
  書いていたが、`scripts/entrypoint-claude.sh:792`〜`:793` の `exec zsh -l` が掛かるのは
  `new-session` が同時に作る**最初の窓だけ**で、以後の窓は `scripts/tmux.conf:15` の
  `default-command /bin/zsh`(`-l` 無し)で起こる。**結論(rc の `export` は効く)は真だが、
  理由が偽**であった。

### R-06 `D1-split-01` の条項数が `FR-env-07-13` の新設に追随していなかった

- 起点層・根拠: 検査 A1(01 層内部の整合)。独立レビュー L-01。
- 変更が必要になった条件: `docs/01-requirements/functional.md` は `FR-env-07` を
  「他の**12**条項は不可分」と書き替えたのに、その根拠である
  `docs/01-requirements/decisions/split.md:45` は「他の**11**条項」のままだった。
  `段階可` の集合(`{FR-env-07-5}`)は動いていないので、`D1-split-02` のガードレール
  (集合の増減・理由文が名指す条項の変更は委任範囲外)には触れない算術の訂正である。

## 変更内容の要約

- **合格証を7件発行した**(下表)。`MODULE-*.md` は版を持たないので層代表
  `docs/03-impl/index.md@1.30.0` が代表する(`.claude/directions/03-impl.md`「Frontmatter」)。
- **コード引用 14 トークンを実コードへ当て直した(PATCH)。** 内訳:
  `03-impl/contracts/cli-container.md` 7(`:480`→`:481` ×2 / `:564`・`:621`・`:686`→`:565`・`:622`・`:687` /
  `:517`〜`:523`→`:518`〜`:524` / `:453`→`:454` / `:475`→`:476`)/
  `MODULE-cli-start.md` 6(`:247`〜`:410`→`:248`〜`:411` / `:517`〜`:608`→`:526`〜`:617` /
  `:611`〜`:674`→`:620`〜`:683`)/ `03-impl/index.md` 7(`claude-dev:2604`→`:2607` ×2 /
  `claude-dev-mac:2646`→`:2649` ×2 / `entrypoint:471`・`:482`・`:513`→`:476`・`:487`・`:522`)。
  **置換後に全件を実コードへ当てて、その行が引用の主張どおりの内容であることを確認した。**
- **`FR-env-14` を 02 の3箇所へ足した**(`system.md`「モジュール分割定義」の `MOD-entrypoint` 行 /
  `contracts/cli-container.md`「対応要件」/ `relations.md` の `PLAN-entrypoint-claude`)。
  いずれも `system.md` の要件カバレッジ表が `FR-env-14-11` の主担当を `MOD-entrypoint` と
  定めていることから機械的に導ける。
- **`03-impl/index.md`「02 との差分(未解消のもの)」に1行入れ、「差分なし」を
  「上の1件のほかに差分なし」へ替えた。** 行き先は `docs/issues/108`。
- **手順20 の「各ウィンドウ」を事実へ替えた**(最初の窓だけが `exec zsh -l`。以後の窓でも
  rc が効くのはログインシェルかどうかではなく対話シェルかどうかで決まるから)。
- **`split.md:45` の「他の11条項」を「他の12条項」へ**(算術の訂正。PATCH)。
- **文書が増えたのは 810 バイト**(02⇄03 差分表の1行 556 / 手順20 の理由の書き替え 221 /
  `, FR-env-14` ×3 の 33)。**いずれも偽または不足の記述の置き換えであって、修飾語の追記ではない。**
- **コードは1バイトも変更していない。**

## 更新したドキュメント

| 理由ID | ドキュメント | version 遷移 | 何を変えたか |
|---|---|---|---|
| R-01 | docs/01-requirements/functional.md | 1.20.0(据え置き) | 合格証のみ |
| R-01, R-03 | docs/02-design/system.md | 2.16.0(据え置き) | 合格証 / `MOD-entrypoint` の対応要件に `FR-env-14` |
| R-01, R-03 | docs/02-design/contracts/cli-container.md | 1.14.0(据え置き) | 合格証 / 対応要件に `FR-env-14` |
| R-03 | docs/02-design/relations.md | 1.11.1 → **1.11.2** | `PLAN-entrypoint-claude` の対応要件に `FR-env-14`(PATCH。合格証は有効のまま) |
| R-06 | docs/01-requirements/decisions/split.md | 1.5.0 → **1.5.1** | `D1-split-01` の「他の11条項」→「他の12条項」(PATCH。合格証は有効のまま) |
| R-01, R-02 | docs/03-impl/contracts/cli-container.md | 1.11.0(据え置き) | 合格証 / コード引用7トークン |
| R-01, R-02, R-04 | docs/03-impl/index.md | 1.30.0(据え置き) | 合格証 / コード引用7トークン / 02 との差分表に1行 |
| R-02 | docs/03-impl/relations/MODULE-cli-start.md | (版を持たない) | コード引用6トークン |
| R-05 | docs/03-impl/relations/MODULE-entrypoint-claude.md | (版を持たない) | 手順20 の「各ウィンドウ」の理由を事実へ |
| R-01 | docs/03-impl/tests/entrypoint.md | 1.3.0(据え置き) | 合格証のみ |
| R-01 | docs/03-impl/tests/e2e.md | 1.12.0(据え置き) | 合格証のみ |

**版は `/task-close` の反映が既に1回上げているので、この実行では上げ直していない**
(1実行につき1文書1回。原則6)。`relations.md` と `split.md` はこの実行が初めて触ったので
PATCH を1つ上げた。

## 実装したもの

| 理由ID | 対象 | 内容 | コミット |
|---|---|---|---|
| — | なし | `/doc-check` はコードを変更しない | - |

## 実施した移行

なし

### ロールバック・復旧記録

| 理由ID | 不可逆点 | 切り戻し可能な条件・期限 | 切り戻し手順 / forward-fix のみの理由と復旧手順 | 復元元 | 確認日 | 復旧確認コマンド・結果 |
|---|---|---|---|---|---|---|
| R-01〜R-06 | なし(ドキュメントの編集だけで、外部副作用も公開契約の変更もデータの書き換えも無い) | 期限なし | `git checkout -- docs/` で全量を戻せる | git(直前のコミット `44278fe`) | 2026-08-19 | `git status --porcelain` → 差分は `docs/` 配下のみ。`claude-dev` / `claude-dev-mac` / `scripts/` / `docker-proxy/` に差分なし |

## 機能間連携仕様書の変化

| 種別 | ID | 内容 |
|---|---|---|
| 変更 | MODULE-cli-start / MODULE-entrypoint-claude | 本文のみ(`callers` / `callees` / `contracts` は変更なし) |
| 変化なし | 他 59 本 | 61 本のまま(`check-relations.py` 合格) |

## 検討した代替案

| 論点 | 採用した案 | 棄却した代替案 | 棄却の理由 / 崩れる条件 |
|---|---|---|---|
| `03-impl/contracts/cli-container.md` に残る `claude-dev` / `claude-dev-mac` / `Dockerfile` 側の引用ずれ(独立レビュー L-12・L-14・L-15・L-16 が約30トークンを挙げた) | **直さず、既存の残務行に委ねる** | この場で全件取り直す | **本タスクのコード変更が生んだずれではない**(`docker run` の `:1501`〜`:1529` は実際には `:1688`〜、`spawned_resources` の `:586` は `:736`。いずれも過去のタスクで腐った分で、`docs/pendings.md` の 2026-08-11 の残務行が同じキーで持っている)。1件ずつ意味で当て直す必要があり、CLAUDE.md §3 の停止条件(検証は文書を太らせない・健全性シグナル)に正面から反する。**崩れる条件**: この文書が closure に入るタスクが始まったとき |
| `FR-env-07-13` の充足を `完全` から `部分(P-nn)` へ落とすか(独立レビュー L-06) | **`完全` のまま。`docs/issues/108` が持つ** | `部分(P-nn)` にして新しい棚上げを起こす | `部分(P-nn)` は**意図して棚上げした不完全**(`docs/pendings.md` の項目 + 非空の解消条件)に与える値で、**直すつもりの欠陥は issue が持つ**(`issues-pendings.md`)。108 は `origin_layer: 02` と `closes_when` を持って既に在り、同じものを棚上げとして二重に持たせるのは §8 が禁じている。**崩れる条件**: 人間が経路2 を直さないと裁定したとき(そのときは棚上げへ移す) |
| `UC-01` の関連要件に `FR-env-07` を足すか(独立レビュー L-03) | **足さず、既存の残務行に委ねる** | この場で足す | `FR-env-07`(Docker アクセスの制限)の13条項のうち `UC-01` に載るのは新設の `-13` だけで、他は `UC-03` の旅程である。**要件まるごとを足すと過剰な主張になり、条項単位で載せる書式は 01 の規範に無い** — どちらを採るかは意図の問題で、機械的には決まらない。**崩れる条件**: `usecases.md` の関連要件が条項単位を許す書式になったとき |
| 独立レビューのレンズ | **Codex(`gpt-5.6-sol` / effort high)を先に起動し、その間に自分の検査を回した** | サブエージェント | `codex` CLI が使えたので §0.5 の第1経路。11分34秒で 20 件を返した |

## 副産物

| 種別 | 行き先 | 内容 |
|---|---|---|
| 知見 | 今回限り(理由: 検証手順の気づきであり、製品の決定にはならない) | **コミットの行ずれを「一律 +N」で当てると、ハンクが2つ以上あるファイルでは必ず区間ごとに外れる。** 今回は entrypoint が4ハンクで、正しい累積は +5 / +9 / +17 なのに +4 / +8 / +16 が当てられていた。**`git diff -U0` のハンク表から旧行→新行の写像を作り、置換後に実コードへ当てて内容一致を確認する**のが唯一確実な手順である。同じ型は 2026-08-19 の4回目の `/doc-check` でも起きている(`difflib` で写像を作る形は正しく、一律加算に戻ったのが再発の原因) |
| 新規 issue | なし | 実装欠陥は新たに検出していない。`docs/issues/108` は前回の実行が既に起票済み |
| 残務 | docs/pendings.md | **新規行なし**(43 行のまま)。独立レビューが挙げた L-03・L-04・L-10・L-11・L-12・L-14・L-15・L-16 は既存の行が同じキーで持っている(`issues-pendings.md` §2.1)。**R-03 が直した「02 の2節が `FR-env-14` を欠く」の行と、R-02 が直したぶんの在庫は `/task-close` が裁定する** |
| 鮮度の報告 | 本記録 | closure が開く棚上げは `P-005` の1件。解消条件のうち**衝突の観測は未発火**(文書・コードのどこにも記録が無い)、**規模(数百プロジェクト)は判定不能**(手元の文書から独立に確かめられない)。新しい issue も残務も作っていない(`issues-pendings.md` §8) |
| 解消した issue | なし | `docs/issues/107` は本タスクの実装で解消しているがファイルは残っている(削除は `/task-close` の裁定) |
