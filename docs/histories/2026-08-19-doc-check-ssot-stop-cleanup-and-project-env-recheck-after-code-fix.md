---
id: 2026-08-19-doc-check-ssot-stop-cleanup-and-project-env-recheck-after-code-fix
date: 2026-08-19
task: task-stop-cleanup-and-project-env(フェーズ4 の `/doc-check ssot` 再実行。タスクは未完了のまま)
lane: critical
origin_layer: 03
issue: docs/issues/105(本記録で新規に起票)
summary: 直前の /doc-check ssot の後にコードが動いたので、その差分と下流だけを再検証し、腐った 20 箇所のコード引用を取り直し、握られていない .gitignore 新規作成を起票した
---

# 2026-08-19 `/doc-check ssot task-stop-cleanup-and-project-env`(同日2回目・増分再実行)

## 変更理由

### R-01 直前の `/doc-check ssot` の後にコードが動いた

- 起点層・根拠: 原則2(コード ⇄ 03-impl は完全に一致する)。
  直前の記録 `docs/histories/2026-08-19-doc-check-ssot-stop-cleanup-and-project-env-recertification.md`
  の「追記」が、合格証の発行後に `claude-dev` / `claude-dev-mac` を直したことを述べている。
- 変更が必要になった条件: その修正が `claude-dev` の行番号を **区間ごとに -11 / -7 / +4** ずらした。
  合格証を持つ 20 文書のうち、実際に動いたのは **コードと `docs/03-impl/index.md`** だけなので、
  **A〜E をその差分と下流(03-impl の closure 文書)に絞って**再実行した
  (`/doc-check` SKILL.md §0 の増分再実行の規則)。00・01・02 の合格証は版が一致したまま有効なので
  再検証していない。

## 変更内容の要約

- **コード引用 20 箇所を実コードで取り直した。** 内訳: `03-impl/index.md` 4 / `MODULE-cli-stop` 4 /
  `MODULE-cli-common-spawned-resources` 5 / `MODULE-cli-reset` 6 / `MODULE-cli-start` 11
  (範囲引用の端点を各1件と数えると 30 箇所)。**すべて `path:line` の付け替えのみで、
  述べている事実は変えていない**(PATCH)。
- **`docs/issues/105` を起票した(重大度 中 / 起点層 03)。** `.gitignore` が無く `.git` がある
  プロジェクトへの `.gitignore` **新規作成**(`claude-dev:1552`-`:1553` /
  `claude-dev-mac:1627`-`:1628`)が `set -e` の下で握られておらず、書き込めないと `start` が
  その場で止まる。**直前のタスクが直したのは既存ファイルへの追記の枝だけ**であり、
  `FR-env-14-4` の「表示して起動を続ける」は新規作成の枝で満たされていない。
- **`MODULE-cli-start` の3箇所が偽になっていた**(手順5-2 の 8 / 副作用の順序表 3-2 /
  エラーケース表)。いずれも「`.gitignore` へ書けなくても起動は止めない」と無条件に述べており、
  **握られているのは既存ファイルへの追記だけ**という事実へ書き直し、
  「既知の制限」に1行を足して `105` から追跡できるようにした(MINOR)。
- **`03-impl/index.md` の起票済み欠陥を 4件 → 5件** にした(`105` を加えた)。あわせて
  **実在しない `docs/issues/014` への言及**を落とした(独立レビュー Codex が検出)。
- `MODULE-cli-start` の「既知の制限」に**同じ見出しの表が2つ**並んでいたので1つに畳んだ。
- **コードは1バイトも変更していない。**

## 更新したドキュメント

| 理由ID | ドキュメント | version 遷移 | 何を変えたか |
|---|---|---|---|
| R-01 | docs/03-impl/index.md | 1.27.0 → **1.28.0** | 起票済み欠陥 4件 → **5件**(`105`)/ `014` の言及を削除 / `help\|*)` 分岐の引用 `:2585`・`:2627` → `:2589`・`:2631` |
| R-01 | docs/03-impl/relations/MODULE-cli-start.md | (版を持たない) | `.gitignore` 新規作成の枝が握られていない事実を3箇所へ / 「既知の制限」に1行 + 2つの表を1つに / コード引用 11 箇所 |
| R-01 | docs/03-impl/relations/MODULE-cli-reset.md | (版を持たない) | コード引用 6 箇所(`destructive_*` の呼び出し位置) |
| R-01 | docs/03-impl/relations/MODULE-cli-stop.md | (版を持たない) | コード引用 4 箇所(手順6・手順8 の列挙位置) |
| R-01 | docs/03-impl/relations/MODULE-cli-common-spawned-resources.md | (版を持たない) | コード引用 5 箇所(`stop` 3 / `reset` は `--volumes` 経路を含む 3) |
| R-01 | docs/pendings.md | — | 2026-08-11 の「コード引用の行番号のずれ」から `+102` の一律補正が成立しなくなった事実へ差し替え / `docs/issues/040` の宙づり参照を1行追加 |

**合格証を発行し直していない 19 文書**(00・01・02 の全部と `03-impl/tests/*.md`): この再実行では
1バイトも動いておらず、`version` と `verified.version` が一致したまま有効である(原則6)。

## 実装したもの

| 理由ID | 対象 | 内容 | コミット |
|---|---|---|---|
| — | なし | `/doc-check` はコードを変更しない | - |

## 実施した移行

なし

### ロールバック・復旧記録

| 理由ID | 不可逆点 | 切り戻し可能な条件・期限 | 切り戻し手順 / forward-fix のみの理由と復旧手順 | 復元元 | 確認日 | 復旧確認コマンド・結果 |
|---|---|---|---|---|---|---|
| R-01 | なし(ドキュメントの編集だけで、外部副作用も公開契約の変更もデータの書き換えも無い) | 期限なし | `git checkout -- docs/` で全量を戻せる | git(作業ツリーは直前のコミット `5db067e` から追跡可能) | 2026-08-19 | `git diff --stat` → 変更は `docs/` 配下のみ。`claude-dev` / `claude-dev-mac` / `docker-proxy/` に差分なし |

## 機能間連携仕様書の変化

| 種別 | ID | 内容 |
|---|---|---|
| 変更 | MODULE-cli-start | 本文の事実の訂正と「既知の制限」1行(`callers` / `callees` / `contracts` は変更なし) |
| 変更 | MODULE-cli-stop / -reset / -common-spawned-resources | 本文のコード引用のみ(同上) |
| 変化なし | 他 57 本 | 61 本のまま(`check-relations.py` 合格) |

## 検討した代替案

| 論点 | 採用した案 | 棄却した代替案 | 棄却の理由 / 崩れる条件 |
|---|---|---|---|
| `.gitignore` 新規作成の枝が握られていないことをどう扱うか | **03 に事実を書いて「既知の制限」と `docs/issues/105` で追跡し、01・02 は動かさない** | `MODULE-cli-start` の記述を「起動を止めない」のまま残して実装だけを待つ | 記述を残すと 03 が偽を述べたまま合格証を持つ(原則2)。**崩れる条件**: `/task-close` が同じ降下でコードを直すと決めたとき(そのときは `105` を削除して記述を戻す) |
| closure 外の 03-impl 文書のコード引用のずれ(`contracts/cli-container.md` ほか9件) | **直さない。既存の残務(2026-08-11)が持ち、その本文だけ事実へ直す** | 今回まとめて全部取り直す | 増分再実行の規則は「変わったものとその下流」に絞ることを求めており、これらは本タスクの closure 外である。またずれは +309 行の実装から先に生じていて本再実行の差分ではない。**崩れる条件**: それらの文書を触るタスクが来たとき(残務がそう指名している) |

## 副産物

| 種別 | 行き先 | 内容 |
|---|---|---|
| 知見 | 今回限り(理由: 検証手順そのものの気づきであり、製品の決定にはならない) | **「同じ欠陥を直した」を1文で書くと、直した枝と直していない枝の区別が消える。** `104` は既存 `.gitignore` への追記だけを直したのに、記述は「書き込めないと止まる問題を解消した」と一般化していた。**修正の記述は、直した経路を名指しで書くこと。** |
| 新規 issue | docs/issues/105 | `.gitignore` を新規作成できないと `start` が止まる(重大度 中 / 起点層 03) |
| 残務(1行) | docs/pendings.md | `docs/03-impl/infra/local/docker-resources.md` が実在しない `docs/issues/040` を根拠に引いている |
| 鮮度の報告 | 本記録 | `P-005` は未発火。**`P-006` は3つ目の発火条件(「`reset` のセッション由来資源の削除で実際に不具合が観測されたとき」)に当たる出来事が起きている**(`reset --volumes` の未解析。同日修正済み)が、**required な E2E-01 手順8-15 / 8-16 / 手順10・12 は専有ホストが無く実行できない**。**人間の裁定が要る**(再受容するか、P-006 を書き換えるか) |
| 解消した issue | なし | `002` と `092` は本タスクの実装で解消済みだがファイルは残っている(削除は `/task-close` の裁定) |

## 追記(同日。`/task-close` の中で)

**本記録が起票した `docs/issues/105` は、同じタスクの中でコードを直して解消したので削除した**
(`.claude/directions/issues-pendings.md` §1 の行1)。この検証を走らせた `/doc-check` は
コードを書けないので起票が正しい扱いだったが、呼び出し元の `/task-close` はコードへ戻せる。
**本記録の「検討した代替案」が挙げていた『崩れる条件: `/task-close` が同じ降下でコードを直すと
決めたとき(そのときは `105` を削除して記述を戻す)』が、そのとおりに起きた。**

- 直した内容: `.gitignore` の**新規作成**と、その直後の env パスの追記を `if` で明示的に握り、
  できなかったことを表示して起動を続ける形にした(`claude-dev` / `claude-dev-mac` の両方)。
  **3経路すべてを握った** — この記録の「知見」が指摘したとおり、
  1つでも握り漏らすと「表示して起動を続ける」が経路によって成り立たなくなる。
- 確認: `.gitignore` を**ディレクトリとして**置いたプロジェクト(新規作成が必ず失敗する形)で
  `claude-dev start --no-vnc` を実行し、
  「⚠️ .gitignore を作成できませんでした(起動は続けます)」が表示されたうえで
  **コンテナが起動する**ことを実機で確認した(2026-08-19)。
- `docs/03-impl/relations/MODULE-cli-start.md` の手順5-2・手順12・冪等性の表・異常系・
  既知の制限を実物へ戻した。

**この記録が挙げた `P-006` の3つ目の発火条件については、まだ人間の裁定を得ていない**
(`/task-close` の完了報告で提示する)。
