---
id: 2026-08-19-doc-check-ssot-stop-cleanup-and-project-env-third-recheck
date: 2026-08-19
task: task-stop-cleanup-and-project-env(フェーズ4 の `/doc-check ssot` 3回目。タスクは未完了のまま)
lane: critical
origin_layer: 03
issue: docs/issues/106, docs/issues/107(いずれも本記録で新規に起票)
summary: 2回目の後にコードが +9 行動いたので、その差分と下流だけを再検証し、腐ったコード引用 40 トークンを取り直し、set -e の下で握られていない補助処理2件を起票して 03 を事実へ戻した
---

# 2026-08-19 `/doc-check ssot task-stop-cleanup-and-project-env`(同日3回目・増分再実行)

## 変更理由

### R-01 2回目の `/doc-check ssot` の後にコードが動いた

- 起点層・根拠: 原則2(コード ⇄ 03-impl は完全に一致する)。
  直前の記録 `docs/histories/2026-08-19-doc-check-ssot-stop-cleanup-and-project-env-recheck-after-code-fix.md`
  の「追記」が、合格証の発行後に `.gitignore` の**新規作成の枝**を握るコードを入れたことを述べている。
- 変更が必要になった条件: その修正(コミット `faa4b3d`)が `claude-dev:1549` の1ハンクで
  **8行を17行に置き換え、以降の行番号を一律 +9 ずらした**(`claude-dev-mac` は `:1624` から同じ +9)。
  git のログで、直前の検証以降に動いたのが **`claude-dev` / `claude-dev-mac` の `.gitignore` まわりと、
  `docs/03-impl/relations/MODULE-cli-start.md`・`docs/03-impl/index.md`** だけであることを確認したうえで、
  **A〜E をその差分と下流(03-impl の closure 文書)に絞って**再実行した
  (`/doc-check` SKILL.md §0 の増分再実行の規則)。00・01・02 の合格証は版が一致したまま有効なので
  再検証していない。

## 変更内容の要約

- **コード引用を実コードで取り直した — 行番号トークン 40 個 / 15 箇所。** 内訳(トークン数):
  `MODULE-cli-reset` 12 / `MODULE-cli-common-spawned-resources` 12(同じ6個組が2箇所に反復)/
  `MODULE-cli-start` 7 / `MODULE-cli-stop` 5 / `03-impl/index.md` 4(同じ2個組が2箇所に反復)。
  **`MODULE-cli-start` の `:1670`(`docker run -d` を指すはずが起動メッセージを指していた)は、
  2回目の再実行が取り直さずに残していたものである。**
  すべて `path:line` の付け替えのみで、述べている事実は変えていない(PATCH)。
- **`docs/issues/106` を起票した(重大度 中 / 起点層 03)。** `load_project_env_file` が
  存在検査(`[ ! -f ]`。`claude-dev:165` / `claude-dev-mac:164`)しか行わず、実際の読み取りが
  握られていない入力リダイレクト(`claude-dev:203` / `claude-dev-mac:202`)なので、
  **読み取り権限の無い通常ファイルを `env_file` に指定すると `set -e` の下で `start` が止まる**。
  `NFR-avail-03`(補助機能の失敗が主機能を止めない)を満たさない。
- **`docs/issues/107` を起票した(重大度 中 / 起点層 03)。** ホスト設定の取り込みが
  `_overlay=$(jq … 2>/dev/null)` という代入コマンドであり、`2>/dev/null` は終了ステータスを
  握らないので、**壊れた `~/.claude/settings.json` があると `start` が止まる**
  (`claude-dev:1521` / `claude-dev-mac:1596`)。`NFR-avail-03` は「ホスト設定の取り込み」を
  名指しで補助機能に挙げている。**どちらも同型の最小スクリプトで実測して確認した。**
- **`MODULE-cli-start` の4箇所が偽になっていた**ので事実へ書き直した(MINOR):
  手順5-2 の3(「読めなければ続行する」→ 存在検査しかしていない)/ 異常系の
  「`jq` の解析に失敗 → 続行する」/ `MODULE-cli-common-ensure-infrastructure` の
  「失敗は `docker run` のボリューム自動作成で吸収される」(**ネットワークは吸収されない** —
  `docker run --network` が既存を要求する)/ 副作用の順序表の `#3-2`
  (`.gitignore` への env パス追記を**削除・インフラ作成・認証コピーより前**に置いていたが、
  実装では**後**である)。
- **副作用の順序表の `#3` に macOS 版の順序差を書いた**: macOS 版は
  `ensure_project_config`(`claude-dev-mac:1509`)が `require_setup`(`:1527`)より先に走るため、
  `#2` と `#3` の順序が Linux 版と入れ替わる。
- **`03-impl/index.md` の起票済み欠陥を 4件 → 6件**にした(`106` / `107` を加えた)。
- **コードは1バイトも変更していない。**

## 更新したドキュメント

| 理由ID | ドキュメント | version 遷移 | 何を変えたか |
|---|---|---|---|
| R-01 | docs/03-impl/index.md | 1.28.0 → **1.29.0** | 起票済み欠陥 4件 → **6件**(`106` / `107`)/ `help\|*)` 分岐の引用 `:2589`・`:2631` → `:2598`・`:2640`(2箇所ずつ) |
| R-01 | docs/03-impl/relations/MODULE-cli-start.md | (版を持たない) | 偽になっていた4箇所を事実へ / 副作用の順序表から `#3-2` を落とし `#10` へ畳む / `#3` へ macOS の順序差 / 既知の制限に2行 / コード引用4箇所 |
| R-01 | docs/03-impl/relations/MODULE-cli-reset.md | (版を持たない) | コード引用9箇所(`--volumes` 解析ブロックと `destructive_*` の呼び出し位置) |
| R-01 | docs/03-impl/relations/MODULE-cli-stop.md | (版を持たない) | コード引用5箇所(手順6 の削除・手順8 の列挙・旧い名前の案内) |
| R-01 | docs/03-impl/relations/MODULE-cli-common-spawned-resources.md | (版を持たない) | コード引用6箇所(契機と `[DS-02]` 開示行の2箇所に反復) |
| R-01 | docs/pendings.md | — | 残務1行(`MODULE-cli-start` の程度語2箇所) |
| R-01 | docs/issues/index.md | — | 生成物の再生成(13 件 → 15 件) |

**合格証を発行し直していない 19 文書**(00・01・02 の全部と `03-impl/tests/*.md`): この再実行では
1バイトも動いておらず、`version` と `verified.version` が一致したまま有効である(原則6)。

## 実装したもの

| 理由ID | 対象 | 内容 | コミット |
|---|---|---|---|
| — | なし | `/doc-check` はコードを変更しない | - |

## 実施した移行

なし

### ロールバック・復旧記録

| 理由ID | 不可逆点 | 切り戻し可能な条件・期限 | 切り戻し手順 / forward-fix のみの理由と復旧手順 | 復元元 | 確認日 | 復旧確認コマンド・結果 |
|---|---|---|---|---|---|---|
| R-01 | なし(ドキュメントの編集だけで、外部副作用も公開契約の変更もデータの書き換えも無い) | 期限なし | `git checkout -- docs/` で全量を戻せる(新規の issue 2本は `rm` で戻す) | git(作業ツリーは直前のコミット `faa4b3d` から追跡可能) | 2026-08-19 | `git status --porcelain` → 変更は `docs/` 配下のみ。`claude-dev` / `claude-dev-mac` / `docker-proxy/` に差分なし |

## 機能間連携仕様書の変化

| 種別 | ID | 内容 |
|---|---|---|
| 変更 | MODULE-cli-start | 本文の事実の訂正4箇所と既知の制限2行(`callers` / `callees` / `contracts` は変更なし) |
| 変更 | MODULE-cli-reset / -stop / -common-spawned-resources | 本文のコード引用のみ(同上) |
| 変化なし | 他 57 本 | 61 本のまま(`check-relations.py` 合格) |

## 検討した代替案

| 論点 | 採用した案 | 棄却した代替案 | 棄却の理由 / 崩れる条件 |
|---|---|---|---|
| `set -e` の下で握られていない補助処理2件をどう扱うか | **03 に事実を書いて「既知の制限」と `docs/issues/106` / `107` で追跡し、01・02 は動かさない** | 03 の記述を「続行する」のまま残して実装だけを待つ | 記述を残すと 03 が偽を述べたまま合格証を持つ(原則2)。**崩れる条件**: `/task-close` が同じ降下でコードを直すと決めたとき(そのときは `106` / `107` を削除して記述を戻す — `103`〜`105` で3回そうなった) |
| 副作用の順序表の `#3-2` をどう直すか | **行を落として `#10` の行へ畳む**(同じ1つのコードブロックなので行が2つある必要が無い) | `#3-2` を `#9-2` へ番号だけ振り直す | 番号を振り直しても「同じブロックが表に2回出る」ことは変わらない。**崩れる条件**: `.gitignore` の追記が手順5-2 と手順12 で別のブロックに分かれたとき |
| 程度語2箇所(「数分」「即座に」)をどう扱うか | **残務1行に落とす** | この場で数値へ書き換える | 「数分」の下限・上限はイメージのビルド時間であり、測定していない値をここで発明することになる(CLAUDE.md §3「検証は文書を太らせない」)。**崩れる条件**: ビルド時間の目標値が 01 の非機能要件に入ったとき |

## 副産物

| 種別 | 行き先 | 内容 |
|---|---|---|
| 知見 | 今回限り(理由: 検証手順そのものの気づきであり、製品の決定にはならない) | **コード引用の取り直しは「変わった文書」ではなく「変わったコード行より下の全引用」に対して行う。** 2回目の再実行は `MODULE-cli-start` を書き直しながら、同じ文書の `:1670` を取り直していない。**取り直しの単位は文書ではなく、ずれの区間である。** |
| 新規 issue | docs/issues/106 | 読み取り権限の無い env ファイルを指定すると `start` が止まる(重大度 中 / 起点層 03) |
| 新規 issue | docs/issues/107 | 壊れた `~/.claude/settings.json` があると `start` が止まる(重大度 中 / 起点層 03) |
| 残務(1行) | docs/pendings.md | `MODULE-cli-start` に程度語が2箇所ある(「数分」「即座に」) |
| 鮮度の報告 | 本記録 | `P-005` は未発火。**`P-006` の3つ目の発火条件は前回の記録が発火と判定したまま、まだ人間の裁定を得ていない**(専有ホストが無く required な手順を実行できない)。新しい issue にも残務にもしていない(`issues-pendings.md` §8) |
| 解消した issue | なし | `002` と `092` は本タスクの実装で解消済みだがファイルは残っている(削除は `/task-close` の裁定) |

## 追記(同日。`/task-close` の中で)

**本記録が起票した `docs/issues/106` と `107` は、同じタスクの中でコードを直して解消したので削除した**
(`.claude/directions/issues-pendings.md` §1 の行1)。あわせて、**同型が3回続けて検出されたことを受けて
`start)` 分岐(`claude-dev:1417`-`:1799`)の代入・リダイレクト・ファイル操作を全件走査した。**

| 元の issue | 直した内容 | 実機での確認 |
|---|---|---|
| `106` 読み取り権限の無い env ファイル | 判定を `[ ! -f ]` から **`[ ! -r ]`** へ | `chmod 000` した env ファイルを指して `claude-dev start --no-vnc` → 「env ファイルを読めませんでした(環境変数を渡さずに起動を続けます)」を表示して**起動完了** |
| `107` 壊れた `~/.claude/settings.json` | 代入を **`|| _overlay=""`** で握り、読めなかったことを表示 | `{ broken` を置いて `claude-dev start --no-vnc` → 「ホストの Claude 設定を読めませんでした(起動は続けます)」を表示して**起動完了**(確認後、退避した実ファイルを復元し JSON として正しいことを確かめた) |

**全件走査の結果、握られていない補助処理は残り2箇所だけ**である
(`~/.local/bin/` の `cp -a` と `~/.ssh/config` の `mktemp` + `sed`)。
**この2つは本タスクが触った経路ではなく以前から在るので、修正せず
`docs/issues/106` として起票し直した**(同じ番号を再利用したのは、直した2件が
そのまま消えた結果として空いたためではなく、**起票の主題が「同型の残り」へ変わった**
ためである。`pattern_survey:` に走査の全文を書いた)。
`docs/03-impl/relations/MODULE-cli-start.md` の手順5-2 の3・異常系の `jq` の行・
既知の制限を実物へ戻し、`docs/03-impl/index.md` の件数を **5件** にした。

**`P-006` の3つ目の発火条件については、`/task-close` の完了報告で人間へ提示する。**
