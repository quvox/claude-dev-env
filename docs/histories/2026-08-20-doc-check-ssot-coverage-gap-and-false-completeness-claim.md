---
id: 2026-08-20-doc-check-ssot-coverage-gap-and-false-completeness-claim
date: 2026-08-20
task: F2 文書整合フロー(`/verify-docs all`)
lane: standard
origin_layer: 02
issue: なし(新規の起票なし)
summary: 02 の要件カバレッジ表が生きた条項2件を落としたまま「割り当て先の無い条項も無い」と主張していた偽を直し、合格証が外れていた5文書に合格証を発行し、logging.md の合格証が実在しない上流版を指していたのを実測値へ直した
---

# 2026-08-20 `/verify-docs all`

## 変更理由

### R-01 要件カバレッジ表が、生きた条項2件を落としたまま完全性を主張していた

- 起点層・根拠: 検査 A2(要件の各節が網羅表にあり、担当する行がちょうど1つ)+ 原則6
  (証明書は「この文書は正しく書かれている」を主張する。偽を含む文書は証明書を持てない)。
- 変更が必要になった条件: `docs/02-design/system.md` の要件カバレッジ表に
  **`FR-env-02-7`(`docs/01-requirements/functional.md:100`)と
  `FR-env-04-9`(同 `:159`)の担当行が無い**。どちらも生きた条項で、コメントで廃止されていない。
  さらに同ファイルが「**割り当て先の無い条項も無い**(機能要件の全 **166** 条項・NFR 10 件・
  SR 19 件がすべて上表に現れる)」と書いており、**実際は生きた FR 条項が 170、表の FR 行が 168**
  であった。**表の不足2件と、数の陳腐化2件の合計4件ぶん、この主張は偽である。**
- 帰属: 構築記録 `fix-start-auxiliary-halts-and-tmux-runtime-env`(`state: awaiting-verify`)。
  その closure は `docs/01-requirements/functional.md` と
  `docs/02-design/contracts/cli-container.md` を含むが、
  **`docs/02-design/system.md` を含まない** — 条項を新設して 02 の契約に倒し方まで書きながら、
  その条項の主担当を宣言する表だけが降下から漏れた。
- 増分の判定: 条項 ID の集合を機械的に突き合わせた(functional / non-functional / 01-system の
  条項 ID ∖ 網羅表の第1列)。差分11件のうち9件は HTML コメント内の廃止済み(`FR-env-12-12` /
  `NFR-avail-01` / `NFR-ops-01` / `NFR-ops-04` / `NFR-perf-03` / `NFR-sec-02` / `NFR-sec-03` /
  `SR-22` / `SR-23`)で、**生きているのは上記2件だけ**である。

### R-02 合格証が5文書で外れていた(自分の版と `verified.version` の MAJOR.MINOR 不一致)

- 起点層・根拠: 原則6(証明書は文書ごと。有効なのは自分の現行 MAJOR.MINOR と一致するときだけ。
  書けるのは verify 系フローだけ)。
- 変更が必要になった条件: 6件の構築記録(すべて `awaiting-verify`)が closure 内の SSOT を
  直接書き、版を上げた結果、`functional.md`(1.22.0 / 証明書 1.20.0)・
  `02-design/system.md`(2.17.0 / 2.16.0)・`02-design/relations.md`(1.12.0 / 1.11.1)・
  `02-design/contracts/cli-container.md`(1.17.0 / 1.14.0)・
  `02-design/environments.md`(1.6.0 / 1.5.0)の5件が無効になっていた。
  **`docs/01-requirements/decisions/split.md` は 1.5.1 / 証明書 1.5.0 で MAJOR.MINOR が
  一致するため有効**であり、対象に入れていない(PATCH 級の編集は証明書を無効にしない)。

### R-03 `logging.md` の合格証が、一度も存在しない上流の版を指していた

- 起点層・根拠: 検査 C(frontmatter の妥当性)。`against` は有効性の条件ではないが
  (原則6)、**書かれている以上は事実でなければならない**。
- 変更が必要になった条件: `docs/02-design/logging.md` の `verified.against` が
  `docs/02-design/architecture.md` の **version 1.9.0** を指していたが、
  **`architecture.md` は 1.9.0 になったことが一度も無い**(`git log` で全 13 版を実測。
  最大は現行の 1.7.0。1.0.0 → 1.2.0 → 1.3.0 → 1.4.0 → 1.5.0 → 1.6.0 → 1.7.0)。
  `logging.md` 自身の版が 1.9.0 であり、**自分の版を上流の版の欄へ書き写した誤り**である。

## 変更内容の要約

- **要件カバレッジ表へ2行足した**(`FR-env-02-7` / `FR-env-04-9`。どちらも
  主担当 `MOD-cli-start`・充足 `完全`)。**主担当は機械的に導けた**: 同じ資産受け渡しを扱う
  兄弟条項(`FR-env-02-2` / `-3` / `-6`、`FR-env-04-5`〜`-8`)がすべて `MOD-cli-start` であり、
  倒し方は `CTR-cli-container`(`docs/02-design/contracts/cli-container.md:157` / `:207`)が
  既に定めている。根拠列にはその契約の節を名指した。
- **完全性の主張の数を 166 → 170 へ直した。** 訂正後の表は **FR 170 / NFR 10 / SR 19 = 199 行**
  で、**重複 0 件・生きた条項の取りこぼし 0 件**を機械的に確認した。
- **合格証を6件発行した**(R-02 の5件 + `logging.md`)。`against` はこの実行で実際に
  突き合わせた現行版を書いた。
- **`logging.md` の `against` の `architecture.md` を 1.9.0 → 1.7.0 へ**(実測値。
  本文は1バイトも変えていないので版は据え置き)。
- **文書が増えたのは 476 バイト**(カバレッジ表の2行 470 / 「166」→「170」の 6)。
  **いずれも欠落と偽の置き換えであって、修飾語の追記ではない。**
- **コードは1バイトも変更していない。**

## 更新したドキュメント

| 理由ID | ドキュメント | version 遷移 | 何を変えたか |
|---|---|---|---|
| R-01, R-02 | docs/02-design/system.md | 2.17.0 → **2.18.0** | カバレッジ表に2行 / 「166」→「170」/ 合格証(MINOR。意味が変わる訂正) |
| R-02 | docs/01-requirements/functional.md | 1.22.0(据え置き) | 合格証のみ |
| R-02 | docs/02-design/relations.md | 1.12.0(据え置き) | 合格証のみ |
| R-02 | docs/02-design/contracts/cli-container.md | 1.17.0(据え置き) | 合格証のみ |
| R-02 | docs/02-design/environments.md | 1.6.0(据え置き) | 合格証のみ |
| R-03 | docs/02-design/logging.md | 1.9.0(据え置き) | 合格証の `against` を実測値へ(本文は無変更) |

## 実装したもの

| 理由ID | 対象 | 内容 | コミット |
|---|---|---|---|
| — | なし | F2 文書整合フローはコードを変更しない | - |

## 実施した移行

なし

### ロールバック・復旧記録

| 理由ID | 不可逆点 | 切り戻し可能な条件・期限 | 切り戻し手順 / forward-fix のみの理由と復旧手順 | 復元元 | 確認日 | 復旧確認コマンド・結果 |
|---|---|---|---|---|---|---|
| R-01〜R-03 | なし(ドキュメントの編集だけで、外部副作用も公開契約の変更もデータの書き換えも無い) | 期限なし | `git checkout -- docs/` で全量を戻せる | git(直前のコミット `05c31a2`) | 2026-08-20 | `git status --porcelain` → 差分は `docs/` 配下のみ。`claude-dev` / `claude-dev-mac` / `scripts/` / `docker-proxy/` / `Makefile` に差分なし |

## 機能間連携仕様書の変化

| 種別 | ID | 内容 |
|---|---|---|
| 変化なし | 61 本すべて | `check-relations.py` 合格(対称性・参照実在・impl パス・必須項目・機能表との 1:1) |

## 検討した代替案

| 論点 | 採用した案 | 棄却した代替案 | 棄却の理由 / 崩れる条件 |
|---|---|---|---|
| カバレッジ表の2行を自分で書くか、キューへ回すか | **自分で書いた(MINOR の自動修正)** | 残務か issue へ回す | 主担当が**上流から一意に導ける**(兄弟条項がすべて `MOD-cli-start`、倒し方は `CTR-cli-container` が確定済み)ため、`/verify-docs` の自動修正の例外「上流から機械的に導出できる転記」に当たる。回した場合、`02-design/system.md` は偽を含んだまま合格証を持てず、収束契約が開かない。**崩れる条件**: 主担当が `MOD-cli-start` 以外だと判明したとき |
| `logging.md` の版を上げるか | **上げない(本文無変更)** | MINOR を1つ上げる | 直したのは合格証ブロックの中の `against` で、**本文は1バイトも変わっていない**。版を上げると自分の合格証を自分で無効にする循環になる。`against` は原則6 が「追跡性であり有効性の条件ではない」と定めており、この実行が発行し直す合格証の中で正した |
| 用語集の空欄8語を埋めるか | **埋めず残務へ1行** | この場で例を埋める | 例を埋めるには「何をこの語に含めないか」の線引きを決める必要があり、**機械的に導けない**(境界のある語 — 安全・破壊的操作・管理ラベル・セッション由来の資源 — は既に埋まっている)。CLAUDE.md §3「検証は文書を太らせない」にも触れる。**崩れる条件**: 用語集が closure に入るタスクが始まったとき |
| 独立レビューのレンズ | **不成立のまま自分の判定で完走した** | 返事を待ち続ける | 指名された `reviewer-3`(codex)へ2度送り、2通目は配信されたが**既読 0/1・窓は待機のまま**で、9分の待ち(`colabtmux wait --timeout 540`)でも応答が無かった。指示が名指しで禁じた空転(本日 11:51 に同型で約50分)を避けた。**レンズの指摘は証拠でありゲートではない**(`/verify-docs` §2)。**崩れる条件**: 返事が届いたとき(そのときは裁定に入る) |

## 副産物

| 種別 | 行き先 | 内容 |
|---|---|---|
| 知見 | 本記録 | **条項を新設するタスクは、その条項の主担当を宣言する 02 の表を closure に入れ落としやすい。** `fix-start-auxiliary-halts-and-tmux-runtime-env` は `functional.md` に条項を足し、`02-design/contracts/cli-container.md` に倒し方まで書いたのに、`02-design/system.md` の要件カバレッジ表だけが漏れた。**表が「割り当て先の無い条項も無い」と件数つきで完全性を主張しているため、漏れは偽の主張として残る。** 件数が本文に書かれている表は、行を足すたびに件数も動く |
| 新規 issue | なし | 実装欠陥は検出していない(F2 はコード ⇄ 03 を見ない) |
| 残務 | docs/pendings.md | **1行追加**(42 → 43 行)。用語集8語の例の空欄。`check-ssot.py` の CS11 参照実在3件・CS20 起点層4件は**既存の行が同じキーで持っており**、新規に書いていない(`issues-pendings.md` §2.1) |
| 鮮度の報告 | 本記録 | closure が開く棚上げは `P-005`(compose 一意化名のハッシュ衝突)の1件。解消条件は非空で、**衝突の観測は未発火**(文書のどこにも記録が無い)。00 の「見直す条件」は `dist.md:151` / `env.md:442` / `sec.md:188` の3件があり、**いずれも未発火**(配布先は公開 GHCR のまま / 本システムが使う環境変数の名前は増えていない — `runtime.env` は既存の `DOCKER_HOST` と `SSH_AUTH_SOCK` を運ぶ経路であって新しい名前ではない / プロジェクトディレクトリの共有運用も秘密情報管理サービスも入っていない)。`D0-env-07` の要確認は論点と「誰が・いつまでに」を持ち、期限なしのまま有効 |
| 解消した issue | なし | `docs/issues/110`(severity 高)は**人間の裁定待ちであり、この実行は触っていない** |

## 残務の恒久受容(2026-08-20 実行)
この一覧は `doc-health.py --as-of --sweep` が落とした行である。落としたこと自体が決定であって、指摘でも新しい issue でもない(`.claude/directions/issues-pendings.md` §2.1)。
- - 2026-08-20 `docs/03-impl/features.md`「到達しない関数についての判断」の隣に `.claude/directions/callgraphs.md` §6 が求める「棄却した候補辺・確認済みの境界辺」節が無いため、`MODULE-entrypoint-claude` の callees 3件が `callgraph-check.py` の CG3 に毎回「中」で出る。3件とも実在することは確認済みで、`scripts/entrypoint-claude.sh:496`(`init-firewall.sh`)/ `:507`(`vm-up.sh`)/ `:545`(`dood-portsync.sh --loop`)がいずれも `/usr/local/bin/` の絶対パスで起動しており、shell 抽出器がソースへ解決できないだけである(2026-08-20 の F3 で行番号を再測。前の版の 476 / 487 / 522 は `2217c84` より前の値で、以後ずれていた)。**同じ節の不在が CG4 の「参考」12件の再提起も生んでいる**: `Makefile::help` の 11 件と `Makefile::setup → login` は、いずれも対象名が `@echo` の文字列の中にしか出てこない(`Makefile:52`-`:68` / `:82`。`setup` の前提条件は `Makefile:76` の `env network volumes build install` で `login` を含まない)ため**辺は実在せず棄却が正しい**が、棄却が残らないので毎回戻る。同節に確認済み3件と棄却12件を記録すれば再提起が止まる。
  - 理由: 対象が実在しない: docs/03-impl/features.md`「到達しない関数についての判断」の隣に
