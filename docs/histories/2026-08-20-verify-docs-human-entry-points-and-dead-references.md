---
id: 2026-08-20-verify-docs-human-entry-points-and-dead-references
date: 2026-08-20
task: F2 文書整合フロー(`/verify-docs all`)
lane: standard
origin_layer: 02
issue: なし(新規の起票なし)
summary: 人間の入口2文書が廃止済みのフェーズ方式を現行として書いていた偽を現行の4フロー像へ置き換え、削除済み issue を指す参照3件と実在しない文書・条項を「いま在る重複」として挙げていた行を落とし、合格証を6文書へ発行した
---

# 2026-08-20 `/verify-docs all`

## 変更理由

### R-01 人間の入口2文書が、廃止済みのフェーズ方式を現行の手順として書いていた

- 起点層・根拠: 検査 C(文書が完全性の主張と矛盾しない・陳腐化した記述を残さない)+
  CLAUDE.md §1「変更指示は作らない」/ §4「フェーズ方式は廃止した」/ 原則6(合格証の書き手)。
- 変更が必要になった条件: `docs/README.md` は `:7`-`:8` で「変更は SSOT を直接書き換えず
  `tasks/task-<slug>/new-features/` に**変更指示**として書く」「SSOT が書き換わるのは
  `/task-close` の1回だけ」と書き、`:65`-`:77` に廃止済みの4フェーズ表
  (`/task-new` → `/task-doc` → `/doc-check` → `/implement` → `/task-close`)を持っていた。
  `docs/ONBOARDING.md` は同じ方式を `:29`・`:40`・`:76`・`:86`-`:94`・`:101`・`:119`・`:127`・
  `:140` の8箇所に持ち、**合格証の書き手を廃止済み `/doc-check` と書き**、
  「上流が変われば下流の合格証は自動的に失効する」と原則6 に反することを書いていた。
  **どちらも人間が最初に読む文書である**(CLAUDE.md §1 が `docs/README.md` を入口と定める)。
- 帰属: どの構築記録の closure にも入っていない。フローモデルの移行(2026-08-18)が
  `.claude/` の側だけを書き換え、`docs/` 側の入口2文書を降下から落としている。
- 増分の判定: 廃止語(`/task-new` / `/task-doc` / `/task-close` / `/implement` / `/doc-check` /
  `/doc-status` / `/reverse-doc` / `/codex-qa` / `new-features` / 「変更指示」/「フェーズ」)の
  全出現を機械で数え、置換後に0件(否定文としての言及だけ)であることを確認した。
- 直し方: 置換だけで直した(節を増やしていない)。置換先の本文は CLAUDE.md §4 の
  フロー表・§6 のスキル一覧・原則6 からの転記であり、この文書が独自に決めたことは無い。

### R-02 削除済み issue を指す参照3件(CS11)

- 起点層・根拠: 検査 C / `check-ssot.py` の CS11(参照実在)。
- 変更が必要になった条件: `docs/02-design/architecture.md:192` が `docs/issues/092` を、
  `docs/03-impl/relations/MODULE-cli-common-write-project-ssh-keys.md:86` と
  `MODULE-cli-ssh-keys-reset.md:30` が `docs/issues/002` を根拠として引いていたが、
  どちらの issue も解消時に削除されている。読者が根拠を辿れない。
- 直し方: **参照そのものを落とした**。`docs/pendings.md` の追跡行は「経緯の履歴が複数あって
  指す先が一意に決まらないため機械的には直せない」と書いていたが、**指す先を選ばずに参照を
  落とす**という手が残っており、事実(`- ` で始まる行がすべて消える欠陥の解消)は本文に残る。
  CS11 は OK になった。追跡行は残る6件(`docs/issues/024`。00〜03 層の外なので機械検査に
  現れない)だけを指す形へ書き直した。

### R-03 `docs/issues/094` が、実在しない文書と条項を「いま在る重複」として挙げていた

- 起点層・根拠: 検査 C(issue が完全性の主張と矛盾しない)。独立レビューが検出。
- 変更が必要になった条件: 「同型の全件(5 件 / のべ 15 箇所)」の #2 が
  `docs/02-design/contracts/cli-orchestrator.md:42` と `FR-orch-03-3` を挙げるが、
  どちらも 2026-08-08 のオーケストレーター廃止で消えており、重複そのものが存在しない。
  **全件の主張(5 件)が偽である。**
- 直し方: 行を落として見出しを実数(4 件 / のべ 13 箇所)へ直し、落とした理由を日付つきで
  1文書いた。**残る番号は動かしていない**(本文が `#5` を名指しで参照しているため)。

### R-04 `docs/pendings.md` の残務3行が古い実測値と廃止済みスキル名を持っていた

- 起点層・根拠: 検査 C / 原則8(残務は1鍵1行。重複鍵は書かない)。独立レビューが検出。
- 変更が必要になった条件: (1) 用語集の例が空の件が **2行に二重で**在り、古い側は
  「23 語のうち 17 語」という古い実測値を持っていた(実測は 8 語)。(2) `P-003` が現存しない
  スキル `/codex-qa` を運用対象として書いていた。(3) 「残る issue 7 件」は実際は 6 件。
- 直し方: (1) 古い側を削除し、起点(削除済み issue 071 の残件であること)を残る行へ吸収した。
  (2) `/browser-qa`(`/verify-tests` の qa scope)へ置換。(3) 実数へ置換。
  **残務は 46 行 → 45 行へ減り、この実行で2行を足して 47 行**(上限 50)。

### R-05 `docs/01-requirements/usecases.md` にテンプレートの `## 未解決事項` が無かった

- 起点層・根拠: 原則9(テンプレートの見出し構造を変えない。節の欠落はその層の規範が
  名指しした場合だけ許される)+ 検査 C。独立レビューが検出。
- 変更が必要になった条件: 01 層の4文書のうち `usecases.md` だけがこの節を持たず、
  `.claude/templates/01-usecases.md:65` は節を定めている。
- 直し方: 同層の `functional.md:396` と同じ書き方で節を回復した(内容は「なし」)。

### R-06 `docs/03-impl/tests/e2e.md` の E2E-01 の要約セルが手順11 を落としていた

- 起点層・根拠: 検査 C(陳腐化)。
- 変更が必要になった条件: 要約セルは手順7・8・9・10 を矢印で列挙して終わるが、
  同文書 `:644` に手順11(読めないホスト資産があっても起動まで到達すること)が在る。
- 直し方: 同文書の手順11 の見出しからの転記で1項を足した(MINOR)。

## 変更点

| ファイル | 変更 | 版 |
|---|---|---|
| docs/README.md | 変更指示・4フェーズ表を4フロー像へ置換。ディレクトリ図に build-records / reverification / sheets を反映し、消えた tasks/ を落とした | 版を持たない文書 |
| docs/ONBOARDING.md | 8箇所の廃止済み記述を置換(合格証の書き手・失効の条件・スキル表・問いのされ方・禁止事項・はじめ方) | 版を持たない文書 |
| docs/02-design/architecture.md | 削除済み issue 092 への参照を落とした / 合格証を再発行 | 1.7.0 → 1.7.1 |
| docs/02-design/system.md | 合格証を再発行(本文は触っていない) | 2.19.0(据え置き) |
| docs/02-design/environments.md | 合格証を再発行(本文は触っていない) | 1.7.0(据え置き) |
| docs/01-requirements/usecases.md | `## 未解決事項` を回復 | 1.7.0 → 1.7.1 |
| docs/03-impl/relations/MODULE-cli-common-write-project-ssh-keys.md | 削除済み issue 002 への参照を落とした | 版を持たない文書 |
| docs/03-impl/relations/MODULE-cli-ssh-keys-reset.md | 同上 | 版を持たない文書 |
| docs/03-impl/tests/e2e.md | E2E-01 の要約セルに手順11 を足した | 1.16.0 → 1.17.0 |
| docs/03-impl/tests/cli-list.md / cli-start.md / makefile.md | 合格証を再発行(F3 からの申し送り。本文は触っていない) | 据え置き |
| docs/issues/094-... | 実在しない #2 を落として全件の実数へ直した | issue |
| docs/pendings.md | 3行を修正・1行を削除・2行を追加(47 行 / 上限 50) | — |

## 検証

- `python3 .claude/scripts/check-ssot.py docs` → **NG 4 件**。CS11 は OK になった(3件を直した)。
  残る4件は CS20(`origin_layer` の欠落。`005` / `010` / `028` / `055`)で、**`docs/pendings.md`
  の 2026-08-12 の行が追跡している既存の欠落**である(埋めるには各 issue の対処案の裁定が要り、
  F2 の自動修正の範囲=機械的に正解が定まるものを超える)。CS8 曖昧語は0件。
- `python3 .claude/scripts/check-relations.py` → 合格(61 ファイル / 61 ID)。
- 独立レビュー: `@review`(reviewer-5 / claude)。指摘 10 件(高3 / 中4 / 低3)。
  **本日 codex は使っていない**(2026-08-20 に2度 Landlock exit 101 で1バイトも書けず、
  人間の裁定で claude レンズへ切り替えた)。作業ツリーへの書き込みが0であることを
  起動前後の `git status --porcelain` の比較で確認した。
