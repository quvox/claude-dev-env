---
id: 2026-08-20-verify-docs-pendings-citations
date: 2026-08-20
task: F2 文書整合フロー(`/verify-docs docs/pendings.md` — 部分再検証)
lane: standard
origin_layer: 02
issue: なし(新規の起票なし)
summary: docs/pendings.md 自身の引用位置4件を実測値へ取り直し、削除済み issue 024 を指す参照2件を先例どおり落とし、F3 が足した重複鍵の残務1行を規範の1鍵1行により削除した
---

# 2026-08-20 `/verify-docs docs/pendings.md`(部分再検証)

対象は `docs/pendings.md` 1件(依存スコープ)。00〜03 の層間トレーサビリティは範囲外である。

## 変更理由

### R-01 残務行が名指しする `path:line` 4件が現物と合っていなかった

- 起点層・根拠: 検査 C(陳腐化した引用)/ `issues-pendings.md` §2.1(残務行は `path:節` を鍵とする
  ため、位置が外れた行は次に読む側を誤った場所へ導く)。
- 事実(すべて実測で一意に定まる):
  - `docs/02-design/system.md` の「1条項につき主担当モジュールはちょうど1つ」は `:186` であり
    `:171` ではない(`grep -n`)。
  - `acquire_lock` の `trap '_release_all_locks; exit 130' INT TERM` は `claude-dev:602` /
    `claude-dev-mac:685` であり `:452` / `:517` ではない。
  - `docs/03-impl/index.md` が廃止済みの `check-changeset.py` を指すのは `:47`・`:54`・`:56` であり
    `:41`・`:48`・`:50` ではない。
  - `Makefile` の `clean:` は `:283`、削除対象の列挙と `read -p` は `:284`-`:290` であり、
    `:263`-`:271` は `status` レシピの問い合わせ失敗の分岐である。
- 直し方: 数値の置換だけ。主張の中身は変えていない(行を増やしていない)。

### R-02 削除済み `docs/issues/024` を指す参照2件(P-005)

- 起点層・根拠: 検査 C / 同ファイル `残務` の 2026-08-19 の行が「参照を落とすか事実を本文へ書くかを
  次に同ファイルを触るタスクが決める」と残していた判断。
- 直し方: 2026-08-20 の F2 が `002` / `092` で採った先例(**指す先の履歴を選ばず参照そのものを落とす**)に
  合わせ、`:88` は事象の説明へ、`:106` の `関連` は P-004 と同じ「なし(解消済み・削除)」の書き方へ置換した。
  同 `残務` 行の件数を 6件 → 3件(残るのは `issues/028-...` の3件)へ実測で取り直した
  (`docs/reverification.md` 側の1件は P-005 の `関連` からの生成物なので、再生成で消える)。

### R-03 F3 が足した残務1行が既存行と同一の鍵だった(削除)

- 起点層・根拠: `issues-pendings.md` §2.1(1)「1鍵1行。書く前に節を grep し、既に在れば**何も書かない**」。
- 事実: 2026-08-20 の F3 が足した行(`02-design/system.md:186` の主担当1つ規則が NFR 行に掛かるか不明。
  該当7行)は、2026-08-19 の行(同ファイル「要件カバレッジ確認」の主担当が複数の行がある。
  主担当を1つに決めるか本文の言い回しを直すかを次のタスクが判断する)と `path:節` も一文の内容も同じである。
  加えて件数が実測と違っていた: 割り当てモジュール列に2つ以上を書く NFR 行は `system.md:363`-`:370` の
  **8行**であり(2026-08-19 の行が挙げる8件と一致)、7行ではない。
- 直し方: 後から書かれた側(F3 の行)を削除し、正確な既存行を残した。**指摘の内容は失われていない**
  (規則の読み方を決めるか本文を直すかの判断は既存行が持っている)。

### R-04 独立レビュー(`@review`)が挙げた引用位置の外れ5件

- 起点層・根拠: 検査 C / `issues-pendings.md` §2.1。いずれも実測で一意に定まる。
  - `docs/02-design/environments.md` の「監査と QA が走るサンドボックスの強度」は `:177`-`:188` であり
    `:70`-`:84` ではない(`:70`-`:84` は「コンテナ・実行環境」表の後半)。
  - `clean` の `ids=` の代入は `Makefile:295`-`:297` であり `:286`-`:289`(表示行)ではない。
  - `status` の `awk` は `Makefile:267` であり `:258` ではない。
  - `docs/03-impl/index.md` に現れる廃止済みの道具は `check-changeset.py` だけで、
    `compose-changeset.py` と `.claude/directions/change-set.md` は同ファイルに1件も無い。
  - 24,000 バイト超の仕様ドキュメントは 13 件(生成物 `03-impl/callgraphs/shell.md` を除く)であり 15 件ではない。
- 直し方: 数値と語の置換だけ。`.gitignore` の `.DS_Store` 行が無いという主張は現在も真なので残し、
  根拠に挙がっていた「3つの `.DS_Store` が未追跡で残っている」だけを落とした(現物は0件)。

### R-05 同一の欠陥を持つ残務2行のうち、位置が外れた側を落とした

- 起点層・根拠: `issues-pendings.md` §2.1(1)。
- 事実: 2026-08-11 の行は E2E-01 の「すぐに」を「手順8-3」と書くが、`docs/03-impl/tests/e2e.md` の
  当該記述は `:77` = **手順7-3** の子項3であり、手順8-3 は実在しない。2026-08-19 の行が同じ欠陥を
  正しい位置で持つ(同一文意・同一根拠・`CS8` が「すぐに」を数えない旨まで含む)。
- 直し方: 位置が外れた 2026-08-11 の行を削除した。日付は古い側が失われるが、鍵(`path:節`)が
  読者を誤った手順へ運ぶ行を残す方が高い。

### R-06 残務行の形が `doc-health.py` の認識前提から外れていることを1行で記録した

- 起点層・根拠: CLAUDE.md §3(キット凍結中の規範の案は `pendings.md` の1行へ)。
- 事実: 48 行のうち日付の直後に `docs/` 始まりのパスを置くのは 8 行で、`LEFTOVER_RE`
  (`.claude/scripts/doc-health.py:437`)の第2群をパスとして使う `:486` の実在検査が大半の行に
  効いていない。既存の 2026-08-20 の行(同じ正規表現が**真の行を誤って落とす**側)とは別の問題
  なので、§2.1 の「同じ節の異なる問題は2行」により別行として足した。
- 付記: この行自身が最初にその型で落とされる形(閉じバッククォートに続けて日本語)で書かれており、
  既存行が記録している回避(閉じバッククォートの後に空白)を当てて復帰させた。
