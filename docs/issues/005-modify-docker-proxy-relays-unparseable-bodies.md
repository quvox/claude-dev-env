---
id: 005-modify-docker-proxy-relays-unparseable-bodies
type: modify
severity: 中
found: 2026-08-03
found_in: task-docs-restructure の /doc-check(Claude 自身の検査 A1 と docker-proxy/main.go の精読。独立レンズの指摘ではない)
related: MODULE-docker-proxy-serve, CTR-docker-api, FR-env-07, AC-03, D0-sec-05
summary: docker-proxy は解釈できないボディを検査せず中継するため、AC-03 の「危険な操作は拒否される」という保証に穴がある
---

# 005 解釈できないボディを中継する経路が AC-03 の保証と食い違う

## 事象

`docker-proxy` はコンテナ作成要求のボディが JSON として解釈できない場合、**検査せずにそのまま
上流の Docker Engine へ中継する**(`docker-proxy/main.go:493`〜`496` が警告ログを出して `return nil`)。
これは意図的な設計で、01 と 03 の双方に明記されている。

- `01-requirements/functional.md` FR-env-07 受け入れ基準8(異常系):
  「IF リクエストのボディが解釈できないならば、システムはそれを中継してよい(独自解釈で誤って
  弾かない。最終判断は Docker Engine が行う)」
- `03-impl/contracts/docker-api.md`「既知の制限」:
  「独自解釈で正常な操作を弾かない代わりに、検査をすり抜ける要求が Docker Engine まで届く」

一方、00 層の受け入れ基準は無条件の保証として書かれている。

- `00-requests/acceptances.md` AC-03:
  「期待される結果: …特権コンテナ・ホストのネットワークやプロセス空間の共有は**拒否**される」
  「こうなったら不合格: ルートディレクトリの持ち込みや特権コンテナが通ってしまう」

**解釈できないボディに特権指定やルート bind が含まれていれば、それは拒否されずに通る。**
したがって AC-03 の「こうなったら不合格」に該当する状態が、仕様上許容された経路として存在する。

## なぜ今すぐ直さないか

- 直すとすれば **fail-closed(解釈できないボディを拒否する)への変更**であり、
  観測可能な振る舞いが変わる。`task-docs-restructure` は「コードを1行も変更しない」タスクなので
  範囲外である。
- 現在の挙動は `D0-sec-05`(委任: Docker API 検査の厳密さと安全側の倒し方)の範囲で選ばれたもので、
  「独自パーサで正常な操作を誤って弾かない」ことを優先している。安全性と可用性のトレードオフであり、
  どちらへ倒すかは**人間の判断**である。

## 論点(人間の判断が要る)

解釈できないボディに対して、

- **A: 現状維持**(中継する)。AC-03 に「ボディが解釈できる要求について」という限定を付け、
  00 の保証を実態に合わせる。→ 00 層の編集が必要。
- **B: fail-closed へ変更**(解釈できないボディは拒否する)。AC-03 の保証をそのまま守る。
  → コード変更。正常な操作を誤って弾くリスクを受け入れる。
- **C: 中間**(コンテナ作成と exec 作成の要求に限って fail-closed、他は現状維持)。
  危険な操作を持ちうるエンドポイントだけ厳しくする。→ コード変更。影響範囲は B より狭い。

実際に解釈できないボディが発生する頻度は測定していない。判断の前に、
`docker-proxy` の警告ログ(`main.go:493`)の出現実績を確認すると材料になる。

## 影響

- 悪用には「Docker Engine は受理するが docker-proxy の JSON パーサは失敗する」ボディを作る必要が
  あり、容易ではない。ただし本システムの隔離は**この1本の境界に依存している**(`SR-04` / `D0-sec-06`)
  ため、穴の存在自体を把握しておく価値がある。
- 利用は信頼できる社内開発用途に限る前提(`SR-05`)があるため、緊急度は高くない。

## 関連

- `docs/issues/004-modify-03-impl-lacks-reimplementation-depth.md`
- `00-requests/decisions/sec.md` の `D0-sec-05`

## 経緯(追記)

- 2026-08-03 **人間の判断: 現状維持 + 限定の明記。** 実装(解釈できないボディを中継する)は変えず、
  `00-requests/acceptances.md` の AC-03 に「この保証の限定」を追記した。理由は、独自解釈を広げると
  Docker の仕様変更のたびに正常な操作を誤って止めるようになるためで、`DSN-dp-01`(判定できない入力は
  通し、判定できる危険だけを止める)と同じ根拠である。**本 issue は AC-03 に限定が明記されたことで
  「文書と実装の食い違い」ではなくなったが、fail-closed へ倒す選択肢は将来の論点として残す**ため
  クローズしない。

## 追加(2026-08-04 `/doc-check ssot task-impl-depth` の新しい実行)

**独立監査の Codex 2本が、それぞれ独立に本件を重大度「高」として挙げた**
(`docs`(00+01)= check A0 で `AC-03` ⇄ `D0-sec-05`、`docs`(01→02)= check A1 で
`FR-env-07` 受入基準8 ⇄ `D0-sec-05`)。Claude は**中に留める**と裁定した。理由は次のとおり。

**`D0-sec-05` の委任範囲そのものが、この論点を明示的に含んでいる**:

> 委任範囲: docker-proxy がリクエストを検査するときの厳密さ(字句的な判定に留めるか、実体解決まで
> 行うか)、**解釈できない入力を通すか止めるか**。

つまり「解釈できないボディを中継するか」は**委任された判断**であり、`FR-env-07` 受入基準8 が
「中継してよい」と書いていることは委任の範囲内での決着である。ガードレールの
「拒否すべき操作を通してはならない」は絶対条件のように読めるが、同じ委任が
「**判定に必要な情報がプロキシの手元に無い場合は、その事実を該当機能の『既知の制限』に書くこと**」
という手続きを併せて定めており、**判定不能な入力の存在を前提にしている**。
したがって 00 内の正面衝突ではない。

### ただし、この実行で新たに確認した具体的な不備が1件ある

`D0-sec-05` のガードレールは残存リスクを **「既知の制限」に書くこと**を求めているが、
`MODULE-docker-proxy-serve.md` では「パース不能なボディは中継を許可する」が
**`## 実装上の判断` の2行目**に書かれており(`D0-sec-05` の記録として)、
**`## 既知の制限` の表には該当行が無い**(同表にあるのは symlink 脱出・関数値経由の未到達・
create/hijack の構造の3件)。

- 「実装上の判断」= 委任の中で何を選んだか
- 「既知の制限」= その選択が残す、利用者に影響する制約

の書き分けからすると、**「解釈できない要求は検査されずに Docker へ渡る」という事実は
『既知の制限』側にも必要**である。`AC-03` が「危険な操作は拒否される」と約束している以上、
残存リスクが読者に見える場所に無いのは実害がある。

| # | 対処 | 種別 |
|---|---|---|
| 1 | `MODULE-docker-proxy-serve` の `## 既知の制限` に「解釈できないリクエストボディは検査せず中継する(拒否すべき操作が含まれていても通る。最終検証は Docker 側)」を1行追加し、`関連 issue` に本 issue を書く | ドキュメント修正。**`issue 038` の relations 全面揃えと同じタスクで行う**(同じ層の同じ性質の修正を分割しないため) |
| 2 | `AC-03` の「危険な操作は拒否される」に、解釈不能な入力についての例外を明記するか判断する | **00 層なので人間判断** |
