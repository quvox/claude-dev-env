---
created: 2026-08-22
---

# 2026-08-22 質問シート

**記入方法**: 各ブロックの **★あなたの記入** の後ろに1行書いてください。
チャットで答えても構いません(その場合、次に動く AI が発言を逐語引用＋日付つきで転記先へ写します)。

## 一括回答(ここだけ埋めれば先へ進めます)

**★あなたの記入**:

## 概念の明確化(00/01。上から確認してください。最大7件)

### 1. 曖昧さなし —「新しい語を持ち込んでいない」

- **場所**: `docs/00-requests/decisions/dist.md`(2026-08-22 に足した決定)
- **明確にしたいこと**: なし。この問い合わせは既にある言葉づかいだけで書けている
- **AI推奨**: 語を増やさない
- **未回答時の既定**: AI推奨のとおりにする
- **波及**: なし
- **間違えたときの戻し方**: 取り違えが起きたら、その語を使っている文書1つを直せば戻る
- **過去の回答**: なし(決定台帳・構築記録の回答・履歴を走査済み)

**★あなたの記入**:

## 論点(概念以外)

### 論点 1. ブラウザ画面を持たない構成でも、Codex の設定にブラウザ操作の登録を書くか

- **選択肢**:
  - A: **書かない**(いまの実装と同じ)。ブラウザ画面ありで起動したときだけ、Codex の設定に
    ブラウザ操作の登録を書き足す。画面なしの軽い構成では設定に何も増えない
  - B: **常に書く**。画面なしの構成でも同じ登録を書く。その構成には接続先のブラウザが無いので、
    Codex がその機能を使おうとすると失敗する
- **AI推奨**: A。理由: 画面なしの構成には接続先のブラウザが存在しないので、登録しても使えず、
  失敗の原因が分かりにくい形で残ります。この選択で後から高くつくこと: 画面なしの構成でも
  外部のブラウザに繋いで使いたい、という要望が出たときに、この条件を外す変更が要ります。
  崩れる条件: 画面なしの構成からコンテナの外のブラウザへ繋ぐ使い方を認めたとき
- **未回答時の既定**: A(いまの実装と文書のまま。この論点は次の検証でもう一度出ます)
- **委任外の根拠**: DS-04(要求に数値や既定が書かれていないとき、AI が決めてよいという標準の委任)の
  対象外 — 根拠: `docs/00-requests/decisions/dist.md` の `D0-dist-06` 項4(起動時に双方の設定へ
  登録する、と条件なしで決めている)。**上流が既に別のことを言っているので、AI の側では決められません。**
  加えて、直すとしたら要求の層の文であり、そこは人間のものです
- **根拠(上流)**: `docs/00-requests/decisions/dist.md` の `D0-dist-06` 項4(起動時に双方の設定へ
  登録する。条件は書かれていない)
- **根拠(同層)**: 同じ起動処理は、ブラウザ画面まわりの設定を「画面ありのときだけ」書いてきた
  (`docs/03-impl/relations/MODULE-entrypoint-claude.md` の手順18)
- **根拠(下流)**: この判断が着地する先は2箇所(コンテナ起動処理の分岐1つと、
  受け入れ基準 `FR-env-12-14` の条件1つ)
- **間違えたときの戻し方**: いつ気づくか = 画面なしの構成で Codex にブラウザ操作を頼んで失敗した
  とき(B を選んだ場合)、または画面なしでも使いたいと言われたとき(A を選んだ場合)。
  何を直せば戻るか = 起動処理の分岐1つと受け入れ基準の1行(どちらも後続の普通の変更で足ります)
- **過去の回答**: なし(決定台帳・構築記録の回答・履歴を走査済み)
- **転記先**: `docs/00-requests/decisions/dist.md` の `D0-dist-06`(A なら項4 へ条件を1文足す。
  B なら実装と `docs/01-requirements/functional.md` の `FR-env-12-14` を戻す)

**★あなたの記入**:
