---
target: docs/01-requirements/system.md
change: replace
version_bump: minor
sections:
  - "## 開発言語・フレームワークの要件"
deletes: []
reason: '`SR-24` の射程に同梱外部バイナリの設置を加える。現行は「コンテナイメージはマルチステージビルドで構成し、**エージェント CLI の導入**を配布ステージの終端レイヤーに置くこと」で、終端に置く理由(更新時の再取得範囲を最小化する = `NFR-perf-01`)は同梱外部バイナリにもそのまま当てはまる — 保守者が差し替えるたびに層が失効するものであり、上流に置くと `vnc-base` 以降の高コスト層まで巻き込む(`DSN-dist-01` の一般原則)。条文がエージェント CLI しか名指していないと、同梱外部バイナリの設置位置を縛る上流が無くなる。`SR-20` / `SR-21` と、廃止済み `SR-22` / `SR-23` の注記は1文字も変えない。SR 番号は動かさない(`SR-25` を新設せず `SR-24` の射程を広げるのは、制約の内容が「終端レイヤーに置く」1つで同じであり、2本に割ると同じ制約が2箇所に逐語で載るため)'
---

## 開発言語・フレームワークの要件

| ID | 要件 | 補足 |
|---|---|---|
| SR-20 | ホスト CLI と補助スクリプトは Bash で実装すること | 追加ランタイムを要求せず、Linux/macOS の標準環境で動くため |
| SR-21 | docker-proxy は Go で実装すること。単一バイナリで配布できること | コンテナへ実行時依存を持ち込まないため |
| SR-24 | コンテナイメージはマルチステージビルドで構成し、**エージェント CLI の導入と同梱外部バイナリの設置**を配布ステージの終端レイヤーに置くこと | 更新時の再取得範囲を最小化するため(`NFR-perf-01`)。どちらも中身が変わるたびに層が失効するものであり、上流のステージに置くとブラウザ確認資産の層まで巻き込んで失効する |

<!-- SR-22(Go の依存の限定と TUI の例外)と SR-23(自己検証題材)は 2026-08-08 に廃止した
     (オーケストレーターの削除)。番号は再利用しない。 -->
