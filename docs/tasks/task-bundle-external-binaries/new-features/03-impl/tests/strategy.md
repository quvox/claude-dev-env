---
target: docs/03-impl/tests/strategy.md
change: replace
version_bump: patch
sections:
  - "## カバレッジの扱い"
  - "## テスト設計の判断"
deletes: []
reason: '`FR-env-13` の6条項を `tests/images.md` へ足すと、この文書の「カバレッジの扱い」表が持つ現状欄(「機能要件の全 141 条項に行がある(非機能要件の 10 行を合わせて対応表は 151 行 = 機能要件の条項 141 件 + 非機能要件 10 件)」)が**事実と食い違う**。反映後の正しい値は機能要件 147 条項・対応表 157 行なので、この1行だけを実値へ改める(独立レビュー Codex が検出)。目標欄・測定コマンド・行カバレッジの行・下の説明文は1文字も変えない。「テスト設計の判断」は CS19 が要求する再読のために `sections` へ載せるが、**既存の2行はどちらも継続**である — レベルの割り当ての既定(`DSN-test-01` / `SR-32`)は動かしておらず、集計を `build-index.py` の再生成で確認するという判断も、まさに今回の件数ずれを検出する仕掛けとして有効なままである'
---

## カバレッジの扱い

| 指標 | 目標 | 現状 | 測定コマンド |
|---|---|---|---|
| 行カバレッジ(Go) | **目標値なし**(01 の非機能要件にカバレッジの目標は無い) | 測定していない | `docs/02-design/environments.md`「lint・テストコマンド」の**カバレッジ計測(docker-proxy)**の行 |
| 受入基準のカバレッジ | すべての受入基準が対応表に行を持つこと(状態は問わない) | 機能要件の全 147 条項に行がある(非機能要件の 10 行を合わせて対応表は 157 行 = 機能要件の条項 147 件 + 非機能要件 10 件。**主担当行はどの条項もちょうど1つである**) | `python3 .claude/scripts/build-index.py --check` で集計を再生成して確認する |

**カバレッジ率ではなく「受入基準に行があるか」を指標にする。** 自動テストを持てない領域が大きい
(Bash と Makefile)ため、行カバレッジは実態を表さない。

## テスト設計の判断

<!-- この層全体に掛かる判断だけを置く。モジュール個別の判断は各 `03-impl/tests/<module>.md` にある。 -->

- 判断なし: レベルの割り当ての既定(`DSN-test-01` / `SR-32`)を動かしていないため、層全体に掛かる判断は無い。既定から外れる割り当ては1件も無く、モジュールごとの割り当ての事実は各 `03-impl/tests/<module>.md` が持つ
- [DS-01] 集計値を手で数えず `python3 .claude/scripts/build-index.py --check` の再生成で確認する — 理由: 条項数・行数・件数の3つは互いに導出関係にあり、手で数えると3つが別々にずれても気づけない(この節の上の表がその3つを1行で持っている) / 見直す条件: `build-index.py` が受入基準の対応表を集計しなくなったとき
