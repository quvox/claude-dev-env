# task-fix-destructive-scope フェーズ1の決定シート(回答済み)

> `/task-doc`(フェーズ2)が 2026-08-04 に memo.md から追い出した。回答の反映先は
> `new-features/00-requests/decisions/env.md`(`D0-env-08` / `D0-env-09` / `D0-env-10`)と
> 各層の変更指示。

## 回答(2026-08-04。人間)

| # | 回答 | 反映先 |
|---|---|---|
| 1 | **A**(起動時のラベルで対象を絞る) | `D0-env-08` 項1・2 / `CTR-cli-container`「破壊的操作の対象の識別」/ `DSN-env-01` |
| 2 | **A**(`D0-env-08` を**決定**として書く) | `new-features/00-requests/decisions/env.md` の `D0-env-08` |
| 3 | **A**(対話時は確認、非対話は中止。`--yes` で明示同意。対象は全プロジェクト) | `D0-env-08` 項3 / `FR-env-03` 受入基準 14〜16 |
| 4 | **A**(削除失敗を検出して非0で終わる。残った資源を列挙) | `D0-env-08` 項4 / `FR-env-03` 受入基準18 |
| 5 | **A**(`start`/`stop`/`logout`/`reset` の4コマンド。プロジェクト単位 + 共有資源単位の2段) | `D0-env-08` 項5 / `FR-env-01` 受入基準 16・17 / `DSN-env-02` |
| 6 | **A**(`docs/issues/028` は含めない) | 変更指示なし(範囲外を維持) |
| a | **承認**(ロックの実装手段と置き場所を AI に委任) | `D0-env-09`。行使結果は `MODULE-cli-common-lock` の「実装上の判断」1〜7 |
| b | **承認**(管理ラベルの名前と値の形式を AI に委任) | `D0-env-10`。行使結果は `CTR-cli-container`「管理ラベル」 |

**フェーズ2 で判明した、論点1=A の前提の誤り**: 推奨案の本文は「`024`(compose の巻き込み)も
同じ1つの機構で直る」と述べていたが、**compose 資源にはホスト CLI のラベルが届かない**
(compose が Claude コンテナの中で docker-proxy 経由に作るため)。したがって**論点1=A だけでは
`docs/issues/024` は閉じない**。この点はフェーズ2 の未決点(人間判断)として memo.md に立てた。

## 決定シート(回答済み)

根拠の三方向: 上流=既に選択を縛るもの / 同層=先例 / 下流=壊れるもの。

| # | 論点 | 選択肢 | 推奨案(理由・下流の代償・崩れる条件) | 未回答時の既定 | 根拠 |
|---|---|---|---|---|---|
| 1 | **対象の絞り込み方式** | **A**: 起動時に `--label claude-dev.managed=1`(+ `claude-dev.project-dir=<絶対パス>`)を付け、破壊的操作は**ラベルで対象を絞る** / **B**: `ancestor` にイメージ ID の履歴を並べる / **C**: 命名の一意化(`028`)で解く | **A**。5件のうち `024`(compose の巻き込み)・`045`(古いイメージの数え落とし)・`029`(全プロジェクト削除)が**同じ1つの機構で直る**。**下流の代償**: `CTR-cli-container` の起動オプションが増え、**既存の稼働コンテナはラベルを持たない**ので移行期の扱いが要る(推奨: ラベル無しは破壊的操作の対象から外して警告する)。**崩れる条件**: 利用者が手で同じラベルを付けた場合(実害なし) | **A** | 上流: `FR-env-01` #9(他プロジェクト稼働中は docker-proxy を止めない)/ `D0-env-05`(共有資源は消さない) 同層: `MODULE-cli-stop` は既に `com.docker.compose.project` ラベルで compose を絞る先例を持つ 下流: `--impact claude-dev` → `MODULE-cli-*` 4本 + `CTR-cli-container` + tests 4本 |
| 2 | **`D0-env-08` の区分** | **A**: **決定**として書く(「破壊的操作は自分が作った資源に限る。識別方法は 02 の契約が定める」)/ **B**: **委任**として書き、識別方法の選択を AI に委ねる | **A**。これは「他プロジェクトの作業を壊さない」という**利用者に対する約束**であり実装の裁量ではない。**下流の代償**: 将来識別方法を変えるとき 00 を読み直す必要がある(方法自体は 02 に置くので 00 は動かない)。**崩れる条件**: 「自分が作った」の定義が曖昧だと委任と変わらない → 決定文に「起動時に付けた印で識別する」と書いて閉じる | **A** | 上流: CLAUDE.md 原則1(実装が従う形で書く=本タスクで同時に実装する)/ `D0-scope-06` 同層: `D0-env-05` が compose 限定で同趣旨 下流: `FR-env-01` / `FR-env-03` の受入基準 |
| 3 | **`logout` の確認と対象範囲(`029`)** | **A**: 対話時は確認プロンプト、非対話は**中止**(`--yes` で明示同意)。対象は従来どおり全プロジェクト / **B**: 対象を自プロジェクトのみに変える / **C**: 確認だけ入れて非対話は続行 | **A**。共有ボリュームを空にする操作なので**対象を絞れない**(B は `logout` の意味を変える)。非対話で中止するのは `reset` の既存挙動と揃う。**下流の代償**: 自動実行には `--yes` が必要になる(後方互換の破れ。利用者は社内の少人数なので受容可能)。**崩れる条件**: 既に `logout` を自動化しているスクリプトがある場合 | **A** | 上流: `FR-env-03` #5 / `AC-06` 同層: `reset` は既に確認プロンプトを持つ(`claude-dev:1377`) 下流: `MODULE-cli-logout` / `tests/cli-logout.md` |
| 4 | **削除失敗の扱い(`025`)** | **A**: 失敗を検出して**非0で終わる**(何が残ったかを列挙)/ **B**: 警告を出して 0 で終わる / **C**: 現状維持 | **A**。「削除しました」と表示して実際は残る状態は `NFR-sec-02` の観点で**利用者に誤った安心**を与える。**下流の代償**: 一部失敗でも非0になる。**崩れる条件**: docker の一時的な応答不良まで非0にすると煩わしい → **消えなかった資源を列挙**して再実行可能にする | **A** | 上流: `FR-env-03` #5 / `NFR-sec-02` 同層: `FR-env-01` #11 は「使用中の compose ネットワークは続行」= **意図的に続行する先例**なので書き分けが要る 下流: `MODULE-cli-logout` / `-reset` / tests 2本 |
| 5 | **排他(`020`)をどこまで入れるか** | **A**: `start` / `stop` / `logout` / `reset` の**4コマンド**に入れる(単位はプロジェクト名 + 共有資源の2段)/ **B**: 共有ボリュームを触る `logout` / `reset` だけ / **C**: 入れず `020` を残す | **A**。`020` の実害は「`start` と `logout` が重なって**認証が空のまま起動する**」なので `start` 側にもロックが要る。**下流の代償**: ロックファイルの置き場所が増える(`flock` はプロセス終了で自動解放されるので残骸の実害は無い)。**崩れる条件**: **macOS に `flock` が無い**(委任 a で代替手段を決める) | **A** | 上流: `FR-env-03` #2 / `NFR-scale-01` 同層: `MODULE-orchestrator-state-io` の「1ファイル1書き込み」 下流: `MODULE-cli-start` / `-stop` / `-logout` / `-reset` の「並行性」節 |
| 6 | **`028`(命名の一意化)を含めるか** | **A**: 含めない(別タスク)/ **B**: 含める | **A**。`container_name` の**11 呼び出し元**と利用者に見える名前が変わり、既存コンテナの移行手順が要る。論点1=A なら `028` を直さずに5件が閉じる。**下流の代償**: 別パスの同名ディレクトリが同一セッション扱いになる問題(`NFR-scale-01` との不一致)は残る。**崩れる条件**: 論点1 で C を選ぶ場合は `028` と一体になる | **A** | 上流: `NFR-scale-01` 同層: `task-fix-start-cleanup` も `028` を範囲外にした 下流: `MODULE-cli-common-container-name` の 11 呼び出し元 |

### 委任にしてよいか確認したい項目

| # | 論点 | 委任範囲 | 制約(ガードレール) |
|---|---|---|---|
| a | ロックの実装手段(`flock` / `mkdir` / その他)と置き場所 | Linux 版・macOS 版それぞれの手段、ロックファイルのパス、待ち時間の有無 | **観測可能な振る舞いを変えない**(ロック待ちで固まらない。取得できなければ理由を表示して非0)。Linux 版と macOS 版で**同じ成否・同じ出力**にする(`D0-scope-03`)。ロック残骸で永久に詰まらないこと |
| b | ラベルの名前と値 | `claude-dev.managed` / `claude-dev.project-dir` の名前・値の形式 | 契約(`CTR-cli-container`)に書くこと。**既存コンテナ(ラベル無し)を破壊的操作の対象にしない**こと |
