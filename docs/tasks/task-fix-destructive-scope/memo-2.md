# task-fix-destructive-scope フェーズ2の解消済み未決点

> `/task-doc`(フェーズ2)が 2026-08-04 に memo.md から追い出した。**未解決の U-20〜U-24 は
> memo.md に残っている。** 検出元の内訳: 自分のパス1 が8件、独立レンズ Codex が13件、
> /doc-check の読みが2件。

## 未決点(解消済み)

| # | 未決点 | 帰着 | 検出元 |
|---|---|---|---|
| U-1 | **compose 資源の識別方法**。論点1=A(管理ラベル)では `docs/issues/024` が閉じない。**compose はコンテナ内で docker-proxy 経由に資源を作るため、ホスト CLI の `--label` が届かない** | **人間判断 → 2026-08-04 回答=A(解消済み)**。`COMPOSE_PROJECT_NAME` を `<正規化名>-<絶対パスの SHA-256 先頭6桁>` にして一意化する。`D0-env-08` 項7 / `DSN-env-03` / `FR-env-01` 受入基準 19・20 / `CTR-cli-container`「compose 資源の識別」/ `MODULE-cli-start` 手順13・判断3 / `MODULE-cli-stop` 手順5・6・判断8・9 に反映。**旧い名前の資源は消さず案内する** | 自分のパス1 と 独立レンズ Codex(独立に同一の穴を検出) |
| U-2 | **`logout` がプロジェクトディレクトリ配下の認証コピーを消すか**。`docs/issues/025` の事象(2) | **人間判断 → 2026-08-04 回答=A(解消済み)**。`logout` が**カレントディレクトリの認証3ファイルだけ**を削除し、削除したパスを表示する。ディレクトリと `settings.json` は残す。他ディレクトリには触らない。`reset` は引き続き触らない(非対称の理由を明記)。`D0-env-08` 項4 / `FR-env-03` 受入基準 20〜22 / `MODULE-cli-logout` 手順5・判断9・10 / `MODULE-cli-reset` 判断2 に反映 | 自分のパス1(論点4 の選択肢が事象(1) だけを覆っており (2) を覆っていなかった) |
| U-3 | **`login` / `login-codex` を共有資源単位のロックの対象に含めるか**。含めないと `docs/issues/020` は閉じない | **人間判断 → 2026-08-04 回答=A(解消済み)**。対象を6コマンドに広げた。`D0-env-08` 項6 / `FR-env-01` 受入基準16 / `CTR-cli-container`「排他(ロックキー)」/ `PLAN-cli-login`・`-login-codex` / `MODULE-cli-login`・`-login-codex`(新規の変更指示)/ `MODULE-cli-common-lock` の `callers` / `features.md` の昇格表(ファンイン 4→6)に反映。**ロックは対話認証の全区間で保持する** | 自分のパス1(`issue 020` の事象表と論点5 の対象範囲の突き合わせ) |
| U-4 | `FR-env-01` 受入基準16 の「削除も作成も一切行ってはならない」が、`start` の手順9(認証コピー)で共有ロックを取れなかった場合と矛盾する(手順5・8 で既にファイル生成と停止中残骸の削除を済ませている) | **ドキュメント記載**(解消済み)。受入基準16 を「**取得できなかったロックが守る資源に対する**削除も作成も行ってはならない」に改め、各ロックが守る資源を括弧で定義した | 自分のパス1(観点「並行性と順序」) |
| U-5 | ロックの置き場所を環境変数で上書きできるか | **委任決定(D0-env-09)**(解消済み)。**上書き不可**と決めた。全員が同じ場所を見なければ排他が成立しないため。`MODULE-cli-common-lock` の実装上の判断3 に記録(判断表は /doc-check で 1〜12 へ振り直した) | 自分のパス1(観点「設定・環境変数の既定値と必須性」) |
| U-6 | 遊休判定に使う Docker への問い合わせが失敗したとき(ネットワークが無い / `docker` が応答しない)の振る舞い | **ドキュメント記載**(解消済み)。**遊休でないと判定して docker-proxy を残す**(安全側)。`CTR-cli-container` の「エラーケース」と `MODULE-cli-stop` の手順9 に記録 | 自分のパス1(観点「外部依存が失敗したときの挙動」) |
| U-7 | `FR-env-01` 受入基準 9・14〜17 と `FR-env-03` 受入基準 14〜19 を実機で確認する手順が無い(既存の `E2E-01` 手順1〜7 は覆っていない) | **ドキュメント記載**(解消済み)。`03-impl/tests/e2e.md` を影響範囲に加え、`### E2E-01` に手順8(**11項目**)を書いた。あわせて手順7-7 の `issue 045` 応急処置を修正後の期待値へ改めた | 自分のパス1(観点「テストデータの準備と後始末」) |
| U-8 | `reset` の非 TTY 時の終了コードを 0 から 1 へ改めることは**後方互換の破れ**である(`yes y \| claude-dev reset` は `--yes` に置き換わる) | **ドキュメント記載**(解消済み)。`D0-env-08` 項3 が全破壊的操作に「非対話は中止」を課したことの帰結として `FR-env-03` 受入基準15 に書き、`MODULE-cli-reset` の実装上の判断5 で撤回の事実を明記した。**差分サマリでも人間へ明示する** | 自分のパス1(観点「この変更で壊れる可能性のある既存の連携」) |
| U-9 | **ラベルを持たない Claude コンテナの集合をどう識別するか**が定まっていなかった(ラベルが無いのだからラベルでは引けない) | **ドキュメント記載**(解消済み)。契約に「残したものをどう列挙するか」節を追加: 遊休判定と同じ集合(`claude-dev-net` の稼働中)から固定名・固定接頭辞・管理ラベル付きを除いた残り。**停止中のものは列挙できないという限界も明記**し、表示にも出す | 独立レンズ Codex(D13・中) |
| U-10 | **`02-design/system.md` の `SCR-01` の公開フラグ一覧に `--yes` が無い**(UI 設計はコマンド面の正なので、合成ビューのコマンド面が一致しない) | **ドキュメント記載**(解消済み)。`02-design/system.md` を影響範囲に加え、`#### SCR-01 cli-commands` に `--yes`・受理文字集合・破壊的操作の状態を書いた。**私は「モジュール分割定義は不変」だけを見て UI 設計節を見落としていた** | 独立レンズ Codex(D13・中) |
| U-11 | **`D0-env-10` のガードレールが「02 と 03 の両方の契約に書くこと」を要求していたが、03 契約の変更指示が無い**(私は意図的に書かなかったが、00 の本文と食い違う) | **ドキュメント記載**(解消済み)。`D0-env-10` を「設計側(02)の契約に書くこと。実装側(03)は `/task-close` がコードから確定させる」に改めた | 独立レンズ Codex(D14・高) |
| U-12 | **`acquire_lock` の手順で「成功したら手順6へ」と書いたため、`owner`(PID と操作名)を書く手順を通らない**。結果として通常取得中のロックと残骸を区別できない | **ドキュメント記載**(解消済み)。手順番号を直し、さらに「**`owner` が無いロックは残骸と判定せず取得失敗として扱う**」を明記した(`mkdir` 成功から `owner` 書き込みまでの窓のロックを奪うと排他が成立しないため)。実装上の判断9 に理由を記録 | 独立レンズ Codex(D14・高) |
| U-13 | **`release_lock` が所有者確認なしに `rm -rf` し、`trap` からキーを外さない**ため、`start` が共有ロックを明示解放したあとに `logout` が同じキーを取ると、`start` の終了時の `trap` が**他人のロックを消す** | **ドキュメント記載**(解消済み)。`release_lock` に **`owner` の PID が自分と一致するときだけ削除**を課し、明示解放したキーを `trap` 用の配列から外すことを書いた(契約の「排他(ロックキー)」と実装上の判断8) | 独立レンズ Codex(D14・高) |
| U-14 | **ロックキーに許容する文字集合が未決**。`stop <name>` は非空だけを検証してそのままキー=パス要素にするため、`/` や `..` を含む名前で残骸回収の `rm -rf` が意図した外を指しうる | **ドキュメント記載**(解消済み)。`FR-env-01` 受入基準18 を追加(`[A-Za-z0-9._-]` 以外は何も削除せず非0)。契約に「ロックキーとして使える文字」節、`02-design/system.md` の `SCR-01` に受理文字集合、`MODULE-cli-stop` 手順1 と `MODULE-cli-common-lock` 手順1 に検証を書いた | 独立レンズ Codex(D13・高) |
| U-15 | **`require_setup` はロック取得前に走ってイメージをビルドする**のに、受入基準16 の「削除も作成も行わない」と `MODULE-cli-start` の「Docker 資源も一切作らない」が矛盾する | **ドキュメント記載**(解消済み)。契約の「排他(ロックキー)」に「**イメージのビルドはロックの保護対象ではない**」を明記し(冪等な共有資源で結果が同じ)、`start` 手順4 と `logout` 手順3 の文言を「手順5 以降の生成物と Docker コンテナ」に限定した | 独立レンズ Codex(D14・高) |
| U-16 | **契約の規則C は `logout` にも遊休判定を要求しているのに、`logout` のフローは docker-proxy を無条件に削除**していた。ラベル無しの既存コンテナが稼働中でも proxy を消す = `FR-env-01` 受入基準9 違反 | **ドキュメント記載**(解消済み)。`logout` に手順8(遊休判定)を追加し、`FR-env-01` 受入基準9 の対象を「`stop` と `logout`」に広げた。実装上の判断8 に理由を記録 | 独立レンズ Codex(D14・高) |
| U-17 | **`rm -rf /auth/* /auth/.*` の終了コードを失敗として記録する**と、`.` と `..` に当たって常に非0になるため、常に「削除失敗」と表示してしまう | **ドキュメント記載**(解消済み)。**判定は削除後の `/auth` の列挙結果だけで行い、`rm -rf` の終了コードは見ない**と契約のエラーケース・`logout` 手順9・実装上の判断4 に書いた | 独立レンズ Codex(D14・高) |
| U-18 | **`sections` に入れていない既存節(`### 並行性` / `## 異常系` / `## 既知の制限` など)に「排他機構を持たない」「確認プロンプトは無い」「削除の失敗をすべて握る」といった旧挙動が残り、合成ビューが矛盾する** | **裁定: 確認済みだが、キットが意図した一時状態なので本フェーズでは書かない。** `.claude/directions/relations.md` §4.1 と `/task-doc` §2 の 03 行は「実装前に書くのは frontmatter / 処理の流れ / 連携先と連携内容 / 実装上の判断だけ。**残りは書かない**」と明示し、`/implement` C-1 が実際に作られた姿へ書き換え、`/task-close` §2 がコードから再生成すると定めている。**ただし `## 目的` は §4.1 が実装前に書く節としているので `MODULE-cli-stop` の 目的(旧2経路の危険を述べていた)は書き直した。** レンズは `.claude/directions/` を読み取り範囲に持っていなかったためこの規範を知らない。**キット自身の緊張(合成ビューの整合と実装前の記述範囲)は `/kit-improve` 案件として申し送る** | 独立レンズ Codex(D14・高。**裁定=仕様どおり**) |
| U-19 | 独立レンズが立てた `docs/issues/024`(compose 資源)の指摘 | **U-1 と同一**(独立に同じ穴を検出した = 証拠として強い)。人間判断(決定シート #1) | 独立レンズ Codex(D13・高) |
| U-25 | **残骸ロックの引き継ぎに排他が壊れる競合窓がある**。2プロセスが同じ死んだ `owner` を観測すると、P1 が `rm -rf` → `mkdir` → `owner` 書き込みを終えた直後に P2 が `rm -rf` で**P1 の新しいロックを消し**、双方が臨界区間に入る | **委任決定(D0-env-09)**(解消済み)。回収を **`mv` による原子的な引き取り**に改めた(成功した1プロセスだけが回収権を持つ)。`MODULE-cli-common-lock` 手順6 と実装上の判断6 に記録 | 独立レンズ Codex(readiness・高) |
| U-26 | **`mkdir` 成功〜`owner` 書き込み〜`trap` 設定の区間で中断すると `owner` の無いロックが恒久的に残る**。判断「`owner` の無いロックは残骸と判定しない」(U-12)がこれを回収不能にし、`D0-env-09` のガードレール「ロック残骸で永久に詰まらないこと」に反する | **委任決定(D0-env-09)**(解消済み)。**`trap` を `mkdir` より前に**仕掛け(手順3)、`owner` が無い場合は**1 秒待って読み直し**、それでも無ければ放棄と判定して `mv` 経路で回収する(手順6)。実装上の判断7・8 に記録 | 独立レンズ Codex(readiness・高) |
| U-27 | `MODULE-cli-stop` の `D0-env-08` 項番号が3箇所ずれていた(reason「項5 の排他」→項6 / 手順6「項4 が明示する例外」→項5 / 判断3「項4 の対象外」→項5)。`DSN-env-02` の関連も「項5」→項6 | **ドキュメント記載**(解消済み。/doc-check の自動修正) | 自分の読み |
| U-28 | `DSN-env-02` の内容と「順序性・冪等性・並行性の背景」が排他の対象を4コマンドと書いていた(U-3 で6コマンドへ広げた結果が反映されていない) | **ドキュメント記載**(解消済み。/doc-check の自動修正) | 自分の読み |
| U-20 | **`reset` が docker-proxy と `claude-dev-net` を無条件に削除する一方、ラベル無しの稼働中 Claude コンテナは残す**(手順3)。`D0-env-08` 項2 は共有資源の停止条件を無条件に「他に稼働中の Claude コンテナが1つも無いときだけ」と定めているが、`FR-env-01` 受入基準9 と契約の規則C は `stop` と `logout` にしか広がっていない。**本タスクが「ラベル無しコンテナを残す」と決めたことで新たに生じた不整合**(変更前は全コンテナを消してから proxy を消していた) | **人間判断 → 2026-08-04 回答=A(解消済み)**。受入基準9 と規則C を `reset` にも広げた。`FR-env-01` 受入基準9(`stop`/`logout`/`reset`。`reset` は `claude-dev-net` も削除禁止)/ `CTR-cli-container` 規則C・遊休判定 / `MODULE-cli-reset` 手順6・判断7 / `MODULE-cli-stop` 判断11(`stop` は `claude-dev-net` を消さない旨)に反映。**代償として `reset` が完全な初期状態に戻らないことがある**ので、その旨を表示する | /doc-check の自分の読み |
| U-21 | **`D0-env-05` を書き換える変更指示が無い**。3点で合成ビューが矛盾する: (a) 項1「`COMPOSE_PROJECT_NAME` を起動ディレクトリ名で一意化する」⇄ `D0-env-08` 項7・`DSN-env-03`(絶対パスのハッシュを足す)/ (b) 項2「`com.docker.compose.project=<正規化名>` を持つコンテナを削除する」⇄ `FR-env-01` 受入基準 19・20(ハッシュ付き名だけを対象、旧名は削除禁止)/ (c) 項2「共有ネットワークと docker-proxy は削除しない」⇄ `D0-env-08` 項2・`FR-env-01` 受入基準9(遊休なら停止する) | **人間判断 → 2026-08-04 回答=A(解消済み)**。`D0-env-05` を変更指示の `sections` に加え、項1・項2 の3点を「命名と削除対象は `D0-env-08` 項7・項1 と `CTR-cli-container` が定める」「共有資源の停止条件は `D0-env-08` 項2 が定める」へ書き換えた(却下した案にも「共有資源を一切停止しない」を追記)。あわせて `D0-env-08` 項4 の出所を `D0-scope-02` から **`MODULE-cli-reset` の実装上の判断2** へ言い換えた | 自分 + 独立レンズ2本が独立に検出(証拠として強い) |
| U-22 | **共有資源単位ロックの保護区間が目的と一致しない**。(a) `start` は認証コピーの後に `shared` を解放し、そのあと手順13 で docker-proxy を作り手順14 でコンテナを作る → `stop` の判断6「判定と削除を同一ロック区間に入れて、他プロジェクトの `start` が作った直後の proxy を消す競合を防ぐ」が成立しない / (b) `logout` が完走した直後に `start` がコンテナを作ると、entrypoint の同期ループ(`FR-env-03` 受入基準3・8)が認証を共有ボリュームへ書き戻し、**`logout` が静かに巻き戻る** / (c) `MODULE-cli-reset` の連携先は「`start` のコンテナ作成との競合を防ぐため」と書くが、`start` のコンテナ作成は `shared` の外 / (d) `login` / `login-codex` は共有ボリュームを作る `ensure_infrastructure` を `shared` の外で実行する | **人間判断 → 2026-08-04 回答=A(解消済み)**。`start` の共有資源単位ロックを「認証コピーの開始から `docker run` の完了まで」へ延ばした。`CTR-cli-container`「排他(ロックキー)」の区間と理由 / `MODULE-cli-start` 手順9・14・判断9 / `MODULE-cli-stop` 判断6 / `MODULE-cli-reset` 連携先に反映 | 自分 + 独立レンズ(独立に検出) |
| U-23 | **`stop <name>` を別ディレクトリから実行したときに compose 一意化名を再現できない**。ハッシュ源は「起動ディレクトリの絶対パス」だが `stop` の入力は `NAME` だけで、`claude-dev.project-dir` ラベルは契約が「表示専用・判定に使わない」と明記しており、しかも手順4 で本体コンテナを削除した後の手順5 で compose を片付けるためラベル自体が読めない → `FR-env-01` 受入基準6 の compose 片付けが `stop <name>` 経路で成立せず、`docs/issues/024` の修正が不完全になる | **人間判断 → 2026-08-04 回答=A(解消済み)**。`stop` が**本体コンテナを削除する前に** `claude-dev.project-dir` ラベルを読み、それを compose 一意化名のハッシュ源にする。契約の「管理ラベル」の用途を「表示専用」から「表示 + compose 名の再現(削除判定には使わない)」へ改め、`MODULE-cli-stop` 手順4 を新設(手順5〜11 へ繰り下げ)+ 判断10。**ラベルが無い場合は compose 片付けをせず手動手順を案内する** | 独立レンズ Codex(readiness) |
| U-24 | **`INT` / `TERM` で中断したときの終了コード・中断位置・部分削除の報告が未規定**。`logout` / `reset` は複数の資源を順に削除するため、途中で Ctrl-C されたときに「どこまで消えたか」「終了コードは何か」を実装者が発明せざるを得ない(`FR-env-03` 受入基準18 は「削除できなかった」場合しか覆っていない) | **人間判断 → 2026-08-04 回答=A(解消済み)**。「進行中の1件を終えてから中断し、削除済みと未削除の資源を列挙して**終了コード 130**」を `CTR-cli-container`「エラーケース」/ `MODULE-cli-logout` 手順12・判断9 / `MODULE-cli-reset` 手順9・判断8 / `MODULE-cli-common-lock`(`trap` で解放されるので残らない)に書いた | 独立レンズ Codex(readiness) |

## 補足: U-20〜U-24 の位置づけ

この5件は **`/doc-check`(フェーズ2 の検証)が提示した決定シート**であり、2026-08-04 に人間が
**全件 A** で回答した。5件はいずれも「対象の絞り込みを厳しくした結果として新たに生じた穴」で、
`docs/issues/024` / `025` / `020` の修正が**部分的にしか成立しない**ことを示していた。
U-21(`D0-env-05` の3点の矛盾)は**下降時に拾えたはずの見落ち**である
(`D0-env-05` は必読物として読んでいたが、書き換えの必要性を判定していなかった)。
