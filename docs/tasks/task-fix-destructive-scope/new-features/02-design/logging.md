---
target: docs/02-design/logging.md
change: replace
sections:
  - "## 主要イベントのログ仕様"
deletes: []
reason: D0-env-08 が破壊的操作に新しい利用者向けの出力を7種類生む(排他で取れない・残骸の引き継ぎ・削除対象の確認・ラベル無しで残したもの・共有資源を残した理由・削除できなかった資源の列挙・中断時の部分削除の報告)。いずれも「利用者が次に取るべき操作」を含む ERROR / INFO であり、既に FR-env-01 受入基準12 の「コンテナ名の衝突による起動の中止」が同じ粒度でこの表に載っている前例がある。表に無いままだと MODULE-cli-start の実装上の判断8 が「表示内容の要件は 02-design/logging.md が定める」と指しているのに定義が存在しない状態になる(FR-env-01 受入基準 9・15〜17・20・21 / FR-env-03 受入基準 14・17・18・23)
---

## 主要イベントのログ仕様

| イベント | レベル | 出力内容 | 対応要件 |
|---|---|---|---|
| コンテナ起動の開始 | INFO | 使用イメージ名とバージョン | FR-env-01 |
| **コンテナ名の衝突による起動の中止** | ERROR | 同名のコンテナが稼働中であること、別ディレクトリの同名プロジェクトである可能性、次に取るべき操作(そのディレクトリで再接続するか、`stop` してから起動し直すか)。**既存のコンテナには触れていないことが分かる表現にする**。管理ラベル `claude-dev.project-dir` があれば、可能性ではなく**どのディレクトリで起動されたかという事実**を出す | FR-env-01(受け入れ基準12) |
| **排他ロックを取得できず中止** | ERROR | 保持している**操作名**と**プロセス ID**、再実行の方法。**削除も作成も行っていないことが分かる表現にする**。待たずに終わったことを示す(固まっていない) | FR-env-01(受け入れ基準16) |
| **ロック残骸の引き継ぎ** | WARN | 残っていたロックの操作名と PID、引き継いで処理を続けること。**処理は中断しない** | FR-env-01(受け入れ基準17) |
| **破壊的操作の削除対象の確認** | INFO | 削除対象のコンテナ名とプロジェクト配下の認証ファイルのパスを**1行ずつ**列挙し、共有ボリュームの認証が消えることを明示して同意を求める。同意が得られなければ「キャンセルしました」を出して終了コード 0 | FR-env-03(受け入れ基準14) |
| **端末が無く `--yes` も無いための中止** | ERROR | プロンプトを出さずに中止したこと、`--yes` の指定方法。**何も削除していないことが分かる表現にする**。終了コード 1 | FR-env-03(受け入れ基準15) |
| **遊休判定に使う Docker への問い合わせが失敗した** | WARN | 判定できなかったこと、**安全側に倒して docker-proxy(と `claude-dev-net`)を残した**こと。処理は続行する | FR-env-01(受け入れ基準9) |
| **管理ラベルを持たないため削除しなかったコンテナ** | INFO | 残したコンテナの**名前**と、「本変更より前に起動した可能性がある」こと、**停止中のものは列挙していない**という限界。削除は行っていないことが分かる表現にする | FR-env-03(受け入れ基準17) |
| **共有資源(docker-proxy / `claude-dev-net`)を残した理由** | INFO | 稼働中の Claude コンテナが残っていること(**その名前**)。`reset` の場合はあわせて**「完全な初期化になっていない」**旨 | FR-env-01(受け入れ基準9) |
| **削除できなかった資源** | ERROR | 消えなかった資源(コンテナ名・docker-proxy・共有ボリューム内のパス・プロジェクト配下のパス)を**1件ずつ**列挙する。**「削除しました」「完了」「全リセット完了」の文言を出してはならない** | FR-env-03(受け入れ基準18) |
| **削除したプロジェクト配下の認証コピー** | INFO | 削除した**パス**を列挙する。他のディレクトリのコピーには触れていないという限界もあわせて出す | FR-env-03(受け入れ基準20・21) |
| **削除の途中で `INT` / `TERM` を受けた** | ERROR | 進行中の1件を終えてから中断したこと、**そこまでに削除した資源と未削除のまま残った資源を1件ずつ**列挙する。成功時の文言は出さない | FR-env-03(受け入れ基準23) |
| **旧い名前の compose 資源 / ラベルを持たない `stop` 対象** | INFO | 片付けの対象に含めなかったこと、残っている可能性、**手動で片付ける方法**(`docker ps --filter label=com.docker.compose.project=<正規化名>` → `docker compose down`) | FR-env-01(受け入れ基準20・21) |
| **`stop <name>` の名前が受理できない文字を含む** | ERROR | 受理できない文字を含むこと、受理する文字集合(`[A-Za-z0-9._-]`)。**何も削除していないことが分かる表現にする** | FR-env-01(受け入れ基準18) |
| noVNC の URL 提示 | INFO | 接続 URL(未公開なら出さない) | FR-env-11 |
| ポート割当 | INFO | ホスト側ポートと転送先 | FR-env-06 |
| ファイアウォールの適用結果 | INFO / WARN | 適用サマリ。失敗時は警告 | FR-env-05(受け入れ基準5) |
| 到達性スモークテスト | WARN | 想定外の到達性が出た場合のみ警告行 | FR-env-05(受け入れ基準6) |
| 既定設定の補完 | INFO / WARN | 生成・追記した鍵。書き換えを見送った場合は警告 | FR-env-12(受け入れ基準8・10) |
| Docker 要求の拒否 | INFO | 拒否理由(bind / 特権 / ホスト空間のいずれか)と対象 | FR-env-07, NFR-sec-01 |
| Docker 中継の失敗 | ERROR | 上流エラーの概要 | FR-env-07 |
| タスクの委譲 | (追記型) | `dispatch`。task_id / attempt / モデル | NFR-ops-01 |
| 実行結果 | (追記型) | `result`。成否と要約 | NFR-ops-01 |
| 仮定の記録 | (追記型) | `assumption`。置いた仮定と理由 | FR-orch-04(受け入れ基準4) |
| 介入の発生と回答 | (追記型) | `intervention`。トリガー種別と回答の要約 | FR-orch-04, NFR-ops-01 |
| 通知の送信失敗 | WARN | 失敗した旨のみ(トークンは出さない) | FR-orch-07(受け入れ基準5) |
| VM の資源逼迫 | WARN | 検知した指標と閾値 | FR-env-08, NFR-ops-01 |

**起動ディレクトリの絶対パスの扱い**: 上の表のうち「コンテナ名の衝突による起動の中止」と
「削除したプロジェクト配下の認証コピー」は、**ホストの絶対パス**(管理ラベル
`claude-dev.project-dir` の値、および削除したファイルのパス)を出す。これは
「出してはならない情報」の**「ホームディレクトリ配下の構造(必要な範囲を超えて出さない)」の
例外ではなく、その「必要な範囲」に当たる**: どちらも**利用者が次に取るべき操作(そのディレクトリへ
移動して再接続する / どのファイルが消えたかを確認する)を決めるために、そのパス自体が必要**だから
である。**必要な範囲を超える例**: 無関係なディレクトリの列挙、ホーム配下の探索結果、
削除対象でないパスの表示。これらは出さない。

**破壊的操作の出力に共通して課す制約**(`D0-env-08` 項5): **削除に失敗した資源がある状態で
成功の文言を出してはならない。** 「削除しました」「完了」「全リセット完了」は、
**列挙した削除対象がすべて消えたことを確認したときにだけ**出す。
判定は削除コマンドの終了コードではなく**削除後の列挙結果**で行う(契約
`CTR-cli-container` の「エラーケース」)。
