# task-fix-logout-records-and-marker 決定シート

**記入方法**: 下の「一括回答」に **1行**書けば終わりです。
**チャットで回答しても構いません**(例: 「すべて推奨どおり」)— その場合 AI が発言を
逐語引用+日付つきで memo.md の「決定シート(回答済み)」に転記します(このファイルには
書き込みません)。
個別に決めたいものがあるときだけ、そのブロックの **★あなたの記入** の後ろに書いてください。

各ブロックには **AI推奨** と **未回答時の既定** という**別々の案**が載っています。

**このタスクは何か**: `claude-dev logout` が「何が消えて何が残ったか」を誤って記録・表示する
3件(`docs/issues/098` / `099` / `100`)を1つの降下で閉じます。前タスク
(`task-fix-logout-zero-target-path`)が0件経路を直したときに、その周辺で見つかった欠陥です。

## 一括回答(ここだけ埋めれば先へ進めます)

**★あなたの記入**:

## 概念の明確化(00/01。上から確認してください。最大7件)

### 1. 語の外延 —「削除対象が0件」のうち**コンテナの集合**について「無い」と「無いことを確認できた」を区別するか

- **場所**: `docs/01-requirements/functional.md:121`(`FR-env-03-19`)/ AC-01・AC-06
- **明確にしたいこと**: 受入基準19 は0件の条件を「**管理ラベルを持つコンテナ(停止中を含む)が無く**、
  …**かつ共有ボリュームが空であることを確認できた**」と書いています。**共有ボリュームだけが
  「確認できた」を要求し、コンテナの集合は「無く」のまま**です。実装は `docker ps` の失敗を
  空集合に変換しているので、**引けなかった状態も「無い」と数えられます**(`docs/issues/099`)。
  この非対称は意図したものですか。
- **AI推奨**: **区別する**(コンテナの集合にも「**無いことを確認できた**」を課す)。
  含む = 「`docker ps` が成功して0行だった」/「停止中を含めて引けて0件だった」。
  含まない = 「`docker ps` が失敗して出力が空だった」。
  理由: この非対称に技術的な根拠がありません。共有ボリュームは一時コンテナ、コンテナの集合は
  `docker ps` で、**どちらも Docker への問い合わせが失敗しうる**という点で同じです。
  片方だけを守ると、`logout` が「消したつもりで残る状態」を作る経路が1本残ります
  (`D0-env-08` の目的そのもの)。
  崩れる条件: `docker ps` の失敗が実際には起こらない(daemon が落ちれば共有ボリュームの検査も
  失敗するので手前で止まる)と判定できたとき — ただし filter の構文エラーや部分的な権限エラーは
  daemon が生きていても起こるため、現時点でその判定はできません。
- **未回答時の既定**: AI推奨のとおりにする
- **波及**: `FR-env-03-19` の1条項(条項 ID は動かしません)/ 02 契約のエラーケース1行の新設 /
  `MODULE-cli-logout` 手順4・6 / 実装2箇所。`relations-query.py --upstream MODULE-cli-logout` = **0件**
- **間違えたときの戻し方**: 気づく時機 = E2E-01 手順8 で `docker ps` を壊した状態を流したとき、
  または「Docker が不調なときに `logout` が確認を求めるようになった」と報告されたとき。
  戻すには受入基準19 の追記1文と実装の1条件を外す(1タスクで戻る)
- **過去の回答**: **あり。** 直前タスクの論点2 で、**共有ボリュームについて同じ形の判断**を
  人間が「01 を先に直す(案B)」と裁定しています(`docs/histories/2026-08-11-fix-logout-zero-target-path.md`
  の「検討した代替案」)。本項はその裁定をコンテナの集合へ広げるかを問うものです

**★あなたの記入**:

### 2. 失敗時に観測される振る舞い — コンテナの集合を確認できなかったときの `logout`

- **場所**: `docs/01-requirements/functional.md:121`(`FR-env-03-19`)/ `docs/02-design/contracts/cli-container.md` のエラーケース
- **明確にしたいこと**: 概念1 で「区別する」とした場合、**引けなかったときに何が起きるべきか**。
  終了コードと表示が変わるので、外から見える振る舞いです。
- **AI推奨**: **(i) 0件の経路に入らず、引けなかったことを表示して受入基準14 の確認へ進み、
  引けなかったこと自体を「消えなかった資源」として記録して終了コード 1 で終わる。**
  理由: `D0-env-08` 項5 の対象は `logout` / `reset` の両方で、**`reset` は既にこの形**です
  (セッション由来の資源の列挙が失敗したら `destructive_plan` + `destructive_failed` に記録して 1 で終わる。
  `claude-dev:2109`)。`logout` だけ別の倒し方にする理由がありません。
  捨てた案: **(ii) 何も削除せずに即座に終了コード 1 で中止する** — 共有ボリュームの認証が消えるはず
  だった実行が「Docker の問い合わせが1つ失敗した」だけで何もしなくなり、**利用者が認証を消す手段を
  失います**(共有ボリュームの検査は成功しているので消せるはずの状態です)。
  下流の代償: 引けなかった実行の終了コードが 0 から 1 へ変わります(自動実行の判定に影響)。
  崩れる条件: 「`logout` は認証が消えれば成功」という定義を採り、コンテナの取りこぼしを
  失敗に数えないと決めたとき。
- **未回答時の既定**: AI推奨のとおりにする
- **波及**: `FR-env-03-19` / 02 契約のエラーケース / `logging.md` の1行 / `MODULE-cli-logout` 手順4・11 / 実装2箇所
- **間違えたときの戻し方**: 気づく時機 = 自動実行で `logout` の終了コードが 1 になったと報告されたとき。
  戻すには記録を `destructive_failed` から表示だけに変える(実装1行 + 受入基準19 の1文)
- **過去の回答**: **あり(同型)。** 直前タスクの概念3「状態を確認できなかったときの振る舞い」で
  人間が「(i) 通常経路へ進める」を選んでいます(`docs/histories/2026-08-11-fix-logout-zero-target-path.md`)。
  本項は**その先の一歩**(引けなかったこと自体を失敗に数えるか)を問うものです

**★あなたの記入**:

### 3. 語の外延 —「削除した資源」に何を挙げてよいか

- **場所**: `docs/02-design/logging.md`「破壊的操作の出力に共通して課す制約」/
  `docs/01-requirements/functional.md:120`(`FR-env-03-18`)
- **明確にしたいこと**: 現在の実装は、消去を確認できなかった共有ボリュームを
  **「削除できなかった資源」と「削除した資源」の両方**に列挙します(`docs/issues/098`)。
  終了コードは 1 で失敗も列挙されるので、利用者は失敗に気づけますが、同時に矛盾した行を読みます。
  「削除した資源」に挙げてよいのは何か、を明文化しますか。
- **AI推奨**: **明文化する。含む = 消えたことを確認できた資源だけ。
  含まない = 消去を確認できなかった資源 / 削除しないと決めた資源(ラベル無しコンテナ・遊休でない
  docker-proxy)。** そして**同じ資源が「削除した」と「削除できなかった」の両方に現れてはならない**と
  書きます。理由: `logging.md` は既に「**成功を表す文言は、列挙した削除対象がすべて消えたことを
  確認したときにだけ出す**」と課していますが、これは見出しの文言についての規定と読めるため、
  資源ごとの一覧に対する規定として実装に届いていませんでした。届く形に書き直します。
  崩れる条件: 「削除した資源」を「削除を試みた資源」の意味で使う設計へ変えたとき
  (そのときは列の名前ごと変わります)。
- **未回答時の既定**: AI推奨のとおりにする
- **波及**: `logging.md` の共通制約1箇所 / 03 契約の「破壊的操作の削除結果の記録」1行 /
  `MODULE-cli-logout` 手順10 / 実装2箇所。**`reset` には同型の二重記録が無い**ので `reset` の
  振る舞いは変わりません(`claude-dev:2109` で確認)
- **間違えたときの戻し方**: 気づく時機 = E2E-01 手順8-18 の `--yes` 側を流したとき。
  戻すには実装の分岐1つ(`_auth_left=""` の扱い)を元に戻す
- **過去の回答**: なし(decisions / 決定シート写し / feedbacks / histories 走査済み)

**★あなたの記入**:

### 4. 語の外延 —「印以外の行」とは何か

- **場所**: `docs/03-impl/relations/MODULE-cli-logout.md` 手順6 の条件 (iii) /
  `docs/03-impl/contracts/cli-container.md`「共有ボリュームの中身の列挙」
- **明確にしたいこと**: 「空」判定の条件 (iii) は「**印以外の行が1つも無い**」ですが、実装は
  「**印の文字列と一致しない行が1つも無い**」を検査しています。`/auth` に印
  `__CLAUDE_DEV_AUTH_LISTED__` と同名のファイル(またはディレクトリ)があると、その行も印として
  除かれ、**中身があるのに空**と判定されます(`docs/issues/100`)。どちらを正としますか。
- **AI推奨**: **「印以外の行」= 印を出した1行より後ろの行**(位置で判定する)を正とし、
  文字列一致で除く方式をやめる。含む = `/auth/__CLAUDE_DEV_AUTH_LISTED__` という名前のファイル
  (中身として数える)。含まない = 一時コンテナが最初に出力した印そのもの1行。
  理由: 印は**列挙の直前に1回だけ**出しているので、位置は常に一意に決まります。文字列一致は
  「中身の名前が印と衝突しない」という前提に依存し、その前提を保証する仕組みがありません。
  崩れる条件: 一時コンテナが印より前に別の行を出すようになったとき(そのときは位置が一意でなくなる)。
- **未回答時の既定**: AI推奨のとおりにする
- **波及**: `MODULE-cli-logout` 手順6・手順10 / 03 契約の該当行 / 実装2箇所(**手順6 と手順10 の
  両方**を直す。判断15 の `[DS-05]`「3箇所目で見直す」は2箇所のままなので発動しません)
- **間違えたときの戻し方**: 気づく時機 = `/auth` に印と同名のファイルを置いて `logout` を流したとき。
  戻すには判定を `grep -vxF` へ戻す(実装2行)
- **過去の回答**: なし(decisions / 決定シート写し / feedbacks / histories 走査済み)

**★あなたの記入**:

## 次に問う候補(回答不要。今回載せなかったもの)

- 「削除を試みた」/「遊休」/「セッション由来」/「一過性の鍵情報」

## 論点(概念以外。従来の決定シート)

### 論点 1. 削除に失敗した実行でラベル無しコンテナの警告を出すか(4件目の欠陥を畳むか)

調査中に**起票されていない4件目**を見つけました。`logout` の結果表示は、失敗が1件以上あると
`exit 1` するのが**ラベル無しコンテナの表示より前**なので、**削除に失敗した実行では
受入基準17 が無条件に課している「名前を表示して残す」が出ません**(`claude-dev:1169`〜`:1176`。
この形は前タスクより前から在ります)。**まさに認証が書き戻される状況で警告が出ない**ことになります。

- **選択肢**:
  - **A**: このタスクに畳む(closure は増えません — `MODULE-cli-logout` 手順11 と実装2箇所だけです)
  - **B**: 畳まず `docs/issues/101` として起票し、別タスクにする
- **AI推奨**: **A(畳む)**。理由: 直す場所が本タスクの3件と**同じ 10 行の中**にあり(手順10 の記録の
  直後が手順11 の結果表示です)、別タスクにすると同じ closure を4回目に開くことになります。
  概念3 で「削除した資源」の外延を決めると手順11 の表示そのものを書き直すので、**同じ降下で
  触る場所です**。下流の代償: 失敗した実行の出力が2〜6行増えます(表示だけで、終了コードは 1 のまま)。
  崩れる条件: 受入基準17 の「表示して残す」に「削除が成功したときに限る」という条件が
  実は意図されていたと分かったとき(現在の条項にはその条件がありません)。
- **未回答時の既定**: AI推奨のとおりにする(= 畳む)
- **根拠(上流)**: `docs/01-requirements/functional.md:119`(`FR-env-03-17`)
  「管理ラベルを持たない Claude コンテナは削除せず、その名前と『本変更より前に起動した可能性がある』
  ことを表示して残さなければならない」— **成否で条件づけられていません**
- **根拠(同層)**: `docs/02-design/logging.md`「管理ラベルを持たないため削除しなかったコンテナ」の
  行は対応要件が `FR-env-03(受け入れ基準17・19)` で、**削除に成功した場合に限る条件を持ちません**。
  同じ理由で 2026-08-11 に「0件の経路でも出す」を明示した先例があります
- **根拠(下流)**: `relations-query.py --upstream MODULE-cli-logout` = **0件**(誰も呼んでいない入口)。
  `_unmanaged_notice` は前タスクで1つの文字列に集約したので、出す位置を増やしても文言は複製されません
- **間違えたときの戻し方**: 気づく時機 = 失敗時の出力が長いと報告されたとき。
  戻すには `printf` の呼び出し1行を消す
- **過去の回答**: なし(この欠陥は本タスクの調査で初めて見つけたもので、issue も未起票です)

**★あなたの記入**:

## 委任にしてよいか確認したい項目

なし(標準委任 `DS-01`〜`DS-08` を超える範囲の判断はありません)。

## 今回 AI が決めたこと(回答不要。開示)

フェーズ1 で決めたのは次の2件です(いずれも問う基準を満たさないため、決めて開示します)。

- **[DS-05]** `MODULE-cli-common-destructive` に「1つの資源は3つの配列のうち1つだけに記録する」
  という番人を置かず、**呼び出し側(`logout` の手順10)で直す** — 理由: 同じ形の二重記録は
  `reset` に存在せず(`claude-dev:2109` で確認)、共有基盤を変えると `reset` にも波及するため。
  共有基盤を触らないので `02-design/relations.md` も動きません / 見直す条件: 3つ目の呼び出し元が
  できたとき、または `reset` に同型の二重記録が見つかったとき(記録先: `MODULE-cli-logout` の
  「実装上の判断」— フェーズ2で書きます)
- **[DS-08]** `docs/issues/098` / `099` / `100` を**1つのタスクに畳む** — 理由: 3件はいずれも
  `claude-dev` の `logout` 分岐の同じ 20 行に在り、`100` の修正は手順6 と手順10 の両方に及ぶので、
  別々のタスクにすると同じ closure を3回開くことになる / 見直す条件: 概念1 の回答で 01 を
  動かさないと決まり、`099` だけが要件変更を伴わなくなったとき(記録先: `memo.md` のタスクリスト)

## 方針合意(個別diff承認は行いません)

- `docs/01-requirements/functional.md`: `FR-env-03-19` の0件条件のうちコンテナの集合に
  「無いことを確認できた」を課し、確認できなかったときの振る舞いを1文足す。**条項 ID は動かさない**(MINOR)
- `docs/02-design/contracts/cli-container.md`: エラーケースに「`logout` が管理ラベル付きコンテナの
  集合を引けなかった」を1行新設する(既存の「セッション由来の資源を引く問い合わせそのものが
  失敗した」と同じ倒し方に揃える)
- `docs/02-design/logging.md`: 「破壊的操作の出力に共通して課す制約」に「削除した資源」の外延
  (消えたことを確認できた資源だけ / 同じ資源を両方の一覧に出さない)を書き足す
- `docs/03-impl/relations/MODULE-cli-logout.md`: 手順4(列挙の失敗)・手順6(印の判定)・
  手順10(記録)・手順11(表示)を書き換え、異常系と実装上の判断を更新する
- `docs/03-impl/contracts/cli-container.md`: 印の判定を「位置で見る」へ、記録の行に排他を足す
- `docs/03-impl/tests/cli-logout.md` / `docs/03-impl/tests/e2e.md`: 受入基準19・18・17 の指し先を
  更新し、E2E-01 手順8 に (a) `docker ps` を壊した状態 (b) 印と同名のファイルを置いた状態の確認を足す
- 実装: `claude-dev` と `claude-dev-mac` の `logout` 分岐(**両 OS で同じ成否・同じ出力にする** —
  `D0-scope-03`)
- 変更指示を書き終えた時点で差分サマリを提示します。個別のdiff承認は取りません。

**★あなたの記入**(異議のある方針だけ書いてください。空欄 = 全方針に合意):

## 記入完了

**★あなたの記入**(記入を終えたら日付を書いてください。例: 2026-08-07):
