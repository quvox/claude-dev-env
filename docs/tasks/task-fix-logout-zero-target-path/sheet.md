# task-fix-logout-zero-target-path 決定シート

**記入方法**: 下の「一括回答」に **1行**書けば終わりです。
**チャットで回答しても構いません**(例: 「すべて推奨どおり」)— その場合 AI が発言を
逐語引用+日付つきで memo.md の「決定シート(回答済み)」に転記します(このファイルには
書き込みません。check-sheet.py SH4 が転記行を合格証拠として読みます)。
個別に決めたいものがあるときだけ、そのブロックの **★あなたの記入** の後ろに書いてください。
(あなたが ★あなたの記入 に書いた内容を AI が書き換えることはありません。**唯一の例外**:
新しい論点を追記したとき、末尾「記入完了」の日付だけは AI が消します — 再記入をお願いします。)

各ブロックには **AI推奨** と **未回答時の既定** という**別々の案**が載っています。

## 一括回答(ここだけ埋めれば先へ進めます)

**★あなたの記入**:

## 概念の明確化(00/01。上から確認してください。最大7件)

### 1. 語の外延 —「共有ボリュームが空」

- **場所**: `docs/01-requirements/functional.md:121`(`FR-env-03-19` の括弧内「共有ボリュームも空」)/ `docs/03-impl/relations/MODULE-cli-logout.md:55`
- **明確にしたいこと**: 「空」を**列挙の結果が空だったこと**と数えるか、**空であることを確かめられたこと**と数えるか。この語の外延が `logout` の早期終了に入る条件を決めます。
- **AI推奨**: 「空」= **空であることを確かめられた状態**。含む = 一時コンテナが起動して `/auth` を列挙し、中身が1件も無かった / 含まない = **一時コンテナを起動できず出力が空だった**(daemon 断・マウント不能・イメージ欠落)。理由: 同じ文書の手順10 が印 `__CLAUDE_DEV_AUTH_LISTED__` でこの2つを区別しており、区別しないと「認証が1バイトも消えていないのに成功と報告される」ため、区別する側が既に選ばれている。崩れる条件: 一時コンテナを使わずに共有ボリュームの中身を読む手段(ホスト側から直接読む等)が入り、起動の成否という概念が消えたとき。
- **未回答時の既定**: AI推奨のとおりにする
- **波及**: `FR-env-03` 条項1件(`-19`)/ 02 は 2件(`logging.md` / `CTR-cli-container` のエラーケース)/ `MODULE-cli-logout` 1件 / 実装2箇所。`relations-query.py --upstream MODULE-cli-logout --depth 4` = **0件**(`logout` は入口機能で呼び出し元を持たない)
- **間違えたときの戻し方**: 気づく時機 = E2E-01 で Docker を壊した状態の確認手順を流したとき(現在この手順は無く、本タスクで新設します)。戻すには `FR-env-03-19` の追記1文と実装の印の判定を外す(1タスクで戻る)
- **過去の回答**: なし(`docs/00-requests/decisions/` / `docs/01-requirements/decisions/` / `docs/feedbacks/` / `docs/histories/` を走査済み。`docs/histories/2026-08-05-doc-check-ssot-destructive-scope-recertification.md` は「振る舞いを変えるかは `FR-env-03` を動かす判断なので本タスクでは決めない」と**先送りを記録しているだけ**で、決めてはいません)

**★あなたの記入**:AI推奨のとおり

### 2. 範囲の境界 —「`logout` の削除対象が0件」に何を数えるか

- **場所**: `docs/01-requirements/functional.md:121`(`FR-env-03-19` の「稼働中コンテナが無く、共有ボリュームも空、プロジェクト配下の認証コピーも無い」)
- **明確にしたいこと**: この「0件」の数え方に、(a) **管理ラベルを持たない稼働中コンテナ**、(b) **遊休の docker-proxy** を数えるか。数えないなら、それらが在るときも0件経路に入ります。
- **AI推奨**: **どちらも「0件」には数えない。ただし (a) が在るときは0件経路でも名前と書き戻しの警告を表示する。** 理由: (a) は `FR-env-03-5` が「削除も停止もしない」と定めた対象なので削除対象に数えると矛盾する。一方 (a) が在ることは `logout` が効かない理由そのもの(稼働中コンテナが 30 秒ごとに認証を書き戻す)なので、数えないことと黙ることは別である。(b) は現状も数えておらず、害は「使われていない proxy が1つ残る」だけで次回の `stop` / `reset` が回収する。崩れる条件: 遊休の docker-proxy が残ること自体が利用者に見える害になったとき(そのときは `FR-env-03-19` の括弧内へ docker-proxy を足す)。
- **未回答時の既定**: AI推奨のとおりにする((b) は「やらないこと」へ回す)
- **波及**: `FR-env-03` 条項1件(`-19`)/ `logging.md` の2行の発火条件 / `MODULE-cli-logout` 手順6 と「戻り値・副作用」/ 実装2箇所
- **間違えたときの戻し方**: 気づく時機 = E2E-01 手順8-13 の (d) を流したとき(既存の手順で、ここに確認項目を1つ足します)。戻すには `FR-env-03-19` の但し書きと実装の表示呼び出しを外す
- **過去の回答**: なし(同上を走査済み)

**★あなたの記入**: AI推奨のとおりにする((b) は「やらないこと」へ回す)

### 3. 失敗時に観測される振る舞い —「共有ボリュームの状態を確認できなかった」ときの `logout`

- **場所**: `docs/01-requirements/functional.md:120`〜`:121`(`FR-env-03-18` と `-19` のあいだ。どちらもこの状態を規定していない)
- **明確にしたいこと**: 確認できなかったとき、利用者に見えるのは (i) 通常経路(削除対象の列挙 → 確認 → 削除 → 手順10 の印で失敗を検出して終了コード 1)か、(ii) 何もせず即座に終了コード 1 か、(iii) 現状(「削除対象がありません」+ 終了コード 0)か。
- **AI推奨**: **(i) 通常経路へ進める。** 理由: 既存の機構をそのまま使え、`D0-env-08` 項5(削除の失敗を握りつぶさない)へ自然に着地する — 手順10 が印の不在を「消去を確認できなかった」として失敗に数え、`FR-env-03-18` の列挙と終了コード 1 が既に効く。(ii) は「確かめられないから何もしない」となり、Docker が一時的に不調なだけの利用者が `logout` を実行できなくなる。下流の代償: 0件のはずの状態で確認プロンプトが出るようになる(列挙されるのは「共有ボリュームの認証情報」1行だけ)。崩れる条件: 一時コンテナの起動失敗が日常的に起き、毎回プロンプトが出て利用者が読み飛ばすようになったとき。
- **未回答時の既定**: AI推奨のとおりにする
- **波及**: `FR-env-03` 条項1件(`-19`)/ `logging.md` に1行新設 / `CTR-cli-container` のエラーケースに1行新設 / `MODULE-cli-logout` 手順6 と「戻り値・副作用」/ 実装2箇所
- **間違えたときの戻し方**: 気づく時機 = E2E-01 の新設手順、または利用者から「Docker を再起動した直後に `logout` が確認を求めた」と報告されたとき。戻すには (iii) へ戻す1文の差し替え(データは失われないので移行手順は不要)
- **過去の回答**: なし(同上を走査済み)

**★あなたの記入**:AI推奨のとおり

## 次に問う候補(回答不要。今回載せなかったもの)

- 「停止中のラベル無しコンテナ」/「完全な初期化」/「一過性の資格情報」

## 論点(概念以外。従来の決定シート)

### 論点 1. `docs/issues/052` の対処 — 0件経路でラベル無しコンテナの警告に到達させるか

`logout` を実行しても、ラベル無しの稼働中コンテナが認証を書き戻しているせいで効かない、
という状態に利用者が到達できません。0件経路がその警告を出さないためです。

- **選択肢**:
  - **A**: `FR-env-03-19` に「ただしラベル無しの稼働中コンテナがあるなら、その名前と書き戻しの警告は表示する」を足す(受入基準17 は触らない)
  - **B**: `FR-env-03-17` の条件節を「集合として列挙するとき」から「`logout` / `reset` を実行したとき」へ広げる
  - **C**: 現状のままとし、`docs/pendings.md` へ受容として記録する
- **AI推奨**: **A**。理由: 直したいのは「0件経路が警告に到達しない」1点で、A はそれを条項1つの追記で閉じる。**B は受入基準17 を触るが、その条項は 01 と 02 が既に食い違っている**(`docs/issues/055`: 17 は停止中のコンテナの表示まで求めるが、02 契約は「停止中は列挙できない。これは意図した限界」と書く)ため、**B を選ぶと 055 も同じ降下で解く必要が生じ、このタスクの範囲が 01 ⇄ 02 の食い違いの解消まで広がります**。C は `D0-scope-07` の起票の閾値で言う「利用者が失敗に気づけない」状態(成功時と同じ表示・同じ終了コード)を残します。下流の代償: `E2E-01` 手順8-13 の (d) に期待結果が1つ増える。崩れる条件: `reset` にも0件の早期終了経路を足すとき(そのときは同じ但し書きを `reset` にも掛ける必要がある。**現在の `reset` にはこの経路が無い** — 調査メモ4)。
- **未回答時の既定**: AI推奨のとおりにする
- **根拠(上流)**: `docs/00-requests/decisions/env.md` `D0-env-08` 項1(「管理ラベルを持たない Claude コンテナは削除せず、**その存在と名前を表示して残す**」)と 項5(削除の失敗を握りつぶさない)。**00 はこの表示を禁じておらず、A は 00 の変更を要しません**
- **根拠(同層)**: `docs/02-design/logging.md:64`・`:65` の2行(「管理ラベルを持たないため削除しなかったコンテナ」INFO / 「認証の巻き戻り」WARN)は、**削除対象が1件以上あるときに限る条件を持たない**。`docs/02-design/system.md:443` の UI 設計も `logout` の状態として「**残した資源**」を集合の空・非空によらず挙げている。先例は表示する側に寄っています
- **根拠(下流)**: `relations-query.py --upstream MODULE-cli-logout --depth 4` = **0件**(入口機能)。`relations-query.py --requirement FR-env-03` = 実装14機能 / **検証しているテスト 0 件(未検証)**。したがって波及は `MODULE-cli-logout` と実装2箇所、確認は E2E-01 手順8 のみ
- **間違えたときの戻し方**: 気づく時機 = E2E-01 手順8-13 の (d)。戻すには `FR-env-03-19` の但し書きを削り、実装の表示呼び出しを外す(1タスクで戻る)
- **過去の回答**: なし。`docs/histories/2026-08-04-doc-check-ssot-destructive-scope-certification.md` は 052 の起票を記録し、`task-fix-destructive-scope` の「やらないこと」により**判断を先送りした**ことだけを残しています

**★あなたの記入**:

### 論点 2. `docs/issues/053` の対処 — 「空」を確かめられないまま0で終わる経路を閉じるか

一時コンテナを起動できない状態で `logout` を実行すると、共有ボリュームに認証が残っているのに
「削除対象がありません」と表示して終了コード 0 で終わります。

- **選択肢**:
  - **A**: 実装だけを直す(手順6 の列挙にも印を入れ、印が無ければ「空」と判定せず通常経路へ進む)。01 は触らない
  - **B**: `FR-env-03-19` に「共有ボリュームの状態を確認できなかった場合はこの経路に入らない」を明記してから A を実装する
  - **C**: 現状のままとし、`docs/pendings.md` へ受容として記録する
- **AI推奨**: **B**。ただし **`docs/issues/053` が B の理由に書いている「A だけだと同じ穴が `reset` 側に残る」は事実として誤りです** — `reset` は0件の早期終了経路も「空」の判定も持たず、共有ボリュームを `docker volume rm -f` で器ごと消します(調査メモ4)。**B を推す理由は別で、`FR-env-03-19` が「共有ボリュームも空」を経路の前提として置いているからです**: 01 にその前提を確かめられなかったときの扱いを書かないと、実装だけを直しても次に読む人が受入基準19 の字面から逆の結論(出力が空なら0件経路でよい)を導けます。下流の代償: 01 の MINOR 変更が1件増える(A なら 02・03 だけで済む)。崩れる条件: 将来 `reset` に0件の早期終了を足すとき — そのときは同じ条件を `reset` にも掛ける必要があります。
- **未回答時の既定**: AI推奨のとおりにする
- **根拠(上流)**: `D0-env-08` 項5(「削除に失敗した資源を握りつぶさない」)と、その理由欄が挙げる過去の5件の欠陥のうち「**削除失敗を握って成功と表示する**」。`logging.md` の共通制約(「成功を表す文言は、列挙した削除対象がすべて消えたことを確認したときにだけ出す」)は**「削除対象がありません」を成功の文言に数えない**ので、現状は字面には反していません — だから 01 に書く必要があります
- **根拠(同層)**: **同じ倒し方の先例が 02 に3つあります**。`docs/02-design/contracts/cli-container.md:138`「**列挙が空で返ったこと(0件)と、問い合わせが失敗したことを同一視してはならない**」/ 同 `:141`「判定不能を『遊休』と読み替えてはならない」/ `docs/02-design/relations.md:187`〜`:188`(`PLAN-cli-reset`)「列挙の問い合わせそのものが失敗した場合も『消えなかった資源』として扱う(0件と区別する)」。**3つとも同じ向きで、B は新しい方針ではなく既存方針の `logout` への適用です**
- **根拠(下流)**: 論点1 と同じ(`--upstream` 0件 / 自動テスト0件)。加えて `CTR-cli-container` のエラーケースに1行、`logging.md` に1行、`E2E-01` に新設手順1つ
- **間違えたときの戻し方**: 気づく時機 = E2E-01 の新設手順(Docker を壊した状態での `logout`)、または利用者から「Docker の不調時に `logout` が確認を求めた」と報告されたとき。戻すには追記1文と印の判定を外す
- **過去の回答**: なし。`docs/histories/2026-08-05-doc-check-ssot-destructive-scope-recertification.md` は 053 の起票と、独立レンズ Codex が「高」と報告したのを `/doc-check` が理由付きで「中」へ改めたことを記録しています(**振る舞いを変えるかは決めていません**)

**★あなたの記入**:

### 論点 3. `docs/issues/089` をこのタスクに畳むか(タスク境界の判断)

`docs/issues/089`: `logout` はセッション由来のコンテナ(`claude-dev.role=spawned`)を
「管理ラベルを持たない」と表示します。02 契約が**名指しで禁じた**表示です。

- **選択肢**:
  - **A**: このタスクに畳む(closure に `MODULE-cli-logout` と実装2箇所が増える。01/02 は動かない)
  - **B**: 畳まず、別タスクとして残す
- **AI推奨**: **A(畳む)**。理由: **論点1 の修正は、089 が壊しているまさにその表示ブロックを新しい経路からも呼びます**(`claude-dev:1108`〜`:1113`)。畳まないと、事実に反する表示が1箇所から2箇所に増えます。089 は 02 契約が既に4つの除外を `logout` と `reset` の双方に課しており(`docs/02-design/contracts/cli-container.md:396`〜`:409`)、`reset` は実装済み(`claude-dev:2091`)で `logout` だけが4つ目を持たない — **上流の変更は不要な、実装が契約に追いついていない1行です**。下流の代償: 実装2箇所に除外ループが1つずつ増え、`MODULE-cli-logout` の手順4 と「既知の制限」の記述が動く。崩れる条件: 089 の修正が新しい Docker への問い合わせを要し、独立に検証したくなったとき(現状は既に取得済みの `spawned` 一覧との突き合わせで足ります)。
- **未回答時の既定**: AI推奨のとおりにする
- **根拠(上流)**: `docs/02-design/contracts/cli-container.md:406`〜`:409`「**4つの除外はいずれも `logout` と `reset` の双方に掛かる**(…)`logout` はセッション由来の資源を削除しない(規則 D を使わない)が、**削除しないことと「ラベルを持たない」と表示することは別である** — 表示すると事実に反する」。02 の側に判断は既に在ります
- **根拠(同層)**: `reset` の実装が同じ除外を持つ(`claude-dev:2091` の `_rc_spawned_c` の突き合わせ)。`docs/issues/089` の `pattern_survey` が4つの除外を1つずつ照合し、**欠けているのは `logout` の4つ目だけ(1件)**と確認済み
- **根拠(下流)**: `relations-query.py --impact` の対象は `MODULE-cli-logout` のみ(`--upstream` 0件)。01/02 の変更なし
- **間違えたときの戻し方**: 気づく時機 = E2E-01 手順8-11 と 8-14 を続けて流したとき。戻すには 089 分の変更指示を落として issue を残す(このタスクの他の変更とは独立している)
- **過去の回答**: なし。`docs/histories/2026-08-07-stop-session-spawned-containers.md` が 089 の起票を記録しています

**★あなたの記入**:

### 論点 4. E2E-01 手順8 の実機実行を誰がやるか(フェーズ3 で追加。2026-08-11)

実装(タスク1〜4)は完了し、`go vet` / `go test` は green です。残る DoD は
**E2E-01 手順8 の実機確認**だけですが、これは無人のフェーズ3 では実行できません:
手順8 は `claude-dev login`(対話 OAuth)から始まり、`logout --yes` / `reset --yes` を含むため、
**このホストで実行すると、あなたの現在の Claude / Codex の認証(`claude-dev-auth` の中身 21 項目)と
claude-dev のイメージ・ボリュームが実際に消えます**。macOS 版は当環境では実行できません。

代わりに **隔離ハーネス**で実機の Docker に対して確認しました(利用者の環境には触れていません):
`claude-dev` の複製の資源名を `cdx-e2e-*` へ、ラベルを `cdx-e2e.*` へ書き換え、専用のボリューム・
ネットワーク・コンテナを作って `logout` を走らせ、`HOME` も一時ディレクトリへ向けました。
修正前(`effdcac`)と修正後で同じ状態を流し、**3つの欠陥がいずれも修正前だけ再現する**ことを
確認しています(実出力は `memo.md` の進捗メモに逐語で残しました)。未確認のまま残るのは
(a) 実イメージ `claude-dev-claude` での手順8-18、(b) macOS 版の実行、(c) 手順8 の他の部分手順の回帰です。

- **選択肢**:
  - **A**: 隔離ハーネスの結果を根拠として `/task-close` へ進み、実機 E2E-01 手順8 は
    `docs/pendings.md` の残務へ1行残して、あなたが都合のよいときに流す
  - **B**: `/task-close` の前にあなたが E2E-01 手順8 を実機で流す(認証は消えるので再ログインが必要)。
    結果を受けてから反映・検証済み記録へ進む
- **AI推奨**: **A**。理由: 変更した3つの振る舞いは、**それぞれ修正前後の差分として実機の Docker 上で
  観測済み**であり、未確認部分は変更したロジックが関与しない箇所(実イメージの中身・OS 差)です。
  `logout` 分岐は両 OS で**バイト単位で同一**であることを確認しました(`diff` で0差分)。
  対して B の代償は、あなたの Claude / Codex 認証の削除と再ログイン、およびイメージの再取得です。
  崩れる条件: 実イメージ `claude-dev-claude` の `bash` / `ls` の挙動が busybox 系と違い、
  終了ステータスの条件(ii)が実イメージでだけ変わるとき(その場合は手順8-18 が落ちる)。
- **未回答時の既定**: AI推奨のとおりにする(= A。`/task-close` へ進む)
- **根拠(上流)**: `docs/02-design/environments.md`「E2Eテスト: 自動テストランナーは無い。実機で
  `claude-dev` を操作する」。**この層が E2E を人手の操作と定めている**ため、無人フェーズでは
  原理的に実行できない
- **根拠(同層)**: `docs/03-impl/tests/cli-logout.md` テスト設計の判断2「本モジュールの自動テストを
  書かず、すべて実機確認(E2E-01 手順8)に寄せる」(`DSN-test-01` / `SR-32`)。状態列は全行
  「未検証(テスト未実装)」であり、**このタスクはその状態を変えない**
- **根拠(下流)**: `relations-query.py --requirement FR-env-03` = 「検証しているテスト 0 件」。
  A を選んでも自動テストの被覆は増減しない(調査メモ 13)
- **間違えたときの戻し方**: 気づく時機 = あとで E2E-01 手順8-18 を流したとき。戻すには
  実装のその1条件(終了ステータス)を外す1行の変更で足りる(`_auth_probe_status` の判定)。
  影響範囲は `MODULE-cli-logout` の1機能のみ(`--impact` = 自身のみ、`--upstream` 0件)
- **過去の回答**: なし

**★あなたの記入**:

## 委任にしてよいか確認したい項目

なし(標準委任 `DS-01`〜`DS-08` を超える範囲の判断はありません)。

## 今回 AI が決めたこと(回答不要。開示)

フェーズ2 で `[DS-01]` `[DS-02]` `[DS-03]` `[DS-05]` を行使しました(降ろし先は
`MODULE-cli-logout` 判断15・16 と `tests/*.md`)。フェーズ3 で足したのは次の2件です:

- **[DS-05]** ラベル無しコンテナの表示文言を、共有シェル関数ではなく**組み立て済みの文字列変数**
  (`_unmanaged_notice`)で手順6 と手順11 に共用させた — 理由: トップレベルにシェル関数を増やすと
  コールグラフの記号が1つ増え、この変更指示が持たない機能を新設することになる /
  見直す条件: 出力点が3箇所目になったとき。降ろし先: `MODULE-cli-logout` 実装上の判断18
(実施順を 1 → 2 → 4 → 3 にしたのは、タスクリストがタスク4 に `(P)`(並列可)を付けて
いる範囲の実施であり、委任の行使ではありません。理由は `memo.md` の進捗メモにあります。)

## 方針合意(個別diff承認は行いません)

- `docs/01-requirements/functional.md`: `FR-env-03-19` に2点(ラベル無し稼働コンテナの表示 / 状態を確認できなかったときはこの経路に入らない)を足す。**条項 ID は動かさない**(MINOR 変更)
- `docs/02-design/logging.md`: 既存2行(ラベル無しコンテナ / 認証の巻き戻り)の発火条件に「削除対象が0件の経路でも出す」を明示し、「共有ボリュームの状態を確認できなかった」を1行新設する
- `docs/02-design/contracts/cli-container.md`: エラーケースに「共有ボリュームが空かの判定に使う列挙が失敗した」を1行新設する(既存の `:138` / `:141` と同じ倒し方に揃える)
- `docs/03-impl/relations/MODULE-cli-logout.md`: 手順6 を書き換え、`docs/issues/052` / `053`(および論点3 が A なら `089`)を追跡する「既知の制限」の行と「戻り値・副作用」の例外記述を削る
- `docs/03-impl/contracts/cli-container.md`: 02 側の新設行に対応する実装側の記述を足す
- `docs/03-impl/tests/cli-logout.md` / `docs/03-impl/tests/e2e.md`: `FR-env-03-19` の行の指し先を更新し、E2E-01 手順8-13 に確認項目1つ、Docker を壊した状態の新設手順1つを足す
- 実装: `claude-dev` と `claude-dev-mac` の同一箇所(手順6 相当)。**両 OS で同じ成否・同じ出力にする**(`D0-scope-03`)
- 変更指示を書き終えた時点で差分サマリを提示します。個別のdiff承認は取りません。

**★あなたの記入**(異議のある方針だけ書いてください。空欄 = 全方針に合意):

## 記入完了

**★あなたの記入**(記入を終えたら日付を書いてください。例: 2026-08-07):
