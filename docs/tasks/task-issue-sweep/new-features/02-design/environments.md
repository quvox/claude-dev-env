---
target: docs/02-design/environments.md
change: replace
version_bump: minor
sections:
  - "## Codex実行設定"
deletes: []
reason: 'issue 031 と issue 065 の解消(決定シート 論点2 = 案A)。**(1) issue 065**: Codex が使えないときに独立レビューをサブエージェントで代替してよいという**プロジェクトの方針**を書く欄がこの表に無く、人間の常設承認(2026-08-05)が会話の中にしか存在しなかった。「代替レビュー(サブエージェント)のモデル・reasoning」の行は**強度**を指名する欄であって**可否**の欄ではない。可否の行を1行新設して常設承認を SSOT へ着地させる。**(2) issue 031**: 「モデル・reasoning(増分監査)」の行が「いつ決めるか」として `docs/issues/031` 自身を指す自己参照になっており、issue を閉じると参照先が実在しなくなる(CS11)。この行が本来書くべきなのは**決める契機**なので、それを書く。**未記入がキットの既定を意味することは既にこの行に書かれており、そこは変えない**(issue 031 が問題にした「未記入なのに最強設定だと思い込む」状態は 2026-08-10 に解消済みである)。**必須フラグの理由・疎通確認・監査での禁止事項は1文字も変えない**。あわせて、この表の「未定」10 行を規範が定める定型「未定(いつ決めるか)」へ揃える — `.claude/directions/02-design.md` は「未定は消さず『未定(いつ決めるか)』と書く」と定めており、`CS8` はその定型だけを宣言された停止点として認めるが、**10 行とも定型を外れていて「未決点が仕様に残っている」として検出され続けていた**(本タスク以前からの既存の違反であり、`/task-new` が凍結した母集団の CS8 11 件のうち 10 件がこれである)。**決める契機は各行に元から書かれている内容をそのまま定型の位置へ移しただけで、意味は変えない**'
reflected: 2026-08-12
---

## Codex実行設定

| 項目 | 値 |
|---|---|
| Codex連携 | 有効(Claude コンテナに同梱。`codex` は PATH 上。認証は共有ボリューム経由) |
| ドキュメント監査 | 有効 |
| QA(E2E + CDP探索) | 無効(未運用。開始時期は未定) |
| フロントエンド | CLI(`codex exec`)。MCP サーバは登録していない |
| 監査の経路 | (a) scoped(スコープ渡し)。添付方式は予備 |
| コマンド | `codex` |
| **必須フラグ** | **監査・QA で codex を呼ぶときは常に `-c features.use_legacy_landlock=true` を付ける** |
| プロファイル | 未定(いつ決めるか)— QA レーンを開始するとき。`docs-audit` / `qa` は未作成で、codex 既定設定で走らせる |
| モデル・reasoning(増分監査) | 未定(いつ決めるか)— **独立レビューのモデルをプロジェクトで固定したくなったとき**。**未記入の行は不在として扱われ、キットの既定が効く** |
| モデル・reasoning(full監査) | 未定(いつ決めるか)— 同上 |
| モデル・reasoning(QA) | 未定(いつ決めるか)— QA レーンを開始するとき |
| **独立レビューを Codex 以外で代替してよいか** | **よい。`codex` CLI がこのホストで使えないときはサブエージェントで代替する**(2026-08-05 に人間が常設で承認した。実行のたびに確認しない)。**どのレンズが走ったかをレポートに必ず明記する** |
| 代替レビュー(サブエージェント)のモデル・reasoning | 未定(いつ決めるか)— 代替レンズの強度をプロジェクトで固定したくなったとき。**未記入なのでキットの既定が効く** |
| タイムアウト / 最大出力 | 監査 900 秒 / QA 未定。最大出力は未定 |
| 最大調査ステップ | 未定 |
| 書き込み許可ディレクトリ | 未定(いつ決めるか)— QA レーンを開始するとき(未運用のため) |
| CDP エンドポイント | `http://localhost:9222`(コンテナ内 Chrome) |
| QA シードコマンド | 未定(いつ決めるか)— QA レーンを開始するとき |
| QA リセットコマンド | 未定(いつ決めるか)— 同上 |
| ブラウザ排他ロック | 未定(いつ決めるか)— 同上。`docs/pendings.md` P-003 で追跡する。**QA レーン開始の前提条件である** |
| Claude のシェルからの到達方法 | コンテナ内 CLI(同一コンテナ内で `codex` を直接呼ぶ) |
| 証跡の保管場所と保持期間 | スクラッチパッドのみ(永続化しない) |
| full 最終監査で「中」が PASS をブロックするか | いいえ |
| CDP 探索を必須にする変更範囲 | 未定(いつ決めるか)— QA レーンを開始するとき(未運用のため) |
| 段階導入フェーズ | 1 観測モード |

**必須フラグの理由**: このコンテナでは codex 既定の bubblewrap サンドボックスが起動できない
(seccomp が `CLONE_NEWUSER` を拒否し、AppArmor が `mount --make-rslave /` を拒否する。
`DSN-dist-02`)。legacy landlock バックエンドはユーザー名前空間を使わないため、コンテナの
confinement を緩めずに動く。**付け忘れると読み取りコマンドがすべて失敗するが `codex exec` の
終了コードは 0 のまま**なので、成否は終了コードではなく最終メッセージと成果物で判定する。

**サンドボックス疎通確認**: `codex sandbox --enable use_legacy_landlock -- /bin/true` が exit 0、
かつ `codex sandbox --enable use_legacy_landlock -- /bin/sh -c 'touch /tmp/x'` が失敗すること。
フラグ無しの `codex sandbox -- /bin/true` は exit 1(bubblewrap)で、これは既知・正常。
`codex doctor` はこの故障を検知しない。

**監査での禁止事項**: 監査(`docs` / `readiness` / `diff`)では承認・サンドボックスを迂回する
フラグを使わない(read-only + landlock で足りる)。**QA レーンのみ例外**として
`-c sandbox_mode="danger-full-access"` で走らせる(landlock 経路では書き込みが成立しないため。
「コンテナ自体が隔離空間であり、これ以上の隔離は不要」という `D0-sec-06` の一貫適用。
2026-07-31 の人間判断)。QA の他の制約(専用シード・ブラウザ排他・テスト成果物ディレクトリ以外を
書かない・終了時にリセット)はそのまま守る。git 未初期化のリポジトリでは `--skip-git-repo-check`
を付ける(これは迂回ではない)。

**イメージ更新時の注意**: `use_legacy_landlock` は codex 0.146.0 時点で deprecated(起動時に
撤去予告が出る)。同梱バージョンはビルド時に最新へ解決されるため、イメージを更新したら上記の
疎通確認を再実行する。撤去された場合の退避先は添付方式。
