# task-issue-sweep 決定シート

**記入方法**: 下の「一括回答」に **1行**書けば終わりです。
**チャットで回答しても構いません**(例: 「すべて推奨どおり」)— その場合 AI が発言を
逐語引用+日付つきで memo.md の「決定シート(回答済み)」に転記します(このファイルには
書き込みません。check-sheet.py SH4 が転記行を合格証拠として読みます)。
個別に決めたいものがあるときだけ、そのブロックの **★あなたの記入** の後ろに書いてください。
(あなたが ★あなたの記入 に書いた内容を AI が書き換えることはありません。**唯一の例外**:
新しい論点を追記したとき、末尾「記入完了」の日付だけは AI が消します — 再記入をお願いします。)

各ブロックには **AI推奨** と **未回答時の既定** という**別々の案**が載っています。

## 一括回答(ここだけ埋めれば先へ進めます)

**★あなたの記入**:

## 概念の明確化(00/01。上から確認してください。最大7件)

### 1. 語の外延 —「issue を片付ける」

- **場所**: `docs/issues/index.md`(36 件)/ `.claude/directions/issues-pendings.md` §1 のゲート
- **明確にしたいこと**: 1件の issue の「片付いた」を何と数えるか。**3つの終わり方**を1つの語で
  まとめてよいか、区別して数えるか
- **AI推奨**: **3つを区別して数える**。
  **含む(=片付いた)**: (a) **解消** = 事象そのものを直してファイルを削除する(コード7件・文書整合6件)/
  (b) **空振り** = 指摘の実体が既に消えており、事実の記述だけを直してファイルを削除する(009 / 030)/
  (c) **降格** = 原則8のゲート行4に当たるため `docs/pendings.md` の残務へ1行だけ残してファイルを削除する(9件)。
  **含まない**: 範囲外として `docs/issues/` に残す 11 件(002 / 005 / 010 / 028 / 046 / 055 / 092 / 097 /
  076 / 079 / 081)。
  理由: 3つは**残る記録の強さが違う**(a・b は記録が消える / c は残務1行が残る / 範囲外は
  `closes_when` つきの追跡が残る)ので、同じ「片付いた」で数えると、あとから「その件はもう
  追跡されていない」ことに気づけない。崩れる条件: 降格した項目が実は AC を塞いでいたと分かったとき
  (そのときは残務行を issue へ戻す)
- **未回答時の既定**: AI推奨のとおりにする
- **波及**: issue 36 件全部の処理方法 / `docs/pendings.md` 残務 9 行 / histories の記録の書き方
- **間違えたときの戻し方**: 降格した9件は `docs/pendings.md` の残務行として残るので、
  そこから `docs/issues/` へ起票し直せる(`origin_layer` と `closes_when` を書き直す)。
  気づくのは、降格した事象が実装や検証を止めたとき
- **過去の回答**: なし(decisions / 決定シート写し / feedbacks / histories 走査済み)

**★あなたの記入**:

### 2. 範囲の境界 — 文書整合6件の「全件」をいつの実測値で固定するか

- **場所**: `memo.md` の調査メモ 12〜15 / `check-changeset.py --ssot docs`
- **明確にしたいこと**: 054 / 077 / 084 / 095 は「同型を全件直す」型の指摘だが、**母集団は起票時から
  減り続けている**(077 は 23 ファイル 181 箇所 → **19 ファイル 188 箇所**、084 は 32 → **19 ファイル**、
  095 は 24 箇所 → **1 箇所**)。どの時点の実測値を「全件」とするか
- **AI推奨**: **2026-08-11 の実測値で母集団を凍結し、memo.md の調査メモに書いた件数を「全件」とする**。
  凍結後にタスクの中で増えた分(自分の変更が新しく作った分を除く)は次のタスクへ回す。
  理由: 母集団を実行のたびに引き直すと、直している最中に増えた分で終わらなくなる
  (`check-changeset.py --ssot` は 125 ファイルを毎回走査する)。崩れる条件: 凍結中に別のタスクが
  同じファイルを動かしたとき(並行タスクは現在ゼロなので起きない)
- **未回答時の既定**: AI推奨のとおりにする
- **波及**: 03-impl/tests/ の 28 ファイル / 00 の3ファイル / 02 の1ファイル / 03 の relations 6本
- **間違えたときの戻し方**: `/task-close` の後にもう一度 `check-changeset.py --ssot docs` を走らせ、
  残った件数を `docs/pendings.md` の残務へ1行で記録する。気づくのはその再走査の時点
- **過去の回答**: なし(decisions / 決定シート写し / feedbacks / histories 走査済み)

**★あなたの記入**:

## 次に問う候補(回答不要。今回載せなかったもの)

- 「再検証候補」/「主担当」/「未検証(テスト未実装)」

## 論点(概念以外。従来の決定シート)

### 論点 1. issue 004 の降格が、00 層の決定 `D0-scope-07` の「閉じずに残る」と正面から食い違う

- **選択肢**:
  **A** = 00 を書き換える。`D0-scope-07` の 関連 行を「残件は `docs/pendings.md` の残務が持つ」へ改め、
  004 を降格して削除する。
  **B** = 004 だけ降格しない。issue のまま残し、範囲外の 12 件目にする(00 は触らない)。
- **AI推奨**: **A**。理由: `D0-scope-07` が「閉じずに残る」と書いた趣旨は**掘り下げの残件を
  見失わないこと**であり、残務1行でも記録は残る。004 の残件4項目(永続データモデル / モデル・effort
  ポリシー / 観点6 テストデータ / 02 契約の復号レベルのエラーケース)はいずれも**現在どの AC も
  塞いでいない**ので、原則8のゲートでは行4(残務1行)に当たる。
  下流の代償: `closes_when` が無くなるので、残件が片付いたかを機械で判定できなくなる。
  同じファイルの `D0-scope-06` が既に「issue を削除して経緯は histories が持つ」と書いており、
  書き方の先例がある。崩れる条件: 残件4項目のどれかが AC を塞ぐと判明したとき
- **未回答時の既定**: AI推奨のとおりにする
- **根拠(上流)**: `docs/00-requests/decisions/scope.md:140`(`D0-scope-07` の 関連 —
  「`docs/issues/004…`(**閉じずに残る**。残件は同 issue の「経緯」)」)
- **根拠(同層)**: 同ファイル `:95`(`D0-scope-06` の 関連 — 「同 issue は本決定の反映と同時に
  解消・削除するため、経緯は `docs/histories/` を参照」)。**同じ決定群の中に両方の書き方がある**
- **根拠(下流)**: 004 を参照するのは SSOT では 00 の1箇所だけ(`grep -rn "issues/004" docs`)。
  ほかは範囲外で残る `docs/issues/005:66` と `docs/issues/010:74`、および histories 3件。
  `relations-query.py` の対象外(issue はコード資産ではない)ので実測は grep による
- **間違えたときの戻し方**: 残務行を `docs/issues/` へ起票し直し、`D0-scope-07` の 関連 行を
  元の文へ戻す(どちらも1行)。気づくのは、03-impl の深度不足が実装を止めたとき
- **過去の回答**: なし(decisions / 決定シート写し / feedbacks / histories 走査済み)

**★あなたの記入**:

### 論点 2. 降格予定の9件のうち 031 / 048 / 065 は、数行で「解消」できる

- **選択肢**:
  **A** = この3件は降格ではなく**解消**する(触る予定の 02 の2ファイルに数行を書く)。降格は残り6件
  (004 / 006 / 066 / 071 / 072 / 080)。
  **B** = 承認どおり9件すべて降格する。
- **AI推奨**: **A**。理由: 3件とも**このタスクで触る予定のファイルの1〜3行**で終わる。
  **048** は 02 の宣言が既に「共有基盤どうしの呼び出しは一方向で循環しないものに限る」へ改まっており、
  issue が問題にした「呼び合わない」という宣言は本文に無い(残るのは追跡の1文だけなので、
  その1文を消せば解消)。**031** は environments.md が既に「未記入の行は不在として扱われ、
  キットの既定が効く」と書いており、残る食い違いは `docs/pendings.md` P-003 の
  「独立監査に必要な設定は確定している」という1文だけ(pendings は SSOT ではないので書き換えは軽い)。
  **065** は「代替レビュー(サブエージェント)のモデル・reasoning」の行は在るが、
  **代替してよいかという可否**の欄が無いだけで、人間の常設承認は既に得ている。
  下流の代償: `environments.md` と `relations.md` が MINOR 上がり、下流が再検証候補になる
  (`docs/reverification.md` の行が増える)。崩れる条件: 065 の常設承認を
  **プロジェクトの方針として SSOT に書くこと**に異議があるとき
- **未回答時の既定**: AI推奨のとおりにする
- **根拠(上流)**: `docs/02-design/environments.md:141`・`:144`(031 / 065)/
  `docs/02-design/relations.md:227`・`:232`(048)/ `.claude/directions/issues-pendings.md` §1 のゲート
- **根拠(同層)**: 095 も同じ型(実測したら母集団が 24 箇所 → 1 箇所に減っていた)で、
  実測に合わせて処理を変える先例がある(`docs/histories/2026-08-08-doc-check-ssot-layer-placement-recertification.md`)
- **根拠(下流)**: `environments.md` を `source` に持つ下流は 0 件、`relations.md:232` を参照する
  ドキュメントも 0 件(`grep -rn "issues/048" docs` は 02 の1箇所と histories 1件のみ)。
  `relations-query.py --impact` の対象はコードなので、この3件には該当なし
- **間違えたときの戻し方**: 書いた行を消して `docs/pendings.md` の残務へ1行戻す(3件で合計6行以内)。
  気づくのは `/doc-check` が当該行を「決めていないことを決めたと書いている」と指摘したとき
- **過去の回答**: あり — 065 については人間が「今後、codex が利用できない時は subagent を使って」と
  **常設で承認済み**(`docs/feedbacks/022-lens-substitution-can-be-approved-standing-not-per-run.md`)。
  ただし**それを SSOT のどこに書くか**は未回答であり、本論点はそこを問う

**★あなたの記入**:

### 論点 3. issue 047 の修正は、`reset` の削除対象を `claude-dev-vm-*` ボリュームへ広げる

- **選択肢**:
  **A** = 広げる。`reset` が `claude-dev-vm-*`(VM モードのゲストディスク)を削除対象に含め、
  削除前の確認の列挙にも出す。
  **B** = 広げない。`reset` は VM ボリュームを残したままにし、issue 047 は「そういう仕様」として
  01/02 に書いて閉じる。
- **AI推奨**: **A**。理由: 02 の契約が既に接頭辞 `claude-dev-vm-` を
  **本システムが名前で所有権を読み取れる共有ボリューム**として挙げており(`D0-env-08` 項1 の対象)、
  `reset` が「ホスト全体を初期状態へ戻す」と説明しながらこれだけ残すのは説明できない。
  `D0-env-05` 項2 が「削除しない」と定める名前付きボリュームは**セッション由来の資源**を指しており、
  本システム自身が固定接頭辞で作るこのボリュームは対象外である。
  下流の代償: **ゲスト VM のディスク(数十 GB になりうる)が `reset` で消える**。
  ゲスト内にだけ置いたデータは戻らない。崩れる条件: 利用者がゲスト VM の中に、
  `/workspace` の外で永続させたいデータを置く運用が出てきたとき
- **未回答時の既定**: AI推奨のとおりにする
- **根拠(上流)**: `docs/00-requests/decisions/env.md:181`(`D0-env-08` 項1 — 固定名または固定接頭辞を
  持つ資源は名前で識別する)/ 同 `:84`(`D0-env-05` 項2 の「削除しない名前付きボリューム」は
  セッション由来の資源についての規定)
- **根拠(同層)**: `docs/02-design/logging.md:61`(`reset` の列挙が接頭辞型の `claude-dev-chrome-*` を
  既に含んでいる — 同じ形の先例)/ `docs/02-design/contracts/cli-container.md:340`
  (共有ボリュームの識別手段が `claude-dev-vm-` を挙げている)
- **根拠(下流)**: `relations-query.py --upstream MODULE-cli-reset` は 0 件(入口機能)。
  `--requirement FR-env-03` は 14 機能で、`reset` 経路に関わるのは
  `MODULE-cli-common-destructive` / `-lock` / `-net-other-running-containers` / `-spawned-resources` と
  `MODULE-makefile-clean` の5本。自動テストは 0 件(シェルは実機確認 — `SR-32`)
- **間違えたときの戻し方**: 削除対象から外す(コード1箇所と `logging.md` の1行)。
  **予防が効いている**: 削除前の確認の列挙(`FR-env-03` 受入基準14)に出るので、`--yes` を付けない
  実行では利用者が中止できる。気づくのは `reset` 後に VM の再 provision が走ったとき
- **過去の回答**: なし(decisions / 決定シート写し / feedbacks / histories 走査済み)

**★あなたの記入**:

## 委任にしてよいか確認したい項目

なし(標準委任 `DS-01`〜`DS-08` とプロジェクトの既存の委任 `D0-*` の範囲で足りる)。

## 今回 AI が決めたこと(回答不要。開示)

<!-- 2026-08-11 に訂正した。当初この節は 023 の倒し方を `[DS-02]`、056 の文言を `[DS-07]` と
     書いていたが、**どちらも標準委任の行使ではない**(独立レビュー Codex の指摘)。
     `DS-02` は「観測される振る舞い(停止か回復か縮退か)」を対象外と明記しており、
     023 の倒し方はまさにそれに当たる。**外から見える振る舞いは 01 の受入基準として書く**のが
     正しい扱いで、フェーズ2の下降で受入基準を新設すること自体は「到達」ではない
     (`.claude/directions/delegation.md` §2)。 -->

- **023 の不正値は警告して SSH agent 転送なしで起動を続ける**(中止しない)。これは委任判断ではなく、
  **`FR-env-04-8` として 01 の受入基準に新設した**(記録先: `new-features/01-requirements/functional.md`)。
  そう倒した理由: `CTR-cli-container`「渡す環境変数」が「値の検証で起動を止めることはしない」と定め、
  `FR-env-04-5`(鍵ファイルが無ければ警告してスキップし続行)が同型の先例だからである。
- **056 の新しい文言は「管理ラベルが付く前に起動した可能性がある」**とした。これも委任判断ではなく、
  **`FR-env-03-17` の受入基準そのものの書き換え**である(記録先: 同上)。
  理由: 「本変更」は読む時点によって指す先が変わるが、「管理ラベルが付く前」は `D0-env-08` 項1 が
  定める事実を指すので時点に依存しない。
- [DS-03] `docker network create` の**標準出力も**捨てる — 理由: 作成したネットワークの ID が
  利用者向け出力に1行混じるため / 見直す条件: 利用者がネットワーク ID を読む必要が生じたとき
  (記録先: `new-features/03-impl/relations/MODULE-cli-common-ensure-infrastructure.md`)
- [DS-01] `reset` の VM ボリューム削除と、削除失敗時のラベル無しコンテナ表示を、**E2E-01 の
  手順8-15 / 手順8-12 で確認する** — 理由: どちらも実 Docker 資源を作らないと再現できない /
  見直す条件: 単体で切り出せる境界ができたとき(記録先: `new-features/03-impl/tests/cli-reset.md`)

## 方針合意(個別diff承認は行いません)

- `docs/00-requests/decisions/scope.md`: `D0-scope-07` の 関連 行だけを書き換える(論点1 の回答に従う)
- `docs/00-requests/decisions/auth.md` / `env.md` / `sec.md`: 削除済み issue へのパス参照を、
  経緯を持つ `docs/histories/` の該当ファイルへ付け替える(054。本文の判断は一切変えない)
- `docs/01-requirements/functional.md`: `FR-env-03-17` の逐語文言を差し替え(056)、
  `FR-env-04` に `CLAUDE_DEV_SSH_BRIDGE_PORT` の不正値のときの条項を1つ足す(023)
- `docs/02-design/logging.md`: `reset` の削除対象の列挙に `claude-dev-vm-*` を足し(047)、
  ラベル無しコンテナの表示文言を差し替える(056)
- `docs/02-design/contracts/cli-container.md`: 環境変数表の `CLAUDE_DEV_SSH_BRIDGE_PORT` の
  「想定外の値のときの扱い」を実装後の姿へ改め(023)、エラーケースの文言を差し替え(056)、
  参照切れを付け替える(054)
- `docs/02-design/environments.md`: 独立レビューの代替の可否を1行足す(065)/
  監査モデルの行から `docs/issues/031` の参照を外す(031)
- `docs/02-design/relations.md`: `PLAN-cli-common-*` の追跡の1文を削除する(048)
- `docs/03-impl/relations/`: 6本は事実の更新(051 / 056 / 087 / 088 / 095 / 101)、
  4本は参照の付け替えだけ(054)
- `docs/03-impl/tests/`: 28 ファイル。旧表記の置換(077 の 188 箇所)、
  「## テスト設計の判断」の新設(084 の 19 ファイル)、`FR-env-01-9` の重複行の解消(074)、
  参照の付け替え(054)、`reset` の E2E 手順の追加(047 / 101)
- `docs/03-impl/index.md`: `docs/issues/009` / `030` を名指す記述を実測値へ書き直す(009 / 030)
- 変更指示を書き終えた時点で差分サマリを提示します。個別のdiff承認は取りません。

**★あなたの記入**(異議のある方針だけ書いてください。空欄 = 全方針に合意):

## 記入完了

**★あなたの記入**(記入を終えたら日付を書いてください。例: 2026-08-07):
