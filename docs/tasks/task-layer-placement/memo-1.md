# task-layer-placement 解決済みの経緯 1(フェーズ1の決定シート)

<!-- memo.md から移した節。**要約ではなく原文のまま**である(`.claude/directions/task-memo.md` §2)。
     フェーズ2 が変更指示へ反映し終えたので、フェーズ2 の手渡しでここへ移した。 -->

## 決定シート(回答済み)

> **回答済み(2026-08-07)。回答待ちの論点は無い。**
> 人間が「一括回答」に **`全て推奨で良い`** と記入した(概念3件・論点4件すべて AI 推奨で確定)。

**ただし、この7件のうち5件は「規範に含まれるべきもの」だった**(人間の指摘、2026-08-07)。
仕分けた結果は次のとおりで、**5件は規範側へ昇格し、シートではなく規範が正になった**。

| シートの項目 | 判定 | 正の所在 |
|---|---|---|
| 概念1(「実装の機構」の外延) | **規範が既に所有**(当初は新設のつもりだった) | `.claude/directions/01-requirements.md:30,49-53` と `layer-fit.md` §1 |
| 概念2(表示内容は 01 か 02 か) | **規範へ昇格した** | `.claude/directions/02-design.md` UI design 節(C)/ `01-requirements.md`(C-2) |
| 概念3(移し先が無い2件) | **一般則は `layer-fit.md` §2 が所有**。個別2件だけが本タスクの判断 | `layer-fit.md:62-67`(移送は両側を1回の下降で) |
| 論点1(`acceptances.md` の技術語) | **プロジェクトの適用**(規範は既に在る) | `.claude/directions/00-requests.md:45`「No technical vocabulary」 |
| 論点2(再試行回数・周期) | **規範へ昇格した** | `.claude/directions/01-requirements.md`(A: 周期を上限に読み替えない) |
| 論点3(測定方法の白箱化) | **規範へ昇格した** | `.claude/directions/01-requirements.md`(B: 測定方法は観測点) |
| 論点4(5周の構成) | **プロジェクトの段取り**。規範化しない | 本 memo の申し送り事項 |

**判断内容は1つも変わっていない** — 人間の「全て推奨で良い」と、昇格させた規範の中身は一致する。
変わったのは**根拠の所在**である。フェーズ2 は**シートではなく規範を引く**こと
(シートはタスクとともに消えるが、規範は残る)。
