# task-layer-placement 解決済みの経緯 2(フェーズ2 の未決点と調査メモ)

<!-- `/doc-check task-layer-placement`(2026-08-08)がローテーションで移した。
     memo.md が 300 行を超えたため(`.claude/directions/task-memo.md` §2)。**要約はしていない**。 -->

## 未決点

**`/implement` は未決点ゼロでなければ始まらない**(CLAUDE.md 原則7)。フェーズ2 の実装ドライラン
(パス1 = 文書だけで自足するか)で出た点と、その決着は次のとおり。**人間判断へ回したものは無い**
(いずれも「値が決まっていない」ではなく「指し先が実在するか」の**事実の照合**であり、
`.claude/directions/delegation.md` §1 の問う基準を満たさない)。

| # | 出た点 | 決着 | 記録先 |
|---|---|---|---|
| 1 | `FR-env-04` と `D0-sec-08` を「設定の置き場と書式は **02 の契約**が定める」と書いたが、02 の `contracts/` は `.claude-dev.yaml` の名前も `ssh_keys` の書式も持たない(名前は `architecture.md` の `DSN-arch-02`、書式は 03 の `MODULE-cli-common-select-ssh-keys` が持つ) | **ドキュメント記載**: 指し先を「02/03 が定める」へ直した(`FR-env-03` の受入基準1・4 が既に使っている言い方に揃える) | 変更指示 `01-requirements/functional.md` / `00-requests/decisions/sec.md` |
| 2 | `FR-orch-05` 受入基準9 を「追記の方式は **02** が定める」と書いたが、`fsync しない` という事実を持つのは 03(`MODULE-orchestrator-state-io.md:35`)で、02 は形式(JSON Lines)までしか持たない | **ドキュメント記載**: 「02/03 が定める」へ直した | 変更指示 `01-requirements/functional.md` |
| 3 | `D0-env-08` 項4 が **03 の機能 ID**(`MODULE-cli-reset` の実装上の判断2)を名指したまま残っていた(自分の下降で気づいた) | **ドキュメント記載**: 層の言い方へ直した(本タスクが 00 に敷いた線と同じ扱い) | 変更指示 `00-requests/decisions/env.md` |
| 4 | 02 の `PLAN-entrypoint-claude` に「UID/GID の追従は所有者で書くファイルを作るより前」という**どの文書にも無かった順序の制約**を書きかけた | **廃止**: 書かない。**SSOT は現在の姿だけを書く**(CLAUDE.md 原則1)ので、既存文書に無い制約を 02 が新しく主張するのは推測の仕様化である。02 に残す順序の制約は `CTR-entrypoint-firewall` が既に持つ1つだけにした | 変更指示 `02-design/relations.md` |
| 5 | `D0-dist-03` / `D0-dist-04` が 02 の `DSN-dist-01` と**同じ主張**(終端レイヤー配置・内容由来のキャッシュキー・時刻由来 cache-bust の却下)を持つ(独立レビューが重大度「高」で検出。フェーズ1 の「変更なし」判定の見落とし) | **ドキュメント記載**: 影響範囲に `dist.md` を追加し、実現手段だけを落とした。決定シート「方針合意」の 00 の行(内容欄から実装の手順を落とす / 技術の選択は残す)の内側なので、シートの再提示は不要と判断した | 変更指示 `00-requests/decisions/dist.md` |
| 6 | `02-design/relations.md` の `PLAN-cli-reset` が「2回目の `reset` で事実に反する表示になる」という**実装の観測事実**を持ち、自ら「`MODULE-cli-reset` の既知の制限が持つ」と書いている(`085` の #10・#11 と同型。独立レビューが検出) | **ドキュメント記載**: `PLAN-cli-reset` を影響範囲に追加し、02 には設計上の期待(冪等)だけを残した | 変更指示 `02-design/relations.md` |
| 7 | `FR-env-01` 受入基準16 へ「プロセス ID」を書き足したが、**保持者をどう記録するかは `D0-env-09` が AI へ委任している**(独立レビューが検出。01 が委任範囲を狭めることになる) | **廃止**: プロセス ID という具体は 01 に書かず、「保持者を利用者が特定できる情報」までとした。具体は `logging.md` と 03 が持つ | 変更指示 `01-requirements/functional.md` |
| 8 | `NFR-ops-02` の測定方法を決定シート 概念3 の推奨どおり「設計の検査」へ書き替えたが、**推奨の前提(03 に受け皿が無い)が事実と違う**(`docs/03-impl/tests/cli-common.md:37`・`:72` に実在。独立レビューが検出) | **ドキュメント記載**: 書き替えを取りやめ、測定方法は原文のまま残した(書き替えると目標値を測れなくなる)。**人間の承認は前提つきだったので、報告で明示する** | 変更指示 `01-requirements/non-functional.md` の `reason` |
| 9 | `MODULE-cli-logout` の「既知の制限」が「この帰結は `D0-env-05` 項2 の理由と食い違う」と書き、削除予定の `docs/issues/090` を参照している(反映後に偽になる。独立レビューが検出) | **`/task-close` の DoD へ回した**: 03 の relations は `/task-close` §2 がコードから再生成して突き合わせる層であり、フェーズ2 で relations の変更指示を新設するより、反映時に1行直すほうが確実である。DoD に明記した | 本 memo の Definition of Done |
| 10 | `FR-env-12-12` の `種別` が語彙の外(`対象外`)で、条項 ID を持つのに受入基準ではない(独立レビューが検出) | **範囲外として起票**: 条項の廃止は 01 の意味に触れる編集で、問う基準に当たる。`docs/issues/093` を起こした | `docs/issues/093` |

## 調査メモ

- **機械検査の母集団**(2026-08-07 実行。`python3 .claude/scripts/check-changeset.py --ssot docs`。
  156 ファイル):

  ```
  CS8  曖昧語・未決点:      違反 42 件
  CS11 参照実在:            違反 19 件
  CS18 要件に降りてきた機構: 違反 17 件
  CS19 理由の網羅:          違反 27 件
  CS20 issue の起点層:      違反 53 件
  変更相対語の候補 12 件(違反ではない)
  NG 違反 158 件
  ```

  **★2026-08-07 フェーズ2 で凍結値を訂正した**: 上の CS8 は当初「8 件」と書き写していたが、
  再実行すると **42 件**であり、内訳の合計(42+19+17+27+53 = 158)が「NG 違反 158 件」と
  一致するのはこちらである(当初の 8 件では合計が合わなかった)。**母集団の総数 158 は変わらない**。

  **本タスクが動かすのは CS18 の 17 件**である(`083`)。CS19 は `084`、CS11 は `054`・`075`、
  CS8 は `031`・P-003 が追跡しており、いずれも範囲外。**この母集団を凍結値として使う** —
  フェーズ4 で再実行し、CS18 が 0 件になり他が増えていないことを確認する。

  **`CS20` の 53 件は範囲外であり、減らさなくてよい。** `.claude/directions/layer-fit.md:109-111`
  が「**このキットより前に起票された issue は `origin_layer` を持たないので、フェーズ1で凍結する
  母集団に違反として並ぶ。それが正しい状態であり、触ったものから埋める**」と定める。
  本タスクが触る9件(`083` `085` `086` `087` `088` `089` `090` `091` `092`)には
  2026-08-07 に `origin_layer` を入れた。

- **★ セッション中にキットが更新された(2026-08-07)。判定の正が変わっている。**
  `.claude/directions/layer-fit.md`(**層の帰属の単独所有者**)と検査 `CS20` が新たに入った。
  **本タスクの判定はこのファイルに従う**:
  - **判定は三つのテスト**(`layer-fit.md` §1): **主語テスト**(00 ⇄ 01)/ **置き換え・観測者テスト**
    (01 ⇄ 02/03。正は `01-requirements.md`)/ **再生成テスト**(02 ⇄ 03。
    「`/task-close` がコードから 03 を再生成したあと、この文は残っているか」)。
    **話題で判定してはならない**(「認証の話だから 02」は判定ではない)。
  - **対処は三つだけ**(同 §2): **移送**(両側とも MINOR。**必ず両側を1回の下降で**)/
    **廃止**(消した側が MINOR)/ **書き換え**(PATCH)。
    **移送で最も多い誤りは片側だけで終わること**なので、落とした1件ごとに移し先を残す。
  - **問う基準の適用**(同 §2.1): **00 に入れる／00 から出す移送は必ず決定シートへ**。
    **01 / 02 / 03 の間の移送は自動修正**であり、`DS-*` の開示行も要らない
    (置き場を直しただけで、決めたのではない)。**廃止だけは別**で、外から見える振る舞いが
    変わるならシートへ。
  - **重大度**(同 §3): 「所有者が2つ」「機構が 01 に在る」「計画が SSOT に在る」
    「意図が 03 にしか無い」の**4つだけが「高」**。**これ以外を「高」に上げてはならない** —
    「全部を「高」にすると1回の検証で終わらない量になり、結果として誰も直さない」。

- **規範側の3つの穴を 2026-08-07 に埋めた**
  (`.claude/improvements/KIT-upper-lower-layer-boundary.md`。`status: 適用済み`):
  (A) **周期を上限に読み替えてはならない**(周期 T のポーリングは最悪遅延が T を超える。
  前タスクで実測した失敗)/ (B) **非機能の測定方法は観測点を書く**(どのテストが実在するかは 03)/
  (C) **利用者に見える文言のうち「約束を担うもの」は 01、静的な項目ラベルは 02**。
  **決定シートの概念1・2 と論点2・3 はこの規範が答えを持つ**ので、フェーズ2 では
  シートではなく規範を根拠に引くこと。

- **規範側の前提が 2026-08-07 に確定した**:
  `.claude/improvements/KIT-where-technology-decisions-belong.md`(`status: 適用済み`)。
  **00 の決定台帳に技術選定を書いてよい**(人間の裁定 A)。ただし線は
  **「選択」と「現状」**である — 人間が選んだ技術(`D0-orch-14` の bubbletea/lipgloss、
  `D0-env-03` の QEMU+virtiofs)は 00 に残す / 実装が現にどうなっているかの事実
  (`D0-auth-03` の `claude-dev:749`〜`:766`、`D0-orch-18` の `orchestrator/trigger.go::Evaluate`、
  `terminology.md` の `BLACKLIST_DOMAINS` 配列)は 03 のものである。
  **判定の目安**は「**内容欄から技術名を外しても意味が通る一文が残るか**」。
  これにより `086` が保留していた 20 件の仕分けが決まる。

- **フェーズ1で現物確認した「00 にコードがある」5件**(`086` の最重要):
  `D0-auth-03`(`claude-dev:749`〜`:766` / `scripts/entrypoint-claude.sh:199`, `:212`)/
  `D0-orch-18`(`orchestrator/trigger.go::Evaluate`)/
  `terminology.md:23`(`scripts/init-firewall-claude.sh` の `BLACKLIST_DOMAINS` 配列16件)/
  `terminology.md:27`(監視デーモンが `STATE=WARN` を書く)。
  いずれも**技術の選択ではなく実装の現状**なので、規範の裁定 A では免責されない。

- **`D0-env-09` の委任範囲は既に開いている**: `docs/00-requests/decisions/env.md:296` は
  「(`flock` / `mkdir` / `ln -s` / **その他**)」と書いており、適用した規範の指針
  (「委任範囲の列挙は開いた形にする」)を**既に満たしている**。`086` が挙げた例だが直す必要は無い。

- **独立レビュー5周の記録(2026-08-07。`lens: subagent`。Codex はアカウントの利用上限で復旧は
  2026-08-11。不変則7 によりどちらが走ったかを明記する)**:

  | 周 | 対象 | 指摘 | 裁定の内訳 |
  |---|---|---|---|
  | 1 | 00 層(SSOT + 変更指示) | 9件(高1) | 確認済み・修正 5 / 誤検知 3(02 の `container` 行の実在を見落とした grep / 検証記録の HTML コメント2件)/ 修正済み 1 |
  | 2 | 01 層(同上) | 13件(高5) | 確認済み・修正 8 / 誤検知 2(`.orchestrator/` は 00 が決めた置き場・用語集の識別子)/ 起票 1(`093`)/ 既に修正済み 2 |
  | 3 | 02 層(同上) | 10件(高2) | 確認済み・修正 6 / 誤検知 2(`logging.md` の所有・契約のエラーケース)/ 範囲外として記録 2(`DSN-mod` の実測値・`environments.md` の CS8) |
  | 4 | 落とした記述の移し先の実在照合(67件) | 移し先なし 0 / 空手形 1 | 空手形1件(`NFR-perf-03` の指し先)を修正。**残り 66 件はすべて移し先の `path:line` を確認済み** |
  | 5 | 変更指示そのものの点検 | 14件(高4) | 確認済み・修正 9 / 誤検知 1(`logging.md` の所有。周3 と同じ論点)/ DoD へ 1 / 起票へ 1(周2 と同じ)/ 低 2 も修正 |

  **周5 が捕まえた最重要の2件**: `#### SCR-01` と `### DSN-orch-02 の適用` が、変更指示の本文で
  **SSOT とは別の親の下に置かれていた**(反映時に同じ節が2箇所へ書き込まれる型。
  `.claude/directions/change-set.md` が実測失敗として明記しているもの)。本文の並びを直した。
