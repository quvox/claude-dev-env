---
target: docs/00-requests/decisions/sec.md
change: replace
sections:
  - "## D0-sec-04 ファイアウォールの実装方式と適用範囲"
  - "## D0-sec-08 SSH 秘密鍵はマウントせず agent 転送だけにする"
  - "## D0-sec-09 レビュー前コードの外向き通信はコンテナ内のファイアウォールで制御する"
  - "## D0-sec-10 KVM デバイスは既定で渡さない"
deletes: []
reason: '`docs/issues/086`「00 の決定が実現手段そのものを固定している」群の2件を直す。(1) `D0-sec-09` は `.claude/directions/00-requests.md` の `decisions/` 節が**実測例としてそのまま名指している**欠陥である — 内容欄が「コンテナ内で iptables のファイアウォールを設定する」だけで、技術名(`iptables`)を消すと文が空になり、**決定の芯(ホスト側ではなくコンテナ内で制御する)は理由欄にしかない**。芯を内容欄へ上げ、実装方式は `D0-sec-04` が既に AI へ委任している(「ブロック対象の表現方法・名前解決のタイミング・どのチェインに規則を置くか」)ので技術名を落とす。**同じ決定の中で委任した事柄を、別の項が固定してはならない。** (2) `D0-sec-08` は設定ファイルの名前とキー名(`.claude-dev.yaml` の `ssh_keys`)と macOS の転送手段(`socat` の TCP ブリッジ)を持つ。前者の置き場と書式は 02 の契約が、後者は `D0-env-04`(macOS 対応は TCP ブリッジで転送する)と 03 が既に持つ。**決定(秘密鍵ファイルをマウントせず、指定された鍵だけを専用 agent に登録してソケットだけを転送する)は1文字も変えない**。(3) `D0-sec-10` の追記が **03 の機能 ID(`MODULE-cli-start`)**を「正」として名指している。**03 は `/task-close` がコードから再生成する層であり、その ID を 00 が握ると再生成のたびに 00 が古くなりうる**。02 の契約(`CTR-cli-container` のエラーケース)への指し先だけを残す。**本タスクが 00 に残す指し先の線**: 02 が所有する合意の ID(`CTR-*` / `DSN-*` / `E2E-*`)は残し、03 の再生成物の ID(`MODULE-*`)とコードの識別子・パス・行番号は落とす。(4) あわせて `D0-sec-10` の**程度語**を落とす(理由欄の「通常はコンテナ内 Chrome での確認で足りる」と、内容欄の「利用者に通常起動を選ばせる」)。CLAUDE.md §8 が禁じる語であり、機械検査 CS8 が両方を検出する。**意味は変えない**(前者は「CPU 仮想化デバイスを要さない」、後者は「VM モードでない起動」と具体に書き直した)。**なお同じ語は 02 の `CTR-cli-container`「エラーケース」にも在るが、本タスクの範囲外の CS8 違反として凍結した母集団に残す**。(5) **`/doc-check` の独立レビューの指摘により `D0-sec-04` を影響範囲へ足した** — `D0-sec-09` から `iptables` を落とすと、`D0-sec-04` の委任範囲に残る「どのチェインに規則を置くか」の**「チェイン」が 00 のどこにも根拠を持たない iptables 固有語になる**。委任の意味(規則の置き場は AI が決めてよい)は変えずに「規則をどこに置くか」へ一般化した。**委任範囲・ガードレール・報告義務の他の項は1文字も変えない**'
reflected: 2026-08-08
---

## D0-sec-04 ファイアウォールの実装方式と適用範囲

- 区分: 委任
- 決めた日: 2026-07-30
- 委任範囲: ブロック対象の表現方法(ホスト名指定か IP 集合か)、名前解決のタイミング(起動時の
  一回解決か追従か)、**規則をどこに置くか**。
- 制約(ガードレール): 適用に失敗してもコンテナの起動を止めないこと(`FR-env-05` の受け入れ基準)。
  適用の成否が起動ログから判別できること。既定の通信(Anthropic API 等、開発に必要な宛先)を
  塞がないこと。
- 報告義務: 決めた内容を該当機能の「実装上の判断」表に `D0-sec-04` と共に記録する
- 関連: FR-env-05 / NFR-sec-01

## D0-sec-08 SSH 秘密鍵はマウントせず agent 転送だけにする

- 区分: 決定
- 決めた日: 2026-07-30
- 内容: 秘密鍵ファイルはコンテナにマウントしない。**プロジェクトごとの設定で指定された鍵だけ**を
  プロジェクト専用 ssh-agent に登録し、そのソケットだけを転送する
  (**設定の置き場と書式は 02/03 が定める**。macOS での転送手段は `D0-env-04` が定める)。
- 理由: 鍵ファイルの露出を避けつつ、ディレクトリごとに異なる鍵を使えるようにする。
- 却下した案: `~/.ssh` の読み取り専用マウント — 別プロジェクトの鍵まで見える。
  ホストの共有 ssh-agent の転送 — 登録済みの全鍵が使えてしまい限定にならない。
- 関連: RQ-env-01 / FR-env-04

## D0-sec-09 レビュー前コードの外向き通信はコンテナ内のファイアウォールで制御する

- 区分: 決定
- 決めた日: 2026-07-30
- 内容: レビュー前コードの外向き通信を、**ホスト側ではなくコンテナ内で制御する**。
  したがって制御はプロジェクト(コンテナ)ごとに独立し、ホストのネットワーク設定には触れない。
  **実装方式(ブロック対象の表現方法・名前解決のタイミング・規則の置き場)は `D0-sec-04` で
  AI に委任する。**
- 理由: レビュー前コードの外部通信を制御する。ホスト側で制御するとプロジェクト単位に分けられない。
- 却下した案: ホスト側ファイアウォールでの制御 — プロジェクト単位に分けられない。
  ネットワークを完全遮断する — 開発に必要な通信(パッケージ取得・API)まで塞がる。
- 関連: RQ-env-01 / FR-env-05

## D0-sec-10 KVM デバイスは既定で渡さない

- 区分: 決定
- 決めた日: 2026-07-30
- 内容: 既定では `/dev/kvm` 等を渡さない。`--kvm` を指定したときだけデバイスを渡す
  (無ければ警告してソフトウェアエミュレーションにする)。
  **★2026-08-04 追記**: **VM モード(`--vm` / `--vm-fresh`)は `/dev/kvm` を必須とし、無ければ
  中止して終了コード 1 で終わる**(ソフトウェアエミュレーションでは実用的な速度が出ないため、
  VM 無しで続行させるのではなく利用者に VM モードでない起動を選ばせる)。`--kvm` 単独指定の「警告して続行」は
  上のとおり変わらない。**`CTR-cli-container` のエラーケースが正**である
  (`docs/issues/018`)。
- 理由: ブラウザでの確認はコンテナ内 Chrome で足り、CPU 仮想化デバイスを要さない。過剰な特権付与を避ける。
- 却下した案: 常時渡す — 必要のない利用者にまで特権を与える。
- 関連: RQ-env-05 / FR-env-08
