---
target: docs/01-requirements/non-functional.md
change: replace
sections:
  - "## 性能"
  - "## 可用性・信頼性"
  - "## セキュリティ"
  - "## 運用・保守性"
deletes: []
reason: '決定シート 論点3(人間の回答 = A「測定方法から実装の名指しを落とす」)と `docs/issues/083`(CS18 の2件)・`docs/issues/086`(測定方法の白箱化8件・下位層 ID)をまとめて直す。**要件と目標値は1件も変えず、測定方法の書き方だけを「観測点」へ戻す**(規範: `.claude/directions/01-requirements.md` の non-functional.md 節 —「測定方法は観測点として書く。どのテストが実在するかは 03 の持ち物であり、名指すとテストを1本足すたびに 01 が古くなる」)。内訳: (a) `NFR-perf-02` の測定方法から `03-impl/environments/images.md` と `vnc-base` というステージ名を落とす(ステージ構成は 02 が定め、実際のビルド定義は 03 が持つ)。(b) `NFR-perf-03` から下位層の ID(`CTR-orchestrator-prompt` の `DSN-prompt-03`。CS18 の指摘)と運用状態の実ファイル名(`.orchestrator/audit.jsonl`)を落とす。(c) `NFR-avail-01` / `NFR-sec-01` / `NFR-sec-03` の測定方法から `E2E-05` / `E2E-03` / `E2E-04` を落とす — **E2E シナリオの ID は 02 がユースケースから導出する 02 の持ち物**であり、01 が名指すと 02 のシナリオ再編で 01 が古くなる(`docs/issues/086` の「CS18 のパターン外の下位層 ID」)。(d) `NFR-sec-03` の測定方法から「`orchestrator` の単体テストが `unset SLACK_BOT_TOKEN` を固定している」という**実在するテストの記述**を落とし、観測点(3種の起動環境を取得して当該変数が無いことを確認する)だけを残す。**どのテストが覆うかは 03 の対応表が正**という指し先は残す(決定シート 論点3 の「崩れる条件」への手当)。(e) `NFR-ops-04` から `MODULE-orchestrator-claude-exec`(CS18 の指摘)を落とし、目標値の「1 ファイルに閉じている」という**コードの構造への要求**を「切り替えのために書き換える箇所が 1 つであること」という観測できる言い方へ改める。(f) **`NFR-ops-02` の測定方法は変えない。** 決定シート 概念3 は「03 のテスト側に受け皿が無いので設計の検査へ書き替える」を推奨し人間がそれを承認したが、**その前提は事実と違う** — 受け皿は `docs/03-impl/tests/cli-common.md:37`(`NFR-ops-02` の行)と `:72`(実機確認で代替するという判断)に実在する(フェーズ2 の独立レビューが検出)。書き替えると**目標値「OS 分岐が 0 件」を測れない測定方法**(設計文書の確認)になり、要件が測定可能でなくなる(`.claude/directions/01-requirements.md` の non-functional.md 節)。**承認の意図(情報を失わない)を満たすのは「そのまま残す」ほうである**ため、**測定方法の判定内容(コンテナ内資産のコードに OS 判定が無いことを確認する)は1文字も変えず**、指し先(どのテストが覆うかは 03 のテスト対応表が正である)だけを測定方法欄に足した(`/doc-check` の再監査が「原文のまま」という書き方と本文の食い違いを検出したので実態に合わせた)。(g) **`NFR-perf-02` の6カテゴリの列挙は残す** — `FR-env-11-1` からは起動する構成要素の列挙を落とすのに、ここに残すのは非対称に見えるが、**この6カテゴリは合否の判定基準そのもの**(追加層の導入物をどれかへ割り当てられなければ不合格)であり、実現方式を差し替えてもカテゴリは残る。落とすと目標値が測れなくなる(`/doc-check` の独立レビューが非対称を指摘したので理由を明記した)'
reflected: 2026-08-08
---

## 性能

| ID | 要件 | 目標値 | 測定方法 | 分割可否 |
|---|---|---|---|---|
| NFR-perf-01 | 日次ビルドの更新後も、利用者の `docker pull` は増分取得に留まること。同梱エージェント CLI が更新されたときに再取得させる層は、その導入層以降のみとする | 同梱 CLI のバージョンが変わらない日は、配布 2 イメージのどの層も再取得が発生しない | 前日分のイメージを持つ環境で `docker pull` を実行し、`Already exists` 以外の層が出ないことを確認する | 段階可(理由: 再取得される層が1つ減るだけでも取得時間の短縮という利用者価値があり、目標「再取得ゼロ」へ層単位で段階的に近づけるため) |
| NFR-perf-02 | ブラウザ確認ありのイメージは、なしのイメージのベースレイヤーを共有し、追加ディスクを VNC/Chrome 分に限定すること | (1) 2 イメージのベース層が同一ダイジェストであること。(2) **ブラウザ確認ありイメージだけが持つ追加層が導入するのは、ブラウザ確認(`FR-env-11`)を成立させる資産だけであること**。具体的には (i) VNC・ディスプレイ・ウィンドウマネージャ・noVNC、(ii) GUI ブラウザとその追加依存、(iii) 日本語入力とロケール(`FR-env-11` 受入基準3)、(iv) GUI セッションの端末・キー操作と gsettings/dconf バックエンド、(v) フォント、(vi) computer-use MCP の6カテゴリであり、**いずれにも属さない導入やファイル追加(開発ツール・言語ランタイム・ビルド済み成果物)が1件も現れないこと**。**同梱エージェント CLI の導入層は両イメージが等しく持つので (2) の対象外**である | `docker history --no-trunc` で両イメージの層を並べ、共通部分より後ろの層の `CREATED BY` を1行ずつ読む。(1) は共通部分の最終層のダイジェスト一致で判定する。(2) は追加層の `CREATED BY` に現れる導入物を上の6カテゴリへ1件ずつ割り当て、**どのカテゴリにも割り当てられないものが1件でもあれば不合格**とする。**対象の層は、02 が定めるステージ構成のうちブラウザ確認資産を積むステージである**。**付随処理(D-Bus の machine-id 生成・GTK の IM モジュールキャッシュ更新など、それ単体では上のどれでもないが、無いと特定のカテゴリが動かないもの)は、動かなくなるカテゴリへ帰属させる**(帰属先が1つも無いものだけを不合格とする) | 段階可(理由: 共有されるベース層が増えるほど追加ディスクが減る形で目標へ段階的に近づけ、部分達成にもディスク削減の価値があるため) |
| NFR-perf-03 | worker への割り当ては、**1タスクが1回の worker 起動で完結する粒度**とすること(同一 Attempt の再開と、レビュー指摘による改訂は起動回数に数えない)。**worker へ渡す文脈の絞り方は 02 の設計が定める**(要件としては上限値を課さない) | 1タスクあたり worker プロセスの起動は 1 回(再開・改訂を除く) | 運用状態の追記型ログに残る worker の起動イベントの数を、タスク数と突き合わせる(**どの記録がその数を持つかは 02/03 が定める**) | 不可分 |

## 可用性・信頼性

| ID | 要件 | 目標値 | 測定方法 | 分割可否 |
|---|---|---|---|---|
| NFR-avail-01 | クライアント端末が失われてもコントローラは生存し、再実行で復旧できること。tmux サーバごと失われた場合も運用状態から再開できること | 端末全終了 → 再実行で、完了済みタスクの再実行が 0 件 | 端末をすべて閉じてから `claude-dev orchestrate` を再実行し、完了済みタスクの状態が変化しないことを確認する(**どの E2E シナリオがこの確認を担うかは 02 のテスト戦略が定める**) | 不可分 |
| NFR-avail-02 | コンテナは SSH 切断・tmux デタッチで停止しないこと | 切断後もコンテナが running を維持する | `claude-dev list` で状態を確認する | 不可分 |
| NFR-avail-03 | 補助機能(ファイアウォール適用・通知・VM 起動・ホスト設定の取り込み)の失敗が、主機能(コンテナ起動・run の継続)を止めないこと | 補助機能の失敗時も主機能の成功率 100% | 各補助機能を意図的に失敗させ、起動と run が継続することを確認する | 不可分 |

## セキュリティ

| ID | 要件 | 目標値 | 測定方法 | 分割可否 |
|---|---|---|---|---|
| NFR-sec-01 | 隔離と最小権限: Docker 生ソケット・SSH 秘密鍵ファイルをコンテナへ渡さないこと。API キー/トークンをイメージに焼き込まないこと。docker-proxy をホストへ公開しないこと。コンテナの confinement(seccomp / AppArmor)を緩めないこと | 上記4項目すべてが常に成立(例外なし) | `docker inspect` でマウント・`SecurityOpt`・公開ポートを確認する。イメージ内に認証情報が無いことを確認する。**危険な Docker 操作が拒否されることを実機で確認する**(**どの E2E シナリオが担うかは 02 のテスト戦略が定める**) | 不可分 |
| NFR-sec-03 | 秘密情報(通知トークン等)の到達範囲が必要な発信源に限られること。エージェント(worker・対話 Claude)の環境に含まれないこと | **worker・レビューアー・対話 Claude の3種すべて**について、そのプロセス環境に**通知トークン `SLACK_BOT_TOKEN` が存在しない**こと。これを持ってよいのは通知を実際に送るコントローラとフックだけである。**環境変数として渡る秘密はこの1つだけである**(認証情報は共有ボリューム上のファイルとして渡り、環境変数を経由しない)ため、環境の検査でこの要件の全体を測れる | 3種それぞれの起動環境を取得し、`SLACK_BOT_TOKEN` が含まれないことを確認する。**どのテストがこの確認を覆っているか(自動テストか実機確認か、実装済みか未実装か)は 03 のテスト対応表が正である** | 不可分 |

## 運用・保守性

| ID | 要件 | 目標値 | 測定方法 | 分割可否 |
|---|---|---|---|---|
| NFR-ops-02 | OS 依存はホスト CLI に閉じ、コンテナ内資産(イメージ・entrypoint・firewall・docker-proxy)は OS 非依存に保つこと | コンテナ内資産に OS 分岐が 0 件 | コンテナ内資産のコードに OS 判定が無いことを確認する(**どのテストがこの確認を覆っているかは 03 のテスト対応表が正である**) | 不可分 |
| NFR-ops-03 | 利用可能な操作を一覧できること | `make help` で全ターゲットが表示される。CLI は引数なし/未知の引数でヘルプを表示する | 実機で確認する | 不可分 |
| NFR-ops-04 | モデル/effort ポリシーは 1 箇所で切り替え可能であること。運用状態は機械が読み書きし、人間が編集しない前提であること | 第1文: **工程別のモデル/effort を変えるために書き換える箇所が 1 つであること**。**第2文(運用状態の読み書き主体)は測らない** | 第1文: その 1 箇所を示せることを確認する(**実装の在り処は 03 の実装仕様が持つ**)。第2文: **測らない**。理由 = 「人間が編集しない」は利用者の**運用上の前提**であって、システムが強制する振る舞いではない。強制するには権限設定か改竄検知の仕組みが要るが、それを求める決定は 00 に無い。前提が破られたことをシステムが検知する要件も無いため、合否を判定できる観測点が存在しない | 不可分 |
