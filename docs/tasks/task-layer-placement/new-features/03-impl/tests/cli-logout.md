---
target: docs/03-impl/tests/cli-logout.md
change: replace
sections:
  - "## 受入基準 ⇄ テスト対応表"
  - "## テスト設計の判断"
  - "## 未検証(テスト未実装)の全件"
anchors:
  - { section: "## テスト設計の判断", after: "## 機能間連携仕様書 ⇄ テスト" }
deletes: []
reason: '`docs/issues/090` の裁定で 01 に新設する条項 `FR-env-03-24`(`logout` の後にセッション由来の資源が `stop` では回収できなくなる)の**受け皿**を作る。`.claude/directions/03-impl.md` と機械検査 `CS13` は「01 の変更指示が触れる条項は 02 のカバレッジ表と 03 のテスト対応表に行を持つ」ことを要求するので、条項を足す下降と同じ回で足す。**状態は「未検証(テスト未実装)」**である(シェル実装に自動テストランナーを設けないという `DSN-test-01` / `SR-32` の方針により、確認は実機の E2E-01 手順8 に載る。不変則3 が唯一の例外として認める行である)。あわせて **`CS19` が要求する「テスト設計の判断」の節をこのファイルへ新設する** — 本ファイルには節そのものが無く、変更指示が触れる以上は空のまま残せない(`docs/issues/084` が `03-impl/tests/` 全体で 27 件の同型を追跡しており、**本タスクが触るこの1件だけをここで埋める。残る 26 件は 084 のままである**)。**既存の行・状態は1つも変えない**。**★`/doc-check` の独立レビュー3本が独立に検出した2点をここで閉じた**: (1) 対応表と未検証表が指す実機確認手順は「E2E-01 手順8」だったが、**手順8 の部分手順に `logout` の後で `stop` を走らせる並びが1つも無かった**(空手形)。同じ下降で `03-impl/tests/e2e.md` の変更指示を新設して部分手順16 を作り、指し先を `手順8-16` に確定した。(2) 判断1 の「見直す条件」(`reset` 側だけに固有の確認手順が生じたとき → `tests/cli-reset.md` に行を立てる)は**書いた時点で既に真だった** — 受入基準9 は `logout` 側(手順8-5)と `reset` 側(手順8-12)で手順が分かれており、現物はファイルを分けずに本ファイルの2行で表している。**判断の意図(状態を持つ場所を1つに保つ)は変えず、現物に合う条件へ書き直した**'
reflected: 2026-08-08
---

## 受入基準 ⇄ テスト対応表


<!-- 受入基準 14〜18 は `logout` と `reset` の双方に効く共通の振る舞いである。
     重複を作らないため、対応表は主担当である本ファイルだけが持つ
     (`tests/cli-reset.md` の受入基準表は「対象外」を維持する)。
     実機確認の手順は `logout` と `reset` の両方について実施する。
     受入基準22 は `reset` 側の振る舞い(プロジェクト配下を触らない)だが、`logout` の
     受入基準20 と対になる非対称なので、重複を作らないためこの表で持つ。 -->

| 受入基準 ID | 種別 | レベル | テスト識別子 | 状態 |
|---|---|---|---|---|
| FR-env-01-9 | 境界値 | — | -(実機確認手順。**`logout` 側**。`docs/03-impl/tests/e2e.md` の E2E-01 手順8-5) | 未検証(テスト未実装) |
| FR-env-01-9 | 境界値 | — | -(実機確認手順。**`reset` 側**(docker-proxy と `claude-dev-net` の両方)。`docs/03-impl/tests/e2e.md` の E2E-01 手順8-12) | 未検証(テスト未実装) |
| FR-env-03-5 | 正常系 | — | - | 未検証(テスト未実装) |
| FR-env-03-14 | 正常系 | — | -(実機確認手順。`logout` / `reset` の両方。`docs/03-impl/tests/e2e.md` の E2E-01 手順8) | 未検証(テスト未実装) |
| FR-env-03-15 | 境界値 | — | -(実機確認手順。`logout` / `reset` の両方。`docs/03-impl/tests/e2e.md` の E2E-01 手順8) | 未検証(テスト未実装) |
| FR-env-03-16 | 正常系 | — | -(実機確認手順。`logout` / `reset` の両方。`docs/03-impl/tests/e2e.md` の E2E-01 手順8) | 未検証(テスト未実装) |
| FR-env-03-17 | 正常系 | — | -(実機確認手順。`logout` / `reset` の両方。`docs/03-impl/tests/e2e.md` の E2E-01 手順8) | 未検証(テスト未実装) |
| FR-env-03-18 | 異常系 | — | -(実機確認手順。`logout` / `reset` の両方。`docs/03-impl/tests/e2e.md` の E2E-01 手順8) | 未検証(テスト未実装) |
| FR-env-03-19 | 境界値 | — | -(実機確認手順。`docs/03-impl/tests/e2e.md` の E2E-01 手順8) | 未検証(テスト未実装) |
| FR-env-03-20 | 正常系 | — | -(実機確認手順。`docs/03-impl/tests/e2e.md` の E2E-01 手順8) | 未検証(テスト未実装) |
| FR-env-03-21 | 境界値 | — | -(実機確認手順。`docs/03-impl/tests/e2e.md` の E2E-01 手順8) | 未検証(テスト未実装) |
| FR-env-03-22 | 正常系 | — | -(実機確認手順。`docs/03-impl/tests/e2e.md` の E2E-01 手順8。`reset` について確認する) | 未検証(テスト未実装) |
| FR-env-03-23 | 異常系 | — | -(実機確認手順。`logout` / `reset` の両方。`docs/03-impl/tests/e2e.md` の E2E-01 手順8-13) | 未検証(テスト未実装) |
| FR-env-03-24 | 境界値 | — | -(実機確認手順。`logout` の後に `stop` がセッション由来の資源を片付けられないことを確認する。`docs/03-impl/tests/e2e.md` の E2E-01 手順8-16) | 未検証(テスト未実装) |


## テスト設計の判断

| # | 判断 | 理由 | 見直す条件 |
|---|---|---|---|
| 1 | **`logout` と `reset` に共通して効く受入基準(`FR-env-01` 受入基準9 / `FR-env-03` 受入基準14〜18・23)の対応表を、主担当である本ファイルだけが持つ。`reset` 側だけ別の手順で確かめるものは、同じ条項の行を本ファイルの中で2行に分ける**(現に `FR-env-01` 受入基準9 が `logout` 側=手順8-5 と `reset` 側=手順8-12 の2行になっている) | 同じ条項の行を `tests/cli-reset.md` にも置くと、状態(未検証 / 実装済み)が2箇所に分かれて必ずずれる。手順が分かれても**状態を持つ場所は1つに保つ**のが目的なので、分岐はファイルをまたがずに行の分割で表す。`tests/cli-reset.md` が持つのは `reset` にしか無い条項(`FR-env-01` 受入基準25)だけである | **`reset` にしか無い条項が増えたとき**(そのときは `tests/cli-reset.md` にその条項の行を立てる。共通条項の手順が分かれるだけなら本ファイルの行を増やす) |
| 2 | **本モジュールの自動テストを書かず、すべて実機確認(E2E-01 手順8)に寄せる** | シェル実装に自動テストランナーを設けないという `DSN-test-01`(`SR-32`)の方針による。確認プロンプト・非 TTY・削除失敗・中断シグナルはいずれも実端末と実 Docker 資源を要し、模擬すると確認したい振る舞いから離れる | 02 の `DSN-test-01` が変わり、シェルにテストランナーを置くと決めたとき |
| 3 | **`FR-env-03-24`(`logout` 後にセッション由来の資源が `stop` で回収できないこと)を実機確認 E2E-01 手順8-16 に載せる** | 確認には「所有者ラベルを持つ資源を作ったうえで `logout` し、その後 `stop` を実行する」という実 Docker 資源の並びが要る。**確認するのは「消えないこと」であり、削除の失敗ではない**(`FR-env-03` 受入基準18 の対象外)。手順を末尾に置くのは、`logout` が Claude コンテナを消すと以降の部分手順の前提が壊れるためである | `reset` 以外の回収手段を設けたとき(そのときは回収経路の確認が要る) |

## 未検証(テスト未実装)の全件

| # | 対象 | なぜ未実装か | 解消の条件 |
|---|---|---|---|
| 1 | FR-env-01 — 受入基準 9(境界値。**`logout` 側**の遊休判定) | 自動テストランナーを設けない方針(`DSN-test-01` / `SR-32`)。ラベルを持たない稼働中コンテナを用意する必要があるため実機確認で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
| 2 | FR-env-03 — 受入基準 5(正常系) | 自動テストランナーを設けない方針(`DSN-test-01` / `SR-32`)。実機確認で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
| 3 | FR-env-03 — 受入基準 14(正常系) | 自動テストランナーを設けない方針(`DSN-test-01` / `SR-32`)。確認プロンプトは対話端末を要するため実機確認で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
| 4 | FR-env-03 — 受入基準 15(境界値) | 自動テストランナーを設けない方針(`DSN-test-01` / `SR-32`)。実機確認で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
| 5 | FR-env-03 — 受入基準 16(正常系) | 自動テストランナーを設けない方針(`DSN-test-01` / `SR-32`)。実機確認で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
| 6 | FR-env-03 — 受入基準 17(正常系) | 自動テストランナーを設けない方針(`DSN-test-01` / `SR-32`)。ラベルを持たないコンテナを用意する必要があるため実機確認で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
| 7 | FR-env-03 — 受入基準 18(異常系) | 自動テストランナーを設けない方針(`DSN-test-01` / `SR-32`)。削除失敗を人為的に起こす必要があるため実機確認で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
| 8 | FR-env-03 — 受入基準 19(境界値) | 自動テストランナーを設けない方針(`DSN-test-01` / `SR-32`)。実機確認で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
| 9 | FR-env-03 — 受入基準 20(正常系) | 自動テストランナーを設けない方針(`DSN-test-01` / `SR-32`)。実機確認で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
| 10 | FR-env-03 — 受入基準 21(境界値) | 自動テストランナーを設けない方針(`DSN-test-01` / `SR-32`)。実機確認で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
| 11 | FR-env-03 — 受入基準 22(正常系) | 自動テストランナーを設けない方針(`DSN-test-01` / `SR-32`)。`reset` がプロジェクト配下を触らないことの確認なので実機確認で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
| 12 | FR-env-01 — 受入基準 9(境界値。**`reset` 側**。docker-proxy と `claude-dev-net`) | 自動テストランナーを設けない方針(`DSN-test-01` / `SR-32`)。ラベルを持たない稼働中コンテナを用意する必要があるため実機確認で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
| 13 | FR-env-03 — 受入基準 23(異常系。中断時の終了コード 130) | 自動テストランナーを設けない方針(`DSN-test-01` / `SR-32`)。削除の途中でシグナルを送る必要があるため実機確認で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
| 14 | MODULE-cli-logout — 機能全体 | シェル実装のため自動テストランナーが無く実機確認で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
| 15 | CTR-cli-container(破壊的操作の対象の識別) — 契約の結合テスト | 自動テストランナーを設けない方針(`DSN-test-01` / `SR-32`)。管理ラベルの発行側(`MOD-cli-start`)と組み合わせた実機確認(E2E-01 手順8-5・8-8・8-9・8-10・8-13)で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
| 16 | FR-env-03 — 受入基準 24(境界値。`logout` 後にセッション由来の資源が `stop` で回収できないこと。**手順は E2E-01 手順8-16**) | 自動テストランナーを設けない方針(`DSN-test-01` / `SR-32`)。所有者ラベルを持つ資源を実際に作ってから `logout` する必要があるため実機確認で代替する | 自動化の予定は無い(方針を変える場合は 02 の `DSN-test-01` から見直す) |
