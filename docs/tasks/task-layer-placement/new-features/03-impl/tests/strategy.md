---
target: docs/03-impl/tests/strategy.md
change: replace
sections:
  - "## カバレッジの扱い"
  - "## テスト設計の判断"
deletes: []
reason: '`docs/issues/085` #6 と**同型**として同じ下降で扱うと宣言しながら、02 側(`environments.md` の「lint・テストコマンド」表)に受け皿を作っただけで 03 側からコマンド文字列を落としていなかった(`/doc-check` の独立レビュー2本が独立に検出)。`.claude/directions/02-design.md` は「The command strings written here are authoritative」と定めるので、**同じコマンド文字列を 02 と 03 の両方が持つと所有者が2つになる**。`relations-query.py --health` の側(#6 の手当)は「02 の表へ足し、03 側は指し先だけにする」と両側を処理しているので、扱いを揃える。**測定の方針(目標値なし・測定していない)は変えない**。あわせて、**本タスクが条項 `FR-env-03-24` を新設することで古くなる数**を同じ下降で直す — 「機能要件の全 209 条項 / 対応表は 224 行 / 条項 222 件」を「210 条項 / 225 行 / 223 件」へ改める(`docs/02-design/system.md` の要件カバレッジ確認と同じ数え直しである)。`CS19` の要求により「テスト設計の判断」を再読し、**2件(うち1件は「判断なし」の宣言)がいずれも継続(変更なし)と判断した** — 本タスクはテストの実行方法も分類も変えないためである'
reflected: 2026-08-08
---

## カバレッジの扱い

| 指標 | 目標 | 現状 | 測定コマンド |
|---|---|---|---|
| 行カバレッジ(Go) | **目標値なし**(01 の非機能要件にカバレッジの目標は無い) | 測定していない | `docs/02-design/environments.md`「lint・テストコマンド」の**カバレッジ計測(docker-proxy)**の行 |
| 受入基準のカバレッジ | すべての受入基準が対応表に行を持つこと(状態は問わない) | 機能要件の全 210 条項に行がある(非機能要件の 13 行を合わせて対応表は 225 行 = 条項 223 件 + `FR-env-01-9` が主担当行を3つ持つ重複2行。重複は `docs/issues/074` が追跡する) | `python3 .claude/scripts/build-index.py --check` で集計を再生成して確認する |

**カバレッジ率ではなく「受入基準に行があるか」を指標にする。** 自動テストを持てない領域が大きい
(Bash と Makefile)ため、行カバレッジは実態を表さない。

## テスト設計の判断

<!-- この層全体に掛かる判断だけを置く。モジュール個別の判断は各 `03-impl/tests/<module>.md` にある。 -->

- 判断なし: レベルの割り当ての既定(`DSN-test-01` / `SR-32`)を動かしていないため、層全体に掛かる判断は無い。既定から外れる割り当ては1件も無く、モジュールごとの割り当ての事実は各 `03-impl/tests/<module>.md` が持つ
- [DS-01] 集計値を手で数えず `python3 .claude/scripts/build-index.py --check` の再生成で確認する — 理由: 条項数・行数・件数の3つは互いに導出関係にあり、手で数えると3つが別々にずれても気づけない(この節の上の表がその3つを1行で持っている) / 見直す条件: `build-index.py` が受入基準の対応表を集計しなくなったとき
