# task-layer-placement 決定シート

**記入方法**: 下の「一括回答」に **1行**書けば終わりです。
個別に決めたいものがあるときだけ、そのブロックの **★あなたの記入** の後ろに書いてください。
(あなたが ★あなたの記入 に書いた内容を AI が書き換えることはありません。**唯一の例外**:
新しい論点を追記したとき、末尾「記入完了」の日付だけは AI が消します — 再記入をお願いします。)

各ブロックには **AI推奨** と **未回答時の既定** という**別々の案**が載っています。

## 一括回答(ここだけ埋めれば先へ進めます)

**★あなたの記入**:全て推奨で良い

## 概念の明確化(00/01。上から確認してください。最大7件)

### 1. 語の外延 —「実装の機構」(何を1件と数えて落とすのか)

- **場所**: `docs/issues/086-modify-upper-layers-carry-implementation-mechanisms.md`(59件の内訳表)/
  `.claude/directions/01-requirements.md:190-195`(置き換えテストと観測者テスト)
- **明確にしたいこと**: 上位層から落とす対象を**何をもって1件と数えるか**。
  規範の2つのテストは「実現方式を差し替えてもこの文は真か(置き換えテスト)」
  「システムの内側を見ずに確かめられるか(観測者テスト)」である。
- **AI推奨**:
  **含む** = (a) コードの識別子・ファイル名・行番号(`orchestrator/trigger.go::Evaluate` /
  `BLACKLIST_DOMAINS` / `claude-dev:749`〜`:766`)、
  (b) 内部の実行順序と周期・回数(「VNC → WM → Chrome → noVNC の順に起動」「30 秒ごと」「最大 20 回」)、
  (c) 下位層の ID(`CTR-*` / `MODULE-*` / `DSN-*` / `E2E-*`)、
  (d) 内部トポロジと内部資源名(`DOCKER_HOST=tcp://claude-dev-docker-proxy:2375` /
  `claude-dev-config` ボリューム名)、
  (e) プロトコルの内部値(`403` / `502`)。
  **含まない** = **利用者が打つ・見るもの**(CLI 名・引数・終了コード・受理する文字種・表示の有無・
  `/workspace`)と、**人間が選んだ技術そのもの**(`bubbletea` / `QEMU+virtiofs`。
  2026-08-07 の規範裁定 A)。
  理由: 前者は実現方式を差し替えると偽になり、後者は差し替えても真であり続ける。
  崩れる条件: 利用者に見える値が内部値と一致してしまう場合(`403` は利用者が `docker` の
  エラーとして見るという反論が成り立つ。そのときは (e) だけ 01 に残す)。
- **未回答時の既定**: AI推奨のとおりにする
- **波及**: 00 で 25 件 / 01 で 34 件 / 02 で 11 件(`086` と `085` の内訳表)
- **間違えたときの戻し方**: 落とした記述は全件が移し先(02/03)に在ることを確認してから落とすので、
  戻すときは移し先から引き写せばよい。**フェーズ2 の変更指示に、落とした1件ごとの移し先を
  `path:line` で残す**(前回の5周でこの照合を怠って1件消失した実績があるため)。
- **過去の回答**: なし(`docs/00-requests/decisions/` / `docs/feedbacks/` / `docs/histories/` 走査済み)

**★あなたの記入**:

### 2. 範囲の境界 —「利用者に見える表示内容」は 01 と 02 のどちらが持つか

- **場所**: `docs/02-design/system.md:514`・`:521-525`・`:527-530`(`docs/issues/085` の #7〜#9)/
  `docs/01-requirements/functional.md` の `FR-env-03-14`・`-15`・`-17`・`-18`・`FR-env-01-16`・`-18`
- **明確にしたいこと**: 破壊的操作の4状態(確認 / 中止 / 一部失敗 / 残した資源)で
  **何を表示するか**の全文が、いま 02 の UI 設計節に在る。これを 01 の受入基準へ戻すのか、
  02 に残すのか。**両方に書くのは禁止**(同じ規範が2箇所にあると必ずずれる)。
- **AI推奨**: **01 が持つ**(02 の UI 節は「項目と状態の名前」までに戻す)。
  理由: 表示の有無と内容は**外から観測できる**ので置き換えテストに掛かる。現に
  `FR-env-01-26`・`-27`(種別つき1行ずつ / 0件なら無表示)は既に 01 の条項として書かれており、
  同じ性質のものが 02 と 01 に分かれている。
  崩れる条件: 画面の**レイアウト**(どの順で並べるか、どこで改行するか)まで 01 へ持ち込もうと
  したとき — そこは 02 の UI 設計が正である(CLAUDE.md §8「ピクセル単位の見た目は書かない」)。
- **未回答時の既定**: AI推奨のとおりにする
- **波及**: 02 で3箇所 / 01 で6条項の文面(**条項 ID は増やさない**。既存条項へ書き足す)
- **間違えたときの戻し方**: 02 の当該節は削除ではなく「項目名だけを残す」形にするので、
  戻すときは 01 から文面を引き写せる。
- **過去の回答**: なし

**★あなたの記入**:

### 3. 範囲の境界 —「移し先が無い2件」をどうするか

- **場所**: `docs/00-requests/decisions/env.md`(`D0-env-02` の **ControlMaster**)/
  `docs/01-requirements/non-functional.md:62`(`NFR-ops-02` の測定方法
  「コンテナ内資産のコードに OS 判定が無いことを確認する」)
- **明確にしたいこと**: `086` が調べた 59 件のうち **57 件は移し先(02/03)に同じ事実が在る**が、
  この2件だけ**受け皿が無い**。落とすと情報が消える。
- **AI推奨**: **落とさずに残し、受け皿を作る**。
  ControlMaster は `docs/02-design/environments.md` の接続手順へ1行、
  `NFR-ops-02` の測定は `docs/03-impl/tests/` に受け皿が無いので**測定方法を
  「`02-design/system.md` の分割定義が OS 依存をホスト CLI に閉じていること」へ書き替える**
  (コード検査ではなく設計の検査にする)。理由: 「情報を失わない」が本タスクの前提であり、
  移し先が無いものを落とすのは移動ではなく削除である。
  崩れる条件: ControlMaster を 02 に書くと 02 が実装の細部を持つことになる、という
  `085` と同型の指摘が出る場合(そのときは 03 の `MODULE-cli-forward` に置く)。
- **未回答時の既定**: AI推奨のとおりにする
- **波及**: 00 で1箇所 / 01 で1箇所 / 02 で1行追加
- **間違えたときの戻し方**: どちらも1行の追記なので、消せば元に戻る。
- **過去の回答**: なし

**★あなたの記入**:

## 次に問う候補(回答不要。今回載せなかったもの)

- 「介入トリガー」/「資源逼迫」/「Codex サンドボックス」/「公開ポート」

## 論点(概念以外。従来の決定シート)

### 論点 1. `acceptances.md` の技術語4件を書き替えるか

- **選択肢**:
  A. **書き替える** — `AC-03`「解釈できない要求は**そのまま Docker へ渡され**、最終的な検証は
     **Docker 側**に委ねられる」を、利用者から見える言い方(「検査できなかった要求は止められず、
     結果は実行してみるまで分からない」)へ改める /
  B. **残す** — 受け入れ基準は人間が書いた文書であり、AI が言い換えると意図がずれる
- **AI推奨**: **A**。理由: `.claude/directions/00-requests.md` は `acceptances.md` について
  **「No technical vocabulary」**を明記しており、規範違反が4件ある状態である。
  下流の代償: `AC-03` の文面が変わるので `UC-03` と `FR-env-07` の `verified` が失効し、
  `/doc-check` の再認証が要る(本タスクではどのみち全層を再認証する)。
  崩れる条件: 「Docker 側に委ねられる」が利用者への**限定の宣言**として機能しており、
  言い換えると保証の範囲が曖昧になる場合。
- **未回答時の既定**: AI推奨のとおりにする
- **根拠(上流)**: `.claude/directions/00-requests.md`(`acceptances.md` の節「No technical vocabulary」)
- **根拠(同層)**: `docs/00-requests/terminology.md:24` が「危険な操作」を
  「Docker API 側の拒否対象と混同する」として禁止語にしている先例
- **根拠(下流)**: `docs/01-requirements/usecases.md:121-124`(`UC-03` の例外フローが
  `403` / `502` を持つ)/ `docs/issues/005`(残存リスクの追跡先)
- **間違えたときの戻し方**: `AC-03` は1段落なので、差し戻すなら原文を書き戻すだけでよい。
  実装もテストも動かない。
- **過去の回答**: なし

**★あなたの記入**:

### 論点 2. `01` の再試行回数・周期を落とすと、受入基準が測れなくなる件

- **選択肢**:
  A. **観測できる上限へ書き替える** — 「最大 20 回まで再試行する」→「別ポートで再試行し、
     一定回数で諦めて終了コード 1 で終わる(回数は 02 が定める)」、
     「30 秒ごとに検知して書き戻す」→「更新を 30 秒以内に反映する」 /
  B. **数値を 01 に残す** — 受入基準は測れることが要件であり、数値が無いとテストが書けない /
  C. **要件ごとに分ける** — 利用者が待たされる時間(反映の遅延)は残し、内部の試行回数は落とす
- **AI推奨**: **C**。理由: 前タスクで A を機械的に当てた結果、
  **`FR-env-03-3` を「30 秒以内に反映」と書き替えたら 02 の機構(30 秒**周期**のポーリング)と
  食い違った**(最悪の遅延が 30 秒を超える)。実測済みの失敗なので繰り返さない。
  **利用者が観測できる時間の約束は 01 に残し、内部の試行回数・周期は 02 へ落とす**のが線である。
  下流の代償: `FR-env-11-5`(20 回)などを落とすと、その条項を覆うテストの識別子を
  `03-impl/tests/` 側で 02 の値へ張り直す必要がある(6条項)。
  崩れる条件: 「一定回数」と書いた結果、02 が回数を書き忘れて**どこにも回数が無い**状態に
  なるとき(移し先の実在確認で防ぐ)。
- **未回答時の既定**: AI推奨のとおりにする
- **根拠(上流)**: `.claude/directions/01-requirements.md:190-195`(置き換えテスト / 観測者テスト)
- **根拠(同層)**: `docs/01-requirements/functional.md:88`(`FR-env-01-7` は
  「上限(既定 30 秒 / VM モード 420 秒)」を**利用者が待つ時間**として持っており、これは残す側の先例)
- **根拠(下流)**: `docs/histories/2026-08-07-stop-session-spawned-containers.md`
  (前タスクの5周目が同じ書き替えで自己矛盾を作り、差し戻した記録)
- **間違えたときの戻し方**: 数値は 02 へ移すだけなので、戻すときは 02 から引き写す。
  **落とした数値と移し先を1件ずつ変更指示に残す。**
- **過去の回答**: 前タスク(`task-stop-session-spawned-containers`)の5周目で
  **A を当てて失敗し、観測できる言い方へ戻した**。その経緯が本推奨 C の根拠である。

**★あなたの記入**:

### 論点 3. `NFR-sec-03` / `NFR-perf-02` の測定方法が白箱化している件

- **選択肢**:
  A. **測定方法から実装の名指しを落とす** — `NFR-sec-03` の
     「`orchestrator` の単体テストが、起動スクリプトに `unset SLACK_BOT_TOKEN` が含まれることを
     固定している」を「3種のプロセス環境を取得して当該変数が無いことを確認する」だけにし、
     どのテストが在るかは `03-impl/tests/` が持つ /
  B. **残す** — 測定方法は「どうやって測るか」なので、具体を書かないと測れない
- **AI推奨**: **A**。理由: 測定方法に**どのテストが実在するか**を書くと、テストを1本足すたびに
  01 が古くなる(現に `NFR-perf-02` は `03-impl/environments/images.md` と `vnc-base` ステージ名を
  名指しており、ステージ名を変えると 01 が偽になる)。**測るのは観測点であって実装ではない。**
  下流の代償: `03-impl/tests/orchestrator.md` に「この要件を覆うのはこのテスト」の行が要る
  (既に在ることを確認済み)。8件が対象。
  崩れる条件: 観測点だけを書くと**誰も測らなくなる**という運用上の反論(そのときは
  測定方法に「対応表は `03-impl/tests/` が正」と1行だけ残す形にする)。
- **未回答時の既定**: AI推奨のとおりにする
- **根拠(上流)**: `.claude/directions/01-requirements.md`(非機能要件は目標値と測定方法を持つ。
  実装の構造を要求する場所ではない)
- **根拠(同層)**: `docs/01-requirements/non-functional.md:64`(`NFR-ops-04` は既に
  「第2文は測らない。理由 = 観測点が存在しない」と**測らない判断を明示する形**を持っており、
  この書き方が同層の先例である)
- **根拠(下流)**: `docs/03-impl/tests/orchestrator.md`(テスト識別子の一次情報。
  `NFR-sec-03` の文自身が「テスト識別子と実装状況は `03-impl/tests/orchestrator.md` が正」と書いている)
- **間違えたときの戻し方**: 測定方法の文だけを差し戻せばよい。テストもコードも動かない。
- **過去の回答**: なし

**★あなたの記入**:

### 論点 4. 5周をどう回すか(独立レビューの構成)

- **選択肢**:
  A. **層ごとに周を割る** — 1周目 00 / 2周目 01 / 3周目 02 / 4周目 移し先の実在照合 /
     5周目 修正そのものの点検(前タスクと同じ構成) /
  B. **観点ごとに周を割る** — 1周目 コード識別子 / 2周目 実行順序と数値 / 3周目 下位層 ID /
     4周目 用語集との整合 / 5周目 消失していないかの照合 /
  C. **各周で全層を見る** — 5人の独立レビューに毎回全文を読ませ、指摘の重なりで確度を上げる
- **AI推奨**: **A**。理由: 前タスクで実際に回した構成であり、**4周目(移し先の実在照合)が
  1件の消失を捕まえ、5周目が「既存の理由21行が無言で削られていた」という最重要の欠陥を
  捕まえた**実績がある。B は観点の抜けが層の抜けより見つけにくい。C は同じ指摘が5回出て
  裁定コストだけが増える。
  下流の代償: 層ごとなので、**層をまたぐ不整合**(00 から落として 01 に移したものが
  01 の他の条項と衝突する)は4周目まで出てこない。
  崩れる条件: 00 と 01 の件数差が大きく、1周目が軽く3周目が重くなりすぎるとき
  (そのときは 01 を2周に割る)。
- **未回答時の既定**: AI推奨のとおりにする
- **根拠(上流)**: 人間の指示(2026-08-07)「確認→修正を、subagent を用いて5周して」
- **根拠(同層)**: `docs/histories/2026-08-07-stop-session-spawned-containers.md`
  (前タスクの5周の構成と、各周が何を捕まえたか)
- **根拠(下流)**: `.claude/directions/external-agent.md`(不変則1〜7。レビュー役は書かない・
  指摘は証拠であって裁定ではない)
- **間違えたときの戻し方**: 周の割り方はいつでも変えられる。2周目までに層の重みが偏っていたら
  残りの周を割り直す。
- **過去の回答**: なし

**★あなたの記入**:

## 委任にしてよいか確認したい項目

(なし。本タスクの実現方法は標準委任 `DS-01`〜`DS-08` の範囲に収まる。
**規範側の委任は 2026-08-07 に `KIT-where-technology-decisions-belong` で確定済み**)

## 今回 AI が決めたこと(回答不要。開示)

- [DS-04] **`docs/01-requirements/system.md` を影響範囲から外した** — 理由:
  `.claude/directions/01-requirements.md:51` が「`system.md` is exempt — naming technologies is
  its whole purpose」と明記しており、技術を名指すことがこの文書の目的だから /
  見直す条件: 規範から `system.md` の除外が消えたとき
  (記録先: `docs/tasks/task-layer-placement/memo.md` の影響範囲の表)
- [DS-04] **`docs/00-requests/decisions/scope.md` を影響範囲から外した** — 理由:
  `D0-scope-07` が挙げる `flock` 等は**委任範囲の例示**で、2026-08-07 に適用した規範の指針
  (「委任範囲の列挙は開いた形にする」)を満たしているから / 見直す条件: 同指針が変わったとき
  (記録先: 同上)
- [DS-04] **`docs/00-requests/decisions/env.md` の `D0-env-09` は直さない** — 理由: 現物が
  「(`flock` / `mkdir` / `ln -s` / **その他**)」と既に開いた形で書かれており、`086` の指摘が
  現物と合っていないから(独立レビュー D-2 が同じことを指摘した)/ 見直す条件: なし
  (記録先: 同 memo.md の調査メモ)

## 方針合意(個別diff承認は行いません)

- **00**: 決定の**内容欄**からコードの識別子・行番号・実装の手順を落とし、
  **技術の選択そのもの**(`bubbletea` / `QEMU+virtiofs` など)は残す。理由欄は触らない。
  `D0-env-05` 項2 の理由と `D0-env-08` 項8 の理由は、`issues/090`・`091` の裁定どおり書き足す。
- **01**: 下位層 ID・実装ファイル名・内部トポロジ・内部の実行順序を落とし、
  **観測できる言い方**へ書き替える。**条項 ID は1つも動かさない**(CS16)。
  数値は論点2の線(利用者が待つ時間は残す / 内部の試行回数は落とす)で仕分ける。
- **02**: 実装のシンボル名・実行順序・出力文言・03 の実数を落とし、**設計の意図**だけを残す。
  UI 節の表示内容は論点2の裁定により 01 へ戻す。
- **03**: 原則そのまま(移し先として読むだけ)。例外は論点3の受け皿2件。
- 変更指示を書き終えた時点で差分サマリを提示します。個別のdiff承認は取りません。

**★あなたの記入**(異議のある方針だけ書いてください。空欄 = 全方針に合意):

## 記入完了

**★あなたの記入**(記入を終えたら日付を書いてください。例: 2026-08-07):
