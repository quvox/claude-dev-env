# task-relations-code-sync 解決済みの経緯 2

<!-- memo.md から追い出したもの。仕様ドキュメントではない(version / verified を持たない)。
     タスクの削除と一緒に消える(close-task.py)。 -->

## 決定シート(回答済み)

**回答: 2026-08-05 人間が「全部推奨どおり」と回答した。論点1〜6 はすべて推奨案 = A、
委任 a も承認。** 反映先は次のとおり(下の表は提示時の原文をそのまま残す)。

| # | 回答 | どこへ書くか |
|---|---|---|
| 1 | **A**(02 に「必須だが検証はしない」を明記) | `new-features/02-design/contracts/orchestrator-prompt.md` と対応する 03 の契約。**新しい決定は起こさない**(`D0-orch-15` が 2026-08-04 に「強制はツールでは行わない」へ改まっており、その決定を 02 へ書き下すだけ) |
| 2 | **A**(`issue 009` (a) の17件は含めない) | どのドキュメントにも書かない。`03-impl/index.md` の「未解決」欄に残す |
| 3 | **A**(`032` の中・低すべて直す) | closure のとおり |
| 4 | **A**(実装が誤っていたら issue 起票のみ。コードは変えない) | 見つかった時点で `docs/issues/` |
| 5 | **A**(`issue 017` の relations 2行を本タスクで直す) | `MODULE-orchestrator-claude-exec`(目的)/ `MODULE-orchestrator-session`(連携先)。`issue 017` は閉じない |
| 6 | **A**(`issue 056` の8箇所すべて本タスクで直す) | closure に5ファイル追加(下表)。`issue 056` は本タスクで閉じる |
| a | **承認**(コードが正と確認できたものは記述を直してよい) | **新しい委任は起こさない**: 内容とガードレールが既存の **`D0-scope-06`**(委任範囲「旧ドキュメントとコードで表現が食い違う軽微な点はコードを正として記述する」/ 制約「要件・契約・観測可能な振る舞いに関わる食い違いは委任範囲外」/ 報告義務「穴埋めした箇所をタスクの調査メモに残す」)と一致するため、**00 層は変更なしのままでよい**(`docs/00-requests/decisions/scope.md:80`〜`:88` で確認) |

| # | 論点 | 選択肢 | 推奨案(理由・下流の代償・崩れる条件) | 未回答時の既定 | 根拠 |
|---|---|---|---|---|---|
| 1 | **`038` 追加#6(`CTR-orchestrator-prompt` の「必須」の意味)** | **A**: 02 に「**必須だが検証はしない**(欠落は Go のゼロ値になる)」と明記(= 03 とコードが正)/ **B**: 欠落を検証エラーにする(**コード変更**)/ **C**: 「必須」を「期待するフィールド」に言い換える | **A**。`D0-orch-15` は 2026-08-04 に「**スキーマ強制はツールで行わない**」へ改まっており、検証しないことが**決定済みの方針**である。B はその決定に反する。**下流の代償**: 「必須」が「検証されない期待」を意味するので読み手に注意が要る(明記で解消)。**崩れる条件**: 将来スキーマ強制を導入するなら 00 の `D0-orch-15` から改める | **A** | 上流: `D0-orch-15`(★2026-08-04 改め)/ `FR-orch-06` #3 同層: `CTR-cli-container` は環境変数について「受け側は値の不正で起動を止めない」と同型の明記を持つ 下流: `03-impl/contracts/orchestrator-prompt.md` / `MODULE-orchestrator-review` |
| 2 | **`issue 009` (a) の17件(`ctx` 省略の規約)を含めるか** | **A**: 含めない(`/kit-improve` で規約を決めてから別途)/ **B**: 規約なしで17件を機械的に補う / **C**: 規約の策定も本タスクでやる | **A**。規約は**キットの話**で書き込み先が違う(`/kit-improve` は `docs/` を触らない)。**下流の代償**: 17件が残り、`03-impl/index.md` に未解決として書き続ける。**崩れる条件**: 規約が「省略してはならない」に決まると17件は本タスクと同種の作業になる → **その場合は本タスクの着手前に `/kit-improve` を回す**のが得 | **A** | 上流: CLAUDE.md §6 / `docs/issues/009` の対処案 A 同層: `task-impl-depth` も (a) を範囲外にした 下流: `03-impl/index.md` の「コードとの乖離」欄 |
| 3 | **`032` の「低」7件も直すか** | **A**: 中・低すべて直す / **B**: 中10件だけ | **A**。同じファイルを開くので**二度触らない方が安い**。低7件は表記ゆれ・語の精度で判断に迷いが無い。**下流の代償**: 変更行数と照合対象が増える。**崩れる条件**: 低7件のどれかが「実装が誤り」だった場合は issue 起票に回す(記述を実装へ合わせてはならない) | **A** | 上流: CLAUDE.md 原則2 同層: `task-impl-depth` が高5件だけ先に直して中低27件を残し、**その結果が本タスク**である(分けた代償が既に出ている) 下流: `03-impl/index.md` |
| 4 | **実装が誤っていた場合の扱い** | **A**: issue を起票して**記述はコードのまま**にし、コードは変えない / **B**: 軽微ならその場で直す | **A**。本タスクでコードを変えると「何が正だったか」の記録が混ざる。**下流の代償**: 実装の欠陥が残る(issue で追跡)。**崩れる条件**: 明らかな要件違反で修正が数行なら判断を仰ぐ(→ 決定シート) | **A** | 上流: CLAUDE.md 原則2「どちらが正かを勝手に決めない」/ 原則8 同層: `task-impl-depth` は同方針で19件を起票した 下流: `docs/issues/` |

| 5 | **本タスクが書き替える2本の relations に残る測定不能語(`issue 017`)を含めるか** | **A**: `MODULE-orchestrator-claude-exec` の目的「**安全に**起動する」と `MODULE-orchestrator-session` の連携先「`shellSingleQuote` で**安全に**クォートする」の2行だけ本タスクで直す(残りの 017 は3本目)/ **B**: 触らず3本目に全部残す / **C**: `017` の全件(`MODULE-makefile-build-orchestrator` の「素早く」と 01・02 の4箇所も)を本タスクへ取り込む | **A**。`038` #18 は **session の「`shellSingleQuote` でクォートする」という文そのものを書き替える対象**(実体は `respawn-pane` に command をそのまま渡す)なので、同じ文に残る「安全に」を残置すると3本目が同じ行をもう一度開く。置き換え語も上流待ちが無い(「シェルの語分割・展開が起きない形に引用する」= コードから一意に読み取れる)。**下流の代償**: `issue 017` は閉じないので `03-impl/index.md` に残り続ける(3本目が閉じる)。**崩れる条件**: claude-exec の「安全に」が `D0-sec-01`(サンドボックス方針)の言い換えを要するなら 00 起点になり、本タスクの範囲を超える → その場合は A から外して3本目へ | **A** | 上流: `D0-scope-06`(旧記述とコードの食い違いはコードを正とする)/ CLAUDE.md §8「曖昧語の禁止」 同層: 論点3 で「同じファイルを二度触らない方が安い」と同じ判断を既に採っている 下流: `relations-query.py --impact orchestrator/session.go` = 呼び出し元3件(controller / dashboard / main)・要件8件。**いずれも記述の言い換えでは変わらない**。3本目 `task-spec-measurability` の closure から2行が消える |
| 6 | **`docs/issues/056`(SSOT に残る変更相対の言い回し8箇所)を本タスクに含めるか** | **A**: 8箇所すべてを本タスクで直す(closure に 5ファイル追加。うち 02 が2本)/ **B**: 独立したタスクにする / **C**: 現行実装について誤っている2箇所(`MODULE-cli-reset.md:49`・`:94`)だけ本タスクで直し、残り6箇所は `056` に残す | **A**。`docs/feedbacks/015`(部分的に直すと次の検証で必ず再浮上する。**切るなら層ごと切る**)が `038` 自身の教訓であり、同じ性質の残留を3つに割るのは同じ失敗の再演である。`#1`・`#2` は「現行実装は 0 を返す」と**事実に反する**ので原則2 の侵害でもあり、放置の選択肢は無い。**下流の代償**: closure が 5 ファイル増え、**`02-design/system.md` と `02-design/contracts/cli-container.md` の版が上がるので 02 層の合格証を再発行する**(`/doc-check` の検証対象が 02 まで広がる)。さらに `02-design/system.md` は3本目の closure にも入っているので、**先に閉じた本タスクの結果を3本目が読み直す**必要が生じる。**崩れる条件**: 02 の2箇所が「本変更」の指す時点を書き下すのに設計意図の再確認を要するなら 02 起点の判断が混ざるので、その2箇所だけ `056` に残す(= C+`#5`〜`#6`) | **A** | 上流: CLAUDE.md §1「SSOT はいまの姿だけを記述する」/ 原則2 / `D0-scope-06` 同層: `docs/feedbacks/015-partial-fixes-resurface-in-the-next-verification.md`(`038` を生んだ切り方そのもの) 下流: `MODULE-cli-reset` / `-stop` は本タスクの他の行と無関係(**二度触りにはならない**)。`02-design/system.md:328` は3本目が測定不能語で触る行とは別の行 |

### 委任にしてよいか確認したい項目

| # | 論点 | 委任範囲 | 制約(ガードレール) |
|---|---|---|---|
| a | 各食い違いを「記述の誤り」と判定してよい範囲 | `038` / `032` の各行について、コードを読んで**コードが正**と確認できたものは記述を直す | **コードから一意に読み取れる事実だけを書く**(`D0-scope-07` と同じ制約)。要件・契約・観測可能な振る舞いに関わる食い違いは委任範囲外(→ issue または決定シート)。**推測を仕様として書かない** |

### フェーズ1 からの調査メモ

- 2026-08-05 に確認: `02-design/contracts/orchestrator-prompt.md` は「必須」列と「既定値」列を
  **同じ表に並べて持つ**(`:79`〜`:90`。例: `summary` は必須で既定値 `""`)。追加#6 の修正は
  この表に「必須は検証されない期待である」旨を1行足す形になる。`D0-orch-15` は
  「**強制はツールでは行わない**」「**形式の検証が一箇所も無い**という事実は `issue 034` / `038` が
  追跡する」と 2026-08-04 に改まっており(`docs/00-requests/decisions/orch.md:188`〜`:199`)、
  **受け皿は実在する**(`docs/feedbacks/017` の指摘に従って確認した)。
