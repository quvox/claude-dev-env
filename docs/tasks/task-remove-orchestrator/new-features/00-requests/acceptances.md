---
target: docs/00-requests/acceptances.md
change: replace
version_bump: minor
sections:
  - "## 対象外とするシナリオ"
summary: 利用者視点の受け入れ基準 AC-01〜AC-03 と AC-06。前提・操作・期待する結果・不合格の条件で書く
deletes:
  - "## AC-04 オーケストレーターで並列実装し、要判断だけ対応する"
  - "## AC-05 端末が壊れても実行を失わず復旧する"
reason: 'オーケストレーターの全面削除にともなう受け入れ基準の削除(決定シート 概念1・論点1)。(1) `AC-04`(並列実装と要判断)と `AC-05`(端末破壊からの復旧)を削除する — どちらも対応要求が `RQ-orch-01` だけで、その要求ごと消えるため。**`AC-01`〜`AC-03` と `AC-06` の番号は動かさない**(欠番を埋めない。`01-requirements/usecases.md` の AC ⇄ UC カバレッジ表が番号で参照する)。(2) 「対象外とするシナリオ」から 2 行を削除する: 「作業者ごとに隔離された環境で並列実装する」(並列実装そのものが無くなる)と「ブレインストーミングを人間抜きで自動的に完了させる」「完了基準を満たしていない箇所を自動でタスク化して埋める」(参照先の「やらないこと」4・5 が `request.md` から消える)。残るのは「信頼できない第三者のコードを持ち込んで実行する」(やらないこと 2)の 1 行である。**frontmatter の `summary` も本指示のとおり差し替える** — 旧 summary は「AC-01〜AC-06」と範囲で書いており、`AC-04` / `AC-05` を削除すると本文と食い違う(独立レビュー readiness の指摘)'
reflected: 2026-08-10
---

## 対象外とするシナリオ

| シナリオ | 対象外とする理由 |
|---|---|
| 信頼できない第三者のコードを持ち込んで実行する | 「やらないこと」2 に該当する |
