---
target: docs/00-requests/request.md
change: replace
version_bump: minor
sections:
  - "## 背景と目的"
  - "## 対象ユーザー"
  - "## やること"
  - "## やらないこと"
  - "## 完成イメージ"
deletes: []
summary: >
  Claude Code を Docker コンテナで動かす隔離開発環境を、レビュー前コードをホストから隔離し
  チーム全員が同一構成を使えるようにするために提供する。信頼できる社内開発用途に限定。
keywords: [claude-dev, Docker開発環境, 隔離, 配布, CodexCLI]
reason: 'AIオーケストレーターを本システムから全面的に取り除く(決定シート 概念1・論点1・論点2)。(1) 目的3件のうち「各プロジェクトに AIオーケストレーターを1体立てる」を削除し、目的を「隔離」と「配布」の2件にする。(2) 背景から「人間が進行管理(オーケストレーション)を兼ねているために並列度を上げられない」というボトルネックの記述を削除する — 削除後のシステムはこのボトルネックを解こうとしていないため、残すと目的の無い背景になる。(3) やること表から `RQ-orch-01` の行を削除する(これに連なる `FR-orch-01`〜`09` の 9 要件 69 条項も 01 から削除する)。(4) 対象ユーザー表から開発者・保守者双方のオーケストレーター記述を削除する。(5) やらないこと表から 4(ブレインストーミングの自動化)と 5(完了基準の自動タスク化)を削除する — どちらも消える機能に対する除外であり、対象が無くなれば除外そのものが意味を失う。**1〜3 の番号は動かさない**(`acceptances.md` と `SR-05` が番号で参照している。欠番は埋めない)。(6) やらないこと 1 の「worker ごとの隔離」を「コンテナ内で動くプロセスごとの隔離」に言い換える — `worker` は削除する用語であり、`D0-sec-06` と `SR-04` が支える隔離境界の主張そのものは変わらない。(7) 完成イメージから `claude-dev orchestrate` の段落を削除する。frontmatter の `summary` と `keywords` も本指示のとおり差し替える'
reflected: 2026-08-10
---

## 背景と目的

エージェント(Claude Code 等)にコードを書かせ実行させると、レビュー前のコードがホストの
資産(認証情報・ホストのファイルシステム)へ到達しうるという安全上の懸念がある。開発者ごと・
プロジェクトごとに環境がばらつくと、再現性も損なわれる。

目的は次の2つである。

- Claude Code を**隔離された Docker コンテナ**内で動かし、レビュー前コードがホストへ到達する経路を
  作らない。プロジェクトごとにコンテナを分離する。
- 環境一式を **Docker イメージとして配布**し、コマンド一発で立ち上げてチーム全員が同一構成を使える。

## 対象ユーザー

| 呼称 | 誰か | この人の目的 |
|---|---|---|
| 開発者 | 社内の開発者(日本語話者)。本ツールの主たる利用者 | プロジェクトのコンテナを起動して Claude Code で開発する |
| 保守者 | 本ツール自体を保守する社内の開発者 | イメージの配布・更新を行う |

前提として**信頼できる社内開発用途に限定**する(信頼できないコードを扱う用途・本番に近い環境は対象外)。

## やること

| ID | 要求 | 補足 |
|---|---|---|
| RQ-env-01 | ホスト CLI によるコンテナのライフサイクル管理・ポートフォワード・認証・SSH 鍵転送 | Linux は `claude-dev`、macOS は `claude-dev-mac` |
| RQ-env-02 | 開発ツール入りコンテナイメージ(VNC あり/なし)と、noVNC 経由のブラウザ確認 | クライアント PC のブラウザからコンテナ内 Chrome を見る |
| RQ-env-03 | エージェント CLI の同梱(Claude Code と OpenAI Codex CLI)と、認証のホスト共有 | プロジェクト間で再ログインを不要にする |
| RQ-env-04 | ホストの Docker を制限付きで使うための Docker Socket Proxy | 生ソケットを渡さずに `docker` を使えるようにする |
| RQ-env-05 | 重い Docker 案件向けの VM モード(オプトイン) | QEMU+virtiofs、VM 内ネイティブ Docker |
| RQ-env-06 | macOS(Docker Desktop)対応 | OS 依存はホスト CLI に閉じる |
| RQ-dist-01 | イメージの GHCR 配布(マルチアーキ) | チーム全員が pull で同一構成を得る |

## やらないこと

<!-- 番号は一度振ったら動かさない(欠番を埋めない・並べ替えない)。
     4・5 は 2026-08-08 に削除した(対象の機能そのものを廃止したため)。 -->

| # | やらないこと | 理由 |
|---|---|---|
| 1 | **コンテナ内で動くプロセスごとの個別隔離**(microVM 等によるプロセス単位の隔離) | 本構成は単一コンテナ同居型とし、隔離境界はコンテナ/ホスト間にのみ置く。個別隔離が要る用途は別構成(Docker Sandboxes 等)で検討する |
| 2 | **信頼できないコード・本番に近い環境での利用** | 信頼できる社内開発用途に限定するという前提を置いたうえで、隔離の強度を決めているため |
| 3 | ホストの Docker 生ソケットをコンテナへ直接マウントする運用 | ホスト掌握のリスクがあり、原則採用しない |

## 完成イメージ

- 開発者は任意のプロジェクトディレクトリで `claude-dev start` と打つ。プロジェクト専用のコンテナが
  立ち上がり、カレントディレクトリが `/workspace` に見え、認証もファイアウォールも設定済みで、
  tmux の中で `claude` や `codex` がすぐ動く。ホストの秘密鍵ファイルも Docker 生ソケットも
  コンテナには入っていない。
- Web アプリを作ったら `claude-dev forward 5173` で必要なときだけポートを開き、クライアント PC の
  ブラウザで確認する。コンテナ内の Chrome を noVNC 越しに見ることもできる。
- チームの他のメンバーは、ビルドせずに GHCR から pull するだけで同じ環境を得る。
