---
target: docs/01-requirements/usecases.md
change: replace
version_bump: minor
sections:
  - "## シナリオ外要件"
  - "## AC ⇄ UC カバレッジ"
summary: ユースケース UC-01〜UC-03 と UC-06。受け入れ基準を基本フロー・代替フロー・例外フローへ形式化したもの
deletes:
  - "## UC-04 並列実装し、要判断だけ対応する"
  - "## UC-05 端末が壊れても復旧する"
reason: 'オーケストレーターの全面削除にともなうユースケースの削除(決定シート 概念1)。(1) `UC-04`(並列実装し、要判断だけ対応する)と `UC-05`(端末が壊れても復旧する)を削除する — 形式化した受け入れ基準がそれぞれ `AC-04` / `AC-05` で、どちらも `acceptances.md` から消える。関連要件もすべて `FR-orch-*` / `NFR-avail-01` である。**`UC-01`〜`UC-03` と `UC-06` の番号は動かさない**(`02-design/system.md` の E2E シナリオ一覧が番号で参照する)。(2) シナリオ外要件の表から `FR-orch-09`(自己検証)の行を削除する — 要件ごと消える。`FR-env-09`(イメージ配布)の行と非機能要件の行は残すが、非機能要件の行から廃止した 4 件(`NFR-perf-03` / `NFR-avail-01` / `NFR-sec-03` / `NFR-ops-04`)を外す。(3) AC ⇄ UC カバレッジ表から `AC-04` / `AC-05` の行を削除する(6 行 → 4 行)。**新設する `NFR-ops-05`(利用者へ向けた文が日本語であること)も非機能要件なのでこの行に加える**。**frontmatter の `source` は変えない**(`acceptances.md` と `functional.md` はどちらも残る)。**frontmatter の `summary` も本指示のとおり差し替える** — 旧 summary は「UC-01〜UC-06」と範囲で書いており、`UC-04` / `UC-05` を削除すると本文と食い違う(独立レビュー readiness の指摘)'
reflected: 2026-08-10
---

## シナリオ外要件

| 要件 ID | どの UC にも現れない理由 |
|---|---|
| FR-env-09(イメージ配布) | CI がイメージをビルドして GHCR へ公開する処理であり、利用者の操作フローの中に現れない(利用者側の取得操作 `claude-dev pull` は任意で、UC-01 の事前条件「初回セットアップ」を満たす手段の1つに過ぎない)。確認は CI 側の成果物検証で行う |
| NFR-perf-01〜02 / NFR-avail-02〜03 / NFR-ops-02〜03・05 / NFR-scale-01〜02 | 非機能要件であり、特定のユーザ旅程を持たない |

## AC ⇄ UC カバレッジ

| AC ID | 形式化した UC | 未対応の理由 |
|---|---|---|
| AC-01 | UC-01 | - |
| AC-02 | UC-02 | - |
| AC-03 | UC-03 | - |
| AC-06 | UC-06 | - |
