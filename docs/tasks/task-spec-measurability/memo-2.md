# task-spec-measurability memo-2(2026-08-05 フェーズ2 の引き渡しで追い出したもの)

<!-- `.claude/directions/task-memo.md` §2.1「決定シート(回答済み)は、答えたフェーズではなく
     **次の**フェーズが追い出す」に従って `/task-doc` §5 が移した。
     移してよい理由: 回答は `decisions/`(D0-scope-06 / D0-env-06 / D0-orch-07)と
     `new-features/` 26 ファイルへ反映済みで、運用に効く結論は memo.md の
     「やること・やらないこと」と「影響範囲(closure)」が持っているから。
     **要約ではなく節ごと逐語で移している。** -->

## 決定シート(回答済み: `sheet.md`(転記済み))

**2026-08-05 に人間が回答した。**以下が転記(`/clear` を挟んでも失われないように)。
`check-sheet.py task-spec-measurability` は **OK**(概念の明確化 6 行すべて記入済み)。

### 回答の転記

| # | 節 | 回答 | AI 推奨との異同 |
|---|---|---|---|
| 概念1 | 「ホストにポートが公開されている」 | **推奨の通り**(Web アプリ用ポートは含む / noVNC の 6080 番台は含まない) | 一致 |
| 概念2 | 「ブロック対象ドメイン」 | **`NFR-sec-02` を撤回・削除する。ブロックは不要** | **★覆された**(AI は案D=定義して測定可能化を推奨) |
| 概念3 | 「軽微な」(`D0-scope-06`) | **推奨の通り**(程度語をやめ対象で限る) | 一致 |
| 概念4 | 「必要な文脈だけ」 | **推奨の通り**(`DSN-prompt-03` の5項目を要件で列挙) | 一致 |
| 概念5 | 「安全な Docker コンテナ」 | **枕詞は不要。**そのうえで「安全」を定義する:「**ホストのあらゆる情報を破壊しないこと、鍵情報が直接漏洩しないこと(ただし claude や codex のログイントークンのような一過性のものは除く)**」 | **★覆された**(AI は「今回はやらない」を推奨) |
| 概念6 | `NFR-ops-01` の不可分/段階可 | **`NFR-ops-01` は廃止する** | **★覆された**(AI は不可分で残すことを推奨) |
| 論点1〜5 | (空欄) | **未回答時の既定を適用**: 1=B / 2=D / 3=A / 4=A′ / 5=A。ただし**概念2・6 が論点1・2 の前提を消した**ので下の「既定の読み替え」を参照 | — |
| 委任 a・b・c | (空欄) | **提案どおり承認**(既定は「委任にする」) | — |

### 既定の読み替え(概念2・6 の回答が論点1・2 の前提を消したため)

| 論点 | 既定 | 読み替え後 |
|---|---|---|
| 1(`043` の5件) | **B**(中3件を測定可能化、低2件は「測らない」) | `NFR-ops-01` は**削除**されるので対象外。**測定可能化は `NFR-perf-02` / `NFR-sec-03` の2件**、「測らない」明記は `NFR-ops-04` / `NFR-scale-02` の2件 |
| 2(`041` の閉じ方) | **D**(01 で定義し `NFR-sec-02` の目標値を書き替える) | `NFR-sec-02` は**削除**されるので目標値は消える。**定義の置き場は `FR-env-05` 受入基準4・6 側**へ移る(`FR-env-05` は残すという回答による)。案D の趣旨(集合の中身を仕様で固定しない)はそのまま |
| 3・4・5 | A / A′ / A | 変更なし |

### 廃止の理由(原則11 の学習。`docs/feedbacks/021-…` に記録済み)

**「その品質特性自体を本システムでは追わない」。** AI が「測る / 測らない」の2択しか
選択肢に並べておらず、「**追わないと決めて要件を削除する**」を欠いていた。

### 削除の波及(実測。回答の前に測った結果)

- **`NFR-sec-02`**: 参照 8 箇所。`FR-env-05` が残るので**ファイアウォール実装の要件の裏付けは消えない**。
- **`NFR-ops-01`**: 参照 20 箇所以上(PLAN 5件・MODULE frontmatter 5本)。ただし
  `relations-query.py --requirement NFR-ops-01` の 5 機能は**全て他の FR も根拠に持つ**ので、
  **「それだけが根拠の機能」は 1 つも無い**。唯一「それだけが根拠」なのは
  `02-design/logging.md:97`・`:98`(追記型ログの `dispatch` / `result`)の2行である(未決点#1)。

### 根拠(上流/同層/下流)

**人間が書き込むのは `sheet.md` の「★人間の記入」列だけ**。以下は各行の**根拠の所在**であり、
`sheet.md` から参照される。

`sheet.md` の構成: 概念の明確化 6 件 / 論点 5 件 / 委任の確認 3 件。

### 概念の明確化(6件)の根拠

| # | 種別 | 語 | 上流 | 同層 | 下流 |
|---|---|---|---|---|---|
| 1 | 語の外延 | 「ホストにポートが公開されている」(`AC-02`) | `D0-env-02`(「`start` の時点では Web アプリ用のポートマッピングを行わない(**ブラウザ確認ありの noVNC ポートを除く**)」)/ `D0-env-01`(noVNC は 6080 番台の動的割当) | `AC-01` の期待結果が既に「起動時に noVNC の URL が表示される」と書いている | `FR-env-06`(ポートフォワード)/ `FR-env-11`(ブラウザ確認)。`docs/03-impl/tests/` に `AC-02` を検査する行は**無い**(実測) |
| 2 | 語の外延 | 「ブロック対象ドメイン」(`NFR-sec-02` / `FR-env-05` #4・#6) | `D0-sec-04` の委任範囲は**表現方法・解決タイミング・チェイン**だけで、集合の中身を含まない(`docs/issues/041` が本文を引用して確認済み) | `02-design/system.md:58` `MOD-firewall`「ブラックリスト型ファイアウォールの構成」/ `contracts/entrypoint-firewall.md` | `--upstream MODULE-firewall-init --depth 3` = `MODULE-entrypoint-claude` / `MODULE-cli-start` / `MODULE-cli-orchestrate` の3件。`03-impl/tests/firewall.md:30` は `NFR-sec-02` を E2E-01・未検証で保持 |
| 3 | 語の外延 | 「軽微な」(`D0-scope-06` の見出しと委任範囲。`decisions/scope.md:80`・`:84`) | CLAUDE.md 原則4(委任範囲の内か外かで自律判断の可否が決まる)/ `.claude/directions/00-requests.md`(00 への意味のある変更は人間の合意) | `D0-scope-07` は委任範囲を「`03-impl/relations/` と `contracts/` だけ」と**対象ファイルで**限っており、程度語を使っていない先例 | 全タスクの下降が `D0-scope-06` を根拠に自律判断する。`docs/issues/044` は「`D0-scope-06` の委任は軽微な曖昧さに限られる」を理由に手続き逸脱と判定した実例 |
| 4 | 語の外延 | 「必要な文脈だけ」(`FR-orch-02` 受入基準3) | `NFR-perf-03`(第2文は 2026-08-04 に要件から外し `DSN-prompt-03` へ降ろした) | **`DSN-prompt-03` が既に5項目を列挙している**(plan のゴール・完了条件 / 当該タスクの説明と完了条件 / 完了した依存タスクの結果要約 / 前回試行のフィードバック。上限は課さない) | `MODULE-orchestrator-worker`(`Worker.BuildPrompt`)/ `03-impl/tests/orchestrator.md` |
| 5 | 範囲の境界 | 「安全な Docker コンテナ」(`request.md:7` の目的文) | `.claude/directions/00-requests.md`(00 は人間のもの)/ CLAUDE.md §8(層を限定せず曖昧語を禁じる) | `NFR-sec-01` が同じ内容を4項目の測定可能な形で既に書いている | `docs/issues/017` が「目的文の枕詞であり受け入れ基準ではないので**最も優先度が低い**」と明記。下流の受入基準は 1 件も参照していない |
| 6 | 不可分/段階可 | `NFR-ops-01` の3事象(資源逼迫・レート制限・通知) | `.claude/directions/01-requirements.md`(既定は**不可分**)/ `docs/issues/043`「`NFR-ops-01` は受入基準が存在しない」 | `NFR-sec-01` は「上記4項目すべてが常に成立(例外なし)」と不可分で書いた先例 | `02-design/system.md:179` は `NFR-ops-01` を MOD-orchestrator / MOD-hooks / MOD-container-tools / **MOD-vm-mode** の4モジュールへ割り当て済み。`03-impl/tests/orchestrator.md:89` は E2E-04・未検証 |

### 論点(5件)の根拠

| # | 論点 | 選択肢 | 推奨案(理由・下流の代償・崩れる条件) | 未回答時の既定 | 根拠 |
|---|---|---|---|---|---|
| 1 | **`043` の5件をどこまで測定可能にするか** | **A**: 5件すべてに目標値と測定方法を書く / **B**: 中3件(`NFR-perf-02` / `sec-03` / `ops-01`)を測定可能にし、低2件(`ops-04` / `scale-02`)は「**測らない(理由)**」と明記する / **C**: 全件「測らない」と明記する | **B**。`ops-04`(運用状態を機械だけが読み書きする)と `scale-02`(分岐が増えない)は**性質上の期待であって測る対象ではない**(比較基準の版を決めないと一意に測れない = issue 043 自身がそう書いている)。**下流の代償**: 「測らない」と書いた2件は将来の劣化を検知できない。加えて `tests/orchestrator.md:90` と `tests/cli-login-codex.md:29` の2行を「E2E・未検証」から「測らない」へ書き替える必要がある。**崩れる条件**: `scale-02` を測る需要が出たら「CLI サブコマンド数の上限」など別の指標を立てる必要がある | **B** | 上流: CLAUDE.md §8(数値・条件・具体例を書く)/ `docs/issues/043` の重大度付け(低2件) 同層: `NFR-perf-01` は目標値を持つ先例 下流: `03-impl/tests/` の NFR 15 行。**実測: 15 行すべてが「未検証(テスト未実装)」**で、`DSN-test-01`(自動テストランナーを設けずシェル系は実機確認)により**測定方法は E2E の実機確認手順として書く**ことになる |
| 2 | **`041`(ブロック対象ドメイン)の閉じ方** | **D**(`task-fix-start-cleanup` で追記した案): 01 で「**設定で与えられるドメイン集合**(既定は同梱の20件)」と定義し、`NFR-sec-02` の目標値を「設定に列挙したドメインへ到達できないこと」にし、既定20件は 03 に列挙 / **A**: `D0-sec-04` の委任範囲に「集合の中身」を足す / **B**: 00 に決定として集合を起こす / **C**: 許可リストへ変える(**コード変更**) | **D**。実装は `scripts/init-firewall-claude.sh:37`「**ここにブロックしたいドメインを追加してください**」+ 既定20件という**利用者が編集する前提のテンプレート**なので、集合の中身を仕様で固定するのは実態に合わない。D なら「どのドメインを塞ぐべきか」という**人間の裁定が不要**になり、かつ測定可能になる。**下流の代償**: 「既定が妥当か」のレビューは別途必要(issue に残す)。**崩れる条件**: 監査要件などで「塞ぐ集合を仕様で固定せよ」と外部から求められた場合は B | **D** | 上流: `D0-sec-04`(委任は**表現方法**だけ)/ `NFR-sec-02` 同層: `02-design/system.md:58` は既に「ブラックリスト型」と書いている 下流: `MODULE-firewall-init` / `tests/firewall.md` |
| 3 | **`042`(`AC-02` と noVNC)の閉じ方** | **A**: `AC-02` に適用範囲を明記(「**利用者のアプリケーションが待ち受けるポート**は公開されない。noVNC 等の補助 UI はこの限りではない」)/ **B**: `AC-02` をブラウザ確認なしの構成に限定 / **C**: noVNC も `forward` 経由にする(**コード変更**) | **A**。実装は `claude-dev:695` で `USE_VNC=1` が**既定**、`:846`〜`:848` で必ず `-p <6080番台>:6080` を付ける。振る舞いを変えずに記述の齟齬だけ消える。**下流の代償**: 「補助 UI のポートは公開してよい」という例外が受入基準に入る。**崩れる条件**: noVNC を公開しない運用が要件になったら C | **A** | 上流: `D0-env-01`(noVNC は 6080 番台の動的割当)/ `D0-env-02`(ポートは必要なときだけ開く) 同層: `AC-01` は既に「起動時に noVNC の URL が表示される」と書いている 下流: `FR-env-04` / `tests/` に該当検査は無い |
| 4 | **`044`(用語「資源逼迫」)の下降** | **A′**(2026-08-04 に人間が裁定済み): 承認する。ただし 00 の定義から環境変数名(`VM_HEALTH_*`)を落とし「既定値。上書き手段は 03-impl」とする。数値の一次情報は `MODULE-vm-mode-healthd` に置く | **A′ をそのまま実施**(裁定済みなので論点ではなく作業)。**下流の代償**: 既定値を変えるときに 00 の MINOR 更新が要る(用語の意味が変わるので妥当)。**崩れる条件**: 閾値が環境ごとに大きく違う運用になったら、00 は定性的な定義に留めて 02 に表を置く形へ再検討 | **A′** | 上流: `.claude/directions/00-requests.md:52`(定義が揺れる語を残すな)/ `docs/issues/044` の「経緯」 同層: 他の用語も定義に数値を含む 下流: `FR-env-08` #4 / 02 の5箇所 / `issue 017` の7箇所 |
| 5 | **`017` のうち 02・03 に残る語(`高速更新` / `必要な範囲を超えて出さない` / `RAM 逼迫` ×2 / `安全に`)の閉じ方** | **A**: すべて実装の条件・既存の定義へ置換する / **B**: 03 の散文は対象外とし 02 だけ直す / **C**: 全部残して issue を開いたままにする | **A**。`高速更新` は `MODULE-makefile-update-claude` の実装(イメージを作り直さずキャッシュを使う)から一意に書き替えられ、`RAM 逼迫` は `terminology.md` の `資源逼迫` と**別概念**なので実装(`vm-healthd.sh` は CPU 使用率しか見ない)に合わせて書き替えれば消える。**下流の代償**: `logging.md:69` の「必要な範囲を超えて出さない」だけは**マスク規則を新しく決める**ことになり、決めた規則が実装(ログ出力箇所)と食い違えば原則2 違反になる。**崩れる条件**: マスク規則が実装と一致しないと分かったら、その1件は issue に残してコード修正タスクへ回す | **A** | 上流: CLAUDE.md §8(**すべての層に適用**)/ `D0-scope-07` のガードレール 同層: `task-impl-depth` が `軽微` ×3 と `意味のあるまとまり` を実装の条件へ置換した先例 下流: `02-design/relations.md:92` → `MODULE-makefile-update-claude.md:14` → `features.md:76` → `relations/index.md:63` の4段(**生成物を含む**)。`logging.md:69` は `NFR-sec-01` 直結 |

**★論点5 の前提は 2026-08-05 に更新した**: 旧版は「2本目のタスクで同じファイルを開くので、
そこで直すのが最も安い」を推奨理由にしていたが、**2本目が想定した relations 3行は現行 SSOT に
存在しなかった**(`docs/issues/017` の 2026-08-05 追記)。よって理由は成立しない。
また旧版が挙げた `素早く` `安全に` ×2(03 側)も**実測で 0 件**である。

### 委任にしてよいか確認したい項目

| # | 論点 | 委任範囲 | 制約(ガードレール) | 根拠 |
|---|---|---|---|---|
| a | 測定方法の文面 | 各 NFR の「測定方法」列に書くコマンド・観測点・判定条件の具体的な書き方 | **既存の目標値を厳しくしない**(測れる形にするだけ)。**実行できるコマンドだけを書く**(`02-design/environments.md` の実コマンドと一致させる)。測れないものは「測らない(理由)」と明示する。**新しい E2E シナリオを増やさない**(増やすなら `02-design/system.md` の E2E 一覧が起点になるので決定シートへ戻す) | 上流: `D0-scope-07`(読み取れないものは書かず issue へ)/ 同層: `NFR-perf-01` の測定方法の書式 / 下流: `03-impl/tests/` の NFR 15 行(全件「未検証(テスト未実装)」) |
| b | `017` の語の置換文面 | どの語をどの条件に置き換えるか(`高速更新` / `RAM 逼迫` / `安全に`(`decisions/env.md:106`)/ `安全な操作`(`usecases.md:98`)) | **観測可能な振る舞いを変えない**。コードから一意に読み取れる条件だけを書く(読み取れないものは issue へ)。用語集にある語は用語集の定義を使う。**`logging.md:69` のマスク規則は委任に含めない**(論点5 で扱う) | 上流: CLAUDE.md §8 / 同層: `task-impl-depth` の置換先例 / 下流: `features.md` と `relations/index.md`(生成物。`build-index.py` で再生成する) |
| c | 削除する issue への参照の付け替え | 本タスクが解消する `017`/`041`/`042`/`043`/`044`/`049` を参照している**検証履歴コメント**(`terminology.md:18`,`:27` / `non-functional.md:23`,`:25` / `functional.md:41`,`:42` / `decisions/sec.md:29` / `contracts/entrypoint-firewall.md:21`)の書き替え | **コメント(検証履歴)だけを触り、本文・決定・委任は触らない**。付け替え先は `docs/histories/` の当該エントリとし、**新しく `docs/issues/NNN` 形式のパスを増やさない**(`docs/issues/054` が問題にしている形) | 上流: `docs/issues/054` の対処案C / 同層: `decisions/orch.md` が 2026-08-05 に同型の付け替えを行った先例(`034`/`038` → `058`/`059`)/ 下流: 該当なし(コメントを参照する文書は無い) |

