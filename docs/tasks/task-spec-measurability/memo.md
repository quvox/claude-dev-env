---
id: task-spec-measurability
phase: 決定
origin_layer: 00
issue: docs/issues/043-modify-nfr-targets-do-not-measure-whole-requirement.md
date: 2026-08-04
updated: 2026-08-04
source:
  - docs/00-requests/terminology.md
  - docs/00-requests/acceptances.md
  - docs/01-requirements/functional.md
  - docs/01-requirements/non-functional.md
  - docs/02-design/architecture.md
  - docs/02-design/system.md
  - docs/02-design/logging.md
  - docs/02-design/relations.md
  - docs/03-impl/relations/MODULE-firewall-init.md
  - docs/03-impl/relations/MODULE-vm-mode-healthd.md
  - docs/03-impl/index.md
summary: 仕様を測定可能にする(issue 043 の NFR 5件 + 017 の測定不能語 + 041 のブロック対象 + 042 の AC-02 + 044 の用語定義)
---

<!-- タスクの背骨。フェーズ1で作られフェーズ4で削除されるまで、このファイルだけで
     どのフェーズからでも再開できること(/clear を挟んでも)。
     ・仕様ドキュメントではない(version / verified を持たない)
     ・未決点はここに置く。仕様ドキュメントには絶対に書かない
     ・削除は /task-close の機械ゲート経由(close-task.py)。rm 禁止 -->

# task-spec-measurability 仕様を「測れる」形にする

> 解決済みの経緯: (まだ無し)

## 実行順(★重要)

**3本連続タスクの3本目。着手は `task-relations-code-sync`(2本目)を閉じた後。**
重なりは 1本目と `01-requirements/functional.md` / `03-impl/index.md`、2本目と `03-impl/index.md`。

**本タスクが最後である理由**: 1本目・2本目がコードと 03 を動かすので、**測定方法を先に固定すると
書き直しになる**(とくに `NFR-sec-02` の測定方法は1本目の `logout` / `reset` の挙動に依存する)。

## 目的

**「形は整っているが測れない」記述を全部閉じる。** `/doc-check` が繰り返し検出してきた同型の欠落で、
5つの issue が同じ性質を持つ。

| issue | 内容 | severity |
|---|---|---|
| `043` | **NFR 5件**(`NFR-perf-02` / `sec-03` / `ops-01` / `ops-04` / `scale-02`)で、「要件」列が述べる内容の一部しか「目標値」「測定方法」列が測っていない | 中 |
| `017` | 測定不能語が **28 箇所**(`安全に` / `素早く` / `高速更新` / `RAM 逼迫` / `資源逼迫` 7箇所 ほか) | 低 |
| `041` | `NFR-sec-02` と `FR-env-05` #4・#6 が参照する「**ブロック対象ドメイン**」の集合が 00・01 のどこにも無い | 中 |
| `042` | `AC-02`「`start` しただけではポートが公開されていない」が **既定構成(`USE_VNC=1`)で成立しない** | 中 |
| `044` | `terminology.md` 1.1.0 の「資源逼迫」の閾値定義が**承認済み(案A′)だが下降していない** | 中 |

起点層は **00**(`terminology.md` / `acceptances.md` / `decisions/sec.md` が起点になる項目を含む)。

## やること・やらないこと

| 種別 | 内容 |
|---|---|
| やること | `044` の裁定(**案A′ = 承認 + 00 から環境変数名を落とす**)を下降させ、`FR-env-08` #4 と 02 の5箇所を用語集参照へ揃える / `041` を**案D**(01 で「設定で与えられる集合」と定義し、既定20件を 03 に列挙)で閉じる / `042` を**案A**(`AC-02` に適用範囲を明記)で閉じる / `043` の5件の目標値・測定方法を要件本文の全体に合わせる(論点1)/ `017` の残る測定不能語を実装の条件へ置換 / `terminology.md` に合格証を発行できる状態にする |
| やらないこと | **コードの変更**(すべて記述の精密化。`041` で許可リストへ変える案 C は採らない)/ `NFR` の目標値を**新しく厳しくすること**(現状を測れるようにするだけ)/ `docs/issues/006`(E2E 手順の再現性)と `P-003`(QA レーン)— 別件 |

## 影響範囲(closure)

| 層 | SSOT のパス | 変更指示のパス | 変更の種類 |
|---|---|---|---|
| 00 | docs/00-requests/terminology.md | new-features/00-requests/terminology.md | replace(**★起点**。`044` の案A′: 定義から環境変数名を落とし「既定値。上書き手段は 03-impl」とする) |
| 00 | docs/00-requests/acceptances.md | new-features/00-requests/acceptances.md | replace(**★起点**。`042` の案A: `AC-02` に「利用者のアプリケーションが待ち受けるポート」の限定を明記) |
| 00 | docs/00-requests/decisions/sec.md | - | **★下降中に判定**(`041` 案D で `D0-sec-04` の委任範囲に触る必要が出るか。**案D は 01 の語の定義なので触らない見込み**) |
| 01 | docs/01-requirements/functional.md | new-features/01-requirements/functional.md | replace(`FR-env-08` #4 を用語集参照へ / `FR-env-05` #4・#6 の「ブロック対象ドメイン」を定義付きへ) |
| 01 | docs/01-requirements/non-functional.md | new-features/01-requirements/non-functional.md | replace(`043` の5件 + `NFR-sec-02` の目標値を「設定に列挙したドメインへ到達できないこと」へ) |
| 01 | docs/01-requirements/usecases.md | - | **★下降中に判定**(`017` に UC の測定不能語が1件挙がっている) |
| 02 | docs/02-design/architecture.md | new-features/02-design/architecture.md | replace(「資源逼迫」の参照を用語集へ揃える) |
| 02 | docs/02-design/system.md | new-features/02-design/system.md | replace(同上 + `MOD-firewall` の記述) |
| 02 | docs/02-design/logging.md | new-features/02-design/logging.md | replace(`:88` の「検知した指標と閾値」を用語集の定義で確定 + `:100`) |
| 02 | docs/02-design/relations.md | new-features/02-design/relations.md | replace(`PLAN-vm-mode-healthd` の概要の語) |
| 03 | docs/03-impl/relations/MODULE-firewall-init.md | new-features/… | replace(`041` 案D: 既定20件をカテゴリとともに列挙) |
| 03 | docs/03-impl/relations/MODULE-vm-mode-healthd.md | new-features/… | replace(`017`: `RAM 逼迫` を定義のある語へ) |
| 03 | docs/03-impl/relations/MODULE-makefile-update-claude.md / -build-orchestrator.md / MODULE-orchestrator-claude-exec.md / -session.md | - | **★下降中に判定**(`017` の `高速更新` / `素早く` / `安全に`。**2本目が同じファイルを触る可能性**があるので、着手時に残件を確認する) |
| 03 | docs/03-impl/index.md | new-features/03-impl/index.md | replace(「02 との差分」「01 との差異」から解消分を外す) |
| 03 | docs/03-impl/tests/*.md | - | **★下降中に判定**(受入基準の意味を精密化するだけなら行は増えない。増えるなら該当 tests を closure へ) |

## 決定シート(提示中 — 未回答)

| # | 論点 | 選択肢 | 推奨案(理由・下流の代償・崩れる条件) | 未回答時の既定 | 根拠 |
|---|---|---|---|---|---|
| 1 | **`043` の5件をどこまで測定可能にするか** | **A**: 5件すべてに目標値と測定方法を書く / **B**: 中3件(`NFR-perf-02` / `sec-03` / `ops-01`)を測定可能にし、低2件(`ops-04` / `scale-02`)は「**測らない(理由)**」と明記する / **C**: 全件「測らない」と明記する | **B**。`ops-04`(運用状態を機械だけが読み書きする)と `scale-02`(分岐が増えない)は**性質上の期待であって測る対象ではない**(比較基準の版を決めないと一意に測れない = issue 043 自身がそう書いている)。**下流の代償**: 「測らない」と書いた2件は将来の劣化を検知できない。**崩れる条件**: `scale-02` を測る需要が出たら「CLI サブコマンド数の上限」など別の指標を立てる必要がある | **B** | 上流: CLAUDE.md §8(数値・条件・具体例を書く)/ `docs/issues/043` の重大度付け(低2件) 同層: `NFR-perf-01` は目標値を持つ先例 下流: `03-impl/tests/` の NFR 15 行 |
| 2 | **`041`(ブロック対象ドメイン)の閉じ方** | **D**(`task-fix-start-cleanup` で追記した案): 01 で「**設定で与えられるドメイン集合**(既定は同梱の20件)」と定義し、`NFR-sec-02` の目標値を「設定に列挙したドメインへ到達できないこと」にし、既定20件は 03 に列挙 / **A**: `D0-sec-04` の委任範囲に「集合の中身」を足す / **B**: 00 に決定として集合を起こす / **C**: 許可リストへ変える(**コード変更**) | **D**。実装は `scripts/init-firewall-claude.sh:37`「**ここにブロックしたいドメインを追加してください**」+ 既定20件という**利用者が編集する前提のテンプレート**なので、集合の中身を仕様で固定するのは実態に合わない。D なら「どのドメインを塞ぐべきか」という**人間の裁定が不要**になり、かつ測定可能になる。**下流の代償**: 「既定が妥当か」のレビューは別途必要(issue に残す)。**崩れる条件**: 監査要件などで「塞ぐ集合を仕様で固定せよ」と外部から求められた場合は B | **D** | 上流: `D0-sec-04`(委任は**表現方法**だけ)/ `NFR-sec-02` 同層: `02-design/system.md:58` は既に「ブラックリスト型」と書いている 下流: `MODULE-firewall-init` / `tests/firewall.md` |
| 3 | **`042`(`AC-02` と noVNC)の閉じ方** | **A**: `AC-02` に適用範囲を明記(「**利用者のアプリケーションが待ち受けるポート**は公開されない。noVNC 等の補助 UI はこの限りではない」)/ **B**: `AC-02` をブラウザ確認なしの構成に限定 / **C**: noVNC も `forward` 経由にする(**コード変更**) | **A**。実装は `claude-dev:695` で `USE_VNC=1` が**既定**、`:846`〜`:848` で必ず `-p <6080番台>:6080` を付ける。振る舞いを変えずに記述の齟齬だけ消える。**下流の代償**: 「補助 UI のポートは公開してよい」という例外が受入基準に入る。**崩れる条件**: noVNC を公開しない運用が要件になったら C | **A** | 上流: `D0-env-01`(noVNC は 6080 番台の動的割当)/ `D0-env-02`(ポートは必要なときだけ開く) 同層: `AC-01` は既に「起動時に noVNC の URL が表示される」と書いている 下流: `FR-env-04` / `tests/` に該当検査は無い |
| 4 | **`044`(用語「資源逼迫」)の下降** | **A′**(2026-08-04 に人間が裁定済み): 承認する。ただし 00 の定義から環境変数名(`VM_HEALTH_*`)を落とし「既定値。上書き手段は 03-impl」とする。数値の一次情報は `MODULE-vm-mode-healthd` に置く | **A′ をそのまま実施**(裁定済みなので論点ではなく作業)。**下流の代償**: 既定値を変えるときに 00 の MINOR 更新が要る(用語の意味が変わるので妥当)。**崩れる条件**: 閾値が環境ごとに大きく違う運用になったら、00 は定性的な定義に留めて 02 に表を置く形へ再検討 | **A′** | 上流: `.claude/directions/00-requests.md:52`(定義が揺れる語を残すな)/ `docs/issues/044` の「経緯」 同層: 他の用語も定義に数値を含む 下流: `FR-env-08` #4 / 02 の5箇所 / `issue 017` の7箇所 |
| 5 | **`017` の残り(`安全に` ×2 / `素早く` / `高速更新` / `RAM 逼迫` ×2)** | **A**: すべて実装の条件へ置換する(例: `安全に` → 「タイムアウト30秒・失敗時は非0」)/ **B**: 03 の散文は対象外とし、01・02 だけ直す / **C**: 全部残して issue を開いたままにする | **A**。**2本目のタスクで同じファイルを開く**ので、そこで直すのが最も安いが、2本目は「コードへ追随」に範囲を絞っているため語の精度は本タスクが持つ。**下流の代償**: 03 の文が長くなる。**崩れる条件**: 置換に**新しい判断**(「安全」の定義)が必要になった場合は、その1件だけ決定シートへ回す | **A** | 上流: CLAUDE.md §8(**すべての層に適用**)/ `D0-scope-07` のガードレール 同層: `task-impl-depth` が `軽微` ×3 と `意味のあるまとまり` を実装の条件へ置換した先例 下流: `03-impl/index.md` の集計 |

### 委任にしてよいか確認したい項目

| # | 論点 | 委任範囲 | 制約(ガードレール) |
|---|---|---|---|
| a | 測定方法の文面 | 各 NFR の「測定方法」列に書くコマンド・観測点・判定条件の具体的な書き方 | **既存の目標値を厳しくしない**(測れる形にするだけ)。**実行できるコマンドだけを書く**(`02-design/environments.md` の実コマンドと一致させる)。測れないものは「測らない(理由)」と明示する |
| b | `017` の語の置換文面 | どの語をどの条件に置き換えるか | **観測可能な振る舞いを変えない**。コードから一意に読み取れる条件だけを書く(読み取れないものは issue へ)。用語集にある語は用語集の定義を使う |

## 未決点

| # | 未決点 | 帰着 | 検出元 |
|---|---|---|---|
| (フェーズ2 の実装ドライランで埋める) | | | |

## 調査メモ

- `044` の裁定は済んでいる(2026-08-04、案A′)。残作業は `docs/issues/044` の「経緯」に
  (a)〜(e) として列挙済み。閾値は `scripts/vm-healthd.sh:28`〜`:30`(既定 15 / 60 / 12)。
- `041` の実装: `scripts/init-firewall-claude.sh:38`〜`:64` に既定20件
  (paste 系9 / webhook テスト系3 / トンネル系4 + 本番環境の雛形はコメントアウト)。
  `MODULE-firewall-init.md:61` は「設定は環境変数ではなくスクリプト内配列 `BLACKLIST_DOMAINS` を編集する」と記述済み。
- `042` の実装: `claude-dev:695`(`USE_VNC=1` が既定)/ `:846`〜`:848`(`-p <6080番台>:6080`)/
  `:929`〜`:934`(競合時は別ポートで最大20回再試行)。**`tests/` に `AC-02` を検査する行は無い**。
- `043` の5件は `docs/issues/043` の表が「要件が述べていること / 測っていること / 測られていない部分」の
  3列で整理済み。
- `017` は 28 行の表で、各行に `path:line` と該当語が入っている。**「資源逼迫」7箇所は `044` の
  下降で測定不能語ではなくなる**(同 issue に明記済み)。

## 質問キュー(未提示)

| # | 質問 | 前提 | いつ聞くか |
|---|---|---|---|
| (なし) | | | |

## タスクリスト

- [ ] 0. **着手時**: 1本目・2本目の反映後の SSOT に対して `017` / `043` の残件を再確認し closure を確定する
- [ ] 1. `/task-doc task-spec-measurability`(00→03 の1回の下降) _Depends:_ 0, 決定シートの回答
- [ ] 2. `/doc-check task-spec-measurability` が PASS _Depends:_ 1
- [ ] 3. `/implement task-spec-measurability`(**コードは変えない**。記述のみ) _Depends:_ 2
- [ ] 4. `/task-close task-spec-measurability` _Depends:_ 3

## Definition of Done

- [ ] (フェーズ2/3 が具体化する。最低限: `017` / `041` / `042` / `043` / `044` を削除できる状態 / **`terminology.md` に合格証が発行される**(版を持つ全ドキュメントが認証済みになる)/ コード差分が空)

## 進捗メモ

- 2026-08-04 フェーズ1。`issue 043` を起点に `017` / `041` / `042` / `044` を同時対象として宣言。
  3本連続タスクの3本目。決定シート5論点 + 委任2件を提示。
  **`044` は既に裁定済み(案A′)なので論点ではなく作業として扱う。**

## 申し送り事項

- **本タスクの完了で「版を持つ仕様ドキュメント全件が認証済み」になる**(現在は
  `terminology.md` 1件だけが `044` の下降待ちで未認証)。
- `041` の「既定20件が妥当か」のセキュリティレビューは本タスクの範囲外。
  測れる形にした後、必要なら別 issue として起票する。
- **2026-08-10 19:52 以降に `/doc-check full`** を新しいセッションで1回(前タスクからの申し送り)。
  本タスクまで終わっていれば、そこが**全層そろった状態でのマイルストーン監査**になる。
