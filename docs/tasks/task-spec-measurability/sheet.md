# task-spec-measurability 決定シート

**記入方法**: 一番右の「★人間の記入」列だけを埋めてください。AI はこの列を書き換えません。
**記入列にパイプ文字(`|`)は書かないでください**(表が壊れます)。
**「概念の明確化」の節は空欄を残せません。** 既定のままでよい場合は `既定` と書いてください
(何も書かないと実装フェーズへ進めません)。以降の節は空欄なら「未回答時の既定」の扱いです。

**根拠の所在**: `docs/tasks/task-spec-measurability/memo.md`
(「決定シート(回答待ち)」節に上流/同層/下流の三方向を行ごとに書いています)。
このシートだけで判断できないときはそちらをご覧ください。

## 概念の明確化(00/01。上から答えてください。最大7行。空欄禁止)

| # | 種別 | いま曖昧な語・箇所 | 明確にしたいこと | AI推奨(例で示す) | 未回答時の既定 | 波及 | ★人間の記入 |
|---|---|---|---|---|---|---|---|
| 1 | 語の外延 | 「ホストにポートが公開されている」(`acceptances.md` の `AC-02` の期待結果と不合格条件) | `AC-02` が禁じている「公開」に何が含まれ、何が含まれないか | 含む=利用者の Web アプリが待ち受けるポート(`forward` で開くもの)・`docker-proxy` のポート / 含まない=**noVNC の 6080 番台**(`D0-env-01` の動的割当)。理由: `D0-env-02` が既に「ブラウザ確認ありの noVNC ポートを除く」と例外を書いており、`AC-02` だけが例外を落としている。既定構成は `USE_VNC=1` なので現状の文面は literally 成立しない。崩れる条件: noVNC も公開しない運用が要件になったら、`D0-env-01` から変える別タスクになる | 委任(制約: `D0-env-02` の例外の文言をそのまま `AC-02` へ写し、振る舞いは変えない) | `AC-01` / `AC-02` / `D0-env-01` / `D0-env-02` / `FR-env-06` / `FR-env-11`。issue `042` と `049` が閉じる | 推奨の通り |
| 2 | 語の外延 | 「ブロック対象ドメイン」(`NFR-sec-02` の目標値、`FR-env-05` 受入基準4・6) | 仕様がこの語で指す集合は何か。中身を仕様で固定するのか、設定で与えられるものとするのか | **設定で与えられるドメイン集合**と定義する。含む=`init-firewall-claude.sh` の `BLACKLIST_DOMAINS` 配列に列挙されたドメイン(既定は paste 系9・webhook テスト系3・トンネル系4 の計16件)/ 含まない=本番環境の雛形2件(コメントアウトされており適用されない)・メタデータ IP・SMTP ポート(別のルールで塞ぐ)。理由: 実装は「ここにブロックしたいドメインを追加してください」という**利用者が編集する前提のテンプレート**であり、中身を仕様で固定すると実態と食い違う。この定義なら `NFR-sec-02` の目標値を「**設定に列挙したドメイン**へ到達できないこと」と書けて測定可能になる。崩れる条件: 監査要件などで「塞ぐ集合を仕様で固定せよ」と外部から求められたら、00 に決定として起こす必要がある | 委任(制約: 集合の中身は仕様で固定しない。既定の内訳は 03 の `MODULE-firewall-init` にだけ列挙する) | `NFR-sec-02` / `FR-env-05` #4・#6 / `D0-sec-04` / `MODULE-firewall-init` とその上流3モジュール / `tests/firewall.md`。issue `041` が閉じる | NFR-sec-02を撤回・削除する。ブロックは不要。 |
| 3 | 語の外延 | 「軽微な」(`decisions/scope.md` の `D0-scope-06` の見出しと委任範囲。2箇所) | AI が自律的に穴埋めしてよい「軽微な曖昧さ」の外延。程度語のままにするか、対象で限るか | 程度語をやめ、**対象で限る**。含む=`03-impl/relations/` と `03-impl/contracts/` の散文表現の食い違い / 含まない=受入基準・契約の項目や型・観測可能な振る舞い・00/01/02 の本文(現行のガードレールが既に除外しているもの)。理由: `D0-scope-07` は同じ委任を**対象ファイルで**限っており先例がある。程度語のままだと実行ごとに解釈が揺れる。崩れる条件: 「対象ファイルでは限れないが軽微な穴埋めが要る」場面が出たら、その都度決定シートへ回す運用になり委任の価値が下がる | 委任(制約: 委任範囲を**広げない**。現行のガードレール3件の意味を変えない) | 以後の全タスクの下降が根拠にする委任。issue `017` の2箇所が閉じる | 推奨の通り |
| 4 | 語の外延 | 「必要な文脈だけ」(`functional.md` の `FR-orch-02` 受入基準3) | worker へ渡す文脈に何が含まれ、何が含まれないか。要件で列挙するのか設計へ委ねるのか | **要件で列挙する**。含む=plan のゴールと完了条件・当該タスクの説明と完了条件・完了した依存タスクの**結果要約**・前回試行のフィードバック / 含まない=リポジトリ全体・依存タスクの成果物そのもの・文字数やトークン数の上限(上限は課さない)。理由: `DSN-prompt-03` が既にこの5項目を設計判断として持っており、要件は同じ内容を測定可能な形で書けば足りる(発明は不要)。崩れる条件: 設計が渡す情報を増減させると要件も同時に直すことになる(そのときは 01 が起点) | 委任(制約: `DSN-prompt-03` の列挙と1項目も違えない。上限値は課さない) | `FR-orch-02` / `NFR-perf-03` / `DSN-prompt-03` / `MODULE-orchestrator-worker`。issue `017` の1箇所が閉じる | 推奨の通り |
| 5 | 範囲の境界 | 「安全な Docker コンテナ」(`request.md:7` の目的文) | 00 の目的文の枕詞まで今回直すか、今回はやらないと名指すか | **今回はやらない**と名指す。理由: 目的文であって受け入れ基準ではなく、下流の受入基準はここを1件も参照していない。同じ内容は `NFR-sec-01` が4項目の測定可能な形で既に持っている。目的文の書き換えは request.md の MAJOR/MINOR 更新を招き、全層の合格証を失効させる代償のほうが大きい。崩れる条件: 「禁止語ゼロ」を CI の機械検査にする(`docs/issues/017` の案B)なら、この1箇所も直す必要が出る | 委任(制約: 今回は変更しない。`docs/issues/017` を閉じるときに「目的文1件は意図的に残した」と理由付きで記録する) | `request.md` の版と、それを `source` に持つ全層の合格証 | 枕詞は不要。安全とは、ホストのあらゆる情報を破壊しないこと、鍵情報が直接漏洩しないこと（ただし、claudeやcodexのログイントークンのような一過性のものは除く）である。 |
| 6 | 不可分/段階可 | `NFR-ops-01` の3事象(資源逼迫・レート制限・通知)を利用者が把握できること | 3事象のうち一部だけ把握できる状態を合格としてよいか(既定は不可分) | **不可分**(既定どおり)。3事象すべてに観測点を書く。理由: `NFR-sec-01` が「上記4項目すべてが常に成立(例外なし)」と不可分で書いた先例があり、`02-design/system.md` は既に `NFR-ops-01` を4モジュール(orchestrator / hooks / container-tools / vm-mode)へ割り当てているので観測点は実在する。崩れる条件: レート制限の観測点が実装に無いと分かったら、その1事象だけ「未実装」として issue に切り出し、要件は段階可へ落とす | 委任(制約: 観測点が実装に無い事象が出たら、勝手に要件から外さず issue を起票して決定シートへ戻す) | `NFR-ops-01` / `FR-env-08` #4 / `tests/orchestrator.md:89`。issue `043` の1件が閉じる | NFR-ops-01は廃止する |

### 次に問う候補(回答不要。今回載せなかったもの)

- 「安全に」(`decisions/env.md:106` の理由文)/「安全な操作」(`usecases.md:98` の事後条件)/
  「高速更新」/「RAM 逼迫」/「必要な範囲を超えて出さない」

## 論点(概念以外。従来の決定シート)

| # | 論点 | 選択肢 | 推奨案(理由・崩れる条件) | 未回答時の既定 | 根拠(上流/同層/下流) | ★人間の記入 |
|---|---|---|---|---|---|---|
| 1 | **`docs/issues/043` の NFR 5件をどこまで測定可能にするか** | **A**: 5件すべてに目標値と測定方法を書く / **B**: 中3件(`NFR-perf-02` / `NFR-sec-03` / `NFR-ops-01`)を測定可能にし、低2件(`NFR-ops-04` / `NFR-scale-02`)は「**測らない(理由)**」と明記する / **C**: 全件「測らない」と明記する | **B**。`ops-04`(運用状態を機械だけが読み書きする)と `scale-02`(分岐が増えない)は性質上の期待であって測る対象ではない(比較基準の版を決めないと一意に測れない、と issue 043 自身が書いている)。下流の代償=「測らない」と書いた2件は将来の劣化を検知できず、`tests/orchestrator.md:90` と `tests/cli-login-codex.md:29` の2行を「E2E・未検証」から「測らない」へ書き替える。崩れる条件=`scale-02` を測る需要が出たら「CLI サブコマンド数の上限」など別の指標を立て直す必要がある | **B** | 上流: CLAUDE.md §8(数値・条件・具体例)/ `docs/issues/043` が低と付けた2件 / 同層: `NFR-perf-01` は目標値と測定方法を持つ先例 / 下流: `03-impl/tests/` の NFR 15 行(実測で**全件が「未検証(テスト未実装)」**。`DSN-test-01` により測定方法は実機確認手順として書く) | |
| 2 | **`docs/issues/041`(ブロック対象ドメイン)の閉じ方** | **D**: 01 で「設定で与えられるドメイン集合」と定義し、`NFR-sec-02` の目標値を「設定に列挙したドメインへ到達できないこと」にし、既定の内訳は 03 へ列挙 / **A**: `D0-sec-04` の委任範囲に「集合の中身」を足す / **B**: 00 に決定として集合を起こす / **C**: 許可リストへ変える(**コード変更**) | **D**(概念シート #2 の回答がそのまま案になる)。実装は「ここにブロックしたいドメインを追加してください」+ 既定リストという利用者が編集する前提のテンプレートなので、集合の中身を仕様で固定するのは実態に合わない。D なら「どのドメインを塞ぐべきか」の人間の裁定が不要で、かつ測定可能になる。下流の代償=「既定の16件が妥当か」のセキュリティレビューは別途必要(issue に残す)。崩れる条件=監査要件などで「塞ぐ集合を仕様で固定せよ」と外部から求められたら B | **D** | 上流: `D0-sec-04` の委任範囲は表現方法・解決タイミング・チェインだけ(集合の中身を含まない)/ 同層: `02-design/system.md:58` が既に「ブラックリスト型」と書いている / 下流: `MODULE-firewall-init` とその上流3件(`--upstream` で実測)、`tests/firewall.md:30` | |
| 3 | **`docs/issues/042` と `049`(`AC-02` と noVNC)の閉じ方** | **A**: `AC-02` の期待結果と不合格条件を「**利用者の Web アプリ用のポート**」に限定し、noVNC は `D0-env-02` の例外である旨を添える / **B**: `AC-02` をブラウザ確認なしの構成に限定する / **C**: noVNC も `forward` 経由にする(**コード変更**) | **A**(概念シート #1 の回答がそのまま案になる)。実装は `claude-dev:695` で `USE_VNC=1` が既定、`:846`〜`:848` で必ず `-p <6080番台>:6080` を付ける。振る舞いを変えずに記述の齟齬だけ消える。下流の代償=「補助 UI のポートは公開してよい」という例外が受入基準に入る。崩れる条件=noVNC を公開しない運用が要件になったら C(`D0-env-01` の変更を伴う別タスク) | **A** | 上流: `D0-env-01`(noVNC は 6080 番台の動的割当)/ `D0-env-02`(例外を明記済み)/ 同層: `AC-01` が既に「起動時に noVNC の URL が表示される」と書いている / 下流: `FR-env-06` / `FR-env-11`。`03-impl/tests/` に `AC-02` を検査する行は**無い**(実測) | |
| 4 | **`docs/issues/044`(用語「資源逼迫」)の下降** | **A′**(2026-08-04 に人間が裁定済み): 承認する。ただし 00 の定義から環境変数名(`VM_HEALTH_*`)を落とし「既定値。上書き手段は 03-impl」とする。数値の一次情報は `MODULE-vm-mode-healthd` に置く | **A′ をそのまま実施**(裁定済みなので論点ではなく作業。**この行は追認の確認だけです**)。下流の代償=既定値を変えるときに 00 の MINOR 更新が要る(用語の意味が変わるので妥当)。崩れる条件=閾値が環境ごとに大きく違う運用になったら、00 は定性的な定義に留めて 02 に表を置く形へ再検討 | **A′** | 上流: `.claude/directions/00-requests.md:52`(定義が揺れる語を残すな)/ `docs/issues/044` の「経緯」/ 同層: 他の用語も定義に数値を含む / 下流: `FR-env-08` #4・`architecture.md:87`・`system.md:61`・`logging.md:102`,`:129`・`relations.md:99` の6箇所(実測。`non-functional.md:67` は下降済み) | |
| 5 | **`docs/issues/017` のうち 02・03 に残る語の閉じ方** | **A**: すべて実装の条件・既存の定義へ置換する / **B**: 03 の散文は対象外とし 02 だけ直す / **C**: 全部残して issue を開いたままにする | **A**。`高速更新` は実装(イメージを作り直さずキャッシュを使う)から一意に書き替えられ、`RAM 逼迫` は `terminology.md` の `資源逼迫` とは別概念なので実装(`vm-healthd.sh` は CPU 使用率しか見ない)に合わせれば消える。下流の代償=`logging.md:69` の「必要な範囲を超えて出さない」だけは**マスク規則を新しく決める**ことになり、決めた規則が実装のログ出力と食い違えば CLAUDE.md 原則2 違反になる。崩れる条件=マスク規則が実装と一致しないと分かったら、その1件だけ issue に残してコード修正タスクへ回す | **A** | 上流: CLAUDE.md §8(全層に適用)/ `D0-scope-07` のガードレール / 同層: `task-impl-depth` が「軽微」3件と「意味のあるまとまり」を実装の条件へ置換した先例 / 下流: `02-design/relations.md:92` → `MODULE-makefile-update-claude.md:14` → `features.md:76` → `relations/index.md:63` の4段(生成物を含む)。`logging.md:69` は `NFR-sec-01` 直結 | |

| 6 | **`/doc-check task-spec-measurability`(2026-08-05)で独立レンズが1本も立たなかった。** Codex がアカウントの利用上限で `audit_failed(unavailable)`(復旧 2026-08-11 12:56)。`/task-doc` のときに頂いた代替の承認は**その実行限り**で持ち越さない規約なので、改めてお伺いする | **A**: サブエージェントで代替する / **B**: 代替せず Claude 単独の検証で実装フェーズへ進む / **C**: 2026-08-11 の Codex 復旧を待って `/doc-check` をやり直してから `/implement` へ進む | **B**。理由: 本タスクは**コード差分が空**で、変更は記述の精密化と2要件の削除の下降に限られる。機械検査(CS1〜CS10・`check-relations`・`check-contracts`・`callgraph-check`・`build-index`)がすべて通っており、今回の「高」1件も**機械的に判定できる種類**(節の最終形の欠落)だった。加えて `/task-doc` の実行では既にサブエージェントのレンズが6件を挙げ、うち5件が修正済みである。**崩れる条件**: `/implement` が記述以外(コード)に触ることになったら、その時点で C へ切り替える | **B(代替しない)** | 上流: CLAUDE.md 不変則7(代替は人間の裁定。既定は代替しない)/ 同層: `/codex-audit` の fail ポリシー(`task-<slug>` は警告して続行)/ 下流: 2026-08-10 以降に予定している `/doc-check full` が全層そろった状態でのマイルストーン監査になる(申し送り事項) | |

## 委任にしてよいか確認したい項目

| # | 論点 | 委任範囲 | 制約(ガードレール) | 根拠 | ★人間の記入 |
|---|---|---|---|---|---|
| a | 測定方法の文面 | 各 NFR の「測定方法」列に書くコマンド・観測点・判定条件の具体的な書き方 | 既存の目標値を**厳しくしない**(測れる形にするだけ)。**実行できるコマンドだけを書く**(`02-design/environments.md` の実コマンドと一致させる)。測れないものは「測らない(理由)」と明示する。**新しい E2E シナリオを増やさない**(増やすなら `02-design/system.md` の E2E 一覧が起点なので決定シートへ戻す) | 上流: `D0-scope-07`(読み取れないものは書かず issue へ)/ 同層: `NFR-perf-01` の測定方法の書式 / 下流: `03-impl/tests/` の NFR 15 行 | |
| b | `docs/issues/017` の語の置換文面 | どの語をどの条件へ置き換えるか(`高速更新` / `RAM 逼迫` / `安全に`(`decisions/env.md:106`)/ `安全な操作`(`usecases.md:98`)) | **観測可能な振る舞いを変えない**。コードから一意に読み取れる条件だけを書く(読み取れないものは issue へ)。用語集にある語は用語集の定義を使う。**`logging.md:69` のマスク規則は委任に含めない**(論点5 で扱う) | 上流: CLAUDE.md §8 / 同層: `task-impl-depth` の置換先例 / 下流: `features.md` と `relations/index.md`(生成物。`build-index.py` で再生成) | |
| c | 削除する issue への参照の付け替え | 本タスクが解消する `017`/`041`/`042`/`043`/`044`/`049` を参照している**検証履歴コメント**8箇所(`terminology.md:18`,`:27` / `non-functional.md:23`,`:25` / `functional.md:41`,`:42` / `decisions/sec.md:29` / `contracts/entrypoint-firewall.md:21`) | **コメント(検証履歴)だけを触り、本文・決定・委任は触らない**。付け替え先は `docs/histories/` の当該エントリとし、**新しく `docs/issues/NNN` 形式のパスを増やさない**(`docs/issues/054` が問題にしている形) | 上流: `docs/issues/054` の対処案C / 同層: `decisions/orch.md` が 2026-08-05 に同型の付け替えを行った先例 / 下流: 該当なし(コメントを参照する文書は無い) | |

## 方針合意(個別diff承認は行いません)

- **00-requests**: `terminology.md`(資源逼迫の定義から環境変数名を落とす)/ `acceptances.md`(`AC-02` を Web アプリ用ポートに限定)/ `decisions/scope.md`(`D0-scope-06` の「軽微な」を対象で限る)/ `decisions/env.md`(`:106` の「安全に」)/ `decisions/sec.md`(検証履歴コメントのみ)/ `request.md`(概念 #5 が「今回はやらない」なら変更なし)。
- **01-requirements**: `functional.md`(`FR-env-05` #4・#6 / `FR-env-08` #4 / `FR-orch-02` #3)/ `non-functional.md`(`NFR-sec-02` の目標値と `docs/issues/043` の5件)/ `usecases.md`(`:98` の事後条件)。`system.md` は変更なし。
- **02-design**: `architecture.md` / `system.md` / `logging.md` / `relations.md` の語を用語集参照と実装の条件へ揃える。`contracts/entrypoint-firewall.md` は検証履歴コメントのみ。`environments.md` は変更なし。
- **03-impl**: `MODULE-firewall-init`(既定リストの内訳を列挙)/ `MODULE-vm-mode-healthd`(`RAM 逼迫`)/ `MODULE-makefile-update-claude`(`高速更新`)/ `features.md`(概要列の追随)/ `index.md`(層の合格証)/ `tests/` 4本(論点1 次第)。`relations/index.md` は `build-index.py` の生成物。
- **コードは1行も変えない**(このタスクは記述の精密化のみ。コード差分が空であることを DoD に入れます)。
- 変更指示を書き終えた時点で差分サマリを提示します。個別のdiff承認は取りません。
