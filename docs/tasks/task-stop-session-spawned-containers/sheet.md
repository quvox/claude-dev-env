# task-stop-session-spawned-containers 決定シート

**根拠の所在**: `docs/tasks/task-stop-session-spawned-containers/memo.md`
(「調査メモ」に事実、「影響範囲(closure)」に触る文書の一覧があります)

**記入方法**: 各ブロックの末尾にある **★あなたの記入** の後ろに書いてください。
それ以外は書き換えなくて構いません(あなたが ★あなたの記入 に書いた内容を AI が
書き換えることはありません。**唯一の例外**: 新しい論点を追記したとき、末尾「記入完了」の
日付だけは AI が消します — 再記入をお願いします)。

**「概念の明確化」は空欄を残せません。** 各ブロックには **AI推奨** と **未回答時の既定** という
**別々の案**が載っています。AI推奨に従う場合は `推奨どおり`、未回答時の既定に従う場合は `既定` と
書いてください(何も書かないと次のフェーズ(変更指示の作成)へ進めません)。
以降の節は空欄なら「未回答時の既定」の扱いです。

## 概念の明確化(00/01。上から答えてください。最大7件。空欄禁止)

### 1. 語の外延 —「セッションのコンテナ内から起動された Docker コンテナ」

- **場所**: `docs/00-requests/decisions/env.md` の `D0-env-05` 項2 /
  `docs/01-requirements/functional.md` の条項 `FR-env-01-6`
- **明確にしたいこと**: 今回 `stop` が片付ける集合は何を含み、何を含まないか。
  現在の文言は「compose で作られたコンテナ群」で、`docker run` で作られたものが入っていません。
- **AI推奨**:
  **含む** = (a) セッションのコンテナ内から `docker compose up` で作られたコンテナ(現状どおり)/
  (b) セッションのコンテナ内から `docker run` `docker create` で作られたコンテナ(**今回追加**。
  起動していない `created` 状態のものも含む) /
  **含まない** = (c) 別セッションのコンテナ内から作られたコンテナ /
  (d) 利用者がホスト側で直接作ったコンテナ /
  (e) VM モードのゲスト内で作られたコンテナ(ホストの Docker に現れないため識別も削除もできない) /
  (f) **この変更より前に作られたコンテナ**(所有者を示す印を持たないので識別できない)。
  理由: 利用者から見た区別は「このセッションの中から作ったかどうか」だけで、
  compose を経由したかどうかは実装の都合である。
  崩れる条件: 利用者が「セッションをまたいで生かしておきたいコンテナ」を
  コンテナ内から作る運用をしている場合(その場合は (b) を含めない選択になる)。
- **未回答時の既定**: 委任(制約: 含む集合を (a)(b) に限り、(c)〜(f) は含めない。
  含めない理由を `docs/03-impl/relations/MODULE-cli-stop.md` の「既知の制限」に書く)
- **波及**: `FR-env-01`(条項追加)/ `FR-env-07`(条項追加)/ `D0-env-05` / `D0-env-08` /
  `CTR-cli-container` / `CTR-docker-api` / `MODULE-cli-stop` / `MODULE-docker-proxy-serve`
- **過去の回答**: `docs/histories/2026-07-19-stop-compose-teardown.md`(2026-07-19。同じ利用者要望で
  compose 分だけを片付け対象に入れた。**当時 `docker run` 分を対象外にした理由は記録されていない** —
  当時は識別する手段が無かった)

**★あなたの記入**:含む

### 2. 語の外延 —「終了する」(ご要望の文言)

- **場所**: ご要望「そのコンテナ内で起動していた docker コンテナ群が終了しない」/
  `docs/01-requirements/functional.md` の条項 `FR-env-01-6`(現在は compose 分を「削除」と書いている)
- **明確にしたいこと**: 「終了」が **停止(`docker stop`。コンテナは残る)** なのか
  **削除(`docker rm -f`。書き込み層ごと消える)** なのか。
- **AI推奨**: **削除(`docker rm -f`)**。
  理由: 既に対象に入っている compose 分が削除であり(`claude-dev:1685`〜`:1687`)、
  同じ集合の中で停止と削除が混ざると `stop` の結果が説明できなくなる。
  停止だけだと孤児コンテナが名前を占有し続け、次の `start` で同名衝突を起こす。
  **下流の代償**: `docker run`(`--rm` 無し)で作ったコンテナの書き込み層が失われる。
  **名前付きボリュームは削除しない**ので、ボリュームに置いたデータは残る
  (`D0-env-05` 項2 の「名前付きボリュームは保持する」を維持)。
  崩れる条件: 利用者がコンテナの書き込み層(ボリュームではない場所)にデータを置いて
  セッションをまたいで使っている場合。
- **未回答時の既定**: 委任(制約: 削除は `docker rm -f` に限り、名前付きボリュームとイメージは
  削除しない。削除したコンテナ名を標準出力へ列挙する)
- **波及**: `FR-env-01`(条項)/ `D0-env-05` 項2 / `MODULE-cli-stop` / `docs/02-design/logging.md`
- **過去の回答**: `docs/histories/2026-07-19-stop-compose-teardown.md`(片付け範囲は人間確認により
  「`docker compose down` 相当」= コンテナ削除 + compose 既定ネットワーク削除、
  名前付きボリュームは非破壊のため保持)

**★あなたの記入**:削除

### 3. 範囲の境界 —「片付ける資源の種類」

- **場所**: `docs/00-requests/decisions/env.md` の `D0-env-05` 項2 /
  `docs/02-design/contracts/cli-container.md`「識別の手段は資源ごとに違う」
- **明確にしたいこと**: コンテナだけを対象にするのか、セッション内から作られた
  **ネットワーク**(`docker network create`)と**名前付きボリューム**(`docker volume create`)も
  対象にするのか。
- **AI推奨**: **コンテナだけ**(+ 現状どおり compose 既定ネットワーク)。
  理由: 名前付きボリュームは `D0-env-05` 項2 が「保持する」と決めており、
  データを失う破壊的操作になる。ネットワークは残っても利用者の作業を壊さず、ディスクも食わない。
  **下流の代償**: セッション内から `docker network create` した独立ネットワークは残る
  (`docker network prune` で利用者が片付ける)。この残りを「既知の限界」として
  `MODULE-cli-stop.md` に書く。
  崩れる条件: 残ったネットワークがサブネットを食い潰し、次の `start` や compose が
  アドレス枯渇で失敗する運用が実際に起きた場合。
- **未回答時の既定**: 委任(制約: 削除対象はコンテナと compose 既定ネットワークまで。
  名前付きボリューム・イメージ・独立ネットワークは削除しない)
- **波及**: `D0-env-05` 項2 / `FR-env-01`(条項)/ `MODULE-cli-stop` /
  `docs/03-impl/infra/local/docker-resources.md`
- **過去の回答**: `D0-env-05` 項2(名前付きボリュームは保持する)/
  `docs/histories/2026-07-19-stop-compose-teardown.md`(却下案「停止時に名前付きボリュームも消す」=
  破壊的で利用者のデータを失う)

**★あなたの記入**:コンテナとネットワーク。

### 4. 範囲の境界 —「この片付けを行うコマンド」

- **場所**: `docs/00-requests/decisions/env.md` の `D0-env-08`(破壊的操作は
  `stop` / `logout` / `reset` の3つ)
- **明確にしたいこと**: 今回の片付けを `stop` だけに入れるか、`reset`(初期状態へ戻す)
  にも入れるか、`logout` にも入れるか。
- **AI推奨**: **`stop` と `reset` の2つ**(`logout` には入れない)。
  理由: `reset` は「初期状態へ戻す」と説明しており、セッション由来のコンテナが残ると
  その約束と正面から食い違う(`MOD-cli-reset` の責務)。一方 `logout` は認証を消す操作で、
  管理ラベルを持つ Claude コンテナを止めるのはその副作用にすぎず、
  対象を広げると「ログアウトしたら実行中のコンテナ群が消えた」という驚きになる。
  **下流の代償**: `MODULE-cli-reset.md` と `docs/03-impl/tests/cli-reset.md` が影響範囲に入り、
  変更指示とドライランの対象が2機能から3機能に増える。
  崩れる条件: `logout` の後に `start` し直す運用で、セッション由来のコンテナが
  古い認証を掴んだまま動き続けることが実害になる場合。
- **未回答時の既定**: 委任(制約: `stop` にのみ入れる。`reset` / `logout` は現状どおりとし、
  `reset` に入れなかったことを `docs/issues/` へ起票する)
- **波及**: `MODULE-cli-stop` / `MODULE-cli-reset` / `FR-env-01`(条項)/ `CTR-cli-container`
  「削除対象の決め方(3つの規則)」
- **過去の回答**: `D0-env-08` 項2(共有資源を止めてよい条件は3コマンド共通で例外は無い)/
  `D0-env-08` 項4(`logout` と `reset` の非対称を承知の上で残す、という先例がある)

**★あなたの記入**:stopとreset

### 5. 主体と権限 —「所有者の印は誰が付けるか」

- **場所**: `docs/00-requests/decisions/env.md` の `D0-env-08` 項7 と `D0-env-10` /
  `docs/02-design/contracts/cli-container.md`「識別の手段は資源ごとに違う」
- **明確にしたいこと**: 現在の 00 は「コンテナ内の `docker compose` が作る資源には本システムの
  ラベルを**付けられない**(ホスト CLI の `--label` が届かない)」と書いています。
  **docker-proxy はその要求を通過させる位置にいるので、実は付けられます**
  (`docker-proxy/main.go:158`〜`:254` が既に bind を書き換えている)。
  docker-proxy が、利用者が作るコンテナへ所有者ラベルを付けてよいか。
- **AI推奨**: **付けてよい**。
  ただし付けるのは**所有者を表す1つのラベルだけ**で、要求の他の部分は変えない。
  理由: 「本システムが作った資源」は今まで「ホスト CLI が作ったもの」と同義だったが、
  docker-proxy を通る要求は本システムの中を通っており、所有者は本システムが知っている
  (`lookupProjectDir`。`docker-proxy/main.go:70`〜`:126`)。
  ラベルを付けられるなら、`D0-env-08` 項7 が compose 名の一意化という迂回策を選んだ前提
  (「印を足せないなら名前を一意にするしかない」)が変わる。
  **下流の代償**: 利用者が `docker inspect` したときに身に覚えのないラベルが1つ増える。
  `docker-proxy` の検査対象に「書き換え」が1種類増え、単体テストも増える。
  崩れる条件: 利用者のツールがコンテナのラベル集合を厳密一致で検査している場合。
- **未回答時の既定**: 要確認(この論点に依存する作業=本タスクの中核を今回の範囲から外す)
- **波及**: `D0-env-08` 項7 / `D0-env-10`(委任のガードレール)/ `CTR-cli-container`「管理ラベル」 /
  `CTR-docker-api`「検査する要素と判定」/ `MODULE-docker-proxy-serve` / `FR-env-07`(条項)
- **過去の回答**: `D0-env-10`(委任。ラベルの名前・値の形式・**付与する資源の種類**を AI に委ねている。
  ガードレールは「既存コンテナを集合削除の対象にしない」「共有資源の停止判定をラベルに依存させない」
  「`com.docker.*` を使わない」の3つ)/ `DSN-env-01`(固定名の資源にラベルを足すと移行問題を作るだけ、
  という却下理由。**今回の対象は固定名を持たないので、この却下理由は当たらない**)

**★あなたの記入**:つけて良い

### 6. 失敗時に観測される振る舞い —「印を付けられなかったコンテナ」

- **場所**: `docs/01-requirements/functional.md` の条項 `FR-env-07-8`(解釈できないボディは中継する)/
  `docs/issues/005-modify-docker-proxy-relays-unparseable-bodies.md`
- **明確にしたいこと**: 所有者ラベルを付けられない要求が2種類あります。
  (a) ボディが JSON として解釈できない要求(`docker-proxy/main.go:493`〜`:496` が中継する)、
  (b) この変更より前に作られた既存コンテナ。
  このとき利用者に何が見えるか。
- **AI推奨**: **どちらも「片付けの対象外」とし、`stop` は何も表示しない**。
  理由: (a) も (b) も**そのコンテナが存在することを本システムが知る手段が無い**ので、
  「残っているかもしれません」という当てのない警告しか出せず、毎回出る警告は読まれなくなる。
  代わりに `MODULE-cli-stop.md` と `MODULE-docker-proxy-serve.md` の「既知の制限」に事実を書く。
  **下流の代償**: 利用者は残ったコンテナに `docker ps` で自力で気づく必要がある。
  崩れる条件: (a) が実運用で頻発する(= 解釈できないボディを出すツールが常用されている)場合。
  そのときは `FR-env-07-8` を fail-closed へ倒す別タスクになる。
- **未回答時の既定**: 委任(制約: 対象外とし、表示は行わない。事実を 03 の「既知の制限」に書き、
  `docs/issues/005` の関連として記録する)
- **波及**: `FR-env-07`(条項)/ `MODULE-docker-proxy-serve`「既知の制限」/
  `MODULE-cli-stop`「既知の制限」/ `docs/issues/005`
- **過去の回答**: `docs/issues/005`(解釈できないボディの中継は `D0-sec-05` の委任範囲内と裁定済み。
  残存リスクは issue で追跡する)/ `D0-env-08` 項7(印を持たない旧い compose 資源は
  「対象外にして手動の片付け方を**表示する**」— **表示できたのは名前規則で引けたからで、
  今回の (a)(b) は引く手段が無い**という違いがある)

**★あなたの記入**:推奨で良い

## 次に問う候補(回答不要。今回載せなかったもの)

- 「所有者ラベルの名前」/「ラベルのキャッシュ TTL」/「`docker ps` の表示」/「並行 `stop` の順序」

## 論点(概念以外。従来の決定シート)

### 論点 1. 「このセッションが作った」を実現する方式

- **選択肢**:
  A. **docker-proxy がコンテナ作成要求へ所有者ラベルを注入する**(`POST /containers/create` の
     ボディに1つラベルを足して中継する) /
  B. **docker-proxy が作成したコンテナ ID と所有者の対応をファイルに記録する** /
  C. **実現しない**(compose 経由のものだけを片付ける現状を維持する)
- **AI推奨**: **A**。
  理由: ラベルは Docker 側に永続し、docker-proxy が再起動しても・`stop` を別の端末から
  実行しても引ける。B は記録の置き場所(共有 docker-proxy はコンテナで、消えると対応表が消える)と
  記録漏れ(proxy が落ちている間の作成)の両方を新しく抱える。
  **下流の代償**: `CTR-docker-api` の「検査する要素と判定」に「書き換えて許可」の行が1つ増え、
  docker-proxy の単体テストが増える。`FR-env-07` に条項が1つ増える。
  **崩れる条件**: 利用者のワークフローがコンテナのラベル集合の完全一致に依存している場合、
  または Docker がラベル注入を受け付けない API 版が出た場合。
- **未回答時の既定**: 委任にする(`D0-env-10` の委任範囲=「付与する資源の種類」で決める。
  制約: 方式 A に限る。注入するラベルは所有者を表す1つだけ。他のフィールドを変えない)
- **根拠(上流)**: `docs/00-requests/decisions/env.md` の `D0-env-08` 項7
  (「compose が作る資源にはラベルを付けられない」という前提。**A はこの前提を覆す**)と
  `D0-env-10`(ラベルの名前・値・付与する資源の種類は AI への委任)
- **根拠(同層)**: `docs/02-design/contracts/docker-api.md` の `DSN-dp-01`
  (判定できない入力は通し、判定できる危険だけ止める)と、同文書「検査する要素と判定」の
  「バインドマウント → **書き換えて許可**」— **書き換えの前例が既に契約にある**
- **根拠(下流)**: `relations-query.py --impact docker-proxy/main.go` = 機能1件・要件2件・契約1件・
  **テスト15件**(`docker-proxy/main_test.go` / `binds_test.go`)。
  `--impact claude-dev` = 機能31件・要件13件・契約2件・テスト0件
- **過去の回答**: `D0-env-08` 項7(2026-08-04。「印を足せないなら名前を一意にする」と決めた。
  `DSN-env-03` がその実装)/ `DSN-env-01` の却下案「すべての資源にラベルを付けて統一する」
  (**却下理由は「固定名の資源には移行問題を増やすだけ」であり、固定名を持たない今回の対象には
  当たらない**)/ decisions・決定シート写し・feedbacks・histories を走査済み

**★あなたの記入**:A

### 論点 2. 所有者ラベルの値に何を入れるか

- **選択肢**:
  A. **起動ディレクトリの絶対パス**(`claude-dev.project-dir` と同じ値) /
  B. **Claude コンテナ名**(`<NAME>`)
- **AI推奨**: **A**。
  理由: `stop` は引数 `NAME` しか持たず、絶対パスは `claude-dev.project-dir` ラベルから読む。
  A なら**その1回の読み取りで compose 一意化名と所有者ラベルの両方が引ける**
  (`claude-dev:1656`〜`:1663` の既存手順に相乗りする)。
  また docker-proxy が知っているのは `/workspace` のマウント元(=絶対パス)であって
  コンテナ名ではない(`docker-proxy/main.go:119`〜`:123`)ので、B は proxy 側に
  名前を引く処理を足すことになる。
  **下流の代償**: `docker inspect` に**ホストの絶対パス**が現れる。これは
  `claude-dev.project-dir` で既に起きていることで、`docs/02-design/logging.md`
  「起動ディレクトリの絶対パスの扱い」が許容範囲を定めている。
  **崩れる条件**: 同じディレクトリを2つのセッションが同時に使う運用がある場合
  (名前が違えば A では区別できない。ただし `start` は同名衝突を拒否するので通常は起きない)。
- **未回答時の既定**: 委任にする(`D0-env-10`。制約: 値は `claude-dev.project-dir` と同じ
  「起動ディレクトリの絶対パス」とし、接頭辞は `claude-dev.`。`com.docker.*` を使わない)
- **根拠(上流)**: `docs/00-requests/decisions/env.md` の `D0-env-10`
  (委任範囲=ラベルの名前・値の形式・付与する資源の種類。ガードレール4件)
- **根拠(同層)**: `docs/02-design/contracts/cli-container.md`「管理ラベル」表
  (`claude-dev.project-dir` = 起動時のホスト側カレントディレクトリの絶対パス。正規化・短縮をしない)
- **根拠(下流)**: `docs/03-impl/relations/MODULE-cli-stop.md` 手順4
  (本体削除より前にラベルを読む固定の順序)。A ならこの手順が1つ増えるだけで順序は変わらない
- **過去の回答**: `CTR-cli-container`「管理ラベル」(値の検証は行わず、受け側は存在の有無だけを見る。
  ただし `claude-dev.project-dir` の値だけは compose 一意化名のハッシュ源として使う)/
  decisions・決定シート写し・feedbacks・histories を走査済み

**★あなたの記入**:A

### 論点 3. `stop` に「集合として列挙する削除」が入るが、確認(y/N)を求めるか

- **選択肢**:
  A. **求めない**(現状の `stop` の振る舞いを維持し、削除したコンテナ名を事後に列挙する) /
  B. **求める**(`logout` / `reset` と同じく、削除対象を列挙して同意を取る。
     端末が無ければ `--yes` が要る)
- **AI推奨**: **A**。
  理由: `D0-env-08` 項3 は 2026-08-04 に人間が裁定して
  「確認を求めるのは**削除対象を集合として列挙する操作** = `logout` / `reset` の2つに限る。
  `stop <name>` は求めない」と決めており、その根拠は
  「セッションを止めるだけの日常操作が重くなる」だった。今回 `stop` に集合削除が入るので
  **文言上の前提(集合削除かどうか)は変わる**が、裁定の理由(日常操作を重くしない)は変わらない。
  **下流の代償**: 利用者が意図せずコンテナを失う経路が1つ増える(事後の列挙表示でしか気づけない)。
  `D0-env-08` 項3 の書き方を「集合として列挙する操作か」ではなく
  「`logout` / `reset` か」に直す必要がある(**判定基準そのものの修正**)。
  **崩れる条件**: セッション内から作ったコンテナが利用者にとって「失うと痛いもの」である場合
  (概念2 で「停止のみ」を選ぶならこの論点は消える)。
- **未回答時の既定**: 委任にしない(A を既定とする。`D0-env-08` 項3 の裁定を維持する側であるため)
- **根拠(上流)**: `docs/00-requests/decisions/env.md` の `D0-env-08` 項3
  (2026-08-04 に人間が裁定した `stop` の確認免除。項1 のラベル例外とは別根拠であることを明記している)
- **根拠(同層)**: `docs/02-design/contracts/cli-container.md`「エラーケース」の
  「`logout` / `reset` が標準入力を TTY として持たない環境で `--yes` 無しに実行された」行
  (**`stop <name>` はこの行の対象ではない**と明記。`--yes` を受け付けるのも2コマンドだけ)
- **根拠(下流)**: `docs/02-design/logging.md`「破壊的操作の削除対象の確認」行(`FR-env-03-14`)と
  `docs/03-impl/tests/cli-stop.md`(`stop` の対応表には確認に関する行が1つも無い)。
  B を選ぶと `FR-env-03` 側の条項が `stop` にも掛かることになり、影響範囲が
  `MODULE-cli-logout` の記述にまで広がる
- **過去の回答**: `D0-env-08` 項3(2026-08-04 に人間が裁定。「`stop` に確認を足すと日常操作が重くなる」)/
  `DSN-env-03` の却下案「`stop` が削除前に対象名を列挙して確認を取るだけにする」(同じ理由で却下済み)/
  decisions・決定シート写し・feedbacks・histories を走査済み

**★あなたの記入**:A

### 論点 4. 独立レビューが実行できなかった。代替を立てるか

- **選択肢**:
  A. **代替しない**(独立レビュー無しでフェーズ2を終え、`/doc-check` の検証も Claude 単独で行う。
     Codex の復旧後に改めて `readiness` 監査を掛ける) /
  B. **サブエージェントを独立レビュー役として立てる**(このセッションのサブエージェント起動は
     本セッションの標準指示で止められているので、**あなたの明示的な許可が要る**) /
  C. **Codex の復旧(2026-08-11)を待ってからフェーズ3へ進む**
- **AI推奨**: **B**。
  理由: 今回の変更指示は 18 ファイル・新設条項8件におよび、**書いた本人の読み直しでは
  自分の前提を素通りする**(だからこそドライランは2回・別文脈で走らせる設計になっている)。
  実際、私自身のパス1では 3 件しか出ておらず、うち1件は照合値の出所という**設計の穴**だった。
  独立の目で1周させる価値が最も高いのは今である。
  **下流の代償**: 代替レビュー役は Codex より独立性が低い(同じモデル系統・同じ規範を読む)。
  レポートには `lens: subagent` と明記し、**どのレビュー役が実際に走ったか**を残す必要がある
  (不変則7)。**崩れる条件**: サブエージェントの指摘が私自身の見落としと同じ偏りを持つ場合
  (そのときは代替の価値がほぼ無い)。
- **未回答時の既定**: **A**(代替しない)。**不変則7 が「既定は代替しない」と定めており、
  無断で代替してはならないため**。フェーズ3の入場は妨げない(ドライランはゲートではない)。
- **根拠(上流)**: `CLAUDE.md` §7 不変則7(独立レビュー役を黙って差し替えない。人間の判断で、
  実際に走ったレビュー役を必ず明記する)/ `docs/02-design/environments.md`「Codex実行設定」
- **根拠(同層)**: `docs/feedbacks/022-lens-substitution-can-be-approved-standing-not-per-run.md`
  (**代替可否は「常設の承認」でも与えられる**という先例。ただし**この承認がプロジェクトの
  文書のどこにも書かれていない**ことは `docs/issues/065` が追跡している)
- **根拠(下流)**: `.claude/skills/task-doc/SKILL.md` §3(レビュー役が起動しなかった場合は
  代替を自分で決めず、この節の決定シートへ回す)/ `/doc-check` も同じ判断を要する
  (2回目のドライランを走らせるため)
- **過去の回答**: `docs/feedbacks/022`(2026-08-05。人間が常設の承認を与えた記録)/
  `docs/issues/065-modify-lens-substitution-policy-has-no-normative-home.md`
  (**その常設承認を書く欄が `environments.md` に無く、会話の中にしか存在しない**)/
  decisions・決定シート写し・feedbacks・histories を走査済み

**★あなたの記入**:B

### 論点 5. 所有者ラベルを2つ注入することが `D0-env-10` のガードレールに収まるか

- **選択肢**:
  A. **`D0-env-10` のガードレールを広げる**(「所有者を表すものだけ」→「**所有者と種別を表す
     2つだけ**」)。02 と 03 は現状のまま(`claude-dev.role=spawned` +
     `claude-dev.owner-project-dir` の2つを注入する) /
  B. **注入を1つに減らす**(`claude-dev.owner-project-dir` だけにする)。`reset` は
     `--filter label=claude-dev.owner-project-dir`(**キーの存在一致**)で所有者を問わず引く。
     00 のガードレールは現状のまま
- **AI推奨**: **A**。
  理由: ガードレールの実質は「**要求の他のフィールドを変えない**」ことであり、02 と 03 は
  それを一字一句守っている。`claude-dev.role` は**既存の管理ラベル**で、Claude コンテナには
  `claude-dev.role=claude` が既に付いている(`CTR-cli-container`「管理ラベル」)。
  その値域に `spawned` を1つ足すのは `D0-env-10` が委任した「ラベルの名前と値の形式」そのもので
  あり、**新しいラベルの種類を増やしたわけではない**。
  加えて `docker ps --filter label=claude-dev.role=spawned` は**利用者が自分で一覧するときの
  唯一の手掛かり**で、`MODULE-cli-stop` の異常系(片付けを行わなかったときの案内)と
  `docs/02-design/logging.md` がこの形で表示すると定めている。
  **下流の代償**: `docs/00-requests/decisions/env.md` の `D0-env-10` ガードレール1行と
  `D0-env-08` 項8 の「所有者を表す管理ラベルを付ける」の2箇所を書き直す(00 層の変更)。
  **崩れる条件**: 利用者のツールがコンテナのラベル集合を厳密一致で検査している場合、
  増えるラベルが2つか1つかで踏む確率が変わる(ただし1つでも踏むので程度の差である)。
- **未回答時の既定**: **A**(ガードレールを広げる)。**理由: B を選ぶと 02・03・E2E 手順の
  11 箇所を書き直すことになり、`reset` の引き方(キー存在一致)が `stop` の引き方(値一致)と
  別の仕組みになる。ただし既定で進めた場合も 00 の2行は書き直すので、変更そのものは避けられない。**
- **根拠(上流)**: `docs/00-requests/decisions/env.md` の `D0-env-10` 制約
  「**所有者ラベルで注入するのは所有者を表すものだけとし、要求の他のフィールドを変えないこと**」と
  `D0-env-08` 項8「docker-proxy が作成要求を中継するときに**所有者を表す管理ラベル**を付ける」。
  どちらも**単数**で書かれている
- **根拠(同層)**: `docs/tasks/task-stop-session-spawned-containers/new-features/02-design/contracts/cli-container.md`
  の `DSN-env-04` が**2つのラベルの表**を持ち、同じ節が
  「**このラベルは所有と種別を表すだけで、`managed` の意味は持たせない**」と書いている —
  つまり 02 自身が `role=spawned` を**所有ではなく種別**の印だと述べている
- **根拠(下流)**: `MODULE-cli-reset` 判断13(`reset` は `claude-dev.role=spawned` で引く)/
  `MODULE-cli-stop` 異常系と `logging.md`(利用者への案内が
  `docker ps --filter label=claude-dev.role=spawned`)/ `03-impl/tests/e2e.md` 手順8-14-2・E2E-03 手順5
  (実機確認が2つのラベルの存在を見る)
- **過去の回答**: `sheet.md`「委任 1」(2026-08-07。人間が**承認**したときの制約に
  「**注入するのは所有者を表すラベルだけ**とし、要求の他のフィールドを変えない」と書いてある)/
  `D0-env-08` の却下した案「項8 の所有者ラベルに既存の `claude-dev.managed` を使う」
  (`managed` を使うと `logout` の削除対象に入るので**別のラベルで表す**と決めた経緯)

**★あなたの記入**:

## 委任にしてよいか確認したい項目

### 委任 1. 所有者ラベルの名前・値の形式・注入の実装細部

- **委任範囲**: docker-proxy が注入するラベルの**名前**、**値の書き方**、
  注入する API 経路(`POST /containers/create` だけか、他にもあるか)、
  注入に失敗したときの扱い、キャッシュの扱い。
- **制約(ガードレール)**:
  - 接頭辞は `claude-dev.` に固定する。`com.docker.*`(Docker / compose の予約)を使わない。
  - **注入するのは所有者を表すラベルだけ**とし、要求の他のフィールドを変えない。
  - **注入の失敗でコンテナ作成を拒否しない**(`DSN-dp-01`「判定できない入力は通す」に揃える)。
  - **共有資源(docker-proxy / `claude-dev-net`)を止めてよいかの判定を、このラベルに
    依存させない**(`D0-env-08` 項2)。
  - 決めた内容を `docs/02-design/contracts/cli-container.md` に書く
    (`D0-env-10` のガードレール「書かずに実装するのは範囲外」と同じ)。
- **根拠**: `docs/00-requests/decisions/env.md` の `D0-env-10`(既存の委任。ラベルの名前・値の形式・
  付与する資源の種類が対象)。今回はその「付与する資源の種類」に1種類増えるだけなので、
  **既存の委任の範囲内で扱えるかどうか**を確認したい。
- **未回答時の既定**: 承認しない(この項目は次のシートに再掲します)

**★あなたの記入**(承認 / 却下 / 条件付き(条件を書く)のいずれか):承認

## 方針合意(個別diff承認は行いません)

- `docs/00-requests/decisions/env.md`: `D0-env-05` 項2 の片付け対象を「セッション内から作られた
  コンテナ」へ広げ、`D0-env-08` に識別の項(所有者ラベルの注入)を足し、`D0-env-10` の委任範囲に
  注入ラベルを含める。
- `docs/00-requests/terminology.md`: 「管理ラベル」の定義に、docker-proxy が付与する所有者ラベルを
  含める(現在は「本システムが Docker 資源を作るときに付ける」= ホスト CLI 前提の書き方)。
- `docs/01-requirements/functional.md`: `FR-env-01` に片付け対象の条項を追加、
  `FR-env-07` に docker-proxy のラベル付与の条項を追加。
- `docs/01-requirements/decisions/split.md`: `D1-split-01` が書く条項数(「他の 20 条項は不可分」等)を
  追加後の数に更新する。**段階可の集合は増減させない**(`D1-split-02` の委任範囲外のため)。
- `docs/02-design/contracts/cli-container.md`: 「識別の手段は資源ごとに違う」表に行を追加、
  「管理ラベル」表に注入ラベルを追加、「削除対象の決め方(3つの規則)」を更新、`DSN-env-01` を改める。
- `docs/02-design/contracts/docker-api.md`: 「検査する要素と判定」に「所有者ラベルを付与して許可」を追加。
- `docs/02-design/system.md`: 要件カバレッジ表に新条項の行、結合テスト対象と E2E-01 / E2E-03 を更新。
- `docs/02-design/relations.md`: `PLAN-cli-stop` / `PLAN-docker-proxy-serve` の概要を更新。
- `docs/02-design/logging.md`: `stop` が片付けたコンテナの列挙と、proxy のラベル付与ログの行を追加。
- `docs/03-impl/`: `MODULE-cli-stop` / `MODULE-docker-proxy-serve`(+ 概念4 で `reset` を含めるなら
  `MODULE-cli-reset`)、両契約、テスト4本、`infra/local/docker-resources.md`、`index.md` を更新。
- 変更指示を書き終えた時点で差分サマリを提示します。個別のdiff承認は取りません。

**★あなたの記入**(異議のある方針だけ書いてください。空欄 = 全方針に合意):

## 記入完了

<!-- ここが埋まって初めて「返答済み」になる。空欄の論点ブロックを既定で進めてよいのは
     この日付が入っているときだけ(/clear 後も「記入中」と区別できる) -->

**★あなたの記入**(記入を終えたら日付を書いてください。例: 2026-08-07):
