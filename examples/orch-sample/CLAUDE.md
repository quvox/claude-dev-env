# プロジェクト指示（mathkit）

- 言語は Python（外部依存なし。標準ライブラリのみ）。
- `tests/` の pytest を**すべて通す**ことが成果の基準。テストは期待仕様なので変更しない。
- 実装は `src/mathkit/` のスタブ関数を埋める形で行い、**変更は最小限**にする。
- テスト実行: `pytest`（`pytest.ini` で `pythonpath = src` を設定済み）。
